# HXA-130：Connector 签名索引与来源验证

分类：待决策。当前是否执行见[实施状态](../status.md)。

前置条件：接受CONNECTORS-004；生产安装集成依赖129。

本次复核：纯离线格式fixture可准备；不因为依赖129而阻止文档审查，也不提前开发市场后端。

核对依据：[源码或有效证据](../../adr/connectors/004-signed-index.md)。本轮只审查，不新增测试通过或完成声明。

## 范围与验收要求

状态：设计提案已形成，见 proposed [ADR-CONNECTORS-004](../../adr/connectors/004-signed-index.md)；生产安装集成依赖 HXA-129；纯离线格式与签名fixture可准备，尚无完成证据。签名索引、固定版本、来源及许可证审查设计。市场不扩展 ToolCall 权限；网络安装和更新的实施范围及发布条件经独立审查后确定。当前仅允许 docs 和离线索引 fixture，不提前实现市场运行时。

验证：具体命令在本文件的验证入口；公共规则只定义通用门禁，不再维护重复任务行。

## 验证入口

[公共验收规则](../verification-matrix.md)规定默认/外部测试边界。具体类和 runner 必须实际存在且执行非零；本文件列出的新增用例是实现要求，不是现有证据。

任务验收条目（新增测试在交付前必须实现并执行）：

| 任务 | 验收维度 | 命令或证据入口 |
| --- | --- | --- |
| HXA-130 | proposed design；`./scripts/check-docs.sh`、`./scripts/verify-adr.sh` 通过；见 [ADR-CONNECTORS-004](../../adr/connectors/004-signed-index.md) | 未实现/未验收；生产方案待 ADR-CONNECTORS-004 审查；生产安装集成依赖 HXA-129，纯离线fixture可先准备；文档门禁不替代功能或真实服务证据 |

## 交付

通过本任务验证后更新完成记录和 status；未通过的设备/账号/发行项明确列出。允许已授权且验证通过的切片具名本地提交，不 push、合并或发布，不夹带其他任务改动。
