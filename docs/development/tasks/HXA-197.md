# HXA-197：单个手动 PTY 终端

分类：待实现。当前是否执行见[实施状态](../status.md)。

前置条件：195；组件/映射/租期技术决定；不等196。

本次复核：人工终端范围已接受；剩余是PTY组件许可证和真实目录映射，不重审整份ADR。

2026-09-18 所有者授权在当前会话推进。组件初筛候选为 [ConnectBot termlib](https://github.com/connectbot/termlib)：官方[组件文档](https://termlib.connectbot.org/main/)明确仅负责终端模拟与 Compose 显示，PTY/I/O 由调用方管理；仓库[许可证](https://github.com/connectbot/termlib/blob/main/LICENSE)为 Apache-2.0，README 标注 libvterm 为 MIT。这符合 UI/Runtime 分工，但尚未采纳依赖或复制源码。接线前仍须核对固定发行制品、完整 native 依赖许可证、ABI/16 KiB 兼容、OSC 行为、缓冲上限与设备输入证据；不能把当前 main 分支的说明当作固定版本验收。旧 jackpal Android-Terminal-Emulator 已归档，不作为默认新依赖。

核对依据：[源码或有效证据](../../adr/runtime/002-terminal-and-jobs.md)。本轮只审查，不新增测试通过或完成声明。

固定版本兼容性待决：Maven Central 的 [0.2.1 POM](https://repo.maven.apache.org/maven2/org/connectbot/termlib/0.2.1/termlib-0.2.1.pom) 使用 Compose BOM `2026.09.00`，当前工程为 `2026.06.01`；不能假定可直接接入。先检查制品实际依赖与兼容性，再决定固定可兼容版本或显式升级；与 191 的主题改动整合后重新验证，不在组件调研中隐式更新依赖。

## 2026-09-20 进入 checkpoint 与固定制品检查

191 已按[完成记录](../../completion-records/HXA-191.md)收口，197 成为唯一开发 checkpoint；196 真机待验独立保留。进入前最新完整主机门禁通过，主题最终四象限 60 项、搜索 UI/恢复 32 项与 Room 16 项通过。此前 Runtime 专项证据按原范围保存，未冒充在主题修正后又重跑整套 Runtime。

执行 `python3 scripts/debug/2026-09-20/inspect-termlib.py`，从 Maven Central 下载固定 0.2.1 POM、module、AAR、sources 到忽略的 `build/hxa197-termlib-0.2.1/`，保存 URL、字节数、SHA-256 与 ELF 报告。没有修改生产依赖、锁文件或验证 metadata；没有复制候选源码进应用。

- AAR SHA-256：`01a7dcb57aebdb5492abc9c505963de9d638eee8065ab074bd67b9882f7690d5`；sources SHA-256：`fe728b29a2c0f4612de88af2126ae61fc793a3203ca7564fafcd842754a0ab03`。
- AAR metadata 明确 **minCompileSdk=37**，manifest minSdk=24；当前工程 compileSdk=36，现场只安装 android-36。兼容性缺口不只是 BOM。POM 还声明 Kotlin 2.3.21、core-ktx 1.18.0、appcompat 1.7.1、material 1.14.0、Compose ui/foundation 1.11.2 与 runtime 1.11.4。不得绕过 metadata 或强压传递依赖来宣称可接线。
- 包含 arm64-v8a、armeabi-v7a、x86、x86_64 的 `libjni_cb_term.so`；NDK llvm-readelf 显示全部 LOAD 段 `p_align=0x4000`。这是 ELF 静态对齐检查，**不是 16 KiB 设备运行证明，也不是最终 APK ZIP 对齐验收**。
- 官方 tag `0.2.1` 解引用 commit 为 `27e024fccb2d722b47c57f5da1b4da9bca477b68`。POM 及 Kotlin 文件声明 Apache-2.0，README 声明 libvterm MIT；尚需核对该固定 commit 的完整 native 第三方来源与许可证，不能仅靠 DT_NEEDED 推断静态链接库不存在。
- sources 中 `TerminalEmulatorFactory.create` 的 `onClipboardCopy` 默认 null，`inlineImages` 默认 Off；OSC 52 通过可选回调通知，没有默认复制的回调。但需实际注入 OSC 验证系统剪贴板不变，并检查输入解析/滚动缓冲上限；手动选区复制与 OSC 自动写剪贴板应分开。

官方[组件说明](https://termlib.connectbot.org/main/)确认只负责模拟和显示，PTY/I/O 由调用方管理；此职责划分适合既有应用服务与私有 Runtime。下一步是固定源码许可证/缓冲审查、兼容工具链方案和独立 PTY/组件设备 spike；之后把验证过的组件、目录映射、租期约定补入 ADR 再接生产 UI。不把本次下载或静态检查称为组件已采纳或终端已实现。

## 范围与验收要求

- 依赖：195；ADR-RUNTIME-002 的人工输入与前台范围已接受，仍需明确实际目录映射与组件选型；不依赖 196 完成，初版单 PTY。
- 允许：Runtime/app、必要 storage 元数据、经决策的 native PTY 适配和渲染组件、测试/docs。禁止直接复制竞品受限制许可证代码。
- 先验证 PTY 创建、进程组、输入、resize、Ctrl-C、EOF 和回收；native/rendering 选型与许可证证据补入 ADR-RUNTIME-002；涉及新增核心依赖按ADR流程决定，具体版本记录catalog/lock，兼容性升级允许。
- developer Workspace 有“打开终端”入口；UI 通过应用服务连接 `:proot`。显示当前目录、连接状态、停止/关闭操作；关闭页面仅 detach，关闭会话终止执行；无模型自动输入接口。
- 当前终端是可信共享 UID 环境，不能展示“只允许访问此目录”。Workspace 是起始目录/关联范围，实际访问边界按 ADR-RUNTIME-001；是否直接映射原 Workspace 的决定必须写入接受后的契约，不沿用 Job snapshot 名称掩盖变化。
- 测试：`cd`/env 在同一会话保持、中文/多字节、REPL 输入、窗口调整、Ctrl-C、prompt 后后台子进程、OSC 剪贴板不执行、Activity 重建、关闭终端不杀其他 owner。
- 验收：G1～G3；新增 `ProotTerminalSessionDeviceTest`；实际运行 REPL，并保存中英文界面/软键盘/长输出截图。编译成功不代替可交互验收。

## 验证入口

[公共验收规则](../verification-matrix.md)规定默认/外部测试边界。具体类和 runner 必须实际存在且执行非零；本文件列出的新增用例是实现要求，不是现有证据。

任务验收条目（新增测试在交付前必须实现并执行）：

| 任务 | 验收维度 | 命令或证据入口 |
| --- | --- | --- |
| HXA-197 | REPL/resize/Ctrl-C/切页/进程死亡；新增 ProotTerminalSessionDeviceTest | planned；G1/G2/G3；依赖195及组件/映射决定，不依赖196完成 |

## 交付

通过本任务验证后更新完成记录和 status；未通过的设备/账号/发行项明确列出。允许已授权且验证通过的切片具名本地提交，不 push、合并或发布，不夹带其他任务改动。
