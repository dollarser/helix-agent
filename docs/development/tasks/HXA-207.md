# HXA-207：现有扩展来源的添加到使用闭环

分类：已完成（2026-09-18，[完成记录](../completion-records/HXA-207.md)）。当前是否执行见[实施状态](../status.md)。

前置条件：209授权；202/204/205界面接缝。

本次复核：不依赖126/129/130未来OAuth或市场；不复用201旧三态UI；已有导入服务继续复用。

核对依据：[源码或有效证据](../../adr/connectors/001-portable-bundles.md)。本轮只审查，不新增测试通过或完成声明。

## 范围与验收要求

本任务优先完善现有 MCP/Connector/Skill 的配置到实际使用，复用统一注册、发现、授权和执行管线，不为具体业务服务新增专用Agent循环。保留既有安装工具，但安装/连接成功不等于用户启用或授予权限；验证禁用后发现、模型schema和执行入口一致拒绝。无需为每个配置页面增加Agent工具。 规划规则见[实施指南](../implementation-guide.md#应用能力与-agent-工具的规划规则)。

状态completed（2026-09-18，[完成记录](../completion-records/HXA-207.md)）。依赖HXA-124/127已有基础，复用209的新授权入口及202/204/205的导航/错误/准备视图；不等待完整终端。

允许：app/skills、app/mcp及已有Connector应用服务、能力UI和直接查询适配，extensions/skills、extensions/mcp、相关测试/docs；不新增认证协议、市场后端或持久版本生命周期。先核实已有入口，迁移复用，不把全局Skill伪装成会话scope。

验收：可导入Skill、配置支持的MCP、明确启用、发起实际测试任务、结果返回、禁用后不再曝光、失败可原地修复。新增 `ExtensionJourneyDeviceTest`（app标准androidTest source set）覆盖内容预览后变化、导入取消、重复点击、无效配置、连接成功但未启用、已启用但调用失败、工具禁用/操作询问、重启后实际范围；真实外部服务另记，不用fixture冒充HXA-125通过。

精确主机命令：产品包P1/P2/P3按修改范围执行，并追加：

```bash
./gradlew :extensions:skills:test :extensions:mcp:test
./gradlew :app:testConsumerDebugUnitTest :app:testDeveloperDebugUnitTest
./gradlew :app:assembleConsumerDebugAndroidTest :app:assembleDeveloperDebugAndroidTest
```

双API29/36双flavor按现有owned-runner执行新增类，记录真实数量、失败、跳过及进程退出；测试脚本先保存到日期目录。无新增数据库格式；若实现必须新增共享所有权/版本表，归HXA-129而不是在此隐式扩权。

## 验证入口

[公共验收规则](../verification-matrix.md)规定默认/外部测试边界。具体类和 runner 必须实际存在且执行非零；本文件列出的新增用例是实现要求，不是现有证据。

任务验收条目（新增测试在交付前必须实现并执行）：

| 任务 | 验收维度 | 命令或证据入口 |
| --- | --- | --- |
| HXA-207 | Skill/MCP导入、预览变更、连接但未启用、实际调用、会话授权、禁用/重启/修复；ExtensionJourneyDeviceTest | completed（[完成记录](../completion-records/HXA-207.md)）；P1/P2/P3按范围 + extensions:skills/test、extensions:mcp/test及双API双flavor全绿；[公共命令](../verification-matrix.md) |

## 交付

通过本任务验证后更新完成记录和 status；未通过的设备/账号/发行项明确列出。允许已授权且验证通过的切片具名本地提交，不 push、合并或发布，不夹带其他任务改动。
