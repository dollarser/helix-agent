# HXA-205：首次配置与能力准备修复

分类：待实现。当前是否执行见[实施状态](../status.md)。

前置条件：193已实现服务；不等其远端CI收尾。

本次复核：吸收191首次引导；与194命令详情分开，覆盖真实初始化/修复而非又做Runtime打包。

核对依据：[源码或有效证据](../../../app/src/main/kotlin/com/helix/app/ui/ProotRuntimeSection.kt)。本轮只审查，不新增测试通过或完成声明。

## 范围与验收要求

本任务属于应用准备与配置功能，不新增配置凭据、授权、启用工具或修复 Runtime 的 Agent Tool。准备页复用现有服务，只在用户明确操作时执行；既有工具辅助安装能力不扩展为自动启用。 规划规则见[实施指南](../implementation-guide.md#应用能力与-agent-工具的规划规则)。

- 与HXA-190/191对照，已有引导/主题/搜索不重做。允许app配置/能力/Workspace/UI、developer初始化入口及测试。
- 一个准备视图聚合模型连接、Workspace、所需系统能力与Runtime状态，使用现有服务，按用户当前目标给出下一动作。
- 被动进入页面不拉起Runtime或登录；用户点击初始化/检查/修复才冷绑定。安装中、损坏、无网络、取消、重开状态可恢复。
- consumer不出现无法兑现的Runtime入口，developer不再显示独立APK安装流程。无模型配置时文件管理/浏览器仍可用。
- 验收：P1+P3；新增 `CapabilityReadinessDeviceTest`，新安装/旧配置/离线/取消/旋转/双flavor及实际Runtime初始化复用193脚本。

### 分工与收口

吸收原191的首次配置引导；模型、工作目录、系统能力、Runtime状态分步可达，手动文件与浏览器不等待模型设置。使用193已有服务，不以远端CI尚未关闭为阻止独立UI开发的理由。

### 顺序实施切片

1. **准备状态投影**：从现有服务聚合模型、Workspace、系统能力及 Runtime 状态，按用户目标返回缺项与下一动作；只读刷新不启动服务、登录或安装。
2. **引导与修复入口**：接入已有配置、授权和显式 Runtime 初始化/修复动作，区分 consumer/developer。沿用 HXA-204 的结构化错误与恢复展示，不复制第二套状态或自动扩大权限。
3. **完整设备旅程**：CapabilityReadinessDeviceTest 覆盖新旧配置、离线、取消、旋转、损坏后修复及重开；API29/36 × consumer/developer，实际 Runtime 初始化复用193验证入口。执行 P1/P3 与完整主机门禁后提交完成记录；没有模型配置也能进入手动文件和浏览器。

切片是提交边界，不是 HXA 完成条件。暂不实现深色主题、会话搜索、扩展市场或终端；本任务完成后按 status 交接，不自动扩展范围。

## 验证入口

[公共验收规则](../verification-matrix.md)规定默认/外部测试边界。具体类和 runner 必须实际存在且执行非零；本文件列出的新增用例是实现要求，不是现有证据。

任务验收条目（新增测试在交付前必须实现并执行）：

| 任务 | 验收维度 | 命令或证据入口 |
| --- | --- | --- |
| HXA-205 | 新旧配置/离线/初始化修复/双flavor；CapabilityReadinessDeviceTest | planned；P1/P3 + 193相关Runtime子集 |

## 交付

通过本任务验证后更新完成记录和 status；未通过的设备/账号/发行项明确列出。允许已授权且验证通过的切片具名本地提交，不 push、合并或发布，不夹带其他任务改动。
