# HXA-196：有租期的独立后台命令 Job

分类：待实现。当前是否执行见[实施状态](../status.md)。

前置条件：195；执行授权对齐209；平台可行性。

本次复核：Goal后台续轮已归208；这里是独立进程owner/租期/日志/对账，不强制所有启动出卡。

核对依据：[源码或有效证据](../../adr/runtime/002-terminal-and-jobs.md)。本轮只审查，不新增测试通过或完成声明。

2026-09-18 所有者已授权推进平台与核心切片；当前实现、模拟器证据及剩余产品/真机边界见[切片记录](../../evidence/development/hxa-196-platform-plan-2026-09-18.md)。任务仍保留，不能以核心切片替代整项验收。

当前独立工作树已接入执行线程与持久身份占用机制，验证范围和未接线部分见[执行占用切片](../../evidence/development/hxa-196-execution-ownership-2026-09-18.md)。

继续修正同步提交的准备/冷绑定扣时和客户端 PFD 回收，见[提交链路记录](../../evidence/development/hxa-196-submission-resources-2026-09-18.md)。后台产品接线须复用这些资源与预算边界；不据此关闭异步工具和 Goal 结算范围。

## 范围与验收要求

- 依赖：195；可先做 FGS/owner Spike，生产接线需记录实际Android后台启动/拒绝/租期回收的平台证据；相关产品条款已经接受。
- 允许：上述 Runtime/app 模块，`core:agent` 预算/绑定适配、`core:storage` 迁移、`tools:framework` 仅必要契约，测试/docs。不改变 Goal 自动继续或完成标准。
- 新异步启动返回持久 Job 引用；观察/取消权限由绑定限定，不能枚举其他会话输出。复用现有任务页，新增 Job 投影而不是复制 Turn；用户取消优先于完成竞态，最终只结算一次。
- detached 前先持久记录所有权和租期，再允许 UI 离开；若无法建立合法后台路径，不宣称“后台运行中”。保留旧同步工具 v2 的语义，不能一律返回“accepted”破坏原调用。
- 候选新工具名在开始时固定到 schema/工具文档，启动经当前会话授权解析；仅ASK或专项rm规则需要精确审批；观察只读取已授权任务，取消幂等且不能启动新任务。日志/任务完成不自动发起模型新 Turn。
- 测试：启动前取消、审批过期、额度不足、UI detach、主进程死亡、Runtime 死亡、租期跨重连、退出/取消竞态、FGS 拒绝、退后台降级、结果导入失败重查不重跑。
- 验收：G1～G4；新增 `ProotDetachedJobDeviceTest`。真实物理设备后台/锁屏/Doze 单列；缺设备时保留待验收，不阻断其他独立实现，也不记此 HXA 完成。

## 验证入口

[公共验收规则](../verification-matrix.md)规定默认/外部测试边界。具体类和 runner 必须实际存在且执行非零；本文件列出的新增用例是实现要求，不是现有证据。

任务验收条目（新增测试在交付前必须实现并执行）：

| 任务 | 验收维度 | 命令或证据入口 |
| --- | --- | --- |
| HXA-196 | 租期/预算/owner 死亡/后台拒绝；ProotDetachedJobDeviceTest + 真机 HOME/锁屏 | PARTIAL：平台/核心已合入，模拟器切片证据见上；生产接线与真机 G4 尚未完成 |

## 核心整合后的剩余范围

继续保留一个 HXA，不用新的规划器或工作流拆出第二套状态源：

1. 固定异步 start/status/cancel 工具契约，沿既有 Dispatcher 处理 schema、能力、209 会话授权、ToolCall 审计及预算；提交前持久化绑定，额度不足不启动。旧同步工具语义保持。
2. 将持久 Job 引用接入现有 Tasks/命令详情只读投影，按 session/turn/toolCall/job/execution/inputHash 关联；查询/取消不得跨绑定，完成不自动发起模型新 Turn。
3. 复用既有输出验证与产物导入，区分执行终态与结果导入失败；重查只回收原结果，不重新运行命令。补授权过期、取消竞态、导入失败重查及 UI 离开后恢复的产品旅程。
4. 真机 HOME/锁屏/Doze 验证真实后台路径；缺当前物理设备仅保留待验，不用模拟器或旧 Root 验收代替。全部 G1～G4 达标后才关闭本任务。

### 接线前必须覆盖的边界

- 调度器当前只保留 ToolCall 执行期间的互斥。异步 start 返回后，后台 Job 的未知文件/代码效果不能随着调用槽位释放而消失；复用持久 Job 身份与统一效果准入，覆盖跨 Turn、跨会话和主进程重启，不另造业务工作流。
- Goal 的 `remainingExecutionMillis` 是运行时窗口。后台命令提交须扣除快照、连接与排队耗时，且在 Turn 已返回后继续受其已分配预算约束；不能通过重新查询续租。
- Goal 的当前 TIME heartbeat 与后台 Job 的存活区间可能重叠。接线须明确持久预算预留和结算归属，既不漏掉 Turn 返回后的执行，也不把重叠区间重复计费；不能简单把整个租期再加到已有 Goal 时间上。未知副作用恢复与既有 Goal 完成标准保持。
- 现有结果恢复只识别同步 `bash`/`code.linux.run`，并检查 Turn/ToolCall 的恢复资格。异步启动成功必须与 Job 最终成功分开，按原绑定查询、导入与取消，不把 start 的成功回执当作执行终态。
- 已补客户端绑定耗时扣租期，低于最小窗口时不提交。机制与证据见[本轮 Runtime 修复](../../bug-fixes/2026-09-18-runtime-streaming-terminal-and-lease.md)。三项生产接线仍待实现，不能仅注册 start/status/cancel 后宣告完成。

## 交付

通过本任务验证后更新完成记录和 status；未通过的设备/账号/发行项明确列出。允许已授权且验证通过的切片具名本地提交，不 push、合并或发布，不夹带其他任务改动。

## 2026-09-19 结果回收准入切片

`ExecutionOwnership.acquireReconciliation` 仅供宿主在校验原 session/turn/call 绑定后使用，要求执行身份及 generation 精确匹配且启动调用已退出。结果回收与导入期间保持互斥；结算写入失败或导入异常离开时保留持久 owner，允许原任务重查。即使持久 owner 已清除，当前回收许可关闭前仍不接受其他执行。此入口不授予工具权限，尚未接入生产异步控制工具。

验证：`./gradlew spotlessApply :tools:framework:test --tests com.helix.tools.framework.ExecutionOwnershipTest` 通过，11 项、0 失败/跳过；随后 `./gradlew :tools:framework:test detekt spotlessCheck` 通过。均通过共享 host slot 执行。曾误用不存在的模块级 `:tools:framework:detekt`，任务选择失败；已改用项目根 `detekt` 并通过。这是主机定向验证，不代替产品接线、设备验收或整项完成。

## 2026-09-19 后台身份与提交复查

新增后台绑定 v3，明确 `DETACHED` 类型并按可信当前会话、原 ToolCall、原 Turn 校验；旧同步绑定 v1/v2 仍可读，但不能作为后台控制凭据。写入绑定同时检查调用会话与持久 Turn 的归属。此入口尚未注册为异步工具，不能据此声明产品接线完成。

`DetachedJobClient` 在冷绑定后、发送事务前重查取消及调用方实时剩余额度，只缩短窗口。取消或不足 1 秒返回明确拒绝，不产生 Runtime Job。新增 4 项主机身份边界测试通过；既有会话权限复查 8 项通过。

验证通过共享 host slot 执行 `bash scripts/debug/2026-09-19/run-detached-control-gates.sh hxa196-control-v2`，完整主机 `check-all.sh --all`、developer 测试 APK 构建、API29/36 Job 回归各 19 项通过（38/38），两个独占模拟器均正常退出、双 API APK 哈希一致。证据：`build/hxa196-control-v2.log` 与 `build/hxa196-control-v2-summary.json`。首次静态检查因拒绝分支增加达到方法复杂度阈值失败；合并同类拒绝处理后完整重验通过，未放宽阈值或跳过用例。

预算接线仍需处理：`GoalRunSettlement` 目前会在 Turn 终态调用 `recoverRun` 结算全部 pending reservation；后台租期不能直接作为普通 TIME 预留加入，否则会提前结算或重复计费。Runtime 当前同步 watchdog 使用墙钟，准备/排队后启动前也需再次判断剩余窗口。上述事实是后续实现依据，不构成后台 Goal 预算已完成的证据。

## 2026-09-19 Runtime 单调执行窗口

Runtime 在提交入口采样 `elapsedRealtime`，排队、输入准备、启动前检查、watchdog 和兜底等待共享一个 `JobExecutionWindow`。墙钟仍用于持久显示时间，但不再控制进程截止。输入准备后以及真正启动前检查取消/过期；预算过期不启动进程，记录 `TIMED_OUT`，同时取消则按用户取消结算。

新增 3 项 JVM 边界测试覆盖准备耗时、单调时钟回退和大值；新增设备用例通过延迟 pipe 输入到预算结束后，验证终态 `TIMED_OUT`、无进程退出码、无标准输出且输出归档为空。共享 host slot 下执行 `bash scripts/debug/2026-09-19/run-detached-control-gates.sh hxa196-monotonic-v1 20 5666`：完整主机门禁通过，API29/36 各 20 项 Job 回归通过（40/40），两个独占模拟器正常退出、APK 哈希跨 API 一致。日志 `build/hxa196-monotonic-v1.log`，汇总 `build/hxa196-monotonic-v1-summary.json`。这是上述墙钟/准备窗口缺口的新证据；没有改变 Goal 时间账本，也未注册异步生产工具。

## 2026-09-19 Goal 后台时间账本

既有用量表增加 `TIME_LEASE` 类型，不新增表或另一套 Goal 状态机。租期最多预留 30 分钟并受 Goal 剩余时间限制；同一 run 的普通 TIME 与 TIME_LEASE 不可同时占用计时权，调用方必须显式转移原窗口。部分 checkpoint 使用累计观察时长，减去已结算值后再增加 Goal 用量；重复/较旧观察不重复收费，终态释放未消费容量。观察到的取消超时仍完整记录，不静默截断。

普通 Turn 终态不再把仍 pending 的后台时间预留按进程死亡处理，Goal run 保持待结算；原 Job 结算后再次调用既有 `GoalRunSettlement`，沿原来的完成/暂停标准收口。进程死亡恢复则一次性消耗未知工作的剩余预留，不累加离线墙钟、不重复收费或重启命令。用量、预留和审计仍在同一 Room 事务内；失败回滚两边。字段语义已在 `GoalUsageReservationEntity` 注明，表结构不变。

验证：共享 host slot 执行 `bash scripts/debug/2026-09-19/run-goal-lease-gates.sh hxa196-goal-lease-v1`。完整主机 `check-all.sh --all`、双 flavor 测试 APK 构建通过；`GoalTimeLeaseDeviceTest` 新增 6 项，与既有用量 9 项、run 协调 10 项一起在 API29/36 × consumer/developer 各跑 25 项，共 **100/100**。四个独占模拟器正常退出，同 flavor APK 哈希跨 API 一致。证据：`build/hxa196-goal-lease-v1.log`、`build/hxa196-goal-lease-v1-summary.json`。首次 compile/静态检查因准入条件复杂度达到阈值失败；将计时归属判断归入既有容量检查后重新跑完整门禁通过。

**生产边界**：此切片是账本及结算适配，尚无生产后台启动器创建 TIME_LEASE；`GoalTimeBudget` 的窗口转移/heartbeat、Runtime 终态真实耗时、异步工具和 Tasks 接线仍待完成。累计观察必须来自可信计时器并覆盖 Turn 与 Job 时间区间的并集，不能把两个重叠计时器求和。不能把此次受控 Room/设备测试称为后台 Goal 产品旅程或真机后台验收。

## 2026-09-19 Goal 计时权转移

`GoalTimeBudget.transferToLease` 在同一事务中结算普通 TIME 并预留 TIME_LEASE；拒绝或重复身份回滚原账本和窗口。heartbeat 与转移串行化，按累计单调时长计费。原 Job 的不可变 allocation 与 Goal 活动计时预留分离：短至 1 秒的 Job 仍保留正常 heartbeat 的短窗口，但不会延长 Runtime 租期。Turn 结束释放额外 heartbeat 容量，只保留原 Job 剩余预留；原 Job 已证明停止时可回到普通 TIME 窗口。

新增 `GoalLeaseClockDeviceTest` 8 项覆盖累计计费、事务回滚、原身份完成、短租期、预算截断、无效窗口及 heartbeat/转移竞争。共享 host slot 执行 `bash scripts/debug/2026-09-19/run-goal-lease-gates.sh hxa196-goal-clock-v3 5626`：完整主机门禁、双 flavor 测试 APK 构建通过，API29/36 × consumer/developer 各 33 项，共 **132/132**，四个独占模拟器正常退出，同 flavor APK 哈希跨 API 一致。证据为 `build/hxa196-goal-clock-v3.log` 和 `build/hxa196-goal-clock-v3-summary.json`。

初次运行因格式及返回分支静态检查失败，修复后 v2 通过 128 项；随后审查补上 1 秒租期与首次 heartbeat 的边界，以上 v3 为最新代码的重新验证，不累计旧结果冒充新覆盖。

**剩余边界**：此处完成真实计时器与账本的转移接口，仍无生产异步工具调用该接口。Runtime 终态可信累计耗时、原 Job 完成后的结算回调、工具注册、Tasks 投影及结果导入仍待接线；上述受控设备测试不代替后台产品旅程或物理设备 HOME/锁屏/Doze 验收。

## 2026-09-19 Runtime 终态单调耗时

Runner 在提交时保留同一实例的单调执行窗口，发布终态时记录从准入到终态发布的 `elapsedDurationMs`，包含排队、准备和产物整理，不使用墙钟相减，也不截断已观察到的超时。进程死亡后的 orphan sweep 没有原窗口，耗时保持未知；旧记录同样不补造时间。该字段只允许出现在终态，使用 record schema v2 并纳入 terminalCommit；无该字段的 v1 记录继续按原格式和哈希读取、编码与对账。

新增 2 项 codec 测试验证版本/状态约束、耗时篡改和旧格式身份，新增 1 项窗口测试覆盖超时耗时、时钟回退和大值。既有后台设备旅程加入成功、超时及 Runtime 死亡的耗时断言。共享 host slot 执行 `bash scripts/debug/2026-09-19/run-detached-control-gates.sh hxa196-duration-v2 20 5606`：完整主机门禁通过（record 测试 9/9、窗口测试 4/4），API29/36 Job 回归各 20 项，共 **40/40**；两个独占模拟器正常退出、APK 哈希一致。证据：`build/hxa196-duration-v2.log`、`build/hxa196-duration-v2-summary.json`。

首次完整门禁触发现有 Runner 的类大小阈值；将无实例状态的进程树查询提取为 `ProotProcessTree` 后重新通过，未放宽检查。审查还确认 `publishTerminal.withStopReason` 已统一处理租期到期终态，未将该已有行为误列为新修复。

**剩余范围**：这提供 Runtime 自身的耗时证据，尚未对齐宿主提交前区间与 Goal 共享计时，也没有生产异步工具、Tasks 投影和完整结算链路。不能把 Runtime 时长直接与重叠的 Goal heartbeat 求和；旧记录及 orphan 耗时未知仍走保守恢复。真机 G4 保持待验。

## 2026-09-19 原 Job 控制准入

`ExecutionOwnership.controlExecutor` 为宿主提供绑定控制执行器：仍由 Dispatcher 的 executor guard 获得准入，不能直接执行；可信 resolver 先校验原身份，有持久 owner 时仅匹配的 generation 可查询/取消。无 owner 的历史查询也占用普通执行许可。跨宿主实例、外来 owner、取消和查询异常都不释放原占用；结算清除身份后，当前许可退出前仍拒绝其他执行。普通工具不因名称、只读分类或模型参数获得豁免。

`DetachedJobControl.create` 连接 Room 的 `resolveDetached` 与真实 Runtime query/cancel，输入使用可信当前会话和原调用 ID，返回有界执行事实及 `settlementPending`。再次核对返回的 job/execution/inputHash，不重跑、不续租、不导入产物、不因进程终态自动清除占用。输入原绑定不存在或属于其他会话时，在访问 Runtime 前拒绝。

**接线范围**：这是可执行的宿主控制组件，尚未注册为模型工具或接入 Tasks。异步启动、Goal 预算与最终产物结算须整合后才可宣称后台产品功能完成。

验证：新增 4 项执行占用单测，`ExecutionOwnershipTest` 共 15/15；新增 `DetachedJobControlDeviceTest` 2 项使用真实 Runtime 和测试 Room 原绑定，覆盖占用期间查询/取消及跨会话/不存在原调用拒绝。共享 host slot 执行 `bash scripts/debug/2026-09-19/run-detached-control-gates.sh hxa196-bound-control-v2 22 5586`：完整主机门禁通过，API29/36 各 22 项，共 **44/44**，两个独占模拟器正常退出，APK 哈希一致。证据：`build/hxa196-bound-control-v2.log`、`build/hxa196-bound-control-v2-summary.json`。前置静态检查分别发现返回分支数和嵌套深度超限，整理准入分支并提取普通执行路径后重新通过，未放宽检查。以上不代替模型工具全链路或真机 G4 验收。

## 2026-09-19 异步启动执行器

`DetachedJobLaunch` 复用现有 `LinuxInputSnapshot`、环境筛查与授权复查接口，准备输入后由宿主回调持久化原绑定并分配预算，再转移当前调用的独占执行身份，最后通过真实 `DetachedJobClient` 提交。返回 accepted 只表示接收，不表示执行完成，不自动继续 Goal。租期同时扣除快照、宿主准备、占用落盘、PFD 打开和客户端绑定耗时，不能通过提交阶段延长已分配窗口。

宿主临时目录只保存输入快照，提交后清理；输出接收端使用 `/dev/null`，最终归档仍由 Runtime 的既有结果存储持久化并按原 Job 查询和校验。确认未启动的拒绝先调用宿主拒绝结算，再通过原活跃启动许可释放占用；该许可退出前其他执行仍不能进入。未知、非法或不识别的返回保留原占用并要求核对原调用，不重放命令。

接线审查修正 Runtime 返回语义：提交交接给 runner 后发生异常，返回 `REPLY_JOB_UNAVAILABLE`，不能把可能已经执行的 Job 伪装成拒绝启动；交接前失败仍可返回明确拒绝。通用 `INVALID_SPEC` 不作为释放宿主占用的无副作用证明。

**当前边界**：执行器已有真实 Runtime 启动/产物获取的设备验证入口；预算 prepare/reject 仍是必须由宿主接入的回调，尚未连接真实 Goal 计时器，也没有模型工具注册、Tasks 展示和产物最终导入结算。测试使用现有同步参数解析器驱动执行器，不能据此宣称新的后台工具契约已交付。

验证：新增 `DetachedJobLaunchDeviceTest` 4 项，使用真实 Runtime 验证异步接收后的原绑定和可获取归档、权限复查拒绝、额度不足回退、FGS 拒绝后的占用释放。Goal prepare/reject 在这些测试中为受控回调，不代表真实 Goal 账本接线；交接后异常的协议修正来自调用链审查，未声称新增异常注入旅程已通过。新增主机准入测试覆盖仅原活跃调用可释放确认未启动的 owner，`ExecutionOwnershipTest` 共 16/16。共享 host slot 执行 `bash scripts/debug/2026-09-19/run-detached-control-gates.sh hxa196-launch-v1 26 5574`：完整主机门禁通过，API29/36 各 26 项，共 **52/52**，独占模拟器均正常退出、APK 哈希一致。证据为 `build/hxa196-launch-v1.log`、`build/hxa196-launch-v1-summary.json`。前置静态检查发现测试嵌套过深，提取产物验证辅助方法后完整重验通过。

## 2026-09-19 Goal 宿主预算桥接与后台工具契约

新增 `GoalDetachedBudget`，ChatService 提供对应 prepare/reject 宿主入口。普通 Turn 没有 Goal 绑定时沿用已批准窗口；存在 Goal 绑定却没有活跃计时器时返回零额度，不降级为无预算。租期元数据写入发生在 `GoalTimeBudget.transferToLease` 的同一 Room 事务中，写入失败回滚账本且不切换内存窗口。拒绝提交时优先恢复活跃计时器的普通 TIME；若原 Turn 已退出，则按同一启动调用的单调时间结算原预留，并在原 Turn 终态时重试既有 Goal run 结算，不创建新 run。此 reject 入口仅用于原活跃启动器的明确未启动回执，不用于重启后的未知 Job。

`DetachedJobTools` 定义 start/status/cancel 契约，尚不注册：start 通过 `leaseSeconds` 绑定默认 300 秒、最大 1800 秒租期，提交调用仍有独立短超时；status/cancel 只接受 `originalCallId`，会话来自可信调用上下文。复用 Linux 参数解析时同步入口默认不变；后台解析仍拒绝已过期提交调用。会话效果分类将 start 与原 Shell 使用同一规则，保留 rm 命令精确审批；cancel 只标明命令控制，不假定它会执行新的任意 Shell。

**剩余接线**：以上入口及契约尚未加入生产 Registry，异步启动器还没有调用 ChatService 的预算入口。Job 正常终态还需将 Runtime 与宿主时间区间对齐，接入产物获取/导入和原 run 最终结算；之后再注册工具与接 Tasks。只有这些链路及对应设备旅程完成，才能声明后台 Goal 产品能力交付。

验证：新增 Goal 桥接设备用例 6 项，覆盖原时间段只计一次、迟到拒绝、计时器缺失、跨会话拒绝、元数据写入回滚和普通 Turn；新增后台工具契约主机测试 3 项及效果分类用例。共享 host slot 执行 `bash scripts/debug/2026-09-19/run-goal-detached-bridge-gates.sh hxa196-bridge-v2`：完整主机门禁通过，Goal API29/36 × consumer/developer 各 39 项，共 **156/156**；同一构建的 Runtime API29/36 各 26 项，共 **52/52**。六个独占模拟器正常退出，汇总检查 APK 哈希一致。证据：`build/hxa196-bridge-v2.log`、`build/hxa196-bridge-v2-goal-summary.json`、`build/hxa196-bridge-v2-job-summary.json`。首次设备运行因测试跳过 CANCELLING 而违反 Turn 状态转移规则失败，修正夹具后按真实取消路径完整重验通过；未放宽生产状态机。前置静态检查的说明文本行长也已修正。

## 2026-09-19 正常终态时钟与 Goal 结算

Runtime 终态记录 v3 在原可校验耗时之外保存同次 `elapsedRealtime` 结束点，二者一并进入 terminal commit；旧 v1/v2 仍保持原编码与哈希，Runtime 死亡产生的 orphan 不虚构结束点。宿主以原 allocation 起点与已验证终态结束点结算：Job 晚于 Turn 时计到 Job 结束，Turn 更晚时保留已计的重叠区间并集，迟到查询不增加运行时间。活跃原 Turn 结算后恢复普通 TIME；未知耗时只消耗该 lease 的剩余预留一次，不顺带结算其他模型或工具预留。lease 已结算但 run 收口失败时，重试仍会调用既有 GoalRunSettlement。

新增 5 项 Goal 设备用例覆盖上述时序、未知耗时及不一致时钟；新增协议主机用例覆盖 v3 round-trip、篡改拒绝及 v2 哈希兼容。既有真实 Runtime 设备用例额外检查成功终态时钟与 orphan 未知值。共享 host slot 执行 `bash scripts/debug/2026-09-19/run-goal-detached-bridge-gates.sh hxa196-terminal-clock-v1`：完整主机 `check-all.sh --all` 通过；Goal API29/36 × consumer/developer 各 44 项，**176/176**；同一构建 Runtime 双 API 各 26 项，**52/52**。六个独占模拟器正常关闭，同 flavor APK 哈希一致。证据：`build/hxa196-terminal-clock-v1.log`、`build/hxa196-terminal-clock-v1-goal-summary.json`、`build/hxa196-terminal-clock-v1-job-summary.json`。

本切片提供宿主 settle 入口，其调用前提是校验原 Job 终态并完成结果效果；尚未接入结果收取执行器或 Registry。剩余实现仍是结果获取/导入与授权复查、启动和结算回调接线、工具注册及 Tasks 投影；之后执行对应产品旅程与真机 G4，不将本切片记为 HXA-196 完成。

## 2026-09-19 原结果收取与重试回执

`DetachedJobCollection` 经同一个 ExecutionOwnership 原绑定控制入口执行，查询真实 Runtime 并核对 job/execution/input hash；运行中及 ORPHANED 不释放 owner。成功结果复用 ProotResultStore 校验和持久保存原归档，输出效果成功后写入固定原调用回执，再结算 Goal、请求 Runtime ACK、释放占用。导入异常或预算结算异常保留 owner；前者重取原归档，后者从持久回执跳过已完成的输出效果但重试预算结算。正常失败终态按原失败状态/退出码收口，不伪造成功归档。ACK 不可用与执行失败分开，不触发重跑。

验证：共享 host slot 执行 `bash scripts/debug/2026-09-19/run-detached-control-gates.sh hxa196-collection-v1 29 5658`，完整主机 `check-all.sh --all` 通过，API29/36 各 29 项 Job 回归，**58/58**；两个独占模拟器正常关闭、APK 哈希一致。新增 `DetachedJobCollectionDeviceTest` 3 项使用真实 Runtime、Binder、Room 与归档，覆盖导入失败重试、预算失败后重建收取器沿持久回执继续、跨会话拒绝与命令失败终态。证据：`build/hxa196-collection-v1.log`、`build/hxa196-collection-v1-summary.json`。

范围边界：测试中的输出与预算回调是受控故障注入，尚非原 output 目标授权复查与 ChatService Goal 的完整产品接线；组件尚未注册模型工具。下一步复用既有 Workspace 输出导入并按当前授权校验原目标，将启动、收取与宿主预算回调注册，再接 Tasks 和产品旅程。本轮不关闭 HXA-196。

## 2026-09-19 原输出目标导入与权限复查

新增 `DetachedJobOutput`，只从原 ToolCall 持久参数读取 output，不接受收取请求替换目标。复用 SessionPermissionService 检查原工具禁用及目标 workspace/external 文件写入 DENY，解包后、实际写入前再次检查。原启动已经授权的同一延后效果不因 ASK 重复消费审批；scope 的可访问性、路径 containment、原子发布与容量约束仍由 WorkspaceArtifactStore 执行。同步与后台路径共享 `LinuxOutputImport` 的 result.txt 大小/hash/manifest 校验及写入，没有复制导入实现。持久收取回执避免重复收取覆盖后来用户编辑的文件。

新增 2 项真实 Runtime → 归档 → 原 Workspace 目标设备用例，覆盖新 DENY、工具禁用、恢复许可后重试和收取后用户编辑保护。`bash scripts/debug/2026-09-19/run-detached-output-gates.sh hxa196-output-v3` 的完整主机门禁通过，双 API Job **62/62** 通过；后续同步回归暴露旧夹具缺少 session/turn 身份，以及损坏注入在新提交前取消检查中提前等待尚不存在 Job 的问题。修正测试入口身份，去掉 bind 中补写身份；故障注入用独立观察客户端确认原 Job 已登记后再等待终态，仍断言损坏初传、原结果恢复及只提交一次，未放宽生产校验或删测。

修正后共享 host slot 执行 `bash scripts/debug/2026-09-19/run-linux-output-gates.sh hxa196-output-sync-v4`：最新完整主机门禁通过，同步双 API **14/14**。Job 和同步组的生产 APK 哈希相同；测试 APK 因夹具修正不同，分别保留证据。四个最终独占模拟器均正常关闭。证据：`build/hxa196-output-v3-jobs-summary.json`、`build/hxa196-output-sync-v4-summary.json` 及对应日志。首轮编译缺少 rmCommandHit 参数和静态返回分支检查已修正，后续门禁通过。

当前输出组件已经使用真实权限/Workspace 导入，但仍未注册后台模型工具；启动、收取与 ChatService 预算回调的生产组合、占用期间闭合元数据工具准入、Tasks 及产品旅程继续属于 196 剩余范围。真机 G4 仍单列，不以本轮模拟器回归关闭任务。

## 2026-09-19 developer 生产注册与宿主组合

`DefaultAppContainer` 将唯一 ExecutionOwnership 与延迟 ChatService accessor 传给 flavor seam；developer 的 DetachedJobRegistration 注册 start/status/cancel/collect，consumer 仍不注册 Runtime 工具。生产启动组合真实提交前权限复查、持久绑定和 ChatService prepare/reject；collect 组合真实原目标导入、持久回执及 ChatService settle。注册只连接依赖，不调用预算宿主或启动 Runtime。

新增 collect 契约只接受 originalCallId，和只读 status 分开。主 Dispatcher 的 SessionToolEffectClassifier 通过可信 session 与原绑定读取 output，分类原目标 workspace/external 文件写；模型不能以新的 output 字段替换目标。无宿主解析器的分类保留未知文件效果，不错误声称只读。旧同步解析/执行和 consumer seam 不变。工具使用契约见[终端文档](../../architecture/terminal.md)。

验证：共享 host slot 执行 `bash scripts/debug/2026-09-19/run-detached-control-gates.sh hxa196-registration-v3 32 5658`，完整主机 `check-all.sh --all` 通过；双 API Job **64/64**，两个独占模拟器正常关闭、APK 哈希一致。新增注册入口用例通过真实注册表 executor、真实 ChatService 的普通 Turn 预算入口、真实 Runtime/Binder/Room/Workspace 完成 start→status→collect，并验证注册阶段不读取预算宿主、执行期间占用与收取后释放。新增权限分类主机用例覆盖可信原目标、模型替代参数无效及无 resolver 的未知效果。证据：`build/hxa196-registration-v3.log`、`build/hxa196-registration-v3-summary.json`。前两轮静态方法长度/复杂度及格式检查失败，按职责提取函数后重验通过，未放宽检查阈值。

边界：设备用例直接调用已注册 executor 并经真实 ownership guard，不冒充模型经完整 Dispatcher 的审批/Goal 产品旅程；Goal 绑定回调及相关账本另有切片证据。196 仍需后台占用期间闭合元数据工具准入、Tasks 投影、完整 Dispatcher/Goal/恢复旅程和真机 G4，不能以注册完成关闭任务。

## 2026-09-19 后台占用期间的闭合元数据准入

ExecutionOwnership 增加仅由宿主组合的私有 metadata executor 包装。DefaultAppContainer 明确应用于 Goal lifecycle/report、Plan、Todo；普通工具即使同名或自报 READ_ONLY/METADATA 也不因此获得准入。包装须经同一个 ownership guard，要求可信 session/turn，取消在进入前拒绝；没有执行许可、owner 转移/释放或 Runtime 调用。Dispatcher 的权限/预算/审计以及内置工具自身绑定校验保持原路径。

同时补齐已有“完成前结算工作”的约束：pending TIME_LEASE 时 `goal.report` / `update_goal` 的 complete 返回拒绝，进度读取/上报继续可用。否则在放开元数据后，模型可能先报告 complete，再由后续 Job 结算消费旧报告。该检查不推断任务质量、不创建新 Goal 完成机制；原 Job 收取后仍由模型检查并报告、既有 GoalRunSettlement 收口。

验证：新增 2 项 ExecutionOwnership 主机用例覆盖同名普通工具不得绕过、原宿主/可信绑定/取消边界、对账期间元数据可用和 owner 不变；新增 2 项真实 Room 设备用例覆盖占用期间 Goal report/lifecycle 读取、进度、外会话拒绝及完成时序。共享 host slot 执行 `bash scripts/debug/2026-09-19/run-goal-lease-gates.sh hxa196-metadata-v2 5642`：完整主机 `check-all.sh --all` 通过，Goal API29/36 × consumer/developer 各 62 项，**248/248**；四个独占模拟器正常关闭、同 flavor APK 哈希一致。证据：`build/hxa196-metadata-v2.log`、`build/hxa196-metadata-v2-summary.json`。首次静态检查因 Goal update 方法长度/复杂度失败，提取已有 report 分支后完整重验，未放宽阈值。

本轮占用测试使用受控 owner，Goal 存储与结算是真实实现；不冒充后台 Runtime 与完整模型审批的产品旅程。196 当前继续 Tasks/命令详情的后台投影与操作入口、完整 Dispatcher/Goal/恢复旅程、真机 G4。未关闭任务，也未推进下一 HXA。

## 2026-09-19 后台命令详情与结算凭据

后台 `code.linux.job.start` 已进入既有聊天工具行和 Tasks 命令列表的详情入口。启动调用完成后显示“已提交，当前执行状态待查询”，即使原 Turn 已结束也不宣称 Job 成功或仍在运行。显式 status/cancel/collect 校验原绑定后，将 Runtime 不可变终态写为固定 ID 的本地审计凭据；产物导入、Goal 预算结算与占用释放完成后另记结算凭据。重复观察/收取核对同一 terminalCommit，失败保留待结算事实。

命令详情分别呈现执行终态和宿主结算状态；取消/失败也可能尚待结算，orphan 继续显示未知。后台终态不再继承启动回执的摘要或退出码，未收取时不误报“无输出”。浏览只读本地绑定、凭据和已验证归档，不冷绑定 Runtime、不 ACK、不重放。这里没有新增 Job 状态机或隐式模型续轮。

新增 4 项纯投影测试（两个 flavor 均通过），在真实 Runtime/Room 的既有导入失败和预算失败旅程中补充持久投影断言。新增页面旅程使用持久提交回执 fixture，验证已结束 Turn 下的 SUBMITTED/待结算展示及浏览前后记录不变；它不是完整模型启动旅程。页面回归同时覆盖旧同步命令、导航和 Activity 重建。

最终验证均经共享 host slot：

- `bash scripts/debug/2026-09-19/run-command-browse-regression.sh hxa196-observation-ui-v5 5658`：完整 `check-all.sh --all` 与双 flavor 测试 APK 构建通过；API29/36 的 consumer 各 5 项、developer 各 8 项，**26/26**。日志与汇总为 `build/hxa196-observation-ui-v5.log`、`build/hxa196-observation-ui-v5-summary.json`。
- `bash scripts/debug/2026-09-19/run-detached-control-gates.sh hxa196-observation-v5 32 5674`：完整主机门禁再次通过；API29/36 developer Job 回归各 32 项，**64/64**。日志与汇总为 `build/hxa196-observation-v5.log`、`build/hxa196-observation-v5-summary.json`。

六次独占模拟器运行均正常关闭；同 flavor APK 哈希跨 API 一致，Job 回归与页面矩阵的 developer APK/测试 APK 也一致。初次门禁触发对象函数数及方法长度阈值，提取后台状态投影并整理选择入口后通过，未放宽检查；另一次格式检查遇到生成资源并发读取失败，最终完整门禁重验通过。页面首次 API36 consumer 失败定位为旧测试只等页面外壳、未等异步内容就点击会话按钮；已改为等待命令内容并滚动到按钮，最终四象限重新执行通过。旧失败或中间通过不累计为最终验收。

**剩余范围**：Tasks 独立 Job 行与用户查询/取消/收取入口、完整 Dispatcher/Goal/进程死亡恢复旅程、真机 HOME/锁屏/Doze G4。此切片不关闭 196，未推进下一 HXA；仅具名本地提交，不 push、合并或发布。
