# HXA-196：有租期的独立后台命令 Job

分类：待实现。当前是否执行见[实施状态](../status.md)。

前置条件：195；执行授权对齐209；平台可行性。

本次复核：Goal后台续轮已归208；这里是独立进程owner/租期/日志/对账，不强制所有启动出卡。

核对依据：[源码或有效证据](../../adr/runtime/002-terminal-and-jobs.md)。本轮只审查，不新增测试通过或完成声明。

2026-09-18 所有者已授权推进平台与核心切片；当前实现、模拟器证据及剩余产品/真机边界见[切片记录](../../evidence/development/hxa-196-platform-plan-2026-09-18.md)。任务仍保留，不能以核心切片替代整项验收。

当前独立工作树已接入执行线程与持久身份占用机制，验证范围和未接线部分见[执行占用切片](../../evidence/development/hxa-196-execution-ownership-2026-09-18.md)。

继续修正同步提交的准备/冷绑定扣时和客户端 PFD 回收，见[提交链路记录](../../evidence/development/hxa-196-submission-resources-2026-09-18.md)。后台产品接线须复用这些资源与预算边界；不据此关闭异步工具和 Goal 结算范围。

## 范围与验收要求

- 依赖：195；可先做 FGS/owner Spike，生产接线需记录实际Android后台启动/拒绝/租期回收的平台证据；相关产品条款已经接受。
- 允许：上述 Runtime/app 模块，`core:agent` 预算/绑定适配、`core:storage` 迁移、`tools:framework` 仅必要契约，测试/docs。不改变 Goal 自动继续或完成标准。
- 新异步启动返回持久 Job 引用；观察/取消权限由绑定限定，不能枚举其他会话输出。复用现有任务页，新增 Job 投影而不是复制 Turn；用户取消优先于完成竞态，最终只结算一次。
- detached 前先持久记录所有权和租期，再允许 UI 离开；若无法建立合法后台路径，不宣称“后台运行中”。保留旧同步工具 v2 的语义，不能一律返回“accepted”破坏原调用。
- 候选新工具名在开始时固定到 schema/工具文档，启动经当前会话授权解析；仅ASK或专项rm规则需要精确审批；观察只读取已授权任务，取消幂等且不能启动新任务。日志/任务完成不自动发起模型新 Turn。
- 测试：启动前取消、审批过期、额度不足、UI detach、主进程死亡、Runtime 死亡、租期跨重连、退出/取消竞态、FGS 拒绝、退后台降级、结果导入失败重查不重跑。
- 验收：G1～G4；新增 `ProotDetachedJobDeviceTest`。真实物理设备后台/锁屏/Doze 单列；缺设备时保留待验收，不阻断其他独立实现，也不记此 HXA 完成。

## 验证入口

[公共验收规则](../verification-matrix.md)规定默认/外部测试边界。具体类和 runner 必须实际存在且执行非零；本文件列出的新增用例是实现要求，不是现有证据。

任务验收条目（新增测试在交付前必须实现并执行）：

| 任务 | 验收维度 | 命令或证据入口 |
| --- | --- | --- |
| HXA-196 | 租期/预算/owner 死亡/后台拒绝；ProotDetachedJobDeviceTest + 真机 HOME/锁屏 | PARTIAL：平台/核心已合入，模拟器切片证据见上；生产接线与真机 G4 尚未完成 |

## 核心整合后的剩余范围

继续保留一个 HXA，不用新的规划器或工作流拆出第二套状态源：

1. 固定异步 start/status/cancel 工具契约，沿既有 Dispatcher 处理 schema、能力、209 会话授权、ToolCall 审计及预算；提交前持久化绑定，额度不足不启动。旧同步工具语义保持。
2. 将持久 Job 引用接入现有 Tasks/命令详情只读投影，按 session/turn/toolCall/job/execution/inputHash 关联；查询/取消不得跨绑定，完成不自动发起模型新 Turn。
3. 复用既有输出验证与产物导入，区分执行终态与结果导入失败；重查只回收原结果，不重新运行命令。补授权过期、取消竞态、导入失败重查及 UI 离开后恢复的产品旅程。
4. 真机 HOME/锁屏/Doze 验证真实后台路径；缺当前物理设备仅保留待验，不用模拟器或旧 Root 验收代替。全部 G1～G4 达标后才关闭本任务。

### 接线前必须覆盖的边界

- 调度器当前只保留 ToolCall 执行期间的互斥。异步 start 返回后，后台 Job 的未知文件/代码效果不能随着调用槽位释放而消失；复用持久 Job 身份与统一效果准入，覆盖跨 Turn、跨会话和主进程重启，不另造业务工作流。
- Goal 的 `remainingExecutionMillis` 是运行时窗口。后台命令提交须扣除快照、连接与排队耗时，且在 Turn 已返回后继续受其已分配预算约束；不能通过重新查询续租。
- Goal 的当前 TIME heartbeat 与后台 Job 的存活区间可能重叠。接线须明确持久预算预留和结算归属，既不漏掉 Turn 返回后的执行，也不把重叠区间重复计费；不能简单把整个租期再加到已有 Goal 时间上。未知副作用恢复与既有 Goal 完成标准保持。
- 现有结果恢复只识别同步 `bash`/`code.linux.run`，并检查 Turn/ToolCall 的恢复资格。异步启动成功必须与 Job 最终成功分开，按原绑定查询、导入与取消，不把 start 的成功回执当作执行终态。
- 已补客户端绑定耗时扣租期，低于最小窗口时不提交。机制与证据见[本轮 Runtime 修复](../../bug-fixes/2026-09-18-runtime-streaming-terminal-and-lease.md)。三项生产接线仍待实现，不能仅注册 start/status/cancel 后宣告完成。

## 交付

通过本任务验证后更新完成记录和 status；未通过的设备/账号/发行项明确列出。允许已授权且验证通过的切片具名本地提交，不 push、合并或发布，不夹带其他任务改动。

## 2026-09-19 结果回收准入切片

`ExecutionOwnership.acquireReconciliation` 仅供宿主在校验原 session/turn/call 绑定后使用，要求执行身份及 generation 精确匹配且启动调用已退出。结果回收与导入期间保持互斥；结算写入失败或导入异常离开时保留持久 owner，允许原任务重查。即使持久 owner 已清除，当前回收许可关闭前仍不接受其他执行。此入口不授予工具权限，尚未接入生产异步控制工具。

验证：`./gradlew spotlessApply :tools:framework:test --tests com.helix.tools.framework.ExecutionOwnershipTest` 通过，11 项、0 失败/跳过；随后 `./gradlew :tools:framework:test detekt spotlessCheck` 通过。均通过共享 host slot 执行。曾误用不存在的模块级 `:tools:framework:detekt`，任务选择失败；已改用项目根 `detekt` 并通过。这是主机定向验证，不代替产品接线、设备验收或整项完成。

## 2026-09-19 后台身份与提交复查

新增后台绑定 v3，明确 `DETACHED` 类型并按可信当前会话、原 ToolCall、原 Turn 校验；旧同步绑定 v1/v2 仍可读，但不能作为后台控制凭据。写入绑定同时检查调用会话与持久 Turn 的归属。此入口尚未注册为异步工具，不能据此声明产品接线完成。

`DetachedJobClient` 在冷绑定后、发送事务前重查取消及调用方实时剩余额度，只缩短窗口。取消或不足 1 秒返回明确拒绝，不产生 Runtime Job。新增 4 项主机身份边界测试通过；既有会话权限复查 8 项通过。

验证通过共享 host slot 执行 `bash scripts/debug/2026-09-19/run-detached-control-gates.sh hxa196-control-v2`，完整主机 `check-all.sh --all`、developer 测试 APK 构建、API29/36 Job 回归各 19 项通过（38/38），两个独占模拟器均正常退出、双 API APK 哈希一致。证据：`build/hxa196-control-v2.log` 与 `build/hxa196-control-v2-summary.json`。首次静态检查因拒绝分支增加达到方法复杂度阈值失败；合并同类拒绝处理后完整重验通过，未放宽阈值或跳过用例。

预算接线仍需处理：`GoalRunSettlement` 目前会在 Turn 终态调用 `recoverRun` 结算全部 pending reservation；后台租期不能直接作为普通 TIME 预留加入，否则会提前结算或重复计费。Runtime 当前同步 watchdog 使用墙钟，准备/排队后启动前也需再次判断剩余窗口。上述事实是后续实现依据，不构成后台 Goal 预算已完成的证据。

## 2026-09-19 Runtime 单调执行窗口

Runtime 在提交入口采样 `elapsedRealtime`，排队、输入准备、启动前检查、watchdog 和兜底等待共享一个 `JobExecutionWindow`。墙钟仍用于持久显示时间，但不再控制进程截止。输入准备后以及真正启动前检查取消/过期；预算过期不启动进程，记录 `TIMED_OUT`，同时取消则按用户取消结算。

新增 3 项 JVM 边界测试覆盖准备耗时、单调时钟回退和大值；新增设备用例通过延迟 pipe 输入到预算结束后，验证终态 `TIMED_OUT`、无进程退出码、无标准输出且输出归档为空。共享 host slot 下执行 `bash scripts/debug/2026-09-19/run-detached-control-gates.sh hxa196-monotonic-v1 20 5666`：完整主机门禁通过，API29/36 各 20 项 Job 回归通过（40/40），两个独占模拟器正常退出、APK 哈希跨 API 一致。日志 `build/hxa196-monotonic-v1.log`，汇总 `build/hxa196-monotonic-v1-summary.json`。这是上述墙钟/准备窗口缺口的新证据；没有改变 Goal 时间账本，也未注册异步生产工具。
