# HXA-193：包内 Runtime 可复现资产与升级验收

分类：收尾验收。当前是否执行见[实施状态](../status.md)。

前置条件：可独立；干净CI与资产来源。

本次复核：单APK生产接线已存在；不重做UID改造，不再以help输出当执行证据。

2026-09-17 的[CI现场复核](../../evidence/development/ci-runtime-assets-2026-09-17.md)：最近远端首先失败于 setup-android 默认安装旧 `tools` 包，修复分支已显式关闭默认包列表；默认 RootFS 重建另复现旧 xz-libs 缺包。所有者已授权推送验证，匹配归档已发布、远端变量已配置；2026-09-18 已核验运行35242909407的两个job全部通过，PR #1已合入main。升级恢复与默认滚动镜像重建边界仍开放，不把CI通过视为完整HXA完成。

核对依据：[源码或有效证据](../../../.github/workflows/ci.yml)。本规格不构成验收证明；实际检查范围见上述CI现场复核，任务尚未关闭。

## 范围与验收要求

- 单APK集成已实现且有专项设备和完整本地门禁记录；剩余重点是干净环境Runtime资产可复现、实际CI和升级后结果/配置恢复，不重新开发共享UID架构。
- 对照 .github/workflows/ci.yml、build-proot-assets.sh 和资产lock，验证固定归档输入路径与默认重建路径实际选择。旧记录的来源失效须现测后判断，不能把旧失败当当前网络事实，也不能把本地缓存当干净CI。
- 可升级兼容依赖并同步hash/lock/许可证；不降低制品校验。实际执行 verify-integrated-runtimes.py 与 verify-integrated-runtime-apks.py，--help只说明参数不是验收。
- developer私有非导出服务、同UID不同PID、consumer最终dex/native/assets排除与已有取消/对账继续回归；覆盖已有用户数据/旧验证锚在升级后不错误激活。
- 真实订阅行为归190；签名身份与发行归122/121。本任务仍需自身资产/升级/CI证据，不能因分工而删除这部分验收。远端运行需要现有授权，未运行明确保持待验收。

## 验证入口

[公共验收规则](../verification-matrix.md)规定默认/外部测试边界。具体类和 runner 必须实际存在且执行非零；本文件列出的新增用例是实现要求，不是现有证据。

任务验收条目（新增测试在交付前必须实现并执行）：

| 任务 | 验收维度 | 命令或证据入口 |
| --- | --- | --- |
| HXA-193 | 干净资产构建、实际CI、包内组件与升级恢复 | 既有专项与本地全门禁见[接手记录](../../evidence/development/wip-takeover-2026-09-16.md)；剩余证据按本任务收口，不重列旧JGit阻塞 |

## 交付

通过本任务验证后更新完成记录和 status；未通过的设备/账号/发行项明确列出。允许已授权且验证通过的切片具名本地提交，不 push、合并或发布，不夹带其他任务改动。

## 2026-09-18 收口记录（第二轮执行者）

升级恢复验证已闭合：新增 `UpgradeRecoveryDeviceTest`（3 例：上一版本 SUCCEEDED 结果取回且恰好对账一次、PENDING 半成品冷启动结算 INTERRUPTED 不重放、配置/用户数据跨恢复周期存活），并入规范套件（32→35 例）；API36/API29 独占模拟器各 `OK (35 tests)` 0 失败 0 跳过。实际 CI：main 运行 35248674608 实际最终状态 `completed/success`（`runtime-assets`、`verify` 均 success），PR 运行 35242909407 此前已核验通过。本地全门禁（source/build/artifacts、`git diff --check`）与 4 变体 APK 边界全部通过；锁定归档资产路径构建 PASS，未改 lock、未降校验。全部命令、制品 SHA、日志目录与复现入口见[收口证据](../../evidence/development/hxa-193-upgrade-recovery-2026-09-18.md)。

仍未闭合项（外部、既有、明确记录，不阻塞本任务范围验收）：默认滚动镜像 Docker 重建缺锁定 `xz-libs 5.8.3-r0`（本轮现测复现 exit 2）；持久路径为已验证的锁定固定归档（CI 与设备路径均按其执行）。真实订阅行为归 190，签名与发行归 122/121。

建议 status 更新（供协调者整合，本执行者不直接改全局 status/roadmap/index）：HXA-193 标记为完成（本任务范围），注明上述滚动镜像重建外部边界；完成记录见 `docs/completion-records/HXA-193.md`（index 由协调者维护）。
