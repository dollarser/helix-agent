# HXA-202：任务过程与跨页面操作导航

分类：待实现。当前是否执行见[实施状态](../status.md)。

前置条件：可先做只读导航；相关写入口对齐209。

本次复核：TasksScreen与dashboard已存在；补任务到会话/命令/产物/恢复的路径，不重建任务列表。

核对依据：[源码或有效证据](../../../app/src/main/kotlin/com/helix/app/ui/TasksScreen.kt)。本轮只审查，不新增测试通过或完成声明。

## 范围与验收要求

- 允许app/UI/chat/query/runcontrol和必要只读投影，不增新执行器或任务状态枚举。
- 复用Tasks：统一进入对应会话、命令详情、计划审阅、产物和恢复；同一任务采用稳定ID，不按标题合并。
- 状态区分运行、等用户审批、等外部结果、取消中、已结束及待核查；Turn结束不自动标整个Goal完成。不伪造百分比，未知总量用当前动作/已完成步骤。
- 精确停止并显示结算过程；观察和打开页面不启动/重试任务。实时日志接195，未接时说明结束后可见，不冒充streaming。
- 验收：P1+P3；新增 `TaskJourneyDeviceTest`，跨会话、后台返回、旋转、取消竞态、进程恢复以及快捷入口。

### 分工与收口

现有TasksDashboard与任务列表是基础，不要求重新建聚合表。194详情未交付时保留实际可用结果入口，不放死链接；接线待194交付补验。

## 验证入口

[公共验收规则](../verification-matrix.md)规定默认/外部测试边界。具体类和 runner 必须实际存在且执行非零；本文件列出的新增用例是实现要求，不是现有证据。

任务验收条目（新增测试在交付前必须实现并执行）：

| 任务 | 验收维度 | 命令或证据入口 |
| --- | --- | --- |
| HXA-202 | 跨会话导航/精确停止/后台返回/不伪造进度；TaskJourneyDeviceTest | planned；P1/P3 |

## 交付

通过本任务验证后更新完成记录和 status；未通过的设备/账号/发行项明确列出。允许已授权且验证通过的切片具名本地提交，不 push、合并或发布，不夹带其他任务改动。

## 审查后的实施切片（2026-09-17）

1. **状态投影**：从现有 Turn/Goal/审批/结算事实计算展示，不增加持久状态机。除六类展示状态，还保留暂停、失败、用户取消的原因差异；PAUSED 不归为运行。Goal 聚合隐藏子 Turn 后，仍需展示当前 Turn 的等待审批、等待外部结果及取消中；Turn 结束不意味着 Goal 完成。待核查必须来自未知副作用/恢复事实，不将所有失败标为待核查。
2. **跨页入口**：以 sessionId/goalId/turnId/planId 定位，覆盖同标题不同会话。Goal 入口既选择目标会话，也实际切换到聊天页面；切换和观察不得调用 Continue。任务产物按真实归属查询，不能用全局 recent 列表冒充本任务产物，不能因 recent 截断丢失关联。194 未交付沿用现有结果详情；203 的交付操作、204 的新恢复操作留在各自任务。
3. **停止与结算**：停止指定稳定 ID，显示取消中直至持久结算；验证其他任务不受影响、迟到结果不覆盖取消结论。处理重复点击、返回/旋转与目标已结束竞态。打开/刷新/重建页面不得新增 Turn、Goal run 或审批证明；与已获授权的后台自动续跑区分，测试应使用未激活 Goal 并核对前后计数。
4. **验收**：新增 TaskJourneyDeviceTest 与投影单测；覆盖跨会话、后台返回、旋转、取消竞态、快捷入口、真实进程恢复。恢复不能只靠 Activity 重建或重新插入 fixture：同一安装保留数据库，记录不同 PID，核对历史任务、产物归属和无新增执行。API29/36 × consumer/developer 四象限，skip 不计通过；执行前核对制品对应源码。重用独占模拟器 runner，新增矩阵汇总必须把任何非零退出码判为失败。
5. **收口**：执行公共 P1/P3 与主机 --all，记录准确范围。四象限旅程通过不等于全产品设备套件通过；基线失败先修复。只在全部本任务要求满足后写完成记录、更新索引和交接入口；不要因完成一个切片停止整个已授权 HXA。
