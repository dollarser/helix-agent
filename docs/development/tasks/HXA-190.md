# HXA-190：订阅 Provider 能力与真实账号验收

分类：收尾验收。当前是否执行见[实施状态](../status.md)。

前置条件：可独立；真实账号条件。

本次复核：旧APK安装、文件路径、压缩和主题大包拆出；本任务只收口订阅行为与剩余服务证据。

核对依据：[源码或有效证据](../../evidence/development/hxa190-physical-update-2026-09-10.md)。本轮只审查，不新增测试通过或完成声明。

## 范围与验收要求

- 只收口订阅 Provider 的目录、模型元数据、thinking/工具/图像/推理档位一致性、连接与真实生成，以及登录/刷新/失效/重连结果。已验证匿名/夹具与真实付费账号结果分别记账。
- 对照既有 Provider 修复和真实账号记录，逐供应商列出缺项；不可用账号保留外部验收，不能用stub关闭。没有必要为本任务重做普通文件相对路径、压缩显示、Goal额度或主题。
- 包内模块生命周期与制品归193，首次配置/修复导航归205；不恢复独立APK安装入口。consumer排除和同UID凭据边界沿用当前契约。
- 允许 app/provider、provider/api、runtime/cli 客户端与适配器和对应测试；真实服务必须显式授权和配置profile，失败/跳过如实记录。
- 完成条件是本次所选供应商清单的目录/能力/协议和生命周期证据齐全；暂停供应商单列未验收，不把整个订阅生态都混入无限任务。

## 验证入口

[公共验收规则](../verification-matrix.md)规定默认/外部测试边界。具体类和 runner 必须实际存在且执行非零；本文件列出的新增用例是实现要求，不是现有证据。

任务验收条目（新增测试在交付前必须实现并执行）：

| 任务 | 验收维度 | 命令或证据入口 |
| --- | --- | --- |
| HXA-190 | Codex目录/能力/协议与包内模块体验 | 主机、独占设备、真实账号合成请求与模块恢复；同 UID 边界如实披露 |

## 交付

通过本任务验证后更新完成记录和 status；未通过的设备/账号/发行项明确列出。允许已授权且验证通过的切片具名本地提交，不 push、合并或发布，不夹带其他任务改动。
