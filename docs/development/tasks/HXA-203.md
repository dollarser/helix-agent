# HXA-203：产物可用性与文件交付闭环

分类：待实现。当前是否执行见[实施状态](../status.md)。

前置条件：202导航契约；可独立实现产物服务。

本次复核：ArtifactsScreen、scope与导出基础已存在；不扩成通用文档解析或远程Git项目。

核对依据：[源码或有效证据](../../../app/src/main/kotlin/com/helix/app/ui/ArtifactsScreen.kt)。本轮只审查，不新增测试通过或完成声明。

## 范围与验收要求

- 允许app产物/UI/files、tools/files、core/workspace/storage相关引用与测试，不扩展Git远程操作。
- 复用真实scope、Artifact ID、写入Turn、hash及现有打开/导出；首屏展示文件名、类型、来源和可用性。支持返回产生它的任务。
- 缺文件/权限撤销/内容变化分别显示；只在实际操作成功后报告已打开/已导出。用户导出走已有SAF/分享授权，不扩大Agent范围。
- 大文件有界预览、格式不支持明确回退下载/外部打开；文档提取只有实际解析成功才报告成功，保留解析失败/截断原因，不将文件名当正文。
- 验收：P1+P3；新增 `ArtifactDeliveryDeviceTest`，同名跨scope、旧数据、替换、撤权、Unicode、大文件、取消导出和外部查看器不存在。

### 分工与收口

作为批次A出口，补验会话→任务→命令详情/产物→来源任务的完整路径，沿用202/194稳定身份；不以全局产物页可打开代替任务归属验收。

支持现有格式的预览/导出与真实scope导航；本任务不新增通用PDF/Office/OCR解析器，不把“不支持格式”当可伪造内容的理由。通用解析另需格式/资源/许可证及独立验收范围。Git只读状态/diff已有基础，远程写入/凭据不在本任务。

## 验证入口

[公共验收规则](../verification-matrix.md)规定默认/外部测试边界。具体类和 runner 必须实际存在且执行非零；本文件列出的新增用例是实现要求，不是现有证据。

任务验收条目（新增测试在交付前必须实现并执行）：

| 任务 | 验收维度 | 命令或证据入口 |
| --- | --- | --- |
| HXA-203 | 同名跨scope/打开导出/撤权/大文件/失败；ArtifactDeliveryDeviceTest | planned；P1/P3，存储变更加P2 |

## 交付

通过本任务验证后更新完成记录和 status；未通过的设备/账号/发行项明确列出。允许已授权且验证通过的切片具名本地提交，不 push、合并或发布，不夹带其他任务改动。
