# HXA-206：核心产品闭环综合验收

分类：集成验收。当前是否执行见[实施状态](../status.md)。

前置条件：191、192、209、202～205、207；所选范围。

本次复核：删除工具级ASK/ALLOW/DENY固定用例，使用工具二态+预设/CUSTOM；终端全套由199负责。

核对依据：[源码或有效证据](../../adr/permissions/001-session-authorization.md)。本轮只审查，不新增测试通过或完成声明。

最新授权补充：所有者已将[小模型指导](../small-model-handoff.md)规定的准备切片交给 Claude Code；下文历史“仅规划”不限制这些已授权切片，但不表示整项验收或 198 核心已启动/完成。

## 范围与验收要求

集成验收追加能力边界：纯应用搜索/准备/查看等路径不额外发起模型请求或扩大Agent scope；用户启用的MCP沿已有发现→调用→结果链完成固定任务，禁用后不再曝光或执行。不强制普通任务使用Goal、PRoot或子Agent；这些断言复用相应任务fixture，按改动范围执行，不重复制造状态机。 规划规则见[实施指南](../implementation-guide.md#应用能力与-agent-工具的规划规则)。

- 允许前述范围的局部修复、测试/scripts/docs，不顺带开发市场/定时任务/新推理引擎。
- 固定任务：读取资料形成结果；修改测试文件并打开产物；启用工具在ASK操作下批准；工具DISABLED及CUSTOM操作DENY均阻止执行；三预设/CUSTOM的ALLOW范围内免卡，显式rm规则仍询问；取消长任务；重启后对账；修复失效能力后由用户继续。
- 每项记录用户步骤数、重复弹窗数、结果能否打开、恢复是否重复副作用、实际成功/执行/跳过场景、设备API。比较同fixture前后，不用主观评分或测试数量替代任务成功率。
- P1/P2/P3+完整 `./scripts/check-all.sh`；设备证据必须来自独占runner，新增可复用 `scripts/verify-product-journeys.py`（本HXA创建）验证失败与跳过计数，保存owned进程退出记录。所有新增设备类均实际执行。
- 文档/源码/主机/设备/真实账号/发行分别记账；门禁未通过不写完成记录。交付状态同步status/roadmap/matrix，commit与push分开报告。

- 承接历史 HXA-200 的 Git R1 验收：仅针对现有只读 Git 状态/diff，验证恶意仓库配置、filter/transport 不触发命令或意外网络，保存 debug/release 制品与实际执行证据。现有 JGit lint 修复不替代此验收；不新增 remote Git、凭据或仓库写操作。

### 提前积累证据

从202开始固定上述核心fixture及操作路径，在对应HXA验收记录中保存步骤数、重复弹窗数、结果可用性、失败/跳过及设备/制品身份。194、203补导航与结果；204、205补恢复和准备；195、207、191补各自体验。复用可追溯证据，受后续代码变更影响的路径重新验证；最终仍执行本任务的集成门禁，不提前写完成记录。这里的验收设计不是第二个同时开发的checkpoint。

### 分工与收口

本轮核心产品验收不强制等待完整手动终端199；若发行选择包含终端，121汇总199证据。192的Plan闭环与191主题/搜索纳入核心场景，避免遗漏拆出后的范围。

## 验证入口

[公共验收规则](../verification-matrix.md)规定默认/外部测试边界。具体类和 runner 必须实际存在且执行非零；本文件列出的新增用例是实现要求，不是现有证据。

任务验收条目（新增测试在交付前必须实现并执行）：

| 任务 | 验收维度 | 命令或证据入口 |
| --- | --- | --- |
| HXA-206 | 固定完整任务、实际结果、失败/跳过计数、独占runner | planned；P1/P2/P3 + check-all + 全新增设备类 |

## 后续执行计划（2026-09-20，仅规划）

启动基线：191/207 已整合到 Runtime 产品分支，197 与 211 已交付；核对实际分支和制品后再使用历史证据。此任务可以与未来 198 在不同工作树准备，但不得并发修改公共 runner/共享页面，重型构建和设备运行仍串行占用 host slot。本轮只写计划，不启动实现。

1. **固定核心用户旅程**：在既有 fixture 上为每个场景写清起始状态、用户步骤、可观察结果和恢复副作用次数。覆盖本任务原有全部场景，并纳入 Plan 审阅→执行、主题/搜索、能力修复、MCP 添加→启用→调用→禁用。211 导出从真实会话入口到系统选择器和独立回读，且不新增模型请求；不强制普通任务启动 Goal/PRoot。
2. **复用测试与证据入口**：实现计划中的 `scripts/verify-product-journeys.py`，复用现有独占 runner 和 199 的制品/结果格式，不复制 Runtime 执行层。逐场景关联对应测试与实际结果，统计用户操作、重复审批、产物可打开性；缺失类/零执行/失败不得作为通过，条件跳过记录原因。
3. **权限与恢复产品闭环**：验证工具 ENABLED/DISABLED、预设/CUSTOM 的操作决定；ALLOW 无额外卡、ASK 正确审批、DENY 无副作用，显式 rm 规则不扩大。真实主进程死亡后检查既有结果和执行身份，无重复副作用；能力修复后由用户继续。应用搜索/准备/查看/导出不扩大 Agent 授权。
4. **Git R1 与发布制品边界**：用恶意仓库配置/filter/transport fixture 验证现有只读 status/diff 不执行额外命令、不意外联网，保留 debug/release 实际执行证据；不新增写 Git、remote 或凭据功能。按选定产品范围验证 consumer/developer，终端完整专项交由 199，发行是否纳入终端由 121 汇总。
5. **集成门禁和报告**：执行 P1/P2/P3、`./scripts/check-all.sh --all`、API29/36 × 两 flavor 的适用旅程及全部新增类。汇总相同 fixture 前后结果，不用单测数量替代产品成功率。源码、主机、设备、真实服务、发行分别记账；HXA-125/190 真实服务缺口、196 真机缺口显式引用，不冒称 fixture 已关闭这些任务。

每个实现切片允许本地具名提交；完成记录必须由实际集成结果支持。交接报告写明提交、制品、执行/失败/跳过及剩余项；推送、main 合并和发行分别处理，不作为本计划已执行的动作。

## 交付

通过本任务验证后更新完成记录和 status；未通过的设备/账号/发行项明确列出。允许已授权且验证通过的切片具名本地提交，不 push、合并或发布，不夹带其他任务改动。
