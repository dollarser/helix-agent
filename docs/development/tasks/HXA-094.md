# HXA-094：Root 依赖与授权生命周期验收

分类：收尾验收。当前是否执行见[实施状态](../status.md)。

前置条件：可独立；涉及新授权的集成对齐209。

本次复核：RootAccessController、libsu依赖与HXA-188已有证据；删除重新做依赖Spike的要求。

核对依据：[源码或有效证据](../../../tools/root/src/main/kotlin/com/helix/tools/root/RootAccessController.kt)。本轮只审查，不新增测试通过或完成声明。

## 范围与验收要求

- 复用现有 libsu core/service、RootAccessController 与已接受 ADR-RUNTIME-004。确切版本取 catalog/lock，不重新申请已接受的 JitPack 来源，也不把 6.0.0 写成永不可升级的要求。
- HXA-188 已覆盖被动不授权、显式 grant/deny、服务死亡与重连。剩余核查真实 App 进入后台/返回、进程重建、存活授权在管理器撤销后失效与清理；区分撤销未来授权和杀死既有 shell，不承诺管理器不存在的被动事件。
- 留存自身 PID/Binder/进程组清理证据，未显式授权不重新创建 RootService；必要修复限 tools/root 与 app 生命周期接线。
- 本任务负责底座与依赖证据；HXA-095 负责高层工具路径与scope。二者可同一次独占真机运行，分别列出断言。Root/OEM长稳计入发行矩阵，不无限扩大本切片；已要求的生命周期及资源回收短回归不能省略。
- 重用 LibsuRootAccessDeviceTest 和 RootAccessControllerTest；设备参数与当前 runner 核实后执行，不能借用个人应用授权或改其他应用策略。

## 验证入口

[公共验收规则](../verification-matrix.md)规定默认/外部测试边界。具体类和 runner 必须实际存在且执行非零；本文件列出的新增用例是实现要求，不是现有证据。

任务验收条目（新增测试在交付前必须实现并执行）：

| 任务 | 验收维度 | 命令或证据入口 |
| --- | --- | --- |
| HXA-094 | `./gradlew :tools:root:testDebugUnitTest` | `./gradlew :tools:root:connectedDebugAndroidTest`；libsu/JitPack 依赖证据、Root grant/loss/crash 与 Profile 切换不触发 `su` |

## 交付

通过本任务验证后更新完成记录和 status；未通过的设备/账号/发行项明确列出。允许已授权且验证通过的切片具名本地提交，不 push、合并或发布，不夹带其他任务改动。
