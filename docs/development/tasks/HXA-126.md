# HXA-126：Connector OAuth 登录层

分类：待决策。当前是否执行见[实施状态](../status.md)。

前置条件：接受CONNECTORS-002；两家服务账号/redirect。

本次复核：真实OAuth产品尚未接受，不排入当前实施主链；不要求等所有125厂商完成才做方案。

核对依据：[源码或有效证据](../../adr/connectors/002-oauth.md)。本轮只审查，不新增测试通过或完成声明。

## 范围与验收要求

状态：等待设计审查与真实服务条件，生产实现未开始。已形成 proposed [ADR-CONNECTORS-002](../../adr/connectors/002-oauth.md)，候选 Notion/Atlassian 尚缺独立测试账号和 redirect 准入证据；服务选择参考 HXA-125，不等待所有厂商验收；正式实现仍需接受ADR及可用测试账号；定义 Android public client、浏览器回调、PKCE/state、issuer/resource 绑定、刷新/撤销与进程恢复。至少两家真实服务验收，不导入第三方 host 登录态。实现模块与精确验收命令须在 ADR 审查后补齐，之前不启动功能实现。

验证：具体命令在本文件的验证入口；公共规则只定义通用门禁，不再维护重复任务行。

## 验证入口

[公共验收规则](../verification-matrix.md)规定默认/外部测试边界。具体类和 runner 必须实际存在且执行非零；本文件列出的新增用例是实现要求，不是现有证据。

任务验收条目（新增测试在交付前必须实现并执行）：

| 任务 | 验收维度 | 命令或证据入口 |
| --- | --- | --- |
| HXA-126 | proposed design；`./scripts/check-docs.sh`、`./scripts/verify-adr.sh` 通过；见 [ADR-CONNECTORS-002](../../adr/connectors/002-oauth.md) | 未实现/未验收；OAuth 方案待 ADR-CONNECTORS-002 审查及两家真实 MCP 服务账号/redirect 条件；文档门禁不替代功能或真实服务证据 |

## 交付

通过本任务验证后更新完成记录和 status；未通过的设备/账号/发行项明确列出。允许已授权且验证通过的切片具名本地提交，不 push、合并或发布，不夹带其他任务改动。
