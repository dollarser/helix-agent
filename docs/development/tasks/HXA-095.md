# HXA-095：Root 高层工具作用域与失权验收

分类：收尾验收。当前是否执行见[实施状态](../status.md)。

前置条件：复用094底座；最终会话授权对齐209。

本次复核：Root工具已存在；与094分工为工具scope/数据/撤权后的实际调用，不重复做连接底座。

核对依据：[源码或有效证据](../../../tools/root/src/test/kotlin/com/helix/tools/root/RootToolsTest.kt)。本轮只审查，不新增测试通过或完成声明。

## 范围与验收要求

- 复用 RootSessionManager 与已有 status、file.read、package.info、process.list、bounded log.read，不新建第二套工具或任意 root.exec。
- HXA-188 的有界读取、越界拒绝、服务死亡已有真机证据；收口真实 App→Dispatcher→RootSession 路径、scope失效/过期、用户断开、094底座失权后拒绝旧调用。
- 断言返回内容/截断/路径边界和无副作用，拒绝时不自动申请 Root、不泄露 Provider 凭据；工具启用与用户会话授权按209最终契约复核。
- 复用 RootToolsTest、RootSessionManagerTest、RootHighLevelToolsDeviceTest 和 FixedRootEvaluationDeviceTest，补缺失断言而非改名重写整套测试；Root真机部分与094共享制品和证据。

## 验证入口

[公共验收规则](../verification-matrix.md)规定默认/外部测试边界。具体类和 runner 必须实际存在且执行非零；本文件列出的新增用例是实现要求，不是现有证据。

任务验收条目（新增测试在交付前必须实现并执行）：

| 任务 | 验收维度 | 命令或证据入口 |
| --- | --- | --- |
| HXA-095 | `./gradlew :tools:root:testDebugUnitTest` | `./gradlew :tools:root:connectedDebugAndroidTest`；scope/失权/崩溃 |

## 交付

通过本任务验证后更新完成记录和 status；未通过的设备/账号/发行项明确列出。允许已授权且验证通过的切片具名本地提交，不 push、合并或发布，不夹带其他任务改动。
