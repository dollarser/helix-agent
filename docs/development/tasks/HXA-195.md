# HXA-195：有界日志协议与实时输出

分类：待实现。当前是否执行见[实施状态](../status.md)。

前置条件：194展示接线；协议可先行。

本次复核：已有最终结果不是流式日志；不等后台Job和PTY组件决定。

核对依据：[源码或有效证据](../../adr/runtime/002-terminal-and-jobs.md)。本轮只审查，不新增测试通过或完成声明。

## 范围与验收要求

本任务是宿主日志协议与用户可见实时输出，不将每个日志分片重复写入模型对话，也不因UI订阅增加模型调用或新Agent工具。模型最终工具结果保持既有有界合同，实时展示与模型结果分别验证；不重建Agent循环。 规划规则见[实施指南](../implementation-guide.md#应用能力与-agent-工具的规划规则)。

- 依赖：194；ADR-RUNTIME-002 已接受，可进入日志实现与测试，不等待后台/PTY 选型。
- FGS 与 PTY/许可证 Spike 归 ADR-RUNTIME-002，可另行推进，不作为本阶段前置。
- 允许：`runtime:proot-core/proot-ipc/proot-client/proot-app`、app proot/详情页，必要绑定持久化在 `core:storage`，测试/docs。
- 新 IPC 事务号和版本协商，不复用旧事务号；日志归属与 generation 校验；分页游标、spool、截断、背压、取消后尾部日志。先兼容一次性 Job，不提前加 PTY 或 detached。
- 测试：分片 UTF-8、stdout/stderr 压力、慢读端、重复/过期游标、跨 job 拒绝、低存储、Binder 断开、EOF/取消竞态；最终归档字节/hash 与日志展示关系明确。
- 验收：G1 + G2 + G3；新增 `ProotLogStreamDeviceTest`。UI 必须在命令结束前实际看见日志，不能只用最终文件一次刷新冒充 streaming。

## 验证入口

[公共验收规则](../verification-matrix.md)规定默认/外部测试边界。具体类和 runner 必须实际存在且执行非零；本文件列出的新增用例是实现要求，不是现有证据。

任务验收条目（新增测试在交付前必须实现并执行）：

| 任务 | 验收维度 | 命令或证据入口 |
| --- | --- | --- |
| HXA-195 | 日志游标/背压/配额/兼容；新增 ProotLogStreamDeviceTest | planned；G1/G2/G3；按 accepted ADR-RUNTIME-002，不等待 PTY/后台 |

## 交付

通过本任务验证后更新完成记录和 status；未通过的设备/账号/发行项明确列出。允许已授权且验证通过的切片具名本地提交，不 push、合并或发布，不夹带其他任务改动。
