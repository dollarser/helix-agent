# HXA-192：Plan 审阅到执行的用户闭环验收

分类：收尾验收。当前是否执行见[实施状态](../status.md)。

前置条件：现有路径可补；最终授权联动209。

本次复核：门禁修复/主分支集成不再当未做项；PlanSubmitIntegrationDeviceTest不等于用户审阅执行闭环。

核对依据：[源码或有效证据](../../../app/src/androidTest/kotlin/com/helix/app/plan/PlanSubmitIntegrationDeviceTest.kt)。本轮只审查，不新增测试通过或完成声明。

## 范围与验收要求

- 将本任务收窄为 Plan 用户审阅→执行闭环；旧架构拆分、基线门禁、主分支合入不再作为待实施工作。现有 PlanSubmitIntegrationDeviceTest 验证生产工具/Room，TasksDashboardDeviceTest 验证展示与取消，均不单独证明执行闭环。
- 补真实UI流程：提交计划→审阅具体版本→执行/修改/拒绝→Turn归属与持久状态一致；重复点击、旧版本、取消和进程恢复不会重复执行。
- 证明 Plan批准不自行mint工具proof；在209授权下分别覆盖预设允许操作免卡、ASK动作仍询问、DISABLED/DENY拒绝。不能把“每个L2再出卡”写成验收要求。
- 复用现有 PlanReviewService/PlanExecutionBinding，不新建Plan Engine。可先补独立UI链，最终授权联动在209完成后验证；202只消费导航入口，不作为本任务前置。
- 主机全门禁、API29/36双flavor真实UI设备路径；已有同源码证据可引用，授权变化影响的用例须重跑。

## 验证入口

[公共验收规则](../verification-matrix.md)规定默认/外部测试边界。具体类和 runner 必须实际存在且执行非零；本文件列出的新增用例是实现要求，不是现有证据。

任务验收条目（新增测试在交付前必须实现并执行）：

| 任务 | 验收维度 | 命令或证据入口 |
| --- | --- | --- |
| HXA-192 | Harness 2.0 集成与 Plan 用户闭环 | 全量门禁与 API29/36 双 flavor 设备路径；已有缺陷修复见[基线记录](../../bug-fixes/2026-09-16-pre-hxa-baseline-regressions.md)，不将旧 JGit 红继续列为当前阻塞 |

## 交付

通过本任务验证后更新完成记录和 status；未通过的设备/账号/发行项明确列出。允许已授权且验证通过的切片具名本地提交，不 push、合并或发布，不夹带其他任务改动。
