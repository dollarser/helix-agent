# HXA-198：手动终端多会话与重连

分类：待实现。当前是否执行见[实施状态](../status.md)。

前置条件：197；复用已接受并发边界。

本次复核：多会话范围已接受；不再写等待手动并发ADR接受，不开放模型PTY输入。

核对依据：[源码或有效证据](../../adr/runtime/002-terminal-and-jobs.md)。本轮只审查，不新增测试通过或完成声明。

## 范围与验收要求

- 依赖：197；按已接受 ADR-RUNTIME-002 手动并发条款实现，不放开模型代码并发。
- 允许：上述会话/UI/storage、Runtime 调度与应用仲裁，测试/docs；不新增进程常驻、额外 APK 或 shell 自动重放。
- 最多 2 个 live 手动会话，标题/目录/缓冲独立；同一会话首版只有一个写入连接，其他观察端只读。连接凭据由应用服务创建，不能把 sessionId 当授权。
- 页面切换不重开 shell；Runtime generation 改变使旧句柄失效。进程丢失后的会话卡可查看历史、明确重新创建；不能向复用 PID 发信号。
- 生命周期资源：lease deadline、输出总量、最大会话数及后台限制统一在 Runtime；手动域与 Agent 写/代码任务冲突显示原因，不通过增加线程池掩盖。用户可以关闭手动会话释放 Agent 执行。
- 测试：两个 cwd/env 不串线、输出不串会话、重复 attach、单写入者、取消一个不影响另一个、第三会话拒绝、Agent 等待可取消、主/Runtime 死亡、屏幕重建、session 删除与日志读取竞态。
- 验收：G1～G4；新增 `ProotMultiSessionDeviceTest`。至少 20 次创建/连接/关闭循环后核对自身 PID/FD/线程没有遗留，记录起止证据，不将此短回归称为长稳。

## 验证入口

[公共验收规则](../verification-matrix.md)规定默认/外部测试边界。具体类和 runner 必须实际存在且执行非零；本文件列出的新增用例是实现要求，不是现有证据。

任务验收条目（新增测试在交付前必须实现并执行）：

| 任务 | 验收维度 | 命令或证据入口 |
| --- | --- | --- |
| HXA-198 | 双会话/单写连接/互斥/重连/资源回收；新增 ProotMultiSessionDeviceTest | planned；G1～G4 |

## 后续执行计划（2026-09-20，仅规划）

启动基线：197 交付提交 `4521925d`；211 交付提交 `9bb3c708`。先检查实际分支是否包含这些交付，保留并行改动；在独立工作树实施。此计划不声称已实现双会话，也不授权现在启动实现。

1. **基线与核心会话模型**：阅读 Runtime ADR 与 197 完成记录，核对 `PtySessionStore`、`PtySessionRecord`、`PtySessionProtocol`、`PtySessionClient` 的单会话假设以及应用 admission。将唯一会话入口改为有界两会话集合，保持身份/generation、租期、停止证明与逐会话显式结算。先验证第三会话拒绝、停止一方不影响另一方、失败创建不泄漏名额，再接 UI。旧持久状态要可读；若需要 Room 迁移，补真实迁移用例。
2. **应用仲裁与单写连接**：应用服务维护每会话唯一写入凭据，重复 attach 不产生第二个 shell；观察端只读。保留手动域对 Agent 写/代码执行的互斥，验证等待取消、关闭一方但另一方仍存活时的占用，不以 UI 卡片删除替代执行停止。不得新增 Agent PTY 输入工具或开放并发模型代码。
3. **会话页面**：从现有 `DeveloperManualTerminal`、`ManualTerminalViewModel`、`ManualTerminalConnection`、`ManualTerminalScreen` 扩展创建/选择/重连/停止。复用输入队列与 viewport，两个标题/cwd/env/输出独立。屏幕旋转、切页、后台返回都连接原会话；UNKNOWN 展示历史并走明确恢复出口，不能自动重放或向复用 PID 发送信号。
4. **恢复与资源验收**：实现并实际执行 `ProotMultiSessionDeviceTest`，覆盖上文全部矩阵；用真实主进程/Runtime 死亡测试而非只构造状态。至少 20 次完整生命周期循环，保存 PID/FD/线程起止值和泄漏分析。执行 G1～G4 及完整 `check-all.sh --all`；API29/36 独占模拟器，consumer APK/dex/native 排除也须验证。

每个切片先修已知强制门禁失败再继续，可独立具名本地提交；最终交付需用户能创建两个终端、来回切换并独立结束。真机/长时专项归 199，不以短循环宣称长稳。交接时报告源码提交、主/测试 APK SHA、执行/失败/跳过、owner/closed 证据和未验证边界。

## 交付

通过本任务验证后更新完成记录和 status；未通过的设备/账号/发行项明确列出。允许已授权且验证通过的切片具名本地提交，不 push、合并或发布，不夹带其他任务改动。
