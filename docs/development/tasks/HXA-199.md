# HXA-199：终端专项集成与交付

分类：集成验收。当前是否执行见[实施状态](../status.md)。

前置条件：194～198；同版本共享证据。

本次复核：保留终端性能、后台真机和恢复集合，不与206重复建设另一套执行器或全产品验收。

核对依据：[源码或有效证据](../../architecture/terminal.md)。2026-09-21 所有者已授权执行 199 专项与 206 产品集成验收；历史“仅规划”不再限制本轮实现、修复和本地验证。关键物理设备缺口仍按下述要求独立保留。

准备切片及 HXA-198 已交付，见[交接汇总](../../evidence/development/completed-handoffs-2026-09-22.md)；当前剩余范围为物理专项及相应交付统计。

## 本轮进展（2026-09-21）

实际证据见[199/206 验收记录](../../evidence/development/acceptance-199-206-2026-09-21.md)。双 API Runtime/日志/Job/双终端适用批次 144 项通过，API36 独占模拟器实际 30 分钟脱离 idle 与默认两小时运行中到期通过；保留真实启动/截止时间、原进程退出和显式结算。此前短租期测试的空 PID 文件竞态已修复并在双 API 重验，没有缩短等待来冒充长时验收。

关键物理设备条件仍缺失：OEM/HOME/安全锁屏/Doze/热压及真实 16 KiB 设备没有执行；本任务保持未完成，不生成 199 完成记录。下方收尾清单按已完成的模拟器范围继续，不重新启动历史准备包。

## 范围与验收要求

- 允许：跨阶段缺陷修复、标准 tests/scripts/docs、发行边界检查；不得顺带改主题、Provider、Goal 语义或历史许可证。
- 场景：查看固定测试结果；长命令实时日志与停止；异步结束后回收；手动 REPL；双会话；切页重连；运行中杀主/Runtime；consumer 排除；旧版本数据库/结果迁移。
- 运行 G1～G4 及所有本包设备类，复用已有终端报告校验器 `scripts/verify-terminal-runtime.py` 和实际设备执行入口；报告校验器不负责启动设备，执行入口必须保存设备 owner/关闭记录，二者共同校验失败/跳过计数。
- 专项成功率使用“已通过场景/执行场景”，标明设备/API与跳过原因；记录日志首包、取消延迟、冷启动、包体增量和资源变化，不用测试数量替代产品成功率。
- 更新架构当前章节、工具文档、状态、HXA 完成记录和用户帮助。全量门禁失败、关键物理设备项未验或新 ADR 未接受时不得标记本包完成。允许验证通过的独立切片本地提交，遵循 [统一提交规则](../implementation-guide.md)；不自动推送、合并或发布。

197 单终端的主进程/Runtime 死亡、实际 reboot 对账及 1 MiB 输出压力已交付，按最终制品范围复用其证据。本任务继续验证 30 分钟 detach idle、默认两小时完整租期、OEM/Doze/热压/长稳及真实 16 KiB 设备，不将 197 的短租期或静态对齐视为这些项目通过。

### 分工与收口

与206共用同一runner基础和制品身份格式；本任务只汇总终端专项。已有同版本测试结果可以引用，变更影响的路径才重跑；没有运行的真机项保持未验收。

## 验证入口

[公共验收规则](../verification-matrix.md)规定默认/外部测试边界。具体类和 runner 必须实际存在且执行非零；已有报告校验器与模拟器执行见本轮进展；新增物理设备用例仍须实际实现并执行。

任务验收条目（新增测试在交付前必须实现并执行）：

| 任务 | 验收维度 | 命令或证据入口 |
| --- | --- | --- |
| HXA-199 | 全场景、升级、consumer APK 排除、专项 owned-runner、实际用户验收 | 模拟器及升级证据见本轮记录；补 G1～G4 适用物理专项与统计，不以旧32场景替代 |

## 剩余收尾与重验清单

198 已完成；执行前核对 194～198 在同一待验提交上的影响范围。196 真机 HOME/锁屏/Doze 仍缺证据，应首先补验并共享原始记录；不得用 197 的重启旅程替代后台 Job 真机验收。已有模拟器与实际时长证据按制品身份复用，变化路径重验。

1. **确定制品和证据清单**：固定一个提交、双 flavor 主/测试 APK SHA 与 RootFS 身份。逐项登记固定结果、日志/停止、后台回收、单终端 REPL、双会话、重连、进程死亡、升级和 consumer 排除；复用同版本且未受影响的原始证据，变更路径重新执行。
2. **汇总入口**：复用 `scripts/verify-terminal-runtime.py` 及已有独占模拟器基础，不另造执行引擎。入口检查测试类存在、非零执行、失败与跳过，输出场景结果、命令、时间、制品身份、owner/closed。与 206 共享证据结构，两个入口只负责各自范围。新建输出目录，设备 finally 清理仅针对本次 owner。
3. **自动化矩阵与升级**：API29/36 运行 G1～G4 和新增全部设备类。选定可追溯旧 APK/数据库，先写固定结果和会话，再覆盖安装新 APK，核对读取、恢复及无重放；记录实际旧/新 SHA，不能用清库安装替代升级。验证 consumer release 的组件、dex/native 排除。
4. **真机及实际时长**：分开运行 30 分钟 detach idle、默认两小时租期和运行中到期；记录开始/截止/终止及 PID 证明，不能缩短配置冒充。补 HOME、锁屏、Doze、OEM 后台限制、热压/长稳与真实 16 KiB 设备；保存机型/API/页大小、电源及温度条件。设备不可得时明确缺口，保留任务未完成。
5. **产品统计与收口**：按固定场景记录成功/执行/跳过、日志首包、取消延迟、冷启动、包体与资源变化，报告采样方式和异常。局部缺陷回到所属模块修复后重验影响矩阵；更新终端帮助/架构/完成记录。关键真机项缺失、全量门禁失败时不创建完成声明。

终端专项不扩展到 Provider、主题或全产品工作流；真实账号不是 synthetic fixture 的替代品或默认依赖。只有相关账号用例才使用显式 opt-in profile，缺失跳过、格式错误失败。

## 交付

通过本任务验证后更新完成记录和 status；未通过的设备/账号/发行项明确列出。允许已授权且验证通过的切片具名本地提交，不 push、合并或发布，不夹带其他任务改动。
