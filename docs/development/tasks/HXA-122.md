# HXA-122：发行身份、签名与升级路径

分类：发行队列。当前是否执行见[实施状态](../status.md)。

前置条件：120渠道清单；所有者发行身份/签名条件。

本次复核：旧主/companion同签名与安装顺序已过期；仅主应用身份、包内组件、数据升级/降级边界。

核对依据：[源码或有效证据](../../adr/platform/001-distribution.md)。本轮只审查，不新增测试通过或完成声明。

## 范围与验收要求

离线签名材料流程、developer 包内 Runtime 的组件与制品边界、可回滚升级和来源校验。决定跨商店/官网的稳定主 applicationId、flavor/channel 命名与升级路径：优先让同一产品身份覆盖完整 Standard，并在同 ID 内切换 Standard/Advanced；若渠道必须不同 ID，记录数据迁移与能力差异。不得只改文档、交换 ID 或声称不同 applicationId 可原地升级。当前不做 App 内静默自更新。

### 分工与收口

当前APK manifest/applicationId是实现事实，不因本次重排自动改ID；先确定正式发行身份。回滚不等于强制降级Room，明确可支持的数据恢复方式与不可逆版本边界，禁止用删除用户数据冒充兼容。

## 验证入口

[公共验收规则](../verification-matrix.md)规定默认/外部测试边界。具体类和 runner 必须实际存在且执行非零；本文件列出的新增用例是实现要求，不是现有证据。

任务验收条目（新增测试在交付前必须实现并执行）：

| 任务 | 验收维度 | 命令或证据入口 |
| --- | --- | --- |
| HXA-122 | `./gradlew :app:assembleConsumerRelease :app:assembleDeveloperRelease` | 稳定产品 applicationId、flavor/channel 命名、离线签名、同 ID 升级/回滚、包内 Runtime 非导出组件与 consumer 制品排除；不同 ID 不冒充原地升级 |

## 交付

通过本任务验证后更新完成记录和 status；未通过的设备/账号/发行项明确列出。允许已授权且验证通过的切片具名本地提交，不 push、合并或发布，不夹带其他任务改动。
