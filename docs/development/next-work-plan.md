# CI 收敛后的工作计划

更新：2026-09-20。所有者已进一步授权[小模型工作包](small-model-handoff.md)的准备切片；整项 HXA-198、199、206 的实现/验收不能由此推断完成。本文不替代[当前状态](status.md)和各任务的验收要求。

## 优先级与交付顺序

当前的主要缺口是已交付能力的跨页面、授权、恢复和产物闭环证据，其次才是新增终端容量。191、197、207、211 已交付，不重新开发。196 实现及模拟器验证已整合；所有者本次跳过真机 HOME/锁屏/Doze 验收，该项仍未通过。

| 顺序 | 工作 | 具体切片与完成出口 |
| --- | --- | --- |
| 1 | [HXA-206 核心产品验收](tasks/HXA-206.md) | 固定旅程及结果统计 → 权限/恢复/能力修复 → 搜索主题、扩展与 JSONL 导出 → Git R1 → 四象限与完整主机门禁。交付逐场景成功/失败/跳过、操作数、重复审批数、产物可用性和制品身份 |
| 2，返修中 | [HXA-198 双手动终端](tasks/HXA-198.md) | 两会话身份/配额 → 单写连接与 Agent 仲裁 → 页面选择/重连 → 真实进程死亡与资源回收。最多两个 live 手动会话，不开放 Agent PTY 输入或模型代码并发 |
| 3，等待 198 | [HXA-199 终端专项验收](tasks/HXA-199.md) | 汇总 195～198 → 长输出/输入与租期 → idle/重连/失败结算 → 设备及资源矩阵。OEM、Doze、热压、16 KiB 等缺少环境时单列，不能用模拟器短测代替 |
| 条件队列 | 196 真机、125 Connector、190 真实订阅 | 有对应设备/账号后补实证，保留缺口但不阻塞无依赖的本地工作 |
| 决策队列 | 126／129／130 | 先审查现有 Connector ADR、账号/redirect 与安装所有权，再授权实现；本计划不自动接受 proposed |
| 发行队列 | 120 → 122 → 121 → 123 | 渠道审计、身份/签名/升级决策、候选包验收、获准后提交。206 不等待完整终端；发行若包含终端则需要 199 的相应证据 |

## 分工与资源

建议由熟悉授权、持久结算和 Android 生命周期的执行者负责 206 的验收定义、跨层失败分析与最终复核；198 可交给另一执行者按既有任务规格独立实现。公共 runner、授权 resolver、Runtime admission 和共享 UI 必须先明确单一文件所有者，避免两个任务同时改写。

独立工作树允许并行编辑，不代表可以抢占同一构建和设备资源。重型 Gradle/设备任务通过现有 host slot 串行；每次运行创建并清理自有模拟器。206 与 198 的证据分别绑定实际提交和制品；合并后重跑受影响路径，不能拼接不同源码版本的通过数字冒充整体通过。

每个任务按“可验收切片 → 本地证据 → 复核 → 合并后 CI”交付。任务开始前解决强制基线失败；普通 CI 的 Debug 档不能替代 HXA 要求的 `./scripts/check-all.sh --all` 和设备门禁。详见 [CI 分层](ci.md)。

## 保持应用能力与 Agent 能力分离

搜索、主题、能力准备、终端管理、导出和审计查看保留为应用能力，不为了可见性额外注册 Agent 工具。通用任务优先使用既有工具、MCP、Connector、Skill；206 要验证这些路径不会无故启动模型、Goal 或扩大 scope。

不在本批增加子 Agent、通用工作流引擎、场景专用规划器、自动重放 shell 或远程执行平台。若现有能力确有重复失败，先用旅程证据确定缺口，再提出最小修复或独立 ADR，而不是按其他 Agent 的功能清单补齐。
