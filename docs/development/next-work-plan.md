# CI 收敛后的工作计划

更新：2026-09-22。198 已交付；所有者授权执行后的 206 本地核心产品验收已完成，199 模拟器/真实时长/升级已验，仍待物理设备。本文不替代[当前状态](status.md)和各任务的验收要求。

## 优先级与交付顺序

2026-09-22合并后补充：[引擎复核](../research/execution-engine-deep-review-2026-09-22.md)已撤回过时的R1/R5/R9待修结论。R2/R3调度、R6结算及R7协议结束已完成本地修复整合（完整主机门禁及最终四象限80项通过），当前结果见[修复记录](../bug-fixes/2026-09-22-engine-browser-convergence.md)；R4及R8剩余验证保留为P2，功能任务129→217的设计接受门槛不变。

新增本地对话交互需求按下节顺序推进设计接受与后续开发；有明确条件时继续补齐物理设备及真实服务证据，不新增终端容量或执行框架。191、197、198、206、207、211 已交付，不重新开发。196 实现及模拟器验证已整合；历史真机 HOME/锁屏/Doze 豁免不等于通过，也不覆盖 199。

| 顺序 | 工作 | 具体切片与完成出口 |
| --- | --- | --- |
| 已交付 | [HXA-206 核心产品验收](../completion-records/HXA-206.md) | 四象限、同 fixture 基线对照、Git R1 debug/release 与完整主机门禁通过；真实外部账号及发行仍独立 |
| 已交付 | [HXA-198 双手动终端](../completion-records/HXA-198.md) | 两会话身份/配额 → 单写连接与 Agent 仲裁 → 页面选择/重连 → 真实进程死亡与资源回收。最多两个 live 手动会话，不开放 Agent PTY 输入或模型代码并发 |
| 已整合（本地） | HXA-212/130 市场与签名索引、213 fork、压缩补强 | 合并提交 8bd2d177；历史分支数字不替代本轮验证，见[收敛记录](../evidence/development/branch-integration-2026-09-22.md) |
| 1（需设备） | [HXA-199 终端专项验收](tasks/HXA-199.md) | 双 API、真实 30 分钟 idle/两小时租期、覆盖升级与双 shell 恢复已验；接入物理设备后补 OEM/HOME/锁屏/Doze/热压/长稳/真实 16 KiB，不能用模拟器替代 |
| 条件队列 | 196 真机、125 Connector、190 真实订阅 | 有对应设备/账号后补实证，保留缺口但不阻塞无依赖的本地工作 |
| 决策队列 | 129（安全替换、安装归属与会话启停） | 126 OAuth 核心层已合入 main；130 签名索引与 212 扩展市场已整合到本地 main（见[已完成交接汇总](../evidence/development/completed-handoffs-2026-09-22.md)）；129 先审查收窄后的 ADR-CONNECTORS-003，再实施安全替换、最小归属和会话独立启停；不建设代码 Diff 或通用 journal |
| 发行队列 | 120 → 122 → 121 → 123 | 渠道审计、身份/签名/升级决策、候选包验收、获准后提交。206 不等待完整终端；发行若包含终端则需要 199 的相应证据 |

## 对话交互开发需求

214普通 composer 与215最新消息修订已经联合交付，接口与整合边界见[交接](hxa-214-core-handoff.md)；009已接受。216已与218/219一并整合至本地main，联合主机与设备验证见[本轮收敛记录](../evidence/development/branch-convergence-2026-09-22.md)；后续顺序为 **129（接受后）→ 217（接受后）**。129 的安装替换与会话启停分切片验收，实施准备见任务；217主机成本预评估已完成，不替代Android持久成本与功能验收。

| 优先级 | 工作 | 接受/依赖门槛 |
| --- | --- | --- |
| 已交付 | [214 发送回执与停止](../completion-records/HXA-214.md) | 现行契约修复；不改变Goal新输入语义 |
| P1（216后） | [129 安全替换与会话启停](tasks/HXA-129.md) | ADR-CONNECTORS-003 接受；先稳定身份/归属及安全替换，再会话面板、曝光/执行门禁和恢复；避免先做 UI 后补约束 |
| 已交付 | [215 最新消息会话内修订](../completion-records/HXA-215.md) | 已与214联合收敛，009已接受；更早历史修改以后fork |
| 已交付（本地） | [216 统一输入交付](../completion-records/HXA-216.md) | 完整主机门禁、464设备JUnit与16次普通主进程恢复通过；main与远端CI独立记账 |
| P2 | [217 轻量请求来源记录](tasks/HXA-217.md) | 211/216基线及 proposed ADR-AGENT-010接受 |

129 的安装准备/失败恢复可与对话 UI 工作拆分，但会话上下文和执行门禁必须串行整合。215与216没有彼此技术依赖，主链默认串行，避免同改ChatService、Compose输入区和迁移编号。核心并发、消费/终局事务和Goal调度由熟悉执行引擎的执行者负责；接口冻结后再交接UI/i18n/fixture，不能只接UI任务却自行发明接收/停止语义。217格式与存储成本测量可提前；首期只保存最小来源并导出 JSONL，详情页与详细诊断延期，最终验收须使用216真实输入身份。

本批沿Room与已有引擎实现通用交互，不新增Agent Tool、常驻服务或场景工作流。scope215 runner及测试已经交付；其他scope与新增测试仍按对应任务执行，不把计划命令当通过。新功能不复用206历史结论冒充整产品验收；每任务记录独立验证范围。

## 分工与资源

206 已完成；后续由熟悉持久结算和 Android 生命周期的执行者负责 199 物理专项的条件确认、跨层失败分析与最终复核。公共 runner、授权 resolver、Runtime admission 和共享 UI 必须先明确单一文件所有者，避免两个任务同时改写。

独立工作树允许并行编辑，不代表可以抢占同一构建和设备资源。重型 Gradle/设备任务通过现有 host slot 串行；每次运行创建并清理自有模拟器。206 与 198 的证据分别绑定实际提交和制品；合并后重跑受影响路径，不能拼接不同源码版本的通过数字冒充整体通过。

每个任务按“可验收切片 → 本地证据 → 复核 → 合并后 CI”交付。任务开始前解决强制基线失败；普通 CI 的 Debug 档不能替代 HXA 要求的 `./scripts/check-all.sh --all` 和设备门禁。详见 [CI 分层](ci.md)。

### 216 已交付切片与后续委派边界

216已按以下切片完成本地交付，供后续修改复核；不能把新输入排队倒写成214已交付能力。129的提交协议、迁移、凭据与执行准入由核心所有者负责，接口确定后再并行面板和确定性测试；217先补Kotlin/Room测量，再确定存储形式，不把主机JSON计时当作设备结论。

1. 由主执行者冻结输入身份、revision、预期Turn、消费映射、终局竞争与停止停泊契约，同时收敛Goal新输入条款。
2. 同一核心所有者完成有界输入存储、迁移和消费事务；FIFO、撤回/编辑竞争、附件引用及幂等追加先单独验收。
3. 接入Queue调度：用户FIFO优先于Goal自动续跑，停止停泊旧队列，消费时重新校验配置、附件与出网确认。
4. 接入Steer：仅完整工具批结算后交付，统一请求装配与Provider角色配对，终局竞争不静默转投新Turn。
5. 收敛Goal激活与真实进程恢复；接口冻结后并行交付UI、i18n、协议fixture和设备旅程，最后做联合验收。

核心协调、事务结构、Goal激活与Provider角色规则由强模型负责。能力较弱模型可在契约冻结后承担纯状态测试、既定协议fixture、UI/i18n、runner登记和文档同步；迁移及DAO样板也必须由核心所有者复核，不能自行决定schema或协调语义。不新增Worker、常驻服务或另一套执行框架。

## 保持应用能力与 Agent 能力分离

搜索、主题、能力准备、终端管理、导出和审计查看保留为应用能力，不为了可见性额外注册 Agent 工具。通用任务优先使用既有工具、MCP、Connector、Skill；206 要验证这些路径不会无故启动模型、Goal 或扩大 scope。

不在本批增加子 Agent、通用工作流引擎、场景专用规划器、自动重放 shell 或远程执行平台。若现有能力确有重复失败，先用旅程证据确定缺口，再提出最小修复或独立 ADR，而不是按其他 Agent 的功能清单补齐。
