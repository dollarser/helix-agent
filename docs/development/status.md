# Helix 实施状态

更新时间：2026-09-10。适用工作树：`codex/phone-chat-polish`；本轮修改未提交、推送或合并 main。

## Current summary

当前授权的实现与重构已完成至 [HXA-184](../completion-records/HXA-184.md)，没有正在执行的开发任务。完成记录代表各自工作树和制品的验收范围，不代表 main 已包含本分支修改，也不等于整个产品已完成发布验收。

| 最近交付 | 当前状态与证据 |
| --- | --- |
| 会话、文件界面与上下文 | HXA-161～178 已交付；支持会话管理、模型/推理选择、上下文显示与压缩、非阻塞后台工具任务及结果回收 |
| Goal 完成机制 | [ADR-0040](../adr/0040-model-judged-goal-completion.md)：模型提交结构化完成报告，Harness 校验报告有效性并处理运行状态和结算；已取代 ADR-0028 的强制条件绑定与独立 verifier |
| 独立文件管理 | [HXA-180](../completion-records/HXA-180.md)：Workspace、共享存储和可写 SAF 的手动变更；[HXA-182](../completion-records/HXA-182.md)：复制/移动中断后的持久记录与显式恢复 |
| 职责整理 | HXA-179～181 已收口；[HXA-183](../completion-records/HXA-183.md) 完成七项优先拆分，[HXA-184](../completion-records/HXA-184.md) 完成 B11/C15 提取与装配整理 |
| 最新回归 | HXA-184：主机 2788 通过、8 项外部条件跳过；API29 consumer / API36 developer 各 394 通过、2 项浏览器长稳/诊断条件跳过；自建模拟器均已关闭 |

系统 JNI/Binder 根因、长稳、完整真机矩阵与商店发布仍未关闭。历史主线验证见 [main 验证报告](main-merged-verification.md)，后置测试见 [优化待办](main-optimization-todo.md)。

## In progress

无。当前仅整理本状态文档，不新增产品功能或重新开启已完成 HXA。

## Next task

等待所有者选择提交范围或下一项任务。当前工作树需审查并分批提交；不自动推送或合并 main。长稳按所有者要求后置，未因短回归通过而启动。

## Blocked

| 项目 | 缺少条件 / 决策 |
| --- | --- |
| HXA-125 受保护 Connector 服务验收 | WorkBuddy 来源样本已补齐；仍需独立测试账号验证凭据无效、权限拒绝、厂商撤销和重连；匿名服务与 fixture 不替代这些证据 |
| HXA-126 / HXA-129 | [ADR-0030](../adr/0030-connector-public-client-oauth.md) / [ADR-0031](../adr/0031-connector-version-ownership-journal.md) 待审查；HXA-126 还需两家独立服务账号与 redirect 条件 |
| HXA-130 | [ADR-0032](../adr/0032-connector-offline-signed-index.md) 已准备；离线 fixture 依赖 HXA-129，市场运行时不在当前批次范围 |
| Claude / Grok 真实付费调用 | 账号不可用，按所有者决定暂缓；本地与设备夹具通过不代表真实账号验收 |
| Root 与发布验收 | HXA-094/095 仍需 rooted 物理设备；HXA-122 尚待稳定 applicationId、渠道命名、升级路径与签名发行决策 |

## Current interfaces

- **聊天与装配**：`ChatService` 保留应用入口及运行协调，草稿、请求组装、附件重试、界面投影、工具调用与结算已有独立组件；`AppContainer` 接口与 `DefaultAppContainer` 组合根分离。资源与可变状态仍由原所有者管理，拆文件不产生第二套状态源。详见 HXA-179/183/184。
- **Goal 与后台任务**：Goal 采用 ADR-0040；暂停可由用户继续，blocked 表示不能主动继续。后台能力是有界工具任务及结果回收，生产子 Agent、Agent graph 和声明式 Workflow 尚未实现；ADR-0009 的只读 Spike 不等于产品启用。
- **手动文件管理与 Agent 工具**：两者范围分离。手动界面支持 Workspace、共享存储和可写 SAF 的新建、改名、同来源复制/移动与删除；跨来源继续使用导入/导出。手动授权不扩大 Agent resolver，不能把手动共享根视为 Agent 已获授权的文件范围。边界以 HXA-180/182 与对应 ADR 为准。
- **执行与工具**：QuickJS 使用非导出的 isolated Service；PRoot/CLI 使用独立 APK/UID 与签名保护的 Binder/PFD。`read`、`write`、`edit`、`files.*`、`bash` 继续经过既有 scope、Policy、审批、限制、验证和审计路径；PRoot/CLI 按需冷绑定，不因被动刷新启动。
- **扩展与 Provider**：MCP、Skill、A2A Client 已按 M7 矩阵验收；A2A 是用户配置的外部服务，不是本地子 Agent。订阅适配器凭据保留在独立 CLI Runtime UID，developer/Advanced 接线不代表 consumer/store 开放或厂商官方支持。
- **浏览器**：WebView 由浏览器功能持有并绑定 Activity owner；下载队列和结果映射已分离。真实 AutofillService 的 fill/save 回归通过，不能据此关闭系统 JNI/Binder 累积问题。

## Known limitations

这里只列仍有效的范围限制和验收缺口；已修复缺陷、旧测试数量和机制演进保留在完成记录，不再列为当前故障。

- **系统与长稳**：应用侧释放路径和短回归已有证据；系统 JNI/Binder 根因仍 open，24 小时长稳尚未完成。见 [释放路径调查](native-reference-release-trace.md)、[浏览器引用验证](browser-controller-reference-verification.md) 与 [优化待办](main-optimization-todo.md)。
- **设备覆盖**：API29/36 模拟器及历史 API34/35、16 KiB 模拟器证据不替代物理低内存、OEM、热压力、Doze、安全锁屏和 Root grant/revoke/loss 验收；x86_64 静态制品证据也不等于实际运行。
- **文件恢复**：HXA-182 实现显式对账恢复，不承诺字节偏移续传、断电事务、自动后台队列或跨 Provider 原子事务；既有 picker 导入/导出及旧版无日志暂存不在该恢复管线内。目标/备份变化时保留人工核查，云盘厂商与全部中断阶段仍需外部设备验收。
- **模型与附件**：导入成功不等于模型理解。图片受实际模型视觉能力与端上预算约束；既有文本/图片管线不代表任意文档、音视频解析或 OCR 已实现。真实服务可用性与连接参数需要按服务当前状态验证。
- **后续功能**：持久 Git Workspace、结构化 Git UI、remote Git/凭据，以及生产子 Agent/Workflow 不在当前实现范围；接受 ADR 不构成实现证据。Connector OAuth、版本管理与市场扩展也不因本轮整理自动启动。
- **发行**：Standard 完整产品形态是 ADR-0013 的决定；当前 consumer/developer 构建与 CI debug APK 不是签名 release、完整渠道权限申报或商店审核证据。
- **测试条件跳过**：HXA-184 的 8 项 JVM 跳过需要 supplied Connector/WorkBuddy 与外部验收材料；每台设备的 2 项浏览器跳过需要显式长稳/诊断参数。均未计为通过，具体条件见 HXA-184。

## Connector 扩展线收尾

M13 首版 HXA-124、HXA-127/128 已验证；HXA-125 的受保护服务验收后置，HXA-126/129/130 未完成，不能宣布 M13 整体完成。来源样本与历史移交见 [Connector 工作交接](connector-handoff.md)。旧批次的 main 合并/推送记录不适用于当前未提交分支；当前 Git 状态以实际工作树为准。

## Completed

- M13 / HXA-124 已完成：Connector 开放格式迁移首版在独立 connector 分支完成；ZIP/JSON 导入、MCP/Skill 组件管理与 Codex TOML 导出，604 JVM + 2 Python、双 flavor debug 构建与 API 29/36 每台 6 + 2 阶段专项测试通过。本次经所有者授权合入 main；不代表平台私有连接器/OAuth/CLI 全兼容，见 [完成记录](../completion-records/HXA-124.md)。

已完成任务的详细实现、命令、exit code、设备和限制只在 [HXA 完成记录](../completion-records/README.md) 与各记录中维护；本文件不复制字段级历史。

| 里程碑 | 已完成范围 | 权威证据 |
| --- | --- | --- |
| M0 | HXA-001～003：工程、质量门禁、AppContainer/导航壳 | [M0 完成记录](../completion-records/M0.md)、[逐 HXA 索引](../completion-records/README.md) |
| M1 | HXA-010～016：领域状态、Plan/Goal、Room、恢复、Context Builder | [逐 HXA 索引](../completion-records/README.md) |
| M2 | HXA-020～028：Secret/Provider、三协议、能力探测、模板、聊天 UI | [逐 HXA 索引](../completion-records/README.md) |
| M3 | HXA-030～039：Tool/Schema/Capability/Policy/Approval、Dispatcher/Scheduler、Tool Loop、模型流状态与 batch-safe Turn Coordinator | [逐 HXA 索引](../completion-records/README.md) |
| M4 | HXA-040～049：Workspace/scope、原子文件存储、首批文件工具、copy/move/trash、SAF adapter 与 developer All-files scope、文件管理 UI（Workspace 恒可写、developer all-files 根只读、SAF 接线与导入导出 UI 显式推迟）、受限 zip/tar archive/extract、会话附件导入/持久化与 UTF-8 文本物化（ADR-0014 首批，fail-closed 再校验 + egress 目标/附件集绑定），以及全项目审查收敛（ChatService 每会话 turn 模型 + 单测骨架、删除 WorkspacePath 死代码） | [逐 HXA 索引](../completion-records/README.md)、[文件工具 Bug 修复记录](../bug-fixes/2026-09-02-file-tool-safety-boundaries.md) |
| M5 | HXA-050～054（M5 完成）：Zipline 1.27.0 QuickJS spike（evaluate/memoryLimit/InterruptHandler/eval+Function 封堵/大于 6 MiB 调用线程栈/bindIsolatedService 唯一实例回收，各 25 例 androidTest 全过；16 KiB page 以四 ABI ELF PT_LOAD 对齐为替代证据；ADR-0015 accepted）+ 生产执行协议（isolated 非导出一次性 Service、手搓 onTransact execute/interrupt/info、64 KiB inline + PFD、deadline/interrupt/cancel/watchdog 与 Binder-death 回收、11 状态闭合错误集，36 例 androidTest/设备 + 33 例 JVM）+ IIFE wrapper JSON ABI（doc §3.2 严格 IIFE 为唯一生产路径、host 编码输入 + 局部 `const input` 闭包注入、JSON 输入输出 + 双层输出边界、19 例 wrapper 逃逸攻击套件，55 例 androidTest/设备 + 62 例 JVM）+ 执行接 Tool/Policy/审计（`code.javascript.run`：CODE_EXECUTION/L2/LOCAL_QUICKJS 单并发 lane、模型仅见 code+input 且 limits 固定 §4.1 默认不入 schema、11 状态稳定回填绝无假成功、§4.8 脱敏审计经 dispatcher 单一 emitter、审批卡完整代码块 + input 摘要 + 联网：否 + 固定 limits + 代码 SHA-256 短摘要；JVM 506 例（quickjs 74 + framework 136 + app 148×2）+ 5 例 androidTest × 两台设备全过）+ **攻击和端到端测试（HXA-054：无限循环 10 s wall TIMEOUT、64 MiB 堆 OOM 表面形态按 API 29/36 固定、输出洪泛 PFD 有界失败 + 256 KiB 边界±1、eval/fetch/require 生产链路调用尝试全 fail-closed、crash seam 稳定 CRASHED + 隔离 PID 死亡/主进程存活设备证据、2 MiB 输入/256 KiB 源 PFD 往返 + 超限预检不产生实例 + 服务端 inline-cap fail-closed、取消三竞态 50 轮收敛 + 一次性槽位不重放、verified artifact size+SHA-256 宿主侧重算采信前提 + JVM 失配分支、§10 NUL/emoji 输出方向/深嵌套/环引用（环引用 marker 设备固定为 `circular reference`）；75 例 androidTest/设备 × 两台 + 84 例 JVM 全过）** | [HXA-050 完成记录](../completion-records/HXA-050.md)、[ADR-0015](../adr/0015-zipline-quickjs-execution-base.md)、[HXA-051 完成记录](../completion-records/HXA-051.md)、[HXA-052 完成记录](../completion-records/HXA-052.md)、[HXA-053 完成记录](../completion-records/HXA-053.md)、[HXA-054 完成记录](../completion-records/HXA-054.md) |
| M5A | **完成**：**HXA-055 完成**——图片输入与 Provider 视觉能力：Phase 1 设备测量固化平台事实（Bitmap 像素在 native 内存非 JVM heap；SOF 头可伪造且 `inJustDecodeBounds` 信任至 30000² 但完整解码返回 null；`BitmapFactory` 不自动应用 EXIF 方向；`Bitmap.compress` 重编码剥离全部 EXIF）→ `VisionLimits` 集中预算（10 MiB 输入 / 4096 px 边 / 12 MP 像素 / 3.8 MB b64 per-image / 4× per-request，取 API 29/36 测量与 Provider 上限更严者）；`AttachmentClassification.ImageAttachment` 闭包分支（magic 赢，mediaType 限定 `ImageReference.MEDIA_TYPES`）；端上归一化器（字节门 → bounds 探针 → 2 的幂采样 → ARGB_8888 解码 OOM 捕获 → 手动 EXIF 旋转 → 质量/边长阶梯重编码剥离元数据 → 原子落盘）；production 图片源（只解析当前会话绑定、闭包 mediaType、预算内、magic 一致的 app-private artifact，任何 miss 短而稳定、无路径的 IAE，无裸 base64 回退）；三协议独立 resolver 委托同一 app 侧源（supplier 注入，stream 时解析）；连接测试第 5 阶段视觉探测（保留 1×1 探针图；Completed/Refusal ⇒ vision=true，任何 Error ⇒ 阶段 5 失败，可重试集固定）+ 用户可见手动声明（`MANUAL` 源）+ Provider 行声明按钮；`message_attachments` 绑定**归一化 artifact**，发送/重试/tool-loop backfill/恢复每次重验（存在 + SHA-256 + magic + 预算，miss 即整 turn fail-closed）；出网披露图片行（HIGH_SENSITIVE_IMAGE，归一化大小/尺寸/绑定 hash，强制 Confirm）；视觉未确认发送阻塞（可操作文案：跑连接测试或手动声明）；turn 快照含 capabilities；JVM 753 例 + consumer 设备套件 64 例 × 两台（API 29/36）+ spotless/detekt 全过；验证中修复 AppContainer 初始化顺序 NPE（factory 改 supplier）；**HXA-056 完成**——文本/图片附件端到端硬化与发布边界：`ShareIntentDraft` + MainActivity/manifest 三个 intent-filter（SEND/SEND_MULTIPLE × text/plain/image/*）→ provider-free「分享草稿」会话（文字预填/图片本地导入暂存，**绝不自动发送**，ADR-0014 §5）+ 草稿会话显式单发绑定（`sessions.bindProvider` 的 `providerId IS NULL` 行数门禁：已绑定会话 egress 目标结构性不可换，未测试 Provider/进行中 turn 双门禁）；fixture E2E（`ScriptedSseWire` 记录-回放 wire client，13 例）覆盖发送/流式回复/Tool Loop（backfill 重带附件块）/历史恢复（文本块重建 + 图片 data URL 经 production 图片源重解析）/脱敏与泄露断言（持久化消息体 + 用户可见错误无绝对路径/URI/正文/base64）+ 失败矩阵（grant 失效 fail-closed、Artifact 篡改重试阻塞、重复发送独立 turn、能力快照变化立即撤销视觉、10 MiB+1 过大阻塞、伪造 SOF 解压炸弹 0 归一化产物、取消 CANCELLED 终态 + 重发、进程回收持久化幸存/暂存丢弃）+ 统一稳定拒绝（PDF/OLE2-DOC/RIFF-WAVE/UTF-16LE 同一文案，0 artifact 0 暂存，无解析/解码/upload 副作用）；三 adapter golden request（HXA-055 三编码器 golden + E2E wire body 真机复核）+ 1 真实 vision endpoint smoke（开发机本地 SGLang 服务，OpenAI 兼容，key 为空，四象限色图正确识别 + `image_tokens=64` 证明图像被消费，环境明确记录、不替代 fixture）；JVM（库 232 例 + app 374 例含 `SessionBindProviderTest` 4 例）+ consumer 设备套件 79 例 + developer 92 例 × 两台（API 29/36）+ spotless/detekt 全过；验证中修复既有 `FilesScreenTest` SHA 行竞态（补 wait）并留档低频环境 flake（约 1/12 全量轮次、无异常无状态损坏、重跑必绿、基线 9ecf979 同现象，见记录「验证中缺陷 5」） | [HXA-055 完成记录](../completion-records/HXA-055.md)、[HXA-056 完成记录](../completion-records/HXA-056.md) |
| M5B | **完成**（HXA-057/058/059 全部完成）：**HXA-057 完成**——persisted SAF tree scope 接线（解析 + 只读浏览后端）：`SafTreeScopeResolver` 每次使用 7 步实时复核（grant / aliasing / provider 身份 / 根文档 / URI 变化 / 读写模式，全部 fail-closed，`ScopeNotAvailable` 消息无 URI）；`SafTreeScopeService`（`SafGrantStore` 作 grant registry + 实时 `liveSources`/`revoke`/`grant`/`sweepRevoked`，dead grant 从来源列表即时消失）；模型不透明（模型/Tool/诊断/日志只见 `scope:<saf-…>:<相对路径>` + 显示名，`content://` URI 永不进模型/Room/audit/用户可见错误）；生产 `ContentResolverSafTreeCheck`（live `resolver.query` 判活、空目录 0 行仍活、`persistedUriPermissions` 写权限判定）；生产 `ContentResolverSafTreeReader`（`DocumentsContract` 只读后端：相对路径↔不透明文档 ID 逐级歧义名 fail-closed walk、有界 8 MiB read 窗、超限 copy fail closed 且删部分文件）；`FileManagerService` 对 `saf-` scope 走 SAF 只读分支（list/preview/mime/info/share），变更入口 fail-closed（`SAF 来源为只读`）；`AppContainer` 复用同一 `SafGrantStore`；`FilesScreen` 增「SAF 来源」重新授权/移除面板（OpenDocumentTree picker）。新增 27 例 JVM（resolver 12 + service 5 + file-manager 10）+ 13 例设备（scope 5 + reader 8，in-APK 文档树 fixture 驱动真 `ContentResolver`）；设备验收 24/24 × 两台（API 29/36）+ JVM 458 例 + spotless/detekt/lint/assemble 全过；设备期发现并修复 2 处缺陷（生产 1：`substringBeforeLast` 无分隔符语义致根级文件 walk 错位恒 not-found；夹具 1：子列表 URI 段 `children` 复数，生产 reader 无错）。**HXA-058 实现完成**——文件管理器导入/导出入口（HXA-044 受限管线接线，管线仅追加 `onProgress` 尾参、防御逻辑零改动）：导入 `OpenDocument`/`OpenDocumentTree` picker → `input/`（`ContentResolverSafTreeLister` 有界枚举 200k + `SafTreeImportPlanner` 净化歧义 fail-closed 规划，目录只计一次/深度 32/文件数 10k，skip 全报告）；导出 `CreateDocument` 或 HXA-057 已授权树（WRITE 7 步实时复核先于任何字节写出，`ContentResolverSafTreeDestination` create-document 目标解析：冲突 FAE/overwrite 复用/歧义 fail closed）；冲突策略四选（ASK 默认，绝无默认覆盖，导入 OVERWRITE 先移回收站）；UI 展示来源/目标/名称/大小/策略/进度/取消/逐项目最终结果（成功/冲突/跳过/已取消/失败 + 细节）；**verified 语义**：`SafExportVerifier` 字节重读（读满且仅读满 + SHA-256 相等）才显示"（已校验）"，否则只报平台确认（`sizeVerified` 独立）；失败矩阵覆盖（同名冲突/部分流/大小谎报/磁盘满（流中断替代证据）/目标撤销/取消/进程回收（崩溃临时回收替代证据）/原子性/临时文件回收）；导入/导出是文件管理动作（不创建聊天消息、不发 Provider、不扩大 Agent scope）；未接线 fail closed（`导入/导出未接线`）；JVM feature/files 120 例 + app 双 flavor 各 225 例全绿（新增 51 例）+ spotless/detekt/lint/assemble + check-docs/lockfiles/secrets/adr 全过；26 例设备测试已由仲裁会话运行全绿（feature 35×2 + consumer 94×2 + developer 107×2，API 29/36）；设备期发现并修复 8 项缺陷（生产 2：list/destination 空游标 fail-open→fail-closed；夹具 4：两个 in-APK 树 provider create-document insert 需匹配 `tree/<root>/document/<parent>` 4 段形态、test 包进程无 kotlin-stdlib→`TransferTestDocumentsProvider` 纯 Java 重写（`exported=true`，test APK 不分发）、`OpenableColumns` 列名 `_size`/`_display_name`；测试架构 2：API 30+ 无法为 fixture provider 铸造持久化 WRITE→`WritableOverrideCheck` seam（fail-closed 侧仍真实 check 验证）、UI 正向 verified 回合改投 facade 层、UI 验证只读树全路径拒绝 0 写入；生产 1 另计：工具栏/预览动作行溢出致尾按钮零宽不可点→拆行）；**HXA-059 完成**——Provider 模型自动发现与选择（连接测试第 2 阶段 `listModels` 列表自动带出：「后端可用模型」（probe 界 1000 + UI 显示 200 + 过滤）+ 点选预填编辑表单（不自动保存）+ `Unsupported` 显式手动提示 + phase 2 `Failed` 无列表；状态存储向后兼容第 8 字段；设备阶段由仲裁会话执行：`ProviderModelDiscoveryUiTest` 4/4×2 + 真实 SGLang endpoint smoke 1/1×2（0 跳过）+ consumer 98×2 / developer 112×2 / feature 35×2 + JVM 85+237×2+120 全绿；设备期修复 1 生产缺陷（自托管无 key provider 编辑保存被静默禁用）+ 4 测试断言缺陷 + 1 既有测试单类复跑缺陷；2 处既有环境 flake 如实记录并有 main 同签名复现证据） | [HXA-057 完成记录](../completion-records/HXA-057.md)、[HXA-058 完成记录](../completion-records/HXA-058.md)、[HXA-059 完成记录](../completion-records/HXA-059.md) |
| M6 | HXA-060～069 最小 WebView 浏览器 + Browser snapshot + Browser actions + Browser download（`:feature:browser`：加固 System WebView（逐字段设置 + 设备回读重建再验证）、fail-closed URL 策略（仅 http(s)/about:blank/data:text/html）、标签状态机（typed/codeless 双通道错误页、用户 stop 非错误、`navigationGeneration` 为 HXA-061/062 token 绑定地基）、100 MiB 流式上限下载队列（SAF 显式选位、无自动落盘）、Cookie/缓存清除入口、Compose UI 与 app 接线、无永久 JS 桥；固定版本化 JS 提取有界语义树（宿主 fail-closed 再校验、密码值绝不读取）、node token 绑定 tab/origin/navigation generation/fingerprint/TTL、网页内容标记为不可信数据；Browser actions：11 个 `browser.*` 工具经纯 JVM `:tools:browser` 端口 fail-closed 映射、固定版本化动作脚本 click/type/scroll、动作前 node token 再校验使导航/DOM 变化/TTL 使旧 token 失效、密码/支付/验证码字段宿主 + JS 双闸门拒绝（宿主权威）、截图仅捕获 Helix WebView 落 Workspace 模型只见 model-safe reference；Browser download：`browser.download` 工具经纯 JVM `:tools:browser` 端口 fail-closed（协议/重定向/MIME/size/name/target policy、手动重定向逐跳再校验、100 MiB 上限流式写入共享 Workspace `output/` 落 model-safe reference、APK/DEX/JAR/SO 绝不自动打开或执行）+ 纯 JVM `BrowserDownloader` 执行器与 `:core:workspace` `writeArtifactStream` 上限流式写入；分享/Intent/剪贴板（HXA-064）：`android.*`/`clipboard.*` 4 个工具经纯 JVM `:tools:android` 端口 fail-closed 映射（`android.open_uri` 仅 http(s) 只打开、非 http(s) 方案绝不构建或启动 intent、`isHttpUrl` 纯 JVM 方案门；`android.share` 仅建 `ACTION_CHOOSER` 包 `ACTION_SEND` 由用户选目标、分享文本经 L2 审批卡先预览、Helix 绝不替用户挑目标；`clipboard.read`/`clipboard.write` 按可见前台闸门拒绝、读 4 000 字上限经纯 JVM `boundClipboardText` 有界）+ Context-backed `AndroidSystemBridgeImpl` 生产端口（真实 Intent 构建 + 系统 `ClipboardManager`、可注入 `ForegroundProbe`/`IntentLauncher` seam）；152 例 JVM（`:tools:browser` 36 + `:feature:browser` 116）+ `:core:workspace` 流式写入单测 + 24 例 androidTest/设备 × 两台，另 HXA-064 `:tools:android` 27 例 JVM + 8 例 androidTest/设备 × 两台全过；通知和日历（HXA-065）：`notifications.query`（通知 app allowlist + ≤24h time window 上限过滤、权限关闭返回 `permission-missing` 绝不为空列表冒充成功）+ `calendar.prepare_event`/`calendar.commit_event`（草稿先建无写无权限、commit 经 `WRITE_CALENDAR` 闸门 + 单次写 + 消费草稿）经纯 JVM `:tools:android` 端口（`NotificationsBridge`/`CalendarBridge`）fail-closed 映射 + Context-backed 生产端口（`NotificationListenerService` 快照 + `CalendarContract` 写入、可注入权限/来源 seam）+ `:tools:android` 47 例 JVM + 11 例 androidTest/设备 × 两台全过；HTTP fetch 与 SSRF 网络边界（HXA-066）：`http.fetch` 工具（GET/HEAD、SSRF/redirect/size/timeout）经纯 JVM `HttpFetchBridge` 端口 fail-closed 映射（`REFUSED`→稳定 reason code 绝不用空 body 冒充成功、`TIMEOUT`→TimedOut、`ERROR`→Failed）+ 纯 JVM `SsrfAddressPolicy` 分类器（`core:policy`：整组 A/AAAA/IPv4-mapped 候选判定、cloud-metadata/malformed 整组拒、只把 `Allowed.connectable` 已验证集合交给 transport、连接后 `revalidatePeer` 复验实际 peer 拦 in-flight rebind、原 hostname 做 TLS SNI/证书、每跳 redirect 整跑 origin/DNS/IP/scope 再判定跳数封顶、Standard 仅公网 / Advanced 仅用户预建精确 `NetworkOriginScope` 且模型 URL 不能创建 scope）+ 原始 `java.net.Socket` `HttpFetchBridgeImpl` 生产 transport（有界 GET/HEAD、Content-Length/chunked/读到关闭三帧均封顶 `maxBodyBytes`、`Connection: close` 不复用）+ `:core:policy` 21 例（`SsrfAddressPolicyTest`）+ `:tools:android` 58 例（含 11 `HttpFetchToolsMappingTest`）JVM + 30 例 androidTest/设备 × 两台全过；dataSync 前台服务（HXA-066 后半：`dataSync` + `FOREGROUND_SERVICE_DATA_SYNC` + `foregroundServiceType="dataSync"`，turn 状态驱动起停、等待审批/输入/终态/无 turn 即停、Android 15+ `onTimeout(int,int)` 超限自停、6h/24h 限额谓词、通知停止；9 例 JVM + 3 例 androidTest/设备 × 两台全过）；语音输入（HXA-067）：`ACTION_RECOGNIZE_SPEECH` 系统识别 Activity 把**用户主动**录音转为**可编辑草稿**（纯 JVM `VoiceInputMapper` 4 态决策 `Draft`/`Cancelled`/`Unavailable`/`Available`，成功识别 = inert 文本只进 composer `input`、**绝不自动发送**；用户取消/无匹配/失败因平台 Activity 结果无 error-code extra 统一塌缩为无害 `Cancelled`、具体原因由系统识别 UI 告知；`SpeechRecognitionLauncher` Android seam 查 `isRecognitionAvailable`/建 intent/提取 `EXTRA_RESULTS`，按系统 locale 启动、无 `EXTRA_LANGUAGE`；ChatScreen「语音」按钮经 `rememberLauncherForActivityResult` 收回结果、**不后台常驻监听**、无 mic 权限；9 例 JVM + 7 例 androidTest/设备 × 两台（API 29/36）全过）；Advanced 有界出网规则管理（HXA-068）：高敏出网规则的有界**类型化持久化**（Room v5 `high_sensitivity_rules` 表 + `MIGRATION_4_5` + 无损 `UserScopeCodec` round-trip 全部 scope 构造字段 + fail-closed 重水化重建，损坏/未知行 throw 永不匹配）+ 列表/创建/撤销 UI（仅 developer/Advanced 渲染，consumer/Standard 永无入口，`EgressRuleConsumerAbsenceTest` 正向缺失断言）+ `LiveEgressRules` 单一 fail-closed 闸门（非 ADVANCED → 零规则、连 loader 都不跑；存储抛异常/损坏 → 零规则）+ 引擎 `isLiveFor` 窗口/时钟回拨判定（`createdAt <= now < expiresAt`，回拨像到期一样 fail closed）+ `AppContainer` `ruleProvider` 接线（每次 Policy Engine 评估从 Room 读，匹配规则经 broker 上浮为审批卡 BOUNDED 规则）；规则严格绑定稳定 Provider/MCP ID + 规范 origin + 强制 scope + 四固定 TTL（1h/24h/7d/30d），无通配符/滑动续期/"全部允许"；进程重启重水化仍精确匹配（recreate 回合）+ 切回 Standard 隐藏但不删（ADR-0012 profile 切换 fail closed 不擦存储）；JVM `:core:policy` 100 + `:core:storage` 62 + `:app` 双 flavor 各 243 例（0 失败）+ 设备 `:core:storage` 迁移 41×2 + `EgressRuleUiTest` 1×2 + `EgressRuleConsumerAbsenceTest` 1×2 全过；国际化与 App 语言切换（HXA-069）：全部用户可见文案资源化（`values` 简体中文 base + `values-en` + `values-zh-rCN` 三套键 parity、生产源码无硬编码 CJK 门禁 `check-i18n.sh` 573 键 parity、2 条 allowlist 模型可见注入 + pinned JS 脚本）；纯 JVM 模型只带 res-ID + 参数、UI 边界经共享 `localizedString` 解析、Provider/Tool/审计稳定字段 locale-independent（设备测试确认）；`AppLanguage` 三值闭环 跟随系统/简体中文/English、只持久化选择、API 33+ 经 `android.app.LocaleManager` 与系统 App 语言双向同步、跟随系统不留陈旧 override、Activity/进程重建保持、跨进程含 ADR-0015 隔离 QuickJS fail-closed 一致；`bundle.language.enableSplit=false` 使全部 locale 进 base APK；`:app` 双 flavor 单测 + `connected*AndroidTest` API 29/36 × 双 flavor 全过 | [HXA-060 完成记录](../completion-records/HXA-060.md)、[HXA-061 完成记录](../completion-records/HXA-061.md)、[HXA-062 完成记录](../completion-records/HXA-062.md)、[HXA-063 完成记录](../completion-records/HXA-063.md)、[HXA-064 完成记录](../completion-records/HXA-064.md)、[HXA-065 完成记录](../completion-records/HXA-065.md)、[HXA-066 完成记录](../completion-records/HXA-066.md)、[HXA-067 完成记录](../completion-records/HXA-067.md)、[HXA-068 完成记录](../completion-records/HXA-068.md)、[HXA-069 完成记录](../completion-records/HXA-069.md) |

- M5A / HXA-055 已完成：图片输入与 Provider 视觉能力（production 图片源 + 端上归一化 + `VisionLimits` 集中预算 + 连接测试第 5 阶段视觉探测/手动声明 + 归一化 artifact 绑定重验 + 出网披露图片行；JVM 753 例 + consumer 设备套件 64 例 × 两台 + spotless/detekt 全过）。
- M5A / HXA-056 已完成：文本/图片附件端到端硬化与发布边界（fixture E2E 13 例 + 分享草稿 UI 2 例 + `SessionBindProviderTest` 4 例；发送/流式/Tool Loop/历史恢复/脱敏 + 失败矩阵 + 统一稳定拒绝 + 泄露断言；本地草稿绝不自动发送 + 草稿会话单发绑定；三 adapter golden + 1 真实 vision endpoint smoke（本地 SGLang）；consumer 79 + developer 92 × 两台 + JVM 全过 + spotless/detekt 全过）。**M5A 完成。**
- M5B / HXA-057 已完成：persisted SAF tree scope 接线（每次使用 7 步实时复核 + fail-closed + 模型不透明 + `DocumentsContract` 只读浏览后端 + 来源列表/重新授权/移除面板；JVM 新增 27 例 + 设备 13 例；设备验收 24/24 × 两台（API 29/36）+ JVM 458 例全过；设备期发现并修复 2 处缺陷——生产 1（根级文件 walk 错位）、夹具 1（URI 段单复数）；SAF 写后端显式未实现）。
- M5B / HXA-058 已完成（**设备验收由仲裁会话执行**）：文件管理器导入/导出入口（HXA-044 受限管线接入文件管理器，管线仅追加 `onProgress` 尾参、防御逻辑零改动；导入 picker → `input/` + 有界枚举 200k/歧义 fail-closed 规划（目录只计一次、深度 32、文件数 10k、skip 全报告）、导出 `CreateDocument`/已授权树（WRITE 7 步实时复核先于任何字节写出）+ `SafExportVerifier` 字节重读校验 verified 语义（"（已校验）"只报告有证据的 hash 一致，否则仅报平台确认）+ 冲突策略/进度/取消/逐项目结果 UI + 失败矩阵覆盖（同名冲突/部分流/大小谎报/磁盘满（流中断替代证据）/目标撤销/取消/进程回收（崩溃临时回收替代证据）/原子性/临时文件回收）+ 未接线 fail closed；导入/导出是文件管理动作：不创建聊天消息、不发 Provider、不扩大 Agent scope；JVM feature/files 120 + app 双 flavor 各 225 全绿（新增 51 例）+ spotless/detekt/lint/assemble + check-docs/lockfiles/secrets/adr 全过；设备验收由仲裁会话执行全绿：`./gradlew :feature:files:connectedDebugAndroidTest` 35×2 + `:app:connectedConsumerDebugAndroidTest` 94×2（API 29 首轮 1 例已知 E2E flaky 单类复跑绿）+ `:app:connectedDeveloperDebugAndroidTest` 107×2（API 29/36），设备期修复缺陷 8 项、JVM 复验 120+225×2 全绿，M5B 完成）。
- M5B / HXA-059 已完成（**设备阶段由仲裁会话执行完毕**）：Provider 模型自动发现与选择（连接测试第 2 阶段 `listModels` 列表自动带出 UI：测试通过自动带出「后端可用模型」（probe 界 1000 + UI 显示 200 + 过滤），点选预填编辑表单 model 字段（不自动保存），`Unsupported` → 显式「后端未提供模型列表，请手动输入」，phase 2 `Failed` → 既有稳定错误文案、无列表；状态存储向后兼容可选第 8 字段（模型列表 JSON 数组，坏列表只降级、保留 PASSED）；只读 GET 无新增网络/写路径、key 永不入 UI/日志、模型 id 不入 logcat/Room 诊断、模板不写死模型名；JVM `:provider:api` 85 + app 双 flavor 各 237（新增 12 例）+ feature/files 120 全绿 + spotless/detekt/lint/assemble（含双 flavor 设备测试 APK）+ check-docs/secrets/lockfiles 全过；设备阶段：`ProviderModelDiscoveryUiTest` 4/4 × 双设备（API 29/36）+ 真实 SGLang endpoint smoke（`SglangUiSmokeTest`，HXA-056 同款 `10.0.2.2:30008`、空 key）1/1 × 双设备 0 跳过——五阶段连接测试对真实 27B 服务 PASSED、「后端可用模型 (1)」展示真实模型 id、芯片预填保存持久化到行；consumer 98×2 + developer 112×2 + feature 35×2（2 处既有环境 flake 如实记录并有 main 同签名复现证据）；设备期修复 1 个生产缺陷（自托管无 key provider 编辑表单「保存」被静默禁用——`editTemplateFor` 模板猜测凭空加 key 要求）+ 4 处测试断言缺陷 + 1 处既有测试（`AuditScreenTest`）单类复跑缺陷）。**M5B 全部完成**（HXA-057/058/059）。

架构决定的状态与理由见 [ADR 目录](../adr/README.md)；跨里程碑复核、事实修正和历史取舍见[文档复核记录](../history/documentation-review.md)。“有完成记录”不等于当前发布能力，当前可用边界只看本文件的摘要、接口和限制。
- M8 / HXA-088 已完成：Git Workspace 语义 Spike 与 ADR-0008（roadmap §12 末项；**Spike 证据 + ADR-0008 已由所有者接受（2026-09-05），零产品代码、零新依赖**）——三路径比较：路径 1 主 App Workspace 权威仓库 + 每操作完整原子事务（全部组件已在 084/085 管线设备验证，零新 IPC/挂载面/依赖，**推荐**）/ 路径 2 Runtime 私有目录（设备实证 084 Job 模型中 guest 只能看到 job 快照——私有目录对 guest 不可达；需新 bind 面 + 跨 uid 仓库事务协议 + 纳入 087 卸载/更新/删除语义，成本 > 收益）/ 路径 3 Android Git 库候选调查（JGit 7.7.1.202607240634-r：纯 Java，EPL-2.0 OR GPL-2.0-with-classpath-exception；libgit2 v1.9.7：GPL-2.0-or-later OR MIT，需 arm64 NDK 构建 + JNI；双 git 语义分叉 + 供应链负担——**均未引入依赖**）；真实 084 Job 管线设备测量 × 两台（API 29/36 arm64-v8a 4 KiB 模拟器 = ADR 允许的代表性设备，无真机如实记录）：小仓库（64×8 KiB）归档 31,056 B/13–46 ms + guest ≈251–255 ms + 输出 44,876 B/83 文件；中仓库（512×32 KiB ≈ 16 MiB；guest init+add+commit+gc）归档 312,373 B + guest ≈1.3–1.5 s + 输出 336,608 B/542 文件 + gc 后 `.git` 28 文件/204–364 KiB（**首版预算按 pack 后形态表达**；不可压缩载荷 ≈ 原始 16.8 MiB 仍在 084 限额 ≤128 MiB/≤4,096 文件/单文件 ≤64 MiB 内）；损坏 loose tree 对象 → `git fsck --full` 报告 `bad sha1 file` 从不静默判净（**平台事实：fsck 报告损坏时可退出码 0 → 结构化执行器必须解析输出文本**）；job id → 恰好一个稳定终态记录（幂等查询无重放；输出整归档边界无半解包；配合 086 宿主 kill ORPHANED——"静默半仓库"无落点）；攻击面设备实证（快照携带恶意仓库 × 首版命令集 status/diff/log/init/add/commit）：无防护时 post-commit hook（commit 触发）与 `.gitattributes` filter（add 触发且**清空文件内容** = 进入快照的静默篡改通道）均执行，alias 不展开（结构化子命令不走 alias 表）/submodule 不递归（离线+无 submodule 命令）/credential helper 不触发（首版无远程命令）；有防护（每次 `-c core.hooksPath=/dev/null` + `GIT_CONFIG_GLOBAL`/`GIT_CONFIG_SYSTEM=/dev/null` + `GIT_TERMINAL_PROMPT=0` + `GIT_PAGER=cat`）hook 抑制、首版命令集全成功，**但 filter 仍执行 → 强制宿主侧仓库预扫描**（`filter`/`diff`/`merge` 命令配置 fail-closed 拒绝 `add`/`commit`）；guest 平台事实：Alpine 3.22.5 rootfs 的 `/bin/sh`（BusyBox ash）不支持 `$(...)` 命令替换（backtick 可用）——首版命令模板约束；首版拒绝/禁用策略 + 候选 Tool schema（`code.git.run`，Advanced-only，`status/diff/log` L1 / `init/add/commit` L2，`reset --hard`/`clean` 不进入首版）+ 路径 1 推荐全部记录在 ADR-0008（**已由所有者接受，2026-09-05**；持久 Git 实现由后续 HXA 按 ADR 执行）；验证：`ProotGitSpikeDeviceTest` OK (6 tests) × 两台 + companion 全量 56 例 × 两台 + `:app` 141 例 × 两台 + spotless/test/detekt/lint/check-docs/check-secrets/check-lockfiles 全过；决策记录 6 条（真实管线测量/路径 1 推荐/filter 预扫描强制/fsck 退出码不可信/ash `$(...)` 约束/ADR 状态），详见 [HXA-088 完成记录](../completion-records/HXA-088.md)。
- M8 / HXA-087 已完成：更新/卸载/法律页（roadmap §12 四项全落地）——同签名 APK 更新：「验证 Runtime」点击后 `verifyNowNote` 揭示"需更新"（持久化锚点的规范 lock 指纹 vs 内嵌 lock 漂移 → 握手稳定 `LOCK_MISMATCH` = 085 `NEEDS_UPDATE` 同源）+「重定基线（我已知晓基线变更）」二次显式动作（`clearAnchorForRebaseline` 删锚点 → 再验证对新 lock 重建锚点；移动基线绝不静默接受，083 锚点属性保持）+ 修复页更新态识别（`RuntimeInstallQueries.activeLockSha256` 读活动安装目录 `manifest.json` 内嵌 lock 指纹 vs 本 APK 内嵌 lock；纯 JVM `updateStateFor` fail-closed——无活动/损坏 manifest 绝不读成"一致"；更新 = 同一 082 原子安装路径，失败旧 runtime 保持可用）+ 用户门控 rollback（082 `activateRollback` 原子指针交换无文件移动；回滚后第一版真实 Job SUCCEEDED 设备断言；无回滚版本 `NoRollback` 稳定态）+ 完整删除（Settings「删除 Runtime」确认对话框 → 主 App 清自己锚点（gate 降级 NOT_VERIFIED）+ `EXTRA_REMOVE_RUNTIME` consent 跨 uid → companion 面内二次确认按钮 → `ProotRuntimeRemoval` 只删 `filesDir/runtime`（路径名 fail-closed 校验，错根拒绝）；本包 Workspace 侧文件存活设备断言；companion filesDir 兄弟文件存活断言；删除后重装路径可用）+ 离线法律页（companion `ProotLegalActivity`：导出 + 签名权限 + 无 launcher + 零 INTERNET；内容 = 离线声明（无网络/只随同签名 APK 更新/无应用内自更新/URL 永不访问）+ 构建清单（规范 lock 指纹/ABI/每组件版本·来源 URL·源码 repo@ref·SHA-256·SPDX·补丁·预装包逐包版本+SPDX）+ 许可证全文按 `textRef` 从内嵌资产读取去重离线可读——单一真相在 companion 资产（081 管线产出），主 App 不复制；主 App「许可证与来源」用户点击跨 uid 打开实证）+ consumer 保持无 PRoot（同 FQN 模块缝 + `verifyNowNote`/`removeRuntimeNote` 等 flavor 中性缝）；验证：`scripts/accept-hxa-087-updates.sh`（companion 更新套件 7 例 + 主 App E2E 4 例：法律页权限形状/用户点击打开/删除锚点清除+gate 降级+Workspace 存活/模拟 lock 漂移→`LOCK_MISMATCH`→显式重定基线→再验证重建）全绿 × 两台（API 29/36 arm64-v8a 4 KiB）+ 086 生命周期回归 × 两台 + companion 全量 50 例 + `:app` 141 例 × 两台 + spotless/test/detekt/lint/check-docs/check-secrets/check-lockfiles 全过；决策记录 6 条（需更新只在用户点击揭示/重定基线两次显式/删除双端各清自己+面内二次确认/法律页放 companion 单一真相/更新检测读活动 manifest/读写侧分离），详见 [HXA-087 完成记录](../completion-records/HXA-087.md)。
- M8 / HXA-086 已完成（**模拟器可验证范围**；真机 4/16 KiB smoke、Doze/安全锁屏/热限、最低设备集真机证据无真机，如实停下不做假绿）：真机 smoke 与隔离——companion 通知停止面（`ProotJobNotification` 普通通知 channel `proot-jobs`/tag 每 Job/`IMPORTANCE_LOW`，**非 FGS**：job 路径零 FGS 零 wake lock 不变量保持，`POST_NOTIFICATIONS` 未授予时 Job 不受影响；`ProotJobStopReceiver` 导出 + `com.helix.permission.BIND_PROOT_RUNTIME` 签名权限 + jobId 形状校验 → `ProotJobRunner.cancel` 真实取消断言；四终态统一撤通知）+ companion smoke 套件（python3/node/git/ripgrep 各一条真实 Job：随机 token 经 guest env/sh 回显 + host `smoke-ran-<jobId>` 标记 + Journal 记录三证新鲜度契约，陈旧执行不可能通过）+ companion 隔离 4 例（guest 读不到 companion dataDir 标记/共享存储不可达/网络不可达 `sys.exit(99)` 反证 + 结构性断言 companion 包零 INTERNET/存储权限（`GET_PERMISSIONS` 查询 companion 包名））+ 主 App 侧隔离 E2E 2 例（真实 `ProductionLinuxExecutor` 全链：guest `cat`/`touch` 主 App filesDir 标记 → READ-FAILED/WRITE-FAILED + 内容缺失/文件不存在断言）+ 生命周期 6 相（`ProotLifecycleE2eDeviceTest`，脚本分阶段驱动：强制停止 → 真实 gate 稳定 `Failed`（detail 含 unavailable+force-stopped）；从未打开冷绑定/空闲回收（pkill 60 s）/锁屏（keyevent 26）/低内存（`am kill`）→ 各真实 Job SUCCEEDED；主 App 死亡中途 → ORPHANED 按 job id 对账（exitCode null，绝不重放）；companion host `pkill -9` 中途 → `Unavailable(DEAD_OBJECT)` ∪ `Terminal(ORPHANED)` 双分支稳定非成功（083 冷重绑 + starttime 双证清扫对账——设备实证客户端轮询走冷重绑路径）；长 Job 期间 `dumpsys power` 8 采样 companion uid 零 wake lock 零 FGS）+ 同 jobId 不重复执行（084 `aJobIsStartedExactlyOnce`）+ `scripts/accept-hxa-086-lifecycle.sh`（构建安装 4 APK + 11 阶段，host 侧 job id 经 `run-as` 文件传递——本平台测试 stdout 不跨 raw instrument 流）；验证：companion 全量 **43 例** × 两台（32+4+4+3）+ `:app` developer 全量 **137 例** × 两台（129+2+6，生命周期 2 个 host 括号相全量上下文 assume 跳过）+ 11 阶段脚本全绿 × 两台 + spotless/test/detekt/lint/check-docs/check-secrets/check-lockfiles 全过；设备期修复 058 fixture 跨进程状态累积潜在缺陷（`TransferTestDocumentsProvider.reseedIfTheHostClearedTheBackings`：host 清空 backing 目录后 provider 自恢复——先清 in-memory 行再 `mkdirs()+seed`，失败降级空 fixture 诚实 FNF；live 进程 + 清理 ×3 全绿验证）；决策记录 6 条（普通通知非 FGS / STOP 导出锁签名权限 / 停止测试进程内 receiver seam / companion 死亡双分支稳定结算 / smoke 新鲜度契约 / 最低设备集阻断落点=081 ELF 门禁+082 页大小预检+085 gate）；无新 ADR，详见 [HXA-086 完成记录](../completion-records/HXA-086.md)。
- M8 / HXA-085 已完成：`bash` Tool = `code.linux.run`（roadmap §12：每次审批 + 仅 Advanced + Runtime 单独安装/启用 + 用户完成过零 Job 验证才可用）——developer flavor 注册（consumer 构建不存在，`ProotToolModule` 同 FQN flavor 切换）+ 每执行四态 gate（`READY`/`NOT_INSTALLED`/`DISABLED_OR_FORCED_STOPPED`/`NOT_VERIFIED`；"需更新" 执行期 `LOCK_MISMATCH` → `NEEDS_UPDATE`；用户完成过零 Job 签名/协议/ABI 验证 = 持久化 anchor（`ProotRuntimeSupervisor.anchorPresent()` 新增），进程预先存活/手动打开修复入口都不是条件，被动 Registry 刷新绝不重新绑定）+ 批准后每次执行重新握手（084 `ProotJobClient` 每事务冷绑定，绝不回退主进程 shell）+ STANDARD 由真实 `PolicyEngine` 设备断言 `Deny(ISOLATED_RUNTIME_REQUIRES_ADVANCED)`（generic L2 无 auto-run 通路，ADR-0012）+ 输入 = 经 `WorkspaceArtifactStore` 的有界快照（≤64 文件 × ≤1 MiB，基名去重，真实 Workspace 绝不挂载）+ env 主进程 `ProotEnvScreen` 三重筛选（已知 SecretStore 值按执行时刻现读，理由绝不含值，Runtime 零 SecretStore 能力 → secret inheritance 由构造不可能）+ wire `deadlineMs` = **剩余审批窗口**（companion watchdog `createdAt + deadlineMs` 恰在审批绑定的 deadline 进程组 kill；初稿误传绝对 epoch，设备期修正）+ 输出归档 `manifestSha256` 对已验证终态记录 + `Extraction.manifest` 逐条 sha/size 重验 + 只把验证后的 `result.txt` 经 store 导入 `output` 引用自身 region（首段 ∈ `input|work|output` + store containment 双重强制；**缺失/超限 = SUCCEEDED 无导入**，finding 不是 failure）+ 审批卡完整 script/argv + `codeSha256` 短摘要 +「N 个文件，快照复制，不挂载真实 Workspace」+「超时 N s（进程组 kill）· 输出上限 8 MiB · 联网：否（Runtime 无 INTERNET 权限）」（schema 无网络开关——Advanced/LAN scope 都不能给 PRoot 联网，§6.1）+ Settings Advanced-only PRoot Runtime 区块（状态行 + 验证（off-main `verifyNow`）+ 修复 Runtime 仅用户点击 `openRepair`；恢复后重试 = 新 ToolCall/approval/jobId——审批 deadline 过期 `parsed()` 即 `ParseFailure` + 每执行全新 `jobId`/`executionId`）+ JVM 7 例（`LinuxRunToolTest`，testDeveloper）+ 设备 `LinuxRunToolE2eDeviceTest` 5 例 × 两台（descriptor 无网络键 / STANDARD 真实 Policy Deny / **全链真实 guest 执行 + 导入字节全等 + sha + audit jobId** / 已知 secret 值 wire 前拒绝 / `exit 3` → `JOB_FAILED` 稳定失败）+ `:app` developer 全量 **OK (129 tests) × 两台** + companion 全量 **OK (32 tests) × 两台**（API 29/36 arm64-v8a）+ spotless/detekt/lint/check-docs/secrets/lockfiles 全过；设备期修复 6 项（deadline 相对语义、`importResult` 改用 `Extraction.manifest`（提取器不落盘 manifest.json）、`result.txt` 缺失改 SUCCEEDED 无导入、输出 region 首段判定、输入基名按索引去重、detekt ThrowsCount/Swallowed 结构化）；无新 ADR（实现已接受 ADR-0007 + ADR-0012 + local-code-execution §6.5/6.6/6.7），详见 [HXA-085 完成记录](../completion-records/HXA-085.md)。
- M8 / HXA-084 已完成（真机部分归 HXA-086/088，模拟器可验证部分完成）：PRoot runner（`:runtime:proot-ipc` job 协议扩展——`ProotJobSpec`（argv/显式 script 互斥、cwd 严格单段、env ≤64、deadline/输出上限、`inputManifestSha256`）+ `ProotJobRecord` 严格 codec（terminal commit = canonical committedBody SHA-256、SUCCEEDED 必带 exitCode、ORPHANED/INPUT_INVALID 绝不携带）+ `ProotJobWire` 手工 Parcel + 事务码/状态字节（服务端零跨 APK 异常）；`:runtime:proot-core` archive 原语——manifest codec/`JobPath` 词法防护/`JobZipWriter`（manifest 先行 + 哈希全等门）/`ZipJobExtractor`（自研 central-directory 先行拒绝 symlink——JDK `ZipEntry` 不暴露 unix mode）；companion `ProotJobRunner` 单例——有界 journal（128 条/1 MiB，active/未对账不静默删除、满额拒绝新 Job `JOURNAL_FULL`、对账 payload 即删 + 7 天 tombstone、30 天 `evidenceExpired` marker）+ 生命周期（重复 jobId/executionId → `Duplicate` 绝不二次启动、输入归档 → `INPUT_INVALID`）+ **PRoot 首次设备真实执行**（设备实证 SELinux 平台事实：`untrusted_app` 对 `app_data_file` 的 `execute_no_trans` 被拒、`apk_data_file` 被授予 → 启动链 `setsid→linker64→proot`，loader 以 `jniLibs` 可执行副本发行 + 每次 Job 哈希对 081 锁；PRoot 在途 execve 改写（PTRACE）使 `LD_PRELOAD` exec hook 不可达——实现后整体移除）+ watchdog `scheduleWithFixedDelay` + 进程组 kill（setsid pgid）+ `/proc` 后代 BFS + bounded 双流共享预算 + 输出归档 hash 证明（`AutoCloseOutputStream` PFD）+ 孤儿 `(pid,starttime)` 双证清扫（重用 pid 绝不触碰）；客户端 `ProotEnvScreen`（secret 名 pattern 即使显式加名也拒绝 / 已知 SecretStore 值改名拦截 / 认证结构形状，理由绝不含值，Runtime 零 SecretStore 能力）+ `ProotJobClient`（每事务冷绑定、PFD 所有权约定、`query`/`cancel`/`reconcile`、`awaitTerminal` 的 `Interrupted` 停泊点、`DeadObjectException`→`DEAD_OBJECT`、断连只查询/对账绝不重放、零 FGS/零 wake lock）；`ProotNative.pageSizeBytes()` NDK seam 收编 083 页大小常量；JVM 29 例（core 10 + ipc 11 + client 8）+ companion 设备 32 例 × 两台（含真实 guest 执行/重复 jobId/manifest 失配/取消/TIMED_OUT/对账/额度满/孤儿清扫/script 模式/tmp 隔离）+ `:app` 跨 APK `ProotJobE2eDeviceTest` 4 例 × 两台 + 083 绑定 E2E 8 例 × 两台（API 29/36 arm64-v8a 全绿）+ spotless/detekt/lint/check-docs/secrets/lockfiles 全过；设备期修复 8 项（安装产物 0600 无执行位、PRoot 三环境变量缺失、`--` 分隔符、`Process.pid()` 不存在→类层级反射、exec hook 平台不可达移除、`useLegacyPackaging` 落盘、客户端双 `readException`、jobId 线格式对齐）；无新 ADR，详见 [HXA-084 完成记录](../completion-records/HXA-084.md)。
- M8 / HXA-083 已完成（设备验收含 1 项平台阻塞记录）：独立 Runtime APK/IPC——新增 `:runtime:proot-ipc` 共享模块（`ProotRuntimeProtocol` 常量/事务码、`RuntimeTargetDescriptor` + 严格 fail-closed codec、`PfdManifestChannel` pipe PFD 上限传输、`ProotHandshakeClient` 状态字节客户端、`ProotRuntimeServiceBinder` 无 AIDL 服务侧、`UnavailableCause` 稳定原因闭集；**协议零跨 APK 异常**——API 34 `Parcel.readException` 语义变化 + quickjs house 先例）；companion（`:runtime:proot-app`）`ProotRuntimeService`（bound-only、`onBind` 经 `ProotCallerVerifier` 重验调用方签名集 fail-closed、每次 bind 现算 `ProotHandshakeManifest`（protocol 1 + versionName + ABI + 082 canonical lock 指纹））+ signature permission 同守卫 service 与最小 `ProotRepairActivity`（无 launcher/无 INTERNET/文案入资源）；客户端（`:runtime:proot-client`）`ProotRuntimeProbe`（FLAG_STOPPED 跨包可见）/`ProotLocalStateCheck`/`VerifiedRuntimeStore`（tmp+fsync+rename 锚点，损坏读为无）/`ProotRuntimeSupervisor`（`checkLocalState`→显式 ComponentName + `BIND_AUTO_CREATE` 冷绑定 + 每调用独立 ServiceConnection + 20 s 硬 deadline→握手→锚点比对；`openConnection`/`withConnection` 084 seam；`openRepairActivity` 仅用户点击 + 稳定原因映射；**拒绝即解绑绝不留进程、绝不回退主 App shell**）；`:app` developer flavor `uses-permission`（consumer 不声明）；落实 ADR-0007（启动/Advanced/Registry 刷新零启动——结构性 + 设备断言；`TX_DEBUG_SELF_KILL` debug seam 驱动真实 binder 死亡设备测试）；修复期修订 6 项（`PackageInfoFlags` 守卫 30→**33**——API 30/31/32 真机将 `NoSuchMethodError`，lint NewApi 捕获；**调用方复验从 `onBind` 移入 binder `onTransact` 逐事务执行**（`onBind` 是主线程生命周期回调不携带调用方身份，`getCallingUid()` 返回自身 uid——初稿「实测 uid 正确」结论撤销，AOSP 源码 + 设备探针双证）；companion `<queries>` 补声明两个主 App 包（API 30+ 包可见性）；客户端补 `onNullBinding`/`onBindingDied` 覆写（null binder = 立即 `BIND_REFUSED`，不假超时）；`bindService` `SecurityException` → 稳定 `SIGNATURE_MISMATCH`；SetTextI18n 文案入资源）；验证中固化 5 项环境事实（AndroidJUnitRunner 跑在 `Instr:` 线程主 looper 自由→worker+`future.get(timeout)` 即安全；新鲜 `adb install` 后包为 force-stopped；shell `am start` 受 permission 拒、`su 0 am start` 代用户点击；`pidof` 不匹配 15 字符 comm 用 `ps -A | grep -w`；`am instrument` 原始输出格式解析）；JVM 42 例 + companion 设备 20 例 × 两台（API 29/36 arm64-v8a）+ `scripts/accept-hxa-083-lifecycle.sh` 跨 APK E2E **8/8 × 两台**（三生命周期阶段 + 冷绑定/二次验证/真实进程死亡→冷重绑恢复/用户门控修复入口/null-onBind 立即拒绝）+ spotless/detekt/lint/check-docs/secrets/lockfiles 全过；**初稿「跨 APK 投递 4 例被镜像平台缺陷阻塞」定性撤销**（误诊：实为上列 `onBind` 身份 + `onNullBinding` 两处实现 bug + `<queries>` 缺口，修复后双机全绿；探针日志/AOSP 佐证/原证据链重释见完成记录）；无新 ADR，详见 [HXA-083 完成记录](../completion-records/HXA-083.md)。
- M8 / HXA-082 已完成：RootFS installer（`.partial`/防 Zip Slip/smoke/原子激活/保留一版 rollback 全部落地）：纯 Kotlin 流式 tar 读取器 `TarStream`（512 B 块、typeflag 白名单 `'0'`/`'\0'`/`'5'`/`'2'` + GNU `'L'`/`'K'` 长名续块、256 MiB/10 万成员上限、64 KiB 流式 payload、结尾零块消费保证整档读满可哈希）+ `TarPathSafety` 纯词法防护（成员路径 NUL/换行/反斜杠/`..`/空段拒绝、前导 `/` 剥离、根成员 `/./` 归一 `""` 且只允许目录；symlink 目标词法深度判定，绝对目标按 PRoot chroot 树根基准，悬空合法，宿主绝不解析）；安装器 `RootFsInstaller`（`state/install.lock` 全程排他 + 同 JVM 双锁 held 不崩、启动清扫孤儿/stale `.partial`、解压+SHA-256/size 验证到 `.partial-<rand>/<inst_id>` 单遍流式（设备 heap 201 MB 内，137 MB 存档不整体缓冲）、PRoot 二进制 ≤64 MiB 门、结构 smoke（必需路径 `NOFOLLOW_LINKS` + 全树 ELF 有界 64 KiB 前缀按传入页大小 `PT_LOAD` 预检、`e_phoff` 越界 fail-closed，不执行 native）、`manifest.json` 内嵌 lock 重算、promote rename、**原子激活 active.json→rollback.json→最后删旧 rollback 目录**（任一崩溃点指针不指缺失目录）、失败阶段精确回填且前一版本无损）+ `activateRollback` 原子交换（交换前校验两版本目录 + rollback manifest）；lock 锁定对象修正为**原始 tar**（`f15c9434…`/137,287,680 B = AGP 展开后设备实际读取字节，`§6.4` 步骤 5 闭环；gz `500594df…`/50,955,376 降为构建日志产物；canonical 指纹 `46148505…`、lock 文件 raw `d2d0a59f…`）；`ProotRuntimeInstaller` Android 薄适配（`filesDir/runtime` + 内嵌 lock 基线 + 设备 ABI 门 + 资产流）；体积：APK 61,772,349 B、安装后 61,772,349 B、安装临时峰值 = 解压树 ≈133 MiB + 64 KiB 缓冲（update 最坏 3×133 MiB 磁盘瞬时）；验证中修复 9 项（真实存档根成员 `/./`、`File.exists` 跟随 symlink 误杀绝对 `bin/sh`、lock 指纹测试重言式、设备 OOM×2（137 MB `readBytes` + 51 MB node）、API 29 `Path.of` `NoSuchMethodError`→`Paths.get`、`ByteBuffer.wrap(arr,off,len)` 非切片陷阱、Kotlin/平台小坑、detekt/ktlint 阈值结构化重构）；JVM 83 例 + 设备 12 例 × 两台（API 29/36 arm64-v8a，完整安装流程：137 MB 解压 + smoke + 原子激活 + rollback 交换 + 篡改 fail-closed + 孤儿清扫）+ 资产管线 verify 逐字节一致（assetGate 360 ELF min p_align=16384）+ spotless/detekt/lint/check-docs/secrets/lockfiles 全过；无新 ADR，详见 [HXA-082 完成记录](../completion-records/HXA-082.md)。
- M8 / HXA-081 已完成：构建期固定资产（固定 PRoot/Alpine/bash/git/python/node/ripgrep：真实 `runtime-lock.json` = 5 组件（proot 5.1.107.92 termux deb / proot-loader / libtalloc 2.4.3 / libandroid-shmem 0.7 / alpine-rootfs 3.22.5）+ 53 包 apk 精确 pin（bash 5.2.37-r0、git 2.49.1-r0、python3 3.12.14-r0、nodejs 22.23.2-r0、ripgrep 14.1.1-r0 等，逐包版本+SPDX）；构建管线 `scripts/build-proot-assets.sh`（curl + SHA-256/size 校验 → Docker digest 锁定镜像 + `apk add name=version` → `deterministic_tar.py` 确定性重打包 → assetGate 全量 ELF 扫描 → lock 哈希校验；generate/verify 双模式跑通，verify 对锁定 lock 重建逐字节一致）；ELF `PT_LOAD` 对齐/ABI 检查器 `ElfLoaderAlignChecker`（纯 JVM fail-closed，ELF32/64 LE/BE，构建门禁/安装器预检（HXA-082）/16 KiB 判定（HXA-086）共用；rootfs 360 + PRoot 侧 4 个 ELF 全部 ≥16384 对齐、`e_machine`=EM_AARCH64）；许可证再分发文本入 APK（GPL-2.0-or-later/GPL-3.0-or-later/LGPL-3.0-or-later/BSD-3-Clause + ALPINE-README.md provenance）；体积记录：APK 61,772,291 B、安装后 61,772,291 B、临时峰值 ≈181.6 MiB（存档 48.6 MiB + 解压树 133 MiB）；不在设备下载 executable；JVM 47 例 + 设备 6 例 × 两台（API 29/36 arm64-v8a）+ spotless/detekt/lint/check-docs/secrets/lockfiles 全过；详见 [HXA-081 完成记录](../completion-records/HXA-081.md)）。
- M8 / HXA-080 已完成：Runtime manifest/许可证 schema（`:runtime:proot-core` 纯 JVM 模块：runtime-lock.json/manifest.json/activation 唯一版本真相 schema + 严格 fail-closed codec + canonical 编码指纹 + `RuntimeBaseline` 基线单点强制；JVM 35 例 + 设备 3 例 × 两台（API 29/36 arm64-v8a）；真实 `runtime-lock.json` 与资产归 HXA-081；详见 [HXA-080 完成记录](../completion-records/HXA-080.md)）。
- M11 / HXA-110 已完成：`com.helix.runtime.cli` 独立有网 companion APK 基线；严格 `cli-runtime-lock.json` 固定 Node/Codex/Claude artifact metadata/hash/license/terms，HXA-111/112 前 executable 一律不入包；仅 INTERNET + signature-protected bound Service/事务侧签名复核/有界状态查询，无登录、凭据、Job 或 Agent backend；JVM 5 例 + 设备 3 例 × API 29/36 arm64-v8a（详见 [HXA-110 完成记录](../completion-records/HXA-110.md)）。
- M11 / HXA-111 已完成（Spike，Codex 打包路线停止）：官方 `0.153.3` Linux-musl arm64 app-server 的 size/hash/ELF 与 API 29/36 arm64-v8a shell `--version`/JSONL initialize 通过，但 OpenAI 官方安装面未声明 Android/Termux 支持；生产 lock 保持 `bundled=false`，未实现或宣称登录、凭据、跨 UID/Activity 生命周期。CLI 执行底座 [ADR-0020](../adr/0020-cli-runtime-execution-base.md) 保持 `proposed`，HXA-112 仍独立评估 Claude（详见 [HXA-111 完成记录](../completion-records/HXA-111.md)）。
- M11 / HXA-112 已完成（Spike，Claude Code 打包路线停止）：官方 `2.1.260` `linux-arm64-musl` native tgz 已固定 size/hash/license/terms，ELF 依赖 `/lib/ld-musl-aarch64.so.1`，API 29/36 Android arm64-v8a 直接执行均稳定缺 loader；Anthropic 支持矩阵未列 Android/Termux。artifact metadata 纳入 lock 且 `bundled=false`，未实现或宣称登录、凭据、取消、输出限制、工具拦截或恢复（详见 [HXA-112 完成记录](../completion-records/HXA-112.md)）。
- M11 / HXA-113 已完成（负向兼容性结论）：新增 fail-closed 三门禁（官方 Android Runtime、内置工具禁用/代理、按原 `jobId` 对账且未知结果不重放）；当前首项已失败、后两项无可执行前置，CLI Runtime 明确报告 unsupported/未注册，生产 Provider 协议与 catalog 无 Codex/Claude Code/CLI Agent backend。M11“完成”仅表示实验已收口，不表示订阅后端可用（详见 [HXA-113 完成记录](../completion-records/HXA-113.md)）。
- M11A / HXA-114 已完成（第三方订阅协议 adapter 静态 Spike）：本地 `dsh-plugin-subscriptions@0.6.0` 为 MIT，但自行持有/刷新 access 与 refresh token、可读写 Claude Code credential，并直接复用 CLI OAuth identity/消费订阅 endpoint；不具备 Helix Android Binder/jobId 对账。registry latest 已为 `0.7.0`。该实现只作为事实证据；项目所有者随后接受 [ADR-0021](../adr/0021-third-party-subscription-protocol-adapter.md)，但供应商授权与后续生产门禁仍未成立（详见 [HXA-114 完成记录](../completion-records/HXA-114.md)）。
- M11A / HXA-115 已完成：项目所有者接受 [ADR-0021](../adr/0021-third-party-subscription-protocol-adapter.md) 后，`com.helix.runtime.cli` 独立 UID 内新增 Android Keystore AES-256-GCM subscription vault；严格 per-provider schema、16 KiB 上限、原子覆盖、0600 文件、篡改 fail-closed 和登出删除均通过 JVM 与 API 29/36 arm64-v8a 设备测试。Binder status 只暴露 `LOGGED_IN`/`LOGGED_OUT`/`CREDENTIAL_ERROR`，无 token；OAuth、模型请求、Provider/Job 尚未实现（详见 [HXA-115 完成记录](../completion-records/HXA-115.md)）。
- M11A / HXA-116 已完成：本地插件已与 npm `0.7.0` tarball/integrity 精确一致，审计范围扩到 Codex/Claude/Grok/Copilot；Runtime vault/status 封闭集合同步扩展为四项，但明确报告 adapter/Agent backend 未注册。GitHub Copilot 后续优先走官方 SDK + 项目自有 OAuth App；Grok CLI/proxy 仍只作非官方侧载候选。JVM 5/5，API 29/36 arm64-v8a 各 6/6（详见 [HXA-116 完成记录](../completion-records/HXA-116.md)）。
- M11A / HXA-117 已完成（负向平台结论）：固定 GitHub Copilot SDK `1.0.13` 及 glibc/musl arm64 runtime，确认官方 OAuth App/订阅认证接口存在，但官方发布物没有 Android/bionic，两个 runtime 在 API 29/36 arm64-v8a 均因缺失 ELF loader 按预期拒绝。当前停止打包，OAuth/模型/工具/jobId 测试未执行；[ADR-0022](../adr/0022-github-copilot-sdk-android-base.md) 保持 proposed（详见 [HXA-117 完成记录](../completion-records/HXA-117.md)）。
- M11A / HXA-118 已完成代码与自动化设备验收：Codex 第三方侧载 PKCE/loopback/token exchange/Runtime-owned vault 生命周期已实现，JVM 18/18，API 29/36 arm64-v8a 各 10/10；真实账号多次到达 callback 但尚无 `LOGGED_IN` 证据，adapter 保持 `NOT_REGISTERED`，不表示订阅后端可用（详见 [HXA-118 完成记录](../completion-records/HXA-118.md)）。
- M11A / HXA-119 已完成：依 accepted ADR-0026 固定第三方 client id，实现 GitHub Device Flow、`interval`/`slow_down`、瞬时 poll/exchange 网络恢复、取消/过期/拒绝、Copilot entitlement exchange、Runtime-only vault/logout，以及同时显示 code/URL 后由用户打开浏览器的 UI；JVM 26/26，API 29/36 arm64-v8a 各 12/12。独占 API 36 arm64-v8a 模拟器真人授权最终显示 entitlement verified，0600 vault 文件落盘；adapter 仍保持 `NOT_REGISTERED`（详见 [HXA-119 完成记录](../completion-records/HXA-119.md)）。
- M11A / HXA-137 已完成：在 Runtime UID 内实现 Claude 随机 localhost + PKCE/state OAuth、严格 token/refresh/logout 与最小套餐资格查询；`free`/未知资格 fail closed 且不落盘。JVM 32/32，API 29/36 arm64-v8a 各 15/15；真实 Claude Free 账号在 authorize 页面直接显示 “Upgrade to Max or Pro”，未返回 authorization code，取消后确认 Claude vault 文件不存在。未调用模型、未注册 backend（详见 [HXA-137 完成记录](../completion-records/HXA-137.md)）。
- M11A / HXA-138 已完成：依据官方 Grok Build Device Code 协议固定公开 client id/auth endpoint，实现 code/URL、interval/slow_down、网络恢复、取消/过期/拒绝、已知付费 tier allowlist、refresh/logout 与 Runtime-only vault；JVM 38/38，API 29/36 arm64-v8a 各 17/17。真实 Free 账号完成 `accounts.x.ai/oauth2/device/done` 后 Runtime 稳定结算为拒绝/不具备资格且 Grok vault 不存在；具体 tier/服务端拒绝码因 UI 安全合并未声称。未调用模型/proxy、未注册 backend（详见 [HXA-138 完成记录](../completion-records/HXA-138.md)）。
- M11A / HXA-139 已完成：Grok Device Code 与 verification URL 可分别复制，拒绝/过期/未知 tier/不合格 tier/协议失败改为稳定且无 token 的精确提示；Codex 新增当前官方 Device Code + server-returned PKCE 流并保留 loopback。修复 Android/OkHttp 长轮询读取和 poll timeout 被误判为整次失败的问题。JVM 44/44，独占 API 29/36 arm64-v8a 各 19/19；API 36 真人 Codex 账号最终 `LOGGED_IN`，0600 Runtime 私有 vault 落盘。未调用模型、未注册 backend（详见 [HXA-139 完成记录](../completion-records/HXA-139.md)）。
- M11A / HXA-140 已完成：CLI Runtime 可见 UI 增加固定、无工具、`store=false` 的 Codex 订阅模型 smoke；API 36 arm64-v8a 使用真实订阅 vault 查询实时目录并由 `gpt-6-astra` 返回精确 `HELIX_OK`，证明当前实验协议可少量消费真实额度。请求不接收用户正文，客户端限制 64 字符/256 KiB，主 App 不接收 token/account id/原始错误。Claude/Grok 付费资格暂未核实；官方 CLI/SDK Android/bionic 打包路线按 HXA-111/112/117 低可行性停止；第三方 OAuth 仍只限 developer/Advanced 个人侧载。Provider/Tool/Job 与持久 jobId 对账尚未实现（详见 [HXA-140 完成记录](../completion-records/HXA-140.md)）。
- M11A / HXA-141 已完成：HXA-140 可见 smoke 现由 Runtime 私有持久 Job 内核执行；原子 journal 覆盖 `PENDING`/`RUNNING` 与四种封闭终态、同 jobId + request hash 幂等、hash 冲突拒绝、取消迟到成功不覆盖、重建后 `INTERRUPTED` 不重放，以及 128 条/1 MiB 满额拒绝。API 36 真实订阅再次由 `gpt-6-astra` 返回 `HELIX_OK`，journal 只含模型 ID 与输出 hash。跨 APK Binder/PFD、Provider/Tool 和 Dispatcher/Audit 仍未实现（详见 [HXA-141 完成记录](../completion-records/HXA-141.md)）。
- M11A / HXA-142 已完成其当时的停止线实现，但其 ADR-0024 现已被 ADR-0025 取代：该任务正确保留了官方 CLI 停止和无提前 Provider 的事实，却错误地把供应商授权设成 developer 注册硬门禁；纠正见 HXA-143（历史证据见 [HXA-142 完成记录](../completion-records/HXA-142.md)）。
- M11A / HXA-143 已完成：项目所有者澄清并由 accepted [ADR-0025](../adr/0025-developer-subscription-provider-channel.md) 取代 ADR-0024；废弃的仅是官方 CLI 路线，已跑通的第三方认证可进入 developer/Advanced Provider，且该渠道无需供应商授权。consumer/store 仍以授权为硬门禁；当前跨 APK模型 Job、统一 `ModelEvent` 和 Dispatcher/Audit 尚未实现，因此 Provider 尚未注册。
- M11A / HXA-131 已完成：CLI status Binder 的 protocol/version/permission/显式 ComponentName/wire cap/deadline 已迁移到 developer App 与 Runtime 共用的 `runtime:cli-client`，companion 删除私有重复定义。尚未新增握手客户端、模型 Job 或 Provider（详见 [HXA-131 完成记录](../completion-records/HXA-131.md)）。
- M11A / HXA-132 已完成：developer App 增加 Runtime 精确可见性与 signature permission，共享 client 实现安装/启用/force-stop/签名检查、显式冷绑定、有界 status 握手和 `finally` 空闲解绑。API 29/36 arm64-v8a 均验证未安装、正常、禁用、force-stop 四态，正常后无活动 binding，启动主 App 不会被动拉起 Runtime。模型 Job/Provider/token 通道仍未新增（详见 [HXA-132 完成记录](../completion-records/HXA-132.md)）。
- M11A / HXA-133 已完成：HXA-141 固定 Codex smoke Job 通过共享签名 Binder 提供 submit/query/cancel/reconcile；前台 Job 保持同一冷绑定、终态解绑，断连后仅按原 jobId 对账。API 29/36 arm64-v8a 证明无凭据失败终态持久、重复不重跑、未知 query/cancel、debug Binder death 恢复与 redacted journal。任意 prompt/响应正文、PFD/ModelEvent 和 Provider 尚未实现（详见 [HXA-133 完成记录](../completion-records/HXA-133.md)）。
- M11A / HXA-134 已完成：跨 APK模型 Job 支持严格版本化、有界的 `ModelRequest`/`ModelEvent` PFD 传输，请求/结果先原子持久化并以 SHA-256 对锁，成功结果对账后删除正文并只保留 redacted record。API 29/36 arm64-v8a 均通过通用 fixture、HXA-133 回归、删除和空闲解绑验证。凭据仍由独立 Runtime UID 持有；Provider/对话/Dispatcher/Audit 尚未接入（详见 [HXA-134 完成记录](../completion-records/HXA-134.md)）。
- M11A / HXA-135 已完成：Codex Subscription 作为 developer-only、受管理且显式非官方实验的普通 `ModelProvider` 注册，进入现有 Provider 选择、Session、egress、ChatService、统一事件与 Turn/model-call/message 持久链路；首版 capability 保守为 streaming=true、toolCalls/vision=false。API 29/36 arm64-v8a 对话 E2E 通过，consumer APK 不含实现。真实订阅模型调用沿用 HXA-140/141 的 Runtime 证据，本 HXA 未重复消耗真实额度（详见 [HXA-135 完成记录](../completion-records/HXA-135.md)）。
- M11A / HXA-136 已完成：developer 受管理 Provider 可从 Helix 以固定显式 Component 打开 Runtime 登录/重新认证/退出 UI，安装、启用、同签名校验及 force-stop 恢复均保持在用户动作边界；API 29/36 arm64-v8a 入口通过，API 36 真实 Codex 订阅经普通 `ChatService` 返回 `HELIX_OK` 并持久化完成 Turn/model-call，consumer/store 不含入口（详见 [HXA-136 完成记录](../completion-records/HXA-136.md)）。
- M9 / HXA-090 已完成：Accessibility Service、权限中心、allowlist 与限时 AutomationSession（详见 [HXA-090 完成记录](../completion-records/HXA-090.md)）。
- M9 / HXA-091 已完成：有界 Accessibility snapshot 与绑定 package/window/generation/fingerprint 的 opaque token（详见 [HXA-091 完成记录](../completion-records/HXA-091.md)）。
- M9 / HXA-092 已完成：只消费 snapshot token 的 UI actions，目标变化、过期和敏感界面均 fail closed（详见 [HXA-092 完成记录](../completion-records/HXA-092.md)）。
- M9 / HXA-093 已完成：Accessibility 攻击、预算、熄屏、授权撤销与宿主强停恢复矩阵（详见 [HXA-093 完成记录](../completion-records/HXA-093.md)）。HXA-094/095 已实现 libsu 与高层只读工具的非真机范围，但 rooted grant/revoke/loss、RootService crash 和真实高阶读取门禁未执行，不得写成完成；详见 [M9 非真机进展](m9-non-device-progress.md)。
- M9 / HXA-096 已完成：产品必要性检查确认当前没有必须依赖任意 Root shell 的已批准用例，因此按路线允许的结论保持不实现 `root.exec`，并以 wire/Registry/APK/test 门禁防止其静默出现（详见 [HXA-096 完成记录](../completion-records/HXA-096.md)）。
- M9 / HXA-097 已完成：`android-ui-task` 与九个 `ui.*` 工具已接 developer App，并在 JVM、双 flavor 构建及 API 29/36 专用 AVD 验证；Skill 不授予 Accessibility、session、scope 或 approval（详见 [HXA-097 完成记录](../completion-records/HXA-097.md)）。因此 M9 当前唯一未完成的实质门禁是 HXA-094/095 的 rooted 物理设备验证。
- M7 / HXA-070 已完成：MCP Kotlin SDK Client Android Spike（固定 SDK 0.15.0 + Ktor OkHttp、Helix facade 隔离、16 MiB HTTP/1 MiB SSE wire limits、standalone R8/minApi 29、consumer/developer release、API 29/36 arm64-v8a 专项 fixture 各 12/12；ADR-0017 accepted；详见 [HXA-070 完成记录](../completion-records/HXA-070.md)）。
- M7 / HXA-071 已完成：MCP Server disabled-by-default 配置、SecretStore bearer、initialize/协议与 capability snapshot、规范 origin/residence、tools/resources/prompts 有界 metadata/hash；正文不读取、不进 Context，OAuth/PKCE 不实现；`:extensions:mcp:test` 与全仓静态门禁通过（详见 [HXA-071 完成记录](../completion-records/HXA-071.md)）。
- M7 / HXA-072 已完成：`mcp.<server>.<tool>` 动态 bridge、annotation 不降风险、schema/hash 变更失效、有界结果、EgressSummary/checkpoint 与现有 Dispatcher/Policy/Approval/Audit 接线；API 29 consumer 117 tests（1 skip）与 API 36 consumer 116 tests 均 0 failed（详见 [HXA-072 完成记录](../completion-records/HXA-072.md)）。
- M7 / HXA-073 已完成：基于 HXA-084 真实 PRoot Job 的 MCP stdio bridge（锁定 argv + command 指纹进入输入 manifest、请求正文只进快照、严格 UTF-8/JSON Lines/`jsonrpc=2.0` stdout、独立 stderr cap + 共享总预算、环境筛选、进程组取消、断连查询对账绝不重放）；Job wire 升级 protocol v3 并增加 stdin 相对路径/独立 stderr 上限，Runtime descriptor 封闭能力集加入 `stdio`；JVM 门禁全过，API 29/36 companion 各 18/18、主 App 跨 APK 各 8/8 且 0 skip；验证中修复“输出超限但进程继续运行”和 RootFS 换源污染产物/内容 lock 漂移，均保留 fail-closed（详见 [HXA-073 完成记录](../completion-records/HXA-073.md)）。
- M7 / HXA-074 已完成：Agent Skills `SKILL.md` 严格 frontmatter validator + Discovery catalog；name 按官方 Unicode lowercase/alphanumeric/hyphen 规则并以 NFKC 规范化匹配目录，未知字段保留为不可信数据但绝不进入 catalog/授权；官方仓库 commit `69ef37e…` 的 parser/validator/spec fixture 固定为测试证据，production 无 `skills-ref` 依赖；`:extensions:skills:test` 29/29（官方 fixture 3/3）全过（详见 [HXA-074 完成记录](../completion-records/HXA-074.md)）。
- M7 / HXA-075 已完成：本地目录/zip 安全导入、安装前 scripts/resources/hash preview、commit 前复算和 `name/hash/name` 内容寻址快照；拒绝 traversal、symlink、特殊/加密/重复 entry、多根与文件/字节/路径/压缩比越界。专项验收修复压缩比整数截断和大量空目录绕过文件数门禁/无界物化，Skills 30 tests + framework 141 tests 全绿（详见 [HXA-075 完成记录](../completion-records/HXA-075.md)）。
- M7 / HXA-076 已完成：六个 Skill 工具接入 production Pipeline，五个内置 Skill instruction-only；`skills.list` 改为可继续分页的有界结果，导入 Skill 默认禁用且启用不授予工具/Capability/Approval，remove 仅移动 user-imported snapshot 到 recoverable trash。API 29/36 production Skill E2E 各 1/1，consumer 全量 118/117 项均零失败；设备期修复纯 JVM Skill 模块的 `Stream.toList`/`Files.writeString` API 29 缺陷（详见 [HXA-076 完成记录](../completion-records/HXA-076.md)）。
- M7 / HXA-077 已完成：官方 Java SDK `1.3.1.Final` 的 JVM fixture 通过，但严格 Android R8 因缺失 `java.net.http.HttpClient` 按预期拒绝且未用 warning suppression 降级；production 固定最小 `A2aClientFacade` + OkHttp/kotlinx.serialization。最小方案严格 R8 为 15,544-byte DEX / 169 method IDs，API 29/36 专项 fixture 各 3/3，双 flavor unsigned release/R8 与 Apache-2.0 runtime license closure 已验收；物理真机、签名产物与最终完整 SBOM/notice 归 M12（详见 [HXA-077 完成记录](../completion-records/HXA-077.md)）。
- M7 / HXA-078 已完成：默认禁用 Agent、SecretStore alias、公开/扩展 Agent Card、v1.0 JSONRPC/HTTP+JSON interface 选择、有界 provider/capability/mode/Skill snapshot、逐 Skill 启用与 canonical hash 失效均通过 JVM/Room 和 API 29/36 production fixture；设备期发现并修复 Card 变化只禁用 Room、旧内存 Registry 仍可见的失效窗口，现在 snapshot 持久化后同步清除 descriptor/executor/bridge（详见 [HXA-078 完成记录](../completion-records/HXA-078.md)）。
- M7 / HXA-079 已完成：独立 A2A Tool origin、固定 schema 与 Dispatcher/Policy/Approval/Audit 接线，Send/stream/Get/Cancel/Subscribe、Task/input/snapshot/event 持久关联与 `Last-Event-ID` 对账已验收；API 29/36 production recovery fixture 各 2/2，证明进程重建只查询原 Task、不明确送达停在 `NEEDS_REVIEW` 且不重发、远端 proposal/URL 不取得本机执行权，以及 Workspace Artifact 幂等复用与磁盘 hash 篡改拒绝（详见 [HXA-079 完成记录](../completion-records/HXA-079.md)）。
- M10 / HXA-099 已完成：模式/预算/resourceGate 已实现；M10 尚未完成的 release evidence 是 24 小时连续稳定性、真实 Doze/secure keyguard/OEM 热限制与低内存 kill、物理设备（包括 16 KiB）、真实 crash/ANR `ApplicationExitInfo` 联合证据，以及多 Provider/模型逐 case 评测和 HXA-105 30 分钟真机收益/资源对照；这些均不得用短时 emulator 或 skip 计作绿色（详见 [HXA-099 完成记录](../completion-records/HXA-099.md)）。
- M10 / HXA-100 已完成：45 条固定评测及可复现实验元数据（详见 [HXA-100 完成记录](../completion-records/HXA-100.md)）。
- M10 / HXA-101 已完成：Prompt Injection 固定矩阵与结构性 authority gate（详见 [HXA-101 完成记录](../completion-records/HXA-101.md)）。
- M10 / HXA-102 已完成原子 ledger 与恢复组件验证；Goal/run/Turn、预算持久化、显式 Continue 与 ADR-0028 完成证据已生产接线；真实模型写入完成、复核/完成事务强杀恢复及当前会话核心矩阵已有分项证据。write/edit/PRoot来源、读取中断和剩余后端/非长稳生命周期已完成本轮分项收口；自然LMK/真机/长稳与发布验收仍单列；见 [HXA-102](../completion-records/HXA-102.md)和[当前清单](main-optimization-todo.md)。
- M10 / HXA-103 已完成：API 29/34/35/36 短时资源与生命周期门禁；外部长稳项仍按上项保留（详见 [HXA-103 完成记录](../completion-records/HXA-103.md)）。
- M10 / HXA-104 已完成：隐私、脱敏诊断、`ApplicationExitInfo` 与显式删除边界（详见 [HXA-104 完成记录](../completion-records/HXA-104.md)）。
- M10 / HXA-105 已完成：隔离的有界只读编排 Spike；未进入生产 Tool Registry，ADR-0009 已接受架构约束，生产启用门禁未通过，产品继续单 Agent（详见 [HXA-105 完成记录](../completion-records/HXA-105.md)）。

- M11A / HXA-144 已完成：Claude developer Provider、显式平台封套与共享 Anthropic adapter；API 29/36 普通对话、运行中取消/断连、旧 Runtime 混合版本拒绝、固定账号入口与空闲解绑通过，Runtime 专项 R8 通过。真实 Claude 付费调用按所有者决定保持未核实。详见 [完成记录](../completion-records/HXA-144.md)。

- M11A / HXA-145 已完成：Grok 受管理 Provider、共享 HTTP 生命周期与 API 29/36 对话/取消/恢复夹具验收通过；真实付费调用未核实，见 [完成记录](../completion-records/HXA-145.md)。

- M11A / HXA-146 已完成：Copilot 文本 Provider、短期 token 刷新、设备码/URL 复制；API 29/36 夹具各 7/7，API 36 当前真实账号通过 Claude Haiku 4.5 的普通 probe/Chat 验收，见 [完成记录](../completion-records/HXA-146.md)。不推导所有模型或所有免费账号可用。

- M11 / HXA-147 已完成：M0～M11合并后统一优化，统一导航、模式/Provider、工具摘要、审批/停止/重试/恢复、空状态及小屏大字体中英文验收，见 [完成记录](../completion-records/HXA-147.md)。各测试快照、真实模型与模拟器边界独立记录；累计 Git 修改已审核，并以 `8369043` 提交、推送至 `origin/main`。

本轮M0～M11功能、非真机/非长稳及统一交互验收和暂存收口已完成，最终范围/证据/排除项见 [收尾审计](main-closure-audit.md)。交付提交 `8369043` 已推送并通过远端 SHA 核对，不扩写为商店发布验收。

- M14 / HXA-148 已完成：Skill Creator、完整草稿编辑和只读校验，API29/36原生与中英文大字体、真实模型ACT调用通过，见 [完成记录](../completion-records/HXA-148.md)。

- M14 / HXA-149 已完成：精确 hash 安装、原生 ZIP 选择与预览/启用、API29/36审批及 UI、真实模型完整链路通过，见 [完成记录](../completion-records/HXA-149.md)。

- M14 / HXA-150 已完成：MCP配置预览/精确安装、JSON粘贴、凭据与连接分离及设备回归通过，见 [完成记录](../completion-records/HXA-150.md)。

- M14 / HXA-151 已完成：Skill/MCP 原生操作状态收敛，双 flavor JVM 与 API29/36 各 6 项 UI 回归通过，见 [完成记录](../completion-records/HXA-151.md)。

- M10 / HXA-152 已完成：有界原生对照与归因记录；API29/36 WebView JNI 溢出及无 WebView 的 Autofill/Binder 终止均复现。缺陷仍未修复，见 [完成记录](../completion-records/HXA-152.md)。

- M10 / HXA-153 已完成：JNI 配对定位到 AwContentsIoThreadClient 双映射清理路径，Binder 干预确认系统代理回收后客户端引用释放；生产缺陷仍 open，见 [完成记录](../completion-records/HXA-153.md)。

- M10 / HXA-154 已完成：生产控制器六路径 API29/36 各 400 轮，目标弱引用残留为 0；原生裸创建仍有残留，未修改生产回收策略，见 [完成记录](../completion-records/HXA-154.md)。

- M10 / HXA-155 已完成：API29/36 网络关闭/停止/后台/重建共 800 轮目标引用零残留；上游材料已准备，原生问题仍 open，见 [完成记录](../completion-records/HXA-155.md)。

- M10 / HXA-156 已完成：状态 Git 表述与产品研究文档收口，保留非实测边界，见 [完成记录](../completion-records/HXA-156.md)。

- M13 / HXA-127 已完成：tools.search 与会话有限 schema 窗口、schema/停用失效，双 flavor JVM 与 API29/36 各 3 项通过，见 [完成记录](../completion-records/HXA-127.md)。

- M13 / HXA-128 已完成：样本 CLI/依赖清单及既有 stdio 跨 UID 回归各 4/4，未新增业务 CLI 执行支持，见 [完成记录](../completion-records/HXA-128.md)。

- M13 / HXA-157 已完成：工具发现 64 项截断修复；双 flavor JVM、构建和静态门禁通过，见 [完成记录](../completion-records/HXA-157.md)。

- M10 / HXA-158 已完成：未使用 WebView 分配修复及原生问题折中验证，见 [完成记录](../completion-records/HXA-158.md)。

- M10 / HXA-159 已完成：大文件职责重构、双 flavor 静态/JVM 门禁与双 API 各 74 项回归，见 [完成记录](../completion-records/HXA-159.md)；[Context 替代分析](browser-context-options.md) 保留研究过程，后续迁移见 HXA-160。

- M10 / HXA-160 已完成：Activity 级浏览器宿主、旧回调隔离、JS 对话框与真实 AutofillService；双 API 各 36 项浏览器用例执行通过、2 项既有 opt-in 未启用，MainActivity 各 2 项通过，见 [完成记录](../completion-records/HXA-160.md)。

- M10 / HXA-161 已完成：真机输入区布局与推理接线，见 [完成记录](../completion-records/HXA-161.md)。API36 的 8 项专项和 API34 真机中/高请求通过；独立分支验收，尚未合入 main。

- M10 / HXA-162 已完成：按轮次展示执行记录、全文复制和选词入口，见 [完成记录](../completion-records/HXA-162.md)。

- M10 / HXA-163 已完成：草稿首次发送落库、自动标题/改名与目录归属；API36 UI 9 项及迁移 22 项通过，真机安装与交互核实，见 [完成记录](../completion-records/HXA-163.md)。尚未提交/合入 main/推送。

- M10 / HXA-164 已完成：模型开始前发布用户消息、原生 Markdown 渲染与原文复制；API36 共 10 个不同设备用例通过，真机已更新并核实显示，见 [完成记录](../completion-records/HXA-164.md)。尚未提交/合入 main/推送。

- M10 / HXA-165 已完成：参考移动端截图整理导航、消息正文及圆角输入区，附件改为框内加号；API36 11 项通过，真机安装/键盘/附件选择器已核实，见 [完成记录](../completion-records/HXA-165.md)。尚未提交/合入 main/推送。

- M10 / HXA-166 已完成：会话内模型下拉、跨 Provider 保留历史切换、推理重置；API36 9 项通过，真机菜单核实，见 [完成记录](../completion-records/HXA-166.md)。尚未提交/合入 main/推送。

- M10 / HXA-167 已完成：输入选项统一单行横向滚动，复制及后续扩展复用同一行；API36 8 个不同用例通过，真机已安装核实，见 [完成记录](../completion-records/HXA-167.md)。尚未提交/合入 main/推送。

- M10 / HXA-168 已完成：椭圆选项、去重顶部标题并保留应用导航入口；API36 10 项通过，真机已更新核实，见 [完成记录](../completion-records/HXA-168.md)。尚未提交/合入 main/推送。

- M10 / HXA-169 已完成：独立顶部主菜单、点击标题改名、草稿手动标题保留；API36 12 项通过，真机安装与入口核实，见 [完成记录](../completion-records/HXA-169.md)。尚未提交/合入 main/推送。

- M10 / HXA-170 已完成：统一非会话顶栏、扩展页接已有 Skill/Connector 管理、已归档状态与 SAF 说明；API36 14 项通过，真机入口与布局核实，见 [完成记录](../completion-records/HXA-170.md)。尚未提交/合入 main/推送。

- M10 / HXA-171 已完成：参考图归档、独立文件首页/目录页、搜索/长按/返回；API36 17 项和 1.8 倍字体 3 项通过，真机布局核实，见 [完成记录](../completion-records/HXA-171.md)。尚未提交/合入 main/推送。

- M10 / HXA-172 已完成：归档会话单列、共享存储入口、真实上下文输入快照圆环；API36 23/23，双 app JVM/构建/lint 与真机界面核实通过，见 [完成记录](../completion-records/HXA-172.md)。当时未接窗口设置/自动压缩，后续见 HXA-174；两渠道共享根目录写入仍不在本批；未提交/合入 main/推送。

- M10 / HXA-173 已完成：归档恢复、两 flavor 手动共享根目录及授权返回、模型与圆环同胶囊；双版本设备专项和真机入口通过，见 [完成记录](../completion-records/HXA-173.md)。

- M10 / HXA-174 已完成：窗口默认 200k/服务端发现、比例设置、自动/手动摘要与预算/恢复；主机 1046 项（8 个既有外部 skip）、双 flavor 设备专项和真实 SGLang 通过，见 [完成记录](../completion-records/HXA-174.md)。

- M10 / HXA-175 已完成：140 份临时脚本按日期留存、独占模拟器、紧凑圆环/复制及系统状态栏避让。双 app JVM/构建/lint、API36 32/32 与 API34 真机安装/截图核实通过，见 [完成记录](../completion-records/HXA-175.md)。

- M10 / HXA-176 已完成：长 Turn 和新轮首请求的步骤压缩、用量校准、收益与重试上限、Goal 容量暂停；双 app 主机、API36 85/85（含真实模型三轮摘要与问答）通过，见 [完成记录](../completion-records/HXA-176.md)。本轮真机未连接，未安装新包。

- M10 / HXA-177 已完成：非阻塞跨会话任务、持久结果回收、全局前台服务、Goal BLOCKED/PAUSED 与阻塞复查；受影响 JVM 1140 项（8 个既有 skip）、设备与迁移 160/160 通过，见 [完成记录](../completion-records/HXA-177.md)。参考 Goal 指南的取舍见 ADR-0039；未增加自动续跑或生产子 Agent。

- M10 / HXA-178 已完成：以模型结构化报告替代强制 Goal 绑定验证，支持仅目标创建；主机门禁、双 API 各79、迁移各24及真实模型/最终复核3项通过，见 [完成记录](../completion-records/HXA-178.md)。

- M10 / HXA-179 已完成：ChatService 职责提取，完整主机门禁与 API29/36 各107项回归通过，见 [完成记录](../completion-records/HXA-179.md)。

- M10 / HXA-180 已完成：手动 Workspace/共享存储/SAF 变更与恢复，主机2768项零失败（8既有条件跳过）、双 API 各18项及预览连续复跑通过，见 [完成记录](../completion-records/HXA-180.md)。
- M10 / HXA-181 已完成：当前规范与历史记录分离，文档/ADR/i18n/secrets/diff 通过，见 [完成记录](../completion-records/HXA-181.md)。

- M10 / HXA-182 已完成：预览/回收站/聊天展示职责提取，手动传输持久记录与显式恢复；双 app JVM 727 通过/8 条件跳过，API29/36 各47项含发布中真实进程死亡后的 UI 恢复通过，见 [完成记录](../completion-records/HXA-182.md)。

- M10 / HXA-183 已完成：七项职责拆分；主机897通过/8条件跳过，API29/36各145项app回归及PRoot生命周期/归档/ACK通过，见 [完成记录](../completion-records/HXA-183.md)。

- M10 / HXA-184 已完成：B11/C15 职责与装配整理、审批注册/Goal提醒读取竞态修复；完整主机及独占双API回归通过，见 [完成记录](../completion-records/HXA-184.md)。

## 历史验收索引

以下保留全部已完成 HXA 索引，便于追溯当时的范围、命令、制品和限制。当前机制优先看上方摘要与最新对应完成记录；历史数量不作为当前全量测试数量。

- [路线图](roadmap.md)：任务定义、依赖与状态。
- [验证矩阵](verification-matrix.md)：各任务验收范围与证据入口。
- [大类职责审查](large-class-responsibility-audit-2026-09-10.md)：拆分前快照及 HXA-183/184 收口入口；不因剩余类较长自动新增拆分任务。
