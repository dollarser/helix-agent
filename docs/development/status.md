# Helix 实施状态

更新时间：2026-09-04（M8 分支 `hxa/m8-proot`，基于 main `55ce5bb`；HXA-080～088 全部完成，等待用户授权合并）

## Current summary

| 维度 | 当前事实 |
| --- | --- |
| 已验证范围 | M0～M4：HXA-001～003、010～016、020～028、030～049；**M5 完成**：HXA-050（Zipline/QuickJS spike）、HXA-051（生产执行协议 isolated Service/Binder）、HXA-052（IIFE wrapper/JSON ABI/限制，API 29/36 arm64-v8a）、HXA-053（QuickJS 执行接 Tool/Policy/审计：`code.javascript.run` 全链路 + 11 状态回填 + §4.8 脱敏审计 + 完整代码审批，API 29/36 arm64-v8a）、HXA-054（攻击与端到端测试：8 项攻击点全链路 E2E + 设备证据，75 例 androidTest/设备 × 两台 + 84 例 JVM）；**M5A 完成**：HXA-055（图片输入与 Provider 视觉能力：production 图片源（会话绑定 + hash/magic 重验、fail-closed）+ 端上归一化（字节门/bounds 探针/采样/EXIF 手动旋转/重编码剥离）+ `VisionLimits` 集中预算（API 29/36 测量 + Provider 上限取更严者）+ 连接测试第 5 阶段视觉探测（Completed/Refusal 证明 vision）/用户可见手动声明 + 绑定归一化 artifact 的发送/重试/恢复重验 + 出网披露图片行，API 29/36 arm64-v8a）、HXA-056（文本/图片附件端到端硬化与发布边界：fixture E2E 发送/流式/Tool Loop/历史恢复/脱敏 + `ACTION_SEND`/`SEND_MULTIPLE` 本地草稿（provider-free 会话 + 显式单发绑定，绝不自动发送）+ 失败矩阵（grant 失效/篡改/重复发送/绑定不可换/能力快照变化/过大/解压炸弹/取消/进程回收）+ 统一稳定拒绝（UTF-16/PDF/PPT/DOC/音频/视频/未知二进制）+ 泄露断言（无 URI/绝对路径/正文/base64 入持久化与用户可见错误）+ 三 adapter golden + 1 真实 vision endpoint smoke（本地 SGLang，环境明确记录），API 29/36 arm64-v8a））、HXA-057（persisted SAF tree scope 接线：每次使用 7 步实时复核 + fail-closed + 模型不透明 + `DocumentsContract` 只读浏览后端，API 29/36 arm64-v8a）、HXA-058（文件管理器导入/导出入口：HXA-044 受限管线接入文件管理器——picker 导入到 `input/` + 有界枚举/歧义 fail-closed 规划 + 导出到 `CreateDocument`/已授权树（WRITE 实时复核）+ 字节级重读校验 verified 语义，API 29/36 arm64-v8a）、HXA-059（Provider 模型自动发现与选择：连接测试 phase 2 `listModels` 列表接入 UI——「后端可用模型」（probe 界 1000 + UI 显示 200 + 过滤）+ 点选预填编辑表单（不自动保存）+ `Unsupported` 显式手动提示 + phase 2 `Failed` 无列表；真实 SGLang endpoint smoke 全链通过，API 29/36 arm64-v8a）；**M8 进行中**：HXA-080（PRoot Runtime manifest/许可证 schema：`:runtime:proot-core` 纯 JVM 模块 = runtime-lock.json/manifest/activation 唯一版本真相——严格 fail-closed codec + canonical 编码指纹 + `RuntimeBaseline` 基线单点强制，API 29/36 arm64-v8a 设备验证 codec 一致性）、HXA-081（构建期固定资产：PRoot 5.1.107.92/Alpine 3.22.5 + bash/git/python3/nodejs/ripgrep 53 包 apk 精确锁定 + 真实 runtime-lock.json + 许可证文本入 APK + 全量 ELF `PT_LOAD` 对齐门禁（360/360 ≥16 KiB），API 29/36 arm64-v8a）、HXA-082（RootFS installer：纯 JVM 流式 tar 读取器 + 词法 Zip Slip 防护 + `.partial` 单遍流式安装 + 结构 smoke（必需路径 NOFOLLOW + 按页大小 ELF 对齐预检）+ manifest/promote/原子激活（active→rollback→最后删旧 rollback 目录）+ 一版 rollback 交换 + 孤儿清扫 + 排他状态锁全程持有；lock 锁定对象修正为设备实际读取的 raw tar，API 29/36 arm64-v8a）、HXA-083（独立 Runtime APK/IPC：`:runtime:proot-ipc` 共享协议模块（状态字节零跨 APK 异常 + PFD manifest 通道 + 严格 descriptor codec）+ companion signature permission 双守卫 + `onBind` 调用方签名集重验（fail-closed）+ 显式 ComponentName `BIND_AUTO_CREATE` 冷绑定 supervisor（本地检查→绑定→握手→锚点比对，拒绝即解绑绝不留进程）+ 最小用户门控修复 Activity + 事务侧逐事务调用方校验 + `<queries>` 可见性 + 客户端 `onNullBinding` 立即拒绝映射 + debug null-onBind seam；API 29/36 arm64-v8a 全绿）、HXA-084（PRoot runner：Job 协议/record codec + 有界 journal（128 条/1 MiB，额度满拒绝、对账 payload 即删、30 天 evidence-expired）+ companion 单例 runner（argv/显式 script、env allowlist 筛选在主进程 secret 名/已知值/认证形状三重拒绝、deadline watchdog + 进程组取消、bounded 双流捕获、输出归档 + hash 证明、孤儿 starttime 双证清扫）+ **PRoot 首次设备真实执行**（SELinux `execute_no_trans` 平台事实：app_data 不可 exec、loader 经 `apk_data_file` 可执行副本 + 每次哈希对锁、`LD_PRELOAD` 对在途 execve 改写不可达之实证）+ 客户端 `ProotJobClient` 冷绑定 submit/query/cancel/reconcile/awaitTerminal（`Interrupted` 停泊点、断连只查询对账绝不重放、零 FGS 零 wake lock）+ 跨 APK Job E2E；API 29/36 arm64-v8a 全绿，真机部分归 HXA-086/088）、HXA-085（`bash` Tool = `code.linux.run`：CODE_EXECUTION/L2/LOCAL_PROOT 每次审批（STANDARD 由真实 PolicyEngine `ISOLATED_RUNTIME_REQUIRES_ADVANCED` Deny）+ developer-only 注册 + 每执行四态 gate（READY/未安装/被禁用或强制停止/未验证，用户完成过零 Job 验证 = 持久化 anchor；进程预活/手动修复都不是条件；被动 Registry 刷新绝不重绑）+ 批准后每执行重新握手（084 冷绑定）+ 输入快照复制（经 store，绝不挂载真实 Workspace、单文件 ≤1 MiB、≤64 个）+ 主进程 env 三重筛选（Runtime 零 SecretStore）+ wire deadline = 剩余审批窗口（companion watchdog 恰在审批 deadline 杀进程组）+ 输出归档双重哈希重验 + 只导入验证后的 `result.txt` 进引用自身 region（缺失 = SUCCEEDED 无导入）+ 审批卡完整命令/sha/快照来源/离线声明 + Settings Advanced-only 验证/修复入口（修复仅用户点击，重试 = 新 ToolCall/approval/jobId）；API 29/36 arm64-v8a 全绿，真机归 HXA-086/088））、HXA-086（真机 smoke 与隔离（模拟器可验证范围）：companion 通知停止（普通通知非 FGS + 签名权限 STOP 广播 receiver + 四终态撤通知）+ smoke 套件（python3/node/git/ripgrep 真实 Job + 随机 token 新鲜度契约）+ companion 隔离（guest 不可读 companion dataDir/共享存储/联网 + 结构性零 INTERNET/存储权限）+ 主 App 侧隔离 E2E（真实 `code.linux.run` 全链 guest 不可读写主 App dataDir）+ 生命周期矩阵（冷绑定/空闲回收/锁屏/低内存/强制停止/主 App 死亡 ORPHANED/companion 死亡 DEAD_OBJECT∪ORPHANED 稳定结算/wake lock 零采样）+ 同 jobId 不重复执行 + `scripts/accept-hxa-086-lifecycle.sh` 11 阶段；companion 43 例 + `:app` 137 例 × 两台全绿；真机 4/16 KiB smoke、Doze/安全锁屏/热限、最低设备集真机证据 = 真机缺口，详见 [HXA-086 完成记录](../completion-records/HXA-086.md)）、HXA-087（更新/卸载/法律页：同签名 APK 更新"需更新"检测（锚点 lock 指纹漂移 → 用户点击验证稳定 `LOCK_MISMATCH`）+ 显式重定基线（两次用户动作，移动基线绝不静默接受）+ 修复页更新态按钮（活动安装 manifest 内嵌 lock 指纹比对，更新失败旧 runtime 保持可用）+ 用户门控 rollback 入口（082 原子指针交换，回滚后真实 Job 可用）+ 完整删除（双端各清自己状态 + 面内二次确认，只删 `filesDir/runtime` 绝不误删 Workspace）+ 离线法律页（companion `ProotLegalActivity`：离线声明/source URL 展示不访问/build manifest 规范指纹/每组件版本+SPDX+预装包/许可证全文内嵌，零 INTERNET 离线可读）；companion 50 例 + `:app` 141 例 × 两台全绿 + 086 生命周期回归 × 两台）、HXA-088（Git Workspace 语义 Spike + ADR-0008：三路径比较（主 App 权威仓库每操作完整原子事务 / Runtime 私有目录需新 bind 面+新协议 / Android Git 库 JGit 7.7.1 纯 Java EPL/GPL-CPE·libgit2 1.9.7 GPL/MIT 双实现分叉）+ 真实 084 Job 管线设备测量（小/中仓库归档体积·耗时·guest git 操作·gc 后 `.git` 28 文件/pack 化）+ 损坏 fsck 报告（退出码可 0——必须解析输出文本）+ job id 对账幂等 + 攻击面证据（hook/filter 无防护时执行、hooksPath 防护无法中和 filter→强制宿主侧预扫描、alias/submodule/credential 结构性不触发）+ guest ash 无 `$(...)` 平台事实；**零产品代码、零新依赖；ADR-0008 已由所有者接受（2026-09-05）：路径 1 主 App 权威仓库每操作完整原子事务 + 首版命令集 status/diff/log/init/add/commit + 强制预扫描/防护策略（实现由后续 HXA 按 ADR 执行）**；companion 56 例 + `:app` 141 例 × 两台全绿） |
| 已落地骨架 | Provider 配置与三协议适配、聊天流、Capability/Policy/Approval、Dispatcher/Scheduler、batch-safe Turn Coordinator、多步 Tool Loop 与持久审计、Workspace/文件工具（含受限 zip/tar Archive）、SAF 导入导出适配层与 developer All-files scope、会话附件导入/持久化与 UTF-8 文本物化（ADR-0014 首批）、QuickJS 生产执行模块（`:runtime:quickjs`，ADR-0015 选定 Zipline 1.27.0：isolated Service/Binder 通道 + §3.2 IIFE wrapper JSON ABI）+ `code.javascript.run` 工具（consumer/developer 双变体接线） |
| 尚无业务执行器 | Browser、MCP、Skill、PRoot/CLI、Accessibility、Root |
| 当前检查点 | **M5A 完成**（HXA-055/056：consumer 79 + developer 92 × 两台（API 29/36）+ JVM 全过）+ **M5B / HXA-057 完成**（persisted SAF tree scope：每次使用实时复核 + fail-closed + 模型不透明 + `DocumentsContract` 只读浏览后端；设备验收 24/24 × 两台 + JVM 458 例 + spotless/detekt 全过；设备期修复生产 walk 缺陷 1 处）+ **M5B / HXA-058 完成**（文件管理器导入/导出入口：HXA-044 管线接线 + 冲突策略/进度/取消/逐项目结果 UI + `SafExportVerifier` 字节重读校验；设备验收由仲裁会话执行——feature 35×2 + consumer 94×2 + developer 107×2（API 29/36）+ JVM 120+225×2 全绿，设备期修复缺陷 8 项（生产空游标 fail-open×2、fixture insert 形态×2、跨 uid provider 可达性[Java 重写 + `exported=true`]、`OpenableColumns` 列名、API 30+ 持久化 WRITE seam、工具栏/预览动作行零宽溢出×2），另记录 1 项 HXA-057 同型模式 review 发现）+ **M5B / HXA-059 完成**（Provider 模型自动发现与选择：连接测试第 2 阶段 `listModels` 列表接入 UI——测试通过自动带出「后端可用模型」（probe 界 1000 + UI 显示 200 + 过滤），点选预填编辑表单（不自动保存）；`Unsupported` → 显式手动输入提示；phase 2 `Failed` → 既有稳定错误、无列表；状态存储向后兼容第 8 字段（坏列表只降级、保留 PASSED）；只读 GET、key 永不入 UI/日志；设备阶段由仲裁会话执行——`ProviderModelDiscoveryUiTest` 4/4×2 + 真实 SGLang endpoint smoke 1/1×2（0 跳过，五阶段真实 PASSED + 模型区块 + 芯片预填保存）+ feature 35×2 + consumer 98×2 + developer 112×2（API 29/36；2 处既有环境 flake 如实记录并有 main 同签名复现证据）+ JVM 85+237×2+120 全绿；设备期修复 1 个生产缺陷（自托管无 key provider 编辑保存被静默禁用）+ 4 处测试断言缺陷 + 1 处既有测试单类复跑缺陷）**= M5B 全部完成** + **M8 / HXA-080 完成**（Runtime manifest/许可证 schema：`:runtime:proot-core` 纯 JVM 模块承载 runtime-lock.json/manifest/activation 唯一版本真相——严格 fail-closed codec（未知 key/闭集版本/canonical SHA-256/https 真主机 URL/无 traversal 嵌入引用/manifest 内嵌 lock 完整性重算）+ canonical 编码指纹 + `RuntimeBaseline`（proot+alpine-rootfs+bash/git/python3/nodejs/ripgrep）单点强制；JVM 35 例 + 设备 3 例 × 两台（API 29/36 arm64-v8a）+ spotless/detekt/check-docs/secrets/lockfiles 全过；真实资产与 `runtime-lock.json` 归 HXA-081，详见 [HXA-080 完成记录](../completion-records/HXA-080.md)） + **M8 / HXA-081 完成**（构建期固定资产：真实 `runtime-lock.json` 5 组件锁定（proot 5.1.107.92 termux deb / proot-loader / libtalloc 2.4.3 / libandroid-shmem 0.7 / alpine-rootfs 3.22.5 + 53 包 apk 精确 pin，含 bash 5.2.37-r0、git 2.49.1-r0、python3 3.12.14-r0、nodejs 22.23.2-r0、ripgrep 14.1.1-r0）+ 构建管线（digest 锁定镜像 + `apk add name=version` + 确定性重打包 + SHA-256/size 全量校验，fail-closed，generate/verify 双模式跑通）+ ELF `PT_LOAD` 对齐/ABI 检查器（纯 JVM，构建门禁/安装器预检/16 KiB 判定共用；360/360 ELF ≥16384）+ 许可证文本入 APK + 体积记录（APK 61,772,291 B / 安装后 61,772,291 B / 临时峰值 ≈181.6 MiB），不在设备下载 executable；JVM 47 例 + 设备 6 例 × 两台（API 29/36 arm64-v8a）+ spotless/detekt/lint/check-docs/secrets/lockfiles 全过；无新 ADR，详见 [HXA-081 完成记录](../completion-records/HXA-081.md)） + **M8 / HXA-082 完成**（RootFS installer：纯 Kotlin 流式 tar 读取器（typeflag 白名单 0/\0/5/2 + L/K 长名，64 KiB 流式，结尾零块消费保证整档读满）+ `TarPathSafety` 纯词法 Zip Slip 防护（宿主绝不解析 symlink，绝对目标按 PRoot chroot 树根基准）+ 安装器状态机（`install.lock` 全程排他 + 启动孤儿清扫 + `.partial` 单遍流式解压并哈希验证 + PRoot 二进制 ≤64 MiB 门 + 结构 smoke（必需路径 `NOFOLLOW_LINKS` + 全树 ELF 按传入页大小预检，不执行 native）+ manifest 内嵌 lock 重算 + promote + 原子激活 active→rollback→最后删旧 rollback 目录 + `activateRollback` 交换；lock 锁定对象修正为 raw tar（`f15c9434…`/137,287,680 B，设备实际读取字节闭环，gz 降为构建日志产物）；JVM 83 例 + 设备 12 例 × 两台（API 29/36 arm64-v8a，完整安装流程含 137 MB 解压）+ spotless/detekt/lint/check-docs/secrets/lockfiles 全过；无新 ADR，详见 [HXA-082 完成记录](../completion-records/HXA-082.md)） + **M8 / HXA-083 完成**（独立 Runtime APK/IPC：新增 `:runtime:proot-ipc` 共享协议模块——状态字节协议（服务端每路径回状态字节 + PFD manifest，绝不跨 APK 抛异常，API 34 `readException` 语义变化为硬理由）+ 严格 descriptor codec + `PfdManifestChannel`（pipe PFD 上限传输）+ `ProotHandshakeClient`/`ProotRuntimeServiceBinder`（无 AIDL，quickjs house 风格）+ `TX_DEBUG_SELF_KILL` debug 崩溃注入 seam（`BuildConfig.DEBUG` 门控）；companion `ProotRuntimeService`（`onBind` 调用方签名集重验 fail-closed + 每次 bind 现算 manifest 含 082 canonical lock 指纹）+ signature permission 同时守卫 service 与最小修复 Activity（无 launcher/无 INTERNET）；客户端 `ProotRuntimeSupervisor`（`checkLocalState` 零绑定前置 → 显式 ComponentName + `BIND_AUTO_CREATE` 冷绑定 + 每调用独立 ServiceConnection + 硬 deadline → 握手 → `VerifiedRuntimeStore` 锚点比对/持久化；`openConnection`/`withConnection` 084 Job seam；`openRepairActivity` 仅用户点击、失败映射稳定原因永不崩溃；拒绝即解绑绝不留存活进程）+ `:app` developer `uses-permission`（consumer 不声明）；落实 ADR-0007：启动/Advanced 切换/Registry 刷新零启动（结构性 + 设备断言）；JVM 42 例 + companion 设备 20 例 × 两台（API 29/36 arm64-v8a，080/081/082 既有全绿）+ `scripts/accept-hxa-083-lifecycle.sh` 跨 APK E2E **8/8 × 两台**（冷绑定+锁一致 descriptor/二次验证/真实进程死亡→冷重绑恢复/用户门控修复入口/null-onBind 立即拒绝/启动不拉起/强制停止/未安装）+ spotless/detekt/lint/check-docs/secrets/lockfiles 全过；初稿曾把跨 APK 绑定失败误诊为镜像平台缺陷——实为两处实现 bug（`onBind` 不携带调用方身份、客户端未覆写 `onNullBinding`）+ `<queries>` 缺口，已修复并双机复验（根因与探针证据见完成记录）；无新 ADR，详见 [HXA-083 完成记录](../completion-records/HXA-083.md)） + **M8 / HXA-084 完成**（PRoot runner：`:runtime:proot-ipc` job 协议扩展（`ProotJobSpec`/`ProotJobRecord` 严格 codec + `ProotJobWire` + 事务码/状态字节 + 服务端零跨 APK 异常）+ `:runtime:proot-core` archive 原语（manifest codec/`JobPath` 词法防护/`JobZipWriter`/`ZipJobExtractor` 自研 central-directory symlink 拒绝）+ companion `ProotJobRunner`（单例 + 有界 journal 128/1 MiB fail-closed 额度 + argv/显式 script 双模 + watchdog `scheduleWithFixedDelay` + 进程组 kill + bounded 捕获共享预算 + 输出归档 hash 证明 + 孤儿 `(pid,starttime)` 双证清扫）+ **PRoot 首次设备真实执行**（设备实证 SELinux 平台事实：`untrusted_app` 对 `app_data_file` 的 `execute_no_trans` 被拒而 `apk_data_file` 被授予 → 启动链 `setsid→linker64→proot`，loader 以 `jniLibs` 可执行副本发行且每次 Job 哈希对锁；PRoot 在途 execve 改写使 `LD_PRELOAD` exec hook 不可达——实现后整体移除）+ 客户端 `ProotEnvScreen`（secret 名 pattern/已知 SecretStore 值/认证结构形状三重拒绝，理由不含值，Runtime 零 SecretStore 能力）+ `ProotJobClient`（每事务冷绑定、PFD 所有权约定、`awaitTerminal` 的 `Interrupted` 停泊点、断连只查询/对账绝不重放、零 FGS/零 wake lock）+ `:app` 跨 APK Job E2E 4 例；JVM 29 例（core 10 + ipc 11 + client 8）+ companion 设备 32 例 × 两台 + `:app` E2E 4 例 × 两台 + 083 绑定 E2E 8 例 × 两台（API 29/36 arm64-v8a 全绿）+ spotless/detekt/lint/check-docs/secrets/lockfiles 全过；真机 4 KiB/16 KiB smoke 与 wake lock 证据归 HXA-086/088；无新 ADR，详见 [HXA-084 完成记录](../completion-records/HXA-084.md)） + **M8 / HXA-085 完成**（`bash` Tool = `code.linux.run`：`CODE_EXECUTION`/L2/`LOCAL_PROOT` 每次审批（STANDARD 由真实 `PolicyEngine` 设备断言 `Deny(ISOLATED_RUNTIME_REQUIRES_ADVANCED)`；generic L2 无 auto-run 通路）+ developer flavor 注册（consumer 构建不存在）+ 每执行 gate（`READY`/`NOT_INSTALLED`/`DISABLED_OR_FORCED_STOPPED`/`NOT_VERIFIED`——用户完成过零 Job 验证 = 持久化 anchor（supervisor `anchorPresent()`），进程预活/手动修复都不是条件，被动 Registry 刷新绝不重绑）+ 批准后每执行重新握手（084 每事务冷绑定，绝不回退主进程 shell）+ 输入 = 经 store 的有界快照（≤64 文件 × 1 MiB，基名去重，真实 Workspace 绝不挂载）+ env 主进程三重筛选（已知 SecretStore 值按执行时刻现读，Runtime 零 SecretStore 能力）+ wire `deadlineMs` = 剩余审批窗口（companion watchdog 恰在审批绑定的 deadline 进程组 kill）+ 输出归档 manifest 哈希对已验证终态记录 + 逐条 sha/size 重验 + 只导入验证后的 `result.txt` 进 `output` 引用自身 region（缺失/超限 = SUCCEEDED 无导入）+ 审批卡完整 script/argv + `codeSha256` 短摘要 + 快照来源行 +「超时 N s（进程组 kill）· 输出上限 8 MiB · 联网：否」+ Settings Advanced-only 区块（状态行 + 验证 + 修复 Runtime 仅用户点击；恢复后重试 = 新 ToolCall/approval/jobId——审批 deadline 过期 `parsed()` 即拒 + 每执行全新 jobId）+ JVM 7 例 + 设备 E2E 5 例 × 两台（含真实 guest 执行 + 导入字节全等 + `JOB_FAILED` 稳定失败）+ `:app` 全量 129 例 × 两台 + companion 32 例 × 两台（API 29/36 arm64-v8a 全绿）；无新 ADR（实现 ADR-0007 + ADR-0012 + local-code-execution §6.5/6.6/6.7），详见 [HXA-085 完成记录](../completion-records/HXA-085.md)） + **M8 / HXA-086 完成（模拟器可验证范围；真机部分无真机如实停下）**（真机 smoke 与隔离：companion 通知停止面（普通通知，**非 FGS**——job 路径零 FGS/零 wake lock 不变量保持；STOP 动作经 `com.helix.permission.BIND_PROOT_RUNTIME` 签名权限广播 → 真实 `cancel` 设备断言）+ smoke 套件（python3/node/git/ripgrep 各一条真实 Job，随机 token 回显 + host 标记 + Journal 三证新鲜度契约）+ companion 侧隔离 4 例（guest 读不到 companion dataDir 标记/摸不到共享存储/不可联网 + companion 包结构性零 INTERNET/存储权限）+ 主 App 侧隔离 E2E 2 例（真实 `code.linux.run` 全链：guest `cat`/`touch` 主 App filesDir → READ-FAILED/WRITE-FAILED + 内容/文件不存在断言）+ 生命周期 6 相（强制停止稳定拒绝/冷绑定/空闲回收再启动/锁屏/低内存 → 各真实 Job SUCCEEDED；主 App 死亡中途 → ORPHANED 按 job id 对账；companion 被 host kill 中途 → `DEAD_OBJECT` 或 `Terminal(ORPHANED)` 双分支稳定非成功结算（083 冷重绑 + 清扫对账）；wake lock 8 采样零）+ 同 jobId 不重复执行 + `scripts/accept-hxa-086-lifecycle.sh` 11 阶段全绿 × 两台；companion 全量 43 例 + `:app` developer 全量 137 例 × 两台（API 29/36 arm64-v8a 4 KiB）；设备期修复 058 fixture 跨进程状态累积潜在缺陷（`TransferTestDocumentsProvider` 自恢复 reseed）；**真机缺口**：4/16 KiB 页真机 smoke、Doze/安全锁屏/热限、最低设备集真机证据（无真机，不做假绿——阻断分发已有结构落点：081 ELF 对齐门禁 360/360 ≥16384 + 082 安装器按页大小预检 + 085 gate），详见 [HXA-086 完成记录](../completion-records/HXA-086.md)） + **M8 / HXA-087 完成**（更新/卸载/法律页：「验证 Runtime」点击后 `verifyNowNote` 揭示"需更新"（锚点 lock 指纹 vs 内嵌 lock 漂移 → 稳定 `LOCK_MISMATCH`）+ 「重定基线（我已知晓基线变更）」显式二次动作（删锚点 → 再验证重建——移动基线绝不静默接受，083 锚点属性保持）+ 修复页更新态识别（`RuntimeInstallQueries.activeLockSha256` 读活动安装 manifest 内嵌 lock 指纹 vs 本 APK 内嵌 lock，纯 JVM `updateStateFor` fail-closed；更新 = 同一 082 原子安装路径，失败旧 runtime 保持可用）+ 回滚按钮（082 `activateRollback` 原子指针交换，回滚后真实 Job SUCCEEDED 设备断言）+ 完整删除（Settings 确认对话框 → 主 App 清自己锚点 + `EXTRA_REMOVE_RUNTIME` consent 跨 uid → companion 面内二次确认按钮 → `ProotRuntimeRemoval` 只删 `filesDir/runtime`（错根 fail-closed 拒绝）；gate 降级 NOT_VERIFIED，本包 Workspace 文件设备断言存活）+ 离线法律页（companion `ProotLegalActivity` 签名权限导出/无 launcher/零 INTERNET：离线声明 + 构建清单（规范 lock 指纹/ABI/每组件版本·来源 URL·源码 repo@ref·SHA-256·SPDX·预装包逐包）+ 许可证全文按 textRef 内嵌去重离线可读——单一真相在 companion 资产，主 App 不复制）+ consumer 保持无 PRoot（同 FQN 模块缝）；`scripts/accept-hxa-087-updates.sh` 两阶段全绿 × 两台 + 086 生命周期回归 × 两台 + companion 50 例 + `:app` 141 例 × 两台全绿 + 静态门禁全过，详见 [HXA-087 完成记录](../completion-records/HXA-087.md)） + **M8 / HXA-088 完成**（Git Workspace 语义 Spike + ADR-0008 待决定：三路径比较——路径 1 主 App Workspace 权威仓库 + 每操作完整原子事务（全部组件已在 084/085 管线设备验证，零新 IPC/挂载面/依赖，**推荐**）/ 路径 2 Runtime 私有目录（guest 只能看到 job 快照——私有目录对 guest 不可达设备实证；需新 bind 面 + 跨 uid 仓库事务协议 + 纳入 087 卸载/更新语义，成本 > 收益）/ 路径 3 Android Git 库（JGit 7.7.1.202607240634-r 纯 Java EPL-2.0 OR GPL-2.0-with-classpath-exception；libgit2 v1.9.7 GPL-2.0-or-later OR MIT；双 git 语义分叉 + 供应链负担，**均未引入依赖**）；真实 084 Job 管线设备测量 × 两台（小仓库 552 KiB：归档 31,056 B/13–46 ms + guest ≈251–255 ms + 输出 44,876 B/83 文件；中仓库 16 MiB：归档 312,373 B + guest init+add+commit+gc ≈1.3–1.5 s + gc 后 `.git` 28 文件/204–364 KiB——首版预算按 pack 后形态表达；084 限额 ≤128 MiB/≤4,096 文件/单文件 ≤64 MiB 内）；损坏 loose 对象 → fsck 报告 `bad sha1 file` 从不静默判净（**平台事实：fsck 报告损坏时可退出码 0 → 必须解析输出文本**）；job id → 恰好一个稳定终态记录（无重放，配合 086 宿主 kill ORPHANED 无"静默半仓库"落点）；攻击面设备实证：无防护时 post-commit hook（commit 触发）与 `.gitattributes` filter（add 触发 + **清空文件内容** = 静默篡改通道）均执行、alias/submodule/credential 结构性不触发；有防护（`-c core.hooksPath=/dev/null` + GIT_CONFIG_GLOBAL/SYSTEM=/dev/null + GIT_TERMINAL_PROMPT=0 + GIT_PAGER=cat）hook 抑制但 **filter 仍执行 → 强制宿主侧仓库预扫描（filter/diff/merge 命令配置 fail-closed 拒绝 add/commit）**；guest ash 无 `$(...)`（backtick 可用）命令模板约束；**零产品代码/零新依赖；ADR-0008 已由所有者接受（2026-09-05）：路径 1 主 App 权威仓库每操作完整原子事务 + 首版命令集 status/diff/log/init/add/commit（`reset --hard`/`clean` 不进入首版）+ 强制策略（hooksPath 防护 + 宿主侧 filter/diff/merge 预扫描 fail-closed + fsck 输出文本判定 + 084 归档限额超预算 fail-closed）——实现由后续 HXA 按 ADR 执行**；companion 56 例 + `:app` 141 例 × 两台全绿 + 静态门禁全过，详见 [HXA-088 完成记录](../completion-records/HXA-088.md)） |
| 发布状态 | 仅开发/测试产物；尚无完成签名与发布验收的稳定版本 |

## Completed

已完成任务的详细实现、命令、exit code、设备和限制只在 [HXA 完成记录](../completion-records/README.md) 与各记录中维护；本文件不复制字段级历史。

| 里程碑 | 已完成范围 | 权威证据 |
| --- | --- | --- |
| M0 | HXA-001～003：工程、质量门禁、AppContainer/导航壳 | [M0 完成记录](../completion-records/M0.md)、[逐 HXA 索引](../completion-records/README.md) |
| M1 | HXA-010～016：领域状态、Plan/Goal、Room、恢复、Context Builder | [逐 HXA 索引](../completion-records/README.md) |
| M2 | HXA-020～028：Secret/Provider、三协议、能力探测、模板、聊天 UI | [逐 HXA 索引](../completion-records/README.md) |
| M3 | HXA-030～039：Tool/Schema/Capability/Policy/Approval、Dispatcher/Scheduler、Tool Loop、模型流状态与 batch-safe Turn Coordinator | [逐 HXA 索引](../completion-records/README.md) |
| M4 | HXA-040～049：Workspace/scope、原子文件存储、首批文件工具、copy/move/trash、SAF adapter 与 developer All-files scope、文件管理 UI（Workspace 恒可写、developer all-files 根只读、SAF 接线与导入导出 UI 显式推迟）、受限 zip/tar archive/extract、会话附件导入/持久化与 UTF-8 文本物化（ADR-0014 首批，fail-closed 再校验 + egress 目标/附件集绑定），以及全项目审查收敛（ChatService 每会话 turn 模型 + 单测骨架、删除 WorkspacePath 死代码） | [逐 HXA 索引](../completion-records/README.md)、[文件工具 Bug 修复记录](../bug-fixes/2026-09-02-file-tool-safety-boundaries.md) |
| M5 | HXA-050～054（M5 完成）：Zipline 1.27.0 QuickJS spike（evaluate/memoryLimit/InterruptHandler/eval+Function 封堵/大于 6 MiB 调用线程栈/bindIsolatedService 唯一实例回收，各 25 例 androidTest 全过；16 KiB page 以四 ABI ELF PT_LOAD 对齐为替代证据；ADR-0015 accepted）+ 生产执行协议（isolated 非导出一次性 Service、手搓 onTransact execute/interrupt/info、64 KiB inline + PFD、deadline/interrupt/cancel/watchdog 与 Binder-death 回收、11 状态闭合错误集，36 例 androidTest/设备 + 33 例 JVM）+ IIFE wrapper JSON ABI（doc §3.2 严格 IIFE 为唯一生产路径、host 编码输入 + 局部 `const input` 闭包注入、JSON 输入输出 + 双层输出边界、19 例 wrapper 逃逸攻击套件，55 例 androidTest/设备 + 62 例 JVM）+ 执行接 Tool/Policy/审计（`code.javascript.run`：CODE_EXECUTION/L2/LOCAL_QUICKJS 单并发 lane、模型仅见 code+input 且 limits 固定 §4.1 默认不入 schema、11 状态稳定回填绝无假成功、§4.8 脱敏审计经 dispatcher 单一 emitter、审批卡完整代码块 + input 摘要 + 联网：否 + 固定 limits + 代码 SHA-256 短摘要；JVM 506 例（quickjs 74 + framework 136 + app 148×2）+ 5 例 androidTest × 两台设备全过）+ **攻击和端到端测试（HXA-054：无限循环 10 s wall TIMEOUT、64 MiB 堆 OOM 表面形态按 API 29/36 固定、输出洪泛 PFD 有界失败 + 256 KiB 边界±1、eval/fetch/require 生产链路调用尝试全 fail-closed、crash seam 稳定 CRASHED + 隔离 PID 死亡/主进程存活设备证据、2 MiB 输入/256 KiB 源 PFD 往返 + 超限预检不产生实例 + 服务端 inline-cap fail-closed、取消三竞态 50 轮收敛 + 一次性槽位不重放、verified artifact size+SHA-256 宿主侧重算采信前提 + JVM 失配分支、§10 NUL/emoji 输出方向/深嵌套/环引用（环引用 marker 设备固定为 `circular reference`）；75 例 androidTest/设备 × 两台 + 84 例 JVM 全过）** | [HXA-050 完成记录](../completion-records/HXA-050.md)、[ADR-0015](../adr/0015-zipline-quickjs-execution-base.md)、[HXA-051 完成记录](../completion-records/HXA-051.md)、[HXA-052 完成记录](../completion-records/HXA-052.md)、[HXA-053 完成记录](../completion-records/HXA-053.md)、[HXA-054 完成记录](../completion-records/HXA-054.md) |
| M5A | **完成**：**HXA-055 完成**——图片输入与 Provider 视觉能力：Phase 1 设备测量固化平台事实（Bitmap 像素在 native 内存非 JVM heap；SOF 头可伪造且 `inJustDecodeBounds` 信任至 30000² 但完整解码返回 null；`BitmapFactory` 不自动应用 EXIF 方向；`Bitmap.compress` 重编码剥离全部 EXIF）→ `VisionLimits` 集中预算（10 MiB 输入 / 4096 px 边 / 12 MP 像素 / 3.8 MB b64 per-image / 4× per-request，取 API 29/36 测量与 Provider 上限更严者）；`AttachmentClassification.ImageAttachment` 闭包分支（magic 赢，mediaType 限定 `ImageReference.MEDIA_TYPES`）；端上归一化器（字节门 → bounds 探针 → 2 的幂采样 → ARGB_8888 解码 OOM 捕获 → 手动 EXIF 旋转 → 质量/边长阶梯重编码剥离元数据 → 原子落盘）；production 图片源（只解析当前会话绑定、闭包 mediaType、预算内、magic 一致的 app-private artifact，任何 miss 短而稳定、无路径的 IAE，无裸 base64 回退）；三协议独立 resolver 委托同一 app 侧源（supplier 注入，stream 时解析）；连接测试第 5 阶段视觉探测（保留 1×1 探针图；Completed/Refusal ⇒ vision=true，任何 Error ⇒ 阶段 5 失败，可重试集固定）+ 用户可见手动声明（`MANUAL` 源）+ Provider 行声明按钮；`message_attachments` 绑定**归一化 artifact**，发送/重试/tool-loop backfill/恢复每次重验（存在 + SHA-256 + magic + 预算，miss 即整 turn fail-closed）；出网披露图片行（HIGH_SENSITIVE_IMAGE，归一化大小/尺寸/绑定 hash，强制 Confirm）；视觉未确认发送阻塞（可操作文案：跑连接测试或手动声明）；turn 快照含 capabilities；JVM 753 例 + consumer 设备套件 64 例 × 两台（API 29/36）+ spotless/detekt 全过；验证中修复 AppContainer 初始化顺序 NPE（factory 改 supplier）；**HXA-056 完成**——文本/图片附件端到端硬化与发布边界：`ShareIntentDraft` + MainActivity/manifest 三个 intent-filter（SEND/SEND_MULTIPLE × text/plain/image/*）→ provider-free「分享草稿」会话（文字预填/图片本地导入暂存，**绝不自动发送**，ADR-0014 §5）+ 草稿会话显式单发绑定（`sessions.bindProvider` 的 `providerId IS NULL` 行数门禁：已绑定会话 egress 目标结构性不可换，未测试 Provider/进行中 turn 双门禁）；fixture E2E（`ScriptedSseWire` 记录-回放 wire client，13 例）覆盖发送/流式回复/Tool Loop（backfill 重带附件块）/历史恢复（文本块重建 + 图片 data URL 经 production 图片源重解析）/脱敏与泄露断言（持久化消息体 + 用户可见错误无绝对路径/URI/正文/base64）+ 失败矩阵（grant 失效 fail-closed、Artifact 篡改重试阻塞、重复发送独立 turn、能力快照变化立即撤销视觉、10 MiB+1 过大阻塞、伪造 SOF 解压炸弹 0 归一化产物、取消 CANCELLED 终态 + 重发、进程回收持久化幸存/暂存丢弃）+ 统一稳定拒绝（PDF/OLE2-DOC/RIFF-WAVE/UTF-16LE 同一文案，0 artifact 0 暂存，无解析/解码/upload 副作用）；三 adapter golden request（HXA-055 三编码器 golden + E2E wire body 真机复核）+ 1 真实 vision endpoint smoke（开发机本地 SGLang 服务，OpenAI 兼容，key 为空，四象限色图正确识别 + `image_tokens=64` 证明图像被消费，环境明确记录、不替代 fixture）；JVM（库 232 例 + app 374 例含 `SessionBindProviderTest` 4 例）+ consumer 设备套件 79 例 + developer 92 例 × 两台（API 29/36）+ spotless/detekt 全过；验证中修复既有 `FilesScreenTest` SHA 行竞态（补 wait）并留档低频环境 flake（约 1/12 全量轮次、无异常无状态损坏、重跑必绿、基线 9ecf979 同现象，见记录「验证中缺陷 5」） | [HXA-055 完成记录](../completion-records/HXA-055.md)、[HXA-056 完成记录](../completion-records/HXA-056.md) |
| M5B | **完成**（HXA-057/058/059 全部完成）：**HXA-057 完成**——persisted SAF tree scope 接线（解析 + 只读浏览后端）：`SafTreeScopeResolver` 每次使用 7 步实时复核（grant / aliasing / provider 身份 / 根文档 / URI 变化 / 读写模式，全部 fail-closed，`ScopeNotAvailable` 消息无 URI）；`SafTreeScopeService`（`SafGrantStore` 作 grant registry + 实时 `liveSources`/`revoke`/`grant`/`sweepRevoked`，dead grant 从来源列表即时消失）；模型不透明（模型/Tool/诊断/日志只见 `scope:<saf-…>:<相对路径>` + 显示名，`content://` URI 永不进模型/Room/audit/用户可见错误）；生产 `ContentResolverSafTreeCheck`（live `resolver.query` 判活、空目录 0 行仍活、`persistedUriPermissions` 写权限判定）；生产 `ContentResolverSafTreeReader`（`DocumentsContract` 只读后端：相对路径↔不透明文档 ID 逐级歧义名 fail-closed walk、有界 8 MiB read 窗、超限 copy fail closed 且删部分文件）；`FileManagerService` 对 `saf-` scope 走 SAF 只读分支（list/preview/mime/info/share），变更入口 fail-closed（`SAF 来源为只读`）；`AppContainer` 复用同一 `SafGrantStore`；`FilesScreen` 增「SAF 来源」重新授权/移除面板（OpenDocumentTree picker）。新增 27 例 JVM（resolver 12 + service 5 + file-manager 10）+ 13 例设备（scope 5 + reader 8，in-APK 文档树 fixture 驱动真 `ContentResolver`）；设备验收 24/24 × 两台（API 29/36）+ JVM 458 例 + spotless/detekt/lint/assemble 全过；设备期发现并修复 2 处缺陷（生产 1：`substringBeforeLast` 无分隔符语义致根级文件 walk 错位恒 not-found；夹具 1：子列表 URI 段 `children` 复数，生产 reader 无错）。**HXA-058 实现完成**——文件管理器导入/导出入口（HXA-044 受限管线接线，管线仅追加 `onProgress` 尾参、防御逻辑零改动）：导入 `OpenDocument`/`OpenDocumentTree` picker → `input/`（`ContentResolverSafTreeLister` 有界枚举 200k + `SafTreeImportPlanner` 净化歧义 fail-closed 规划，目录只计一次/深度 32/文件数 10k，skip 全报告）；导出 `CreateDocument` 或 HXA-057 已授权树（WRITE 7 步实时复核先于任何字节写出，`ContentResolverSafTreeDestination` create-document 目标解析：冲突 FAE/overwrite 复用/歧义 fail closed）；冲突策略四选（ASK 默认，绝无默认覆盖，导入 OVERWRITE 先移回收站）；UI 展示来源/目标/名称/大小/策略/进度/取消/逐项目最终结果（成功/冲突/跳过/已取消/失败 + 细节）；**verified 语义**：`SafExportVerifier` 字节重读（读满且仅读满 + SHA-256 相等）才显示"（已校验）"，否则只报平台确认（`sizeVerified` 独立）；失败矩阵覆盖（同名冲突/部分流/大小谎报/磁盘满（流中断替代证据）/目标撤销/取消/进程回收（崩溃临时回收替代证据）/原子性/临时文件回收）；导入/导出是文件管理动作（不创建聊天消息、不发 Provider、不扩大 Agent scope）；未接线 fail closed（`导入/导出未接线`）；JVM feature/files 120 例 + app 双 flavor 各 225 例全绿（新增 51 例）+ spotless/detekt/lint/assemble + check-docs/lockfiles/secrets/adr 全过；26 例设备测试已由仲裁会话运行全绿（feature 35×2 + consumer 94×2 + developer 107×2，API 29/36）；设备期发现并修复 8 项缺陷（生产 2：list/destination 空游标 fail-open→fail-closed；夹具 4：两个 in-APK 树 provider create-document insert 需匹配 `tree/<root>/document/<parent>` 4 段形态、test 包进程无 kotlin-stdlib→`TransferTestDocumentsProvider` 纯 Java 重写（`exported=true`，test APK 不分发）、`OpenableColumns` 列名 `_size`/`_display_name`；测试架构 2：API 30+ 无法为 fixture provider 铸造持久化 WRITE→`WritableOverrideCheck` seam（fail-closed 侧仍真实 check 验证）、UI 正向 verified 回合改投 facade 层、UI 验证只读树全路径拒绝 0 写入；生产 1 另计：工具栏/预览动作行溢出致尾按钮零宽不可点→拆行）；**HXA-059 完成**——Provider 模型自动发现与选择（连接测试第 2 阶段 `listModels` 列表自动带出：「后端可用模型」（probe 界 1000 + UI 显示 200 + 过滤）+ 点选预填编辑表单（不自动保存）+ `Unsupported` 显式手动提示 + phase 2 `Failed` 无列表；状态存储向后兼容第 8 字段；设备阶段由仲裁会话执行：`ProviderModelDiscoveryUiTest` 4/4×2 + 真实 SGLang endpoint smoke 1/1×2（0 跳过）+ consumer 98×2 / developer 112×2 / feature 35×2 + JVM 85+237×2+120 全绿；设备期修复 1 生产缺陷（自托管无 key provider 编辑保存被静默禁用）+ 4 测试断言缺陷 + 1 既有测试单类复跑缺陷；2 处既有环境 flake 如实记录并有 main 同签名复现证据） | [HXA-057 完成记录](../completion-records/HXA-057.md)、[HXA-058 完成记录](../completion-records/HXA-058.md)、[HXA-059 完成记录](../completion-records/HXA-059.md) |
| M8 | **全部 HXA 完成（HXA-080～088）**：HXA-080 —— Runtime manifest/许可证 schema（`:runtime:proot-core` 纯 JVM 模块：runtime-lock.json/manifest.json/activation 唯一版本真相 schema + 严格 fail-closed codec + canonical 编码指纹 + `RuntimeBaseline` 基线单点强制；JVM 35 例 + 设备 3 例 × 两台（API 29/36 arm64-v8a））；HXA-081 —— 构建期固定资产（真实 `runtime-lock.json` 5 组件锁定：proot 5.1.107.92 termux deb + Alpine 3.22.5 + 53 包 apk 精确 pin（bash/git/python3/nodejs/ripgrep 齐备）；构建管线 digest 锁定 + 确定性重打包 + SHA-256/size 全量校验（generate/verify 双模式）；ELF `PT_LOAD` 对齐/ABI 检查器 360/360 ≥16 KiB；许可证文本入 APK；体积记录 APK 61,772,291 B / 临时峰值 ≈181.6 MiB；JVM 47 例 + 设备 6 例 × 两台；不在设备下载 executable）；HXA-082 —— RootFS installer（纯 Kotlin 流式 tar 读取器 + 词法 Zip Slip 防护 + `.partial` 单遍流式安装 + 结构 smoke + manifest/promote/原子激活 + 一版 rollback 交换 + 孤儿清扫 + 排他状态锁；lock 锁定对象修正为设备实际读取的 raw tar；JVM 83 例 + 设备 12 例 × 两台；API 29/36 arm64-v8a）；HXA-083 —— 独立 Runtime APK/IPC（`:runtime:proot-ipc` 状态字节协议 + PFD manifest + 严格 descriptor codec + debug 崩溃注入 seam；companion signature permission 双守卫 + `onBind` 调用方签名集重验 + 每次 bind 现算 manifest；客户端冷绑定 supervisor（本地检查→绑定→握手→锚点）+ 最小用户门控修复 Activity；落实 ADR-0007 冷绑定/零常驻/无回退；JVM 42 例 + companion 设备 20 例 × 两台 + `:app` 跨 APK E2E 8/8 × 两台（脚本编排））；HXA-084 —— PRoot runner（job 协议/record codec + 有界 journal（128 条/1 MiB，active/未对账不静默删除、满额拒绝新 Job、对账 payload 即删、30 天 evidence-expired）+ companion runner（argv/显式 script、env allowlist + 主进程 secret 名/已知值/认证形状三重拒绝、deadline watchdog + 进程组取消、bounded 双流捕获、输出归档 + hash、孤儿双证清扫）+ **PRoot 首次设备真实执行**（SELinux `execute_no_trans` 平台事实 + loader `apk_data_file` 可执行副本哈希对锁 + 在途 execve 改写致 `LD_PRELOAD` hook 不可达之实证）+ 客户端 `ProotJobClient`（冷绑定、`Interrupted` 停泊点、断连只查询/对账、零 FGS/零 wake lock）+ 跨 APK Job E2E；JVM 29 例 + companion 设备 32 例 × 两台 + `:app` E2E 4 例 × 两台；真机归 HXA-086/088）；HXA-085 —— `bash` Tool（`code.linux.run`：CODE_EXECUTION/L2/LOCAL_PROOT 每次审批 + STANDARD 真实 Policy Deny + developer-only + 每执行四态 gate（anchor = 用户完成过零 Job 验证；进程预活/手动修复都不是条件）+ 每执行重新握手 + 有界快照输入（经 store，不挂载真实 Workspace）+ 主进程 env 三重筛选 + wire deadline = 剩余审批窗口 + 输出归档双重哈希 + 只导入验证后 `result.txt` 进自身 region（缺失 = SUCCEEDED 无导入）+ 审批卡完整命令/离线声明 + Settings 验证/修复入口（仅用户点击）；JVM 7 例 + 设备 E2E 5 例 × 两台 + `:app` 全量 129 例 × 两台 + companion 32 例 × 两台；真机归 HXA-086/088）；HXA-086 —— 真机 smoke 与隔离（模拟器可验证范围）：companion 通知停止（普通通知非 FGS + 签名权限 STOP receiver + 真实 cancel 断言）+ smoke 4 工具真实 Job（token 新鲜度契约）+ companion 隔离 4 例 + 主 App 侧隔离 E2E 2 例（真实 tool 全链 guest 不可读写主 App dataDir）+ 生命周期 6 相（冷绑定/空闲回收/锁屏/低内存/主 App 死亡 ORPHANED/companion 死亡 DEAD_OBJECT∪ORPHANED + wake lock 零采样）+ 同 jobId 不重复执行 + 11 阶段验收脚本 × 两台；companion 43 例 + `:app` 137 例 × 两台；真机 4/16 KiB smoke、Doze/锁屏/热限、最低设备集真机证据 = 真机缺口（无真机如实停下）；HXA-087 —— 更新/卸载/法律页（模拟器全验证）：同签名 APK 更新"需更新"检测（`LOCK_MISMATCH` 稳定揭示）+ 显式重定基线（两次用户动作）+ 修复页更新态按钮 + 用户门控 rollback（回滚后 Job 可用）+ 完整删除（只删 runtime 树不误删 Workspace，双端各清自己 + 面内二次确认）+ 离线法律页（source URL/build manifest/许可证全文离线可读，零 INTERNET）；companion 50 例 + `:app` 141 例 × 两台 + 086 回归 × 两台；HXA-088 —— Git Workspace 语义 Spike + ADR-0008（零产品代码/零新依赖）：三路径比较（推荐路径 1 主 App 权威仓库每操作完整原子事务；路径 2 需新 bind 面+新协议；路径 3 JGit/libgit2 双实现分叉）+ 真实 084 管线设备测量（小/中仓库归档·耗时·gc 后 `.git`）+ 损坏 fsck 报告（退出码可 0）+ 攻击面证据（filter 强制宿主预扫描）+ guest ash `$(...)` 缺失事实；**ADR-0008 已由所有者接受（2026-09-05）**；companion 56 例 + `:app` 141 例 × 两台 | [HXA-080 完成记录](../completion-records/HXA-080.md)、[HXA-081 完成记录](../completion-records/HXA-081.md)、[HXA-082 完成记录](../completion-records/HXA-082.md)、[HXA-083 完成记录](../completion-records/HXA-083.md)、[HXA-084 完成记录](../completion-records/HXA-084.md)、[HXA-085 完成记录](../completion-records/HXA-085.md)、[HXA-086 完成记录](../completion-records/HXA-086.md)、[HXA-087 完成记录](../completion-records/HXA-087.md)、[HXA-088 完成记录](../completion-records/HXA-088.md) |

- M8 / HXA-088 已完成：Git Workspace 语义 Spike 与 ADR-0008（roadmap §12 末项；**Spike 证据 + ADR-0008 已由所有者接受（2026-09-05），零产品代码、零新依赖**）——三路径比较：路径 1 主 App Workspace 权威仓库 + 每操作完整原子事务（全部组件已在 084/085 管线设备验证，零新 IPC/挂载面/依赖，**推荐**）/ 路径 2 Runtime 私有目录（设备实证 084 Job 模型中 guest 只能看到 job 快照——私有目录对 guest 不可达；需新 bind 面 + 跨 uid 仓库事务协议 + 纳入 087 卸载/更新/删除语义，成本 > 收益）/ 路径 3 Android Git 库候选调查（JGit 7.7.1.202607240634-r：纯 Java，EPL-2.0 OR GPL-2.0-with-classpath-exception；libgit2 v1.9.7：GPL-2.0-or-later OR MIT，需 arm64 NDK 构建 + JNI；双 git 语义分叉 + 供应链负担——**均未引入依赖**）；真实 084 Job 管线设备测量 × 两台（API 29/36 arm64-v8a 4 KiB 模拟器 = ADR 允许的代表性设备，无真机如实记录）：小仓库（64×8 KiB）归档 31,056 B/13–46 ms + guest ≈251–255 ms + 输出 44,876 B/83 文件；中仓库（512×32 KiB ≈ 16 MiB；guest init+add+commit+gc）归档 312,373 B + guest ≈1.3–1.5 s + 输出 336,608 B/542 文件 + gc 后 `.git` 28 文件/204–364 KiB（**首版预算按 pack 后形态表达**；不可压缩载荷 ≈ 原始 16.8 MiB 仍在 084 限额 ≤128 MiB/≤4,096 文件/单文件 ≤64 MiB 内）；损坏 loose tree 对象 → `git fsck --full` 报告 `bad sha1 file` 从不静默判净（**平台事实：fsck 报告损坏时可退出码 0 → 结构化执行器必须解析输出文本**）；job id → 恰好一个稳定终态记录（幂等查询无重放；输出整归档边界无半解包；配合 086 宿主 kill ORPHANED——"静默半仓库"无落点）；攻击面设备实证（快照携带恶意仓库 × 首版命令集 status/diff/log/init/add/commit）：无防护时 post-commit hook（commit 触发）与 `.gitattributes` filter（add 触发且**清空文件内容** = 进入快照的静默篡改通道）均执行，alias 不展开（结构化子命令不走 alias 表）/submodule 不递归（离线+无 submodule 命令）/credential helper 不触发（首版无远程命令）；有防护（每次 `-c core.hooksPath=/dev/null` + `GIT_CONFIG_GLOBAL`/`GIT_CONFIG_SYSTEM=/dev/null` + `GIT_TERMINAL_PROMPT=0` + `GIT_PAGER=cat`）hook 抑制、首版命令集全成功，**但 filter 仍执行 → 强制宿主侧仓库预扫描**（`filter`/`diff`/`merge` 命令配置 fail-closed 拒绝 `add`/`commit`）；guest 平台事实：Alpine 3.22.5 rootfs 的 `/bin/sh`（BusyBox ash）不支持 `$(...)` 命令替换（backtick 可用）——首版命令模板约束；首版拒绝/禁用策略 + 候选 Tool schema（`code.git.run`，Advanced-only，`status/diff/log` L1 / `init/add/commit` L2，`reset --hard`/`clean` 不进入首版）+ 路径 1 推荐全部记录在 ADR-0008（**已由所有者接受，2026-09-05**；持久 Git 实现由后续 HXA 按 ADR 执行）；验证：`ProotGitSpikeDeviceTest` OK (6 tests) × 两台 + companion 全量 56 例 × 两台 + `:app` 141 例 × 两台 + spotless/test/detekt/lint/check-docs/check-secrets/check-lockfiles 全过；决策记录 6 条（真实管线测量/路径 1 推荐/filter 预扫描强制/fsck 退出码不可信/ash `$(...)` 约束/ADR 状态），详见 [HXA-088 完成记录](../completion-records/HXA-088.md)。
- M8 / HXA-087 已完成：更新/卸载/法律页（roadmap §12 四项全落地）——同签名 APK 更新：「验证 Runtime」点击后 `verifyNowNote` 揭示"需更新"（持久化锚点的规范 lock 指纹 vs 内嵌 lock 漂移 → 握手稳定 `LOCK_MISMATCH` = 085 `NEEDS_UPDATE` 同源）+「重定基线（我已知晓基线变更）」二次显式动作（`clearAnchorForRebaseline` 删锚点 → 再验证对新 lock 重建锚点；移动基线绝不静默接受，083 锚点属性保持）+ 修复页更新态识别（`RuntimeInstallQueries.activeLockSha256` 读活动安装目录 `manifest.json` 内嵌 lock 指纹 vs 本 APK 内嵌 lock；纯 JVM `updateStateFor` fail-closed——无活动/损坏 manifest 绝不读成"一致"；更新 = 同一 082 原子安装路径，失败旧 runtime 保持可用）+ 用户门控 rollback（082 `activateRollback` 原子指针交换无文件移动；回滚后第一版真实 Job SUCCEEDED 设备断言；无回滚版本 `NoRollback` 稳定态）+ 完整删除（Settings「删除 Runtime」确认对话框 → 主 App 清自己锚点（gate 降级 NOT_VERIFIED）+ `EXTRA_REMOVE_RUNTIME` consent 跨 uid → companion 面内二次确认按钮 → `ProotRuntimeRemoval` 只删 `filesDir/runtime`（路径名 fail-closed 校验，错根拒绝）；本包 Workspace 侧文件存活设备断言；companion filesDir 兄弟文件存活断言；删除后重装路径可用）+ 离线法律页（companion `ProotLegalActivity`：导出 + 签名权限 + 无 launcher + 零 INTERNET；内容 = 离线声明（无网络/只随同签名 APK 更新/无应用内自更新/URL 永不访问）+ 构建清单（规范 lock 指纹/ABI/每组件版本·来源 URL·源码 repo@ref·SHA-256·SPDX·补丁·预装包逐包版本+SPDX）+ 许可证全文按 `textRef` 从内嵌资产读取去重离线可读——单一真相在 companion 资产（081 管线产出），主 App 不复制；主 App「许可证与来源」用户点击跨 uid 打开实证）+ consumer 保持无 PRoot（同 FQN 模块缝 + `verifyNowNote`/`removeRuntimeNote` 等 flavor 中性缝）；验证：`scripts/accept-hxa-087-updates.sh`（companion 更新套件 7 例 + 主 App E2E 4 例：法律页权限形状/用户点击打开/删除锚点清除+gate 降级+Workspace 存活/模拟 lock 漂移→`LOCK_MISMATCH`→显式重定基线→再验证重建）全绿 × 两台（API 29/36 arm64-v8a 4 KiB）+ 086 生命周期回归 × 两台 + companion 全量 50 例 + `:app` 141 例 × 两台 + spotless/test/detekt/lint/check-docs/check-secrets/check-lockfiles 全过；决策记录 6 条（需更新只在用户点击揭示/重定基线两次显式/删除双端各清自己+面内二次确认/法律页放 companion 单一真相/更新检测读活动 manifest/读写侧分离），详见 [HXA-087 完成记录](../completion-records/HXA-087.md)。
- M8 / HXA-086 已完成（**模拟器可验证范围**；真机 4/16 KiB smoke、Doze/安全锁屏/热限、最低设备集真机证据无真机，如实停下不做假绿）：真机 smoke 与隔离——companion 通知停止面（`ProotJobNotification` 普通通知 channel `proot-jobs`/tag 每 Job/`IMPORTANCE_LOW`，**非 FGS**：job 路径零 FGS 零 wake lock 不变量保持，`POST_NOTIFICATIONS` 未授予时 Job 不受影响；`ProotJobStopReceiver` 导出 + `com.helix.permission.BIND_PROOT_RUNTIME` 签名权限 + jobId 形状校验 → `ProotJobRunner.cancel` 真实取消断言；四终态统一撤通知）+ companion smoke 套件（python3/node/git/ripgrep 各一条真实 Job：随机 token 经 guest env/sh 回显 + host `smoke-ran-<jobId>` 标记 + Journal 记录三证新鲜度契约，陈旧执行不可能通过）+ companion 隔离 4 例（guest 读不到 companion dataDir 标记/共享存储不可达/网络不可达 `sys.exit(99)` 反证 + 结构性断言 companion 包零 INTERNET/存储权限（`GET_PERMISSIONS` 查询 companion 包名））+ 主 App 侧隔离 E2E 2 例（真实 `ProductionLinuxExecutor` 全链：guest `cat`/`touch` 主 App filesDir 标记 → READ-FAILED/WRITE-FAILED + 内容缺失/文件不存在断言）+ 生命周期 6 相（`ProotLifecycleE2eDeviceTest`，脚本分阶段驱动：强制停止 → 真实 gate 稳定 `Failed`（detail 含 unavailable+force-stopped）；从未打开冷绑定/空闲回收（pkill 60 s）/锁屏（keyevent 26）/低内存（`am kill`）→ 各真实 Job SUCCEEDED；主 App 死亡中途 → ORPHANED 按 job id 对账（exitCode null，绝不重放）；companion host `pkill -9` 中途 → `Unavailable(DEAD_OBJECT)` ∪ `Terminal(ORPHANED)` 双分支稳定非成功（083 冷重绑 + starttime 双证清扫对账——设备实证客户端轮询走冷重绑路径）；长 Job 期间 `dumpsys power` 8 采样 companion uid 零 wake lock 零 FGS）+ 同 jobId 不重复执行（084 `aJobIsStartedExactlyOnce`）+ `scripts/accept-hxa-086-lifecycle.sh`（构建安装 4 APK + 11 阶段，host 侧 job id 经 `run-as` 文件传递——本平台测试 stdout 不跨 raw instrument 流）；验证：companion 全量 **43 例** × 两台（32+4+4+3）+ `:app` developer 全量 **137 例** × 两台（129+2+6，生命周期 2 个 host 括号相全量上下文 assume 跳过）+ 11 阶段脚本全绿 × 两台 + spotless/test/detekt/lint/check-docs/check-secrets/check-lockfiles 全过；设备期修复 058 fixture 跨进程状态累积潜在缺陷（`TransferTestDocumentsProvider.reseedIfTheHostClearedTheBackings`：host 清空 backing 目录后 provider 自恢复——先清 in-memory 行再 `mkdirs()+seed`，失败降级空 fixture 诚实 FNF；live 进程 + 清理 ×3 全绿验证）；决策记录 6 条（普通通知非 FGS / STOP 导出锁签名权限 / 停止测试进程内 receiver seam / companion 死亡双分支稳定结算 / smoke 新鲜度契约 / 最低设备集阻断落点=081 ELF 门禁+082 页大小预检+085 gate）；无新 ADR，详见 [HXA-086 完成记录](../completion-records/HXA-086.md)。
- M8 / HXA-085 已完成：`bash` Tool = `code.linux.run`（roadmap §12：每次审批 + 仅 Advanced + Runtime 单独安装/启用 + 用户完成过零 Job 验证才可用）——developer flavor 注册（consumer 构建不存在，`ProotToolModule` 同 FQN flavor 切换）+ 每执行四态 gate（`READY`/`NOT_INSTALLED`/`DISABLED_OR_FORCED_STOPPED`/`NOT_VERIFIED`；"需更新" 执行期 `LOCK_MISMATCH` → `NEEDS_UPDATE`；用户完成过零 Job 签名/协议/ABI 验证 = 持久化 anchor（`ProotRuntimeSupervisor.anchorPresent()` 新增），进程预先存活/手动打开修复入口都不是条件，被动 Registry 刷新绝不重新绑定）+ 批准后每次执行重新握手（084 `ProotJobClient` 每事务冷绑定，绝不回退主进程 shell）+ STANDARD 由真实 `PolicyEngine` 设备断言 `Deny(ISOLATED_RUNTIME_REQUIRES_ADVANCED)`（generic L2 无 auto-run 通路，ADR-0012）+ 输入 = 经 `WorkspaceArtifactStore` 的有界快照（≤64 文件 × ≤1 MiB，基名去重，真实 Workspace 绝不挂载）+ env 主进程 `ProotEnvScreen` 三重筛选（已知 SecretStore 值按执行时刻现读，理由绝不含值，Runtime 零 SecretStore 能力 → secret inheritance 由构造不可能）+ wire `deadlineMs` = **剩余审批窗口**（companion watchdog `createdAt + deadlineMs` 恰在审批绑定的 deadline 进程组 kill；初稿误传绝对 epoch，设备期修正）+ 输出归档 `manifestSha256` 对已验证终态记录 + `Extraction.manifest` 逐条 sha/size 重验 + 只把验证后的 `result.txt` 经 store 导入 `output` 引用自身 region（首段 ∈ `input|work|output` + store containment 双重强制；**缺失/超限 = SUCCEEDED 无导入**，finding 不是 failure）+ 审批卡完整 script/argv + `codeSha256` 短摘要 +「N 个文件，快照复制，不挂载真实 Workspace」+「超时 N s（进程组 kill）· 输出上限 8 MiB · 联网：否（Runtime 无 INTERNET 权限）」（schema 无网络开关——Advanced/LAN scope 都不能给 PRoot 联网，§6.1）+ Settings Advanced-only PRoot Runtime 区块（状态行 + 验证（off-main `verifyNow`）+ 修复 Runtime 仅用户点击 `openRepair`；恢复后重试 = 新 ToolCall/approval/jobId——审批 deadline 过期 `parsed()` 即 `ParseFailure` + 每执行全新 `jobId`/`executionId`）+ JVM 7 例（`LinuxRunToolTest`，testDeveloper）+ 设备 `LinuxRunToolE2eDeviceTest` 5 例 × 两台（descriptor 无网络键 / STANDARD 真实 Policy Deny / **全链真实 guest 执行 + 导入字节全等 + sha + audit jobId** / 已知 secret 值 wire 前拒绝 / `exit 3` → `JOB_FAILED` 稳定失败）+ `:app` developer 全量 **OK (129 tests) × 两台** + companion 全量 **OK (32 tests) × 两台**（API 29/36 arm64-v8a）+ spotless/detekt/lint/check-docs/secrets/lockfiles 全过；设备期修复 6 项（deadline 相对语义、`importResult` 改用 `Extraction.manifest`（提取器不落盘 manifest.json）、`result.txt` 缺失改 SUCCEEDED 无导入、输出 region 首段判定、输入基名按索引去重、detekt ThrowsCount/Swallowed 结构化）；无新 ADR（实现已接受 ADR-0007 + ADR-0012 + local-code-execution §6.5/6.6/6.7），详见 [HXA-085 完成记录](../completion-records/HXA-085.md)。
- M8 / HXA-084 已完成（真机部分归 HXA-086/088，模拟器可验证部分完成）：PRoot runner（`:runtime:proot-ipc` job 协议扩展——`ProotJobSpec`（argv/显式 script 互斥、cwd 严格单段、env ≤64、deadline/输出上限、`inputManifestSha256`）+ `ProotJobRecord` 严格 codec（terminal commit = canonical committedBody SHA-256、SUCCEEDED 必带 exitCode、ORPHANED/INPUT_INVALID 绝不携带）+ `ProotJobWire` 手工 Parcel + 事务码/状态字节（服务端零跨 APK 异常）；`:runtime:proot-core` archive 原语——manifest codec/`JobPath` 词法防护/`JobZipWriter`（manifest 先行 + 哈希全等门）/`ZipJobExtractor`（自研 central-directory 先行拒绝 symlink——JDK `ZipEntry` 不暴露 unix mode）；companion `ProotJobRunner` 单例——有界 journal（128 条/1 MiB，active/未对账不静默删除、满额拒绝新 Job `JOURNAL_FULL`、对账 payload 即删 + 7 天 tombstone、30 天 `evidenceExpired` marker）+ 生命周期（重复 jobId/executionId → `Duplicate` 绝不二次启动、输入归档 → `INPUT_INVALID`）+ **PRoot 首次设备真实执行**（设备实证 SELinux 平台事实：`untrusted_app` 对 `app_data_file` 的 `execute_no_trans` 被拒、`apk_data_file` 被授予 → 启动链 `setsid→linker64→proot`，loader 以 `jniLibs` 可执行副本发行 + 每次 Job 哈希对 081 锁；PRoot 在途 execve 改写（PTRACE）使 `LD_PRELOAD` exec hook 不可达——实现后整体移除）+ watchdog `scheduleWithFixedDelay` + 进程组 kill（setsid pgid）+ `/proc` 后代 BFS + bounded 双流共享预算 + 输出归档 hash 证明（`AutoCloseOutputStream` PFD）+ 孤儿 `(pid,starttime)` 双证清扫（重用 pid 绝不触碰）；客户端 `ProotEnvScreen`（secret 名 pattern 即使显式加名也拒绝 / 已知 SecretStore 值改名拦截 / 认证结构形状，理由绝不含值，Runtime 零 SecretStore 能力）+ `ProotJobClient`（每事务冷绑定、PFD 所有权约定、`query`/`cancel`/`reconcile`、`awaitTerminal` 的 `Interrupted` 停泊点、`DeadObjectException`→`DEAD_OBJECT`、断连只查询/对账绝不重放、零 FGS/零 wake lock）；`ProotNative.pageSizeBytes()` NDK seam 收编 083 页大小常量；JVM 29 例（core 10 + ipc 11 + client 8）+ companion 设备 32 例 × 两台（含真实 guest 执行/重复 jobId/manifest 失配/取消/TIMED_OUT/对账/额度满/孤儿清扫/script 模式/tmp 隔离）+ `:app` 跨 APK `ProotJobE2eDeviceTest` 4 例 × 两台 + 083 绑定 E2E 8 例 × 两台（API 29/36 arm64-v8a 全绿）+ spotless/detekt/lint/check-docs/secrets/lockfiles 全过；设备期修复 8 项（安装产物 0600 无执行位、PRoot 三环境变量缺失、`--` 分隔符、`Process.pid()` 不存在→类层级反射、exec hook 平台不可达移除、`useLegacyPackaging` 落盘、客户端双 `readException`、jobId 线格式对齐）；无新 ADR，详见 [HXA-084 完成记录](../completion-records/HXA-084.md)。
- M8 / HXA-083 已完成（设备验收含 1 项平台阻塞记录）：独立 Runtime APK/IPC——新增 `:runtime:proot-ipc` 共享模块（`ProotRuntimeProtocol` 常量/事务码、`RuntimeTargetDescriptor` + 严格 fail-closed codec、`PfdManifestChannel` pipe PFD 上限传输、`ProotHandshakeClient` 状态字节客户端、`ProotRuntimeServiceBinder` 无 AIDL 服务侧、`UnavailableCause` 稳定原因闭集；**协议零跨 APK 异常**——API 34 `Parcel.readException` 语义变化 + quickjs house 先例）；companion（`:runtime:proot-app`）`ProotRuntimeService`（bound-only、`onBind` 经 `ProotCallerVerifier` 重验调用方签名集 fail-closed、每次 bind 现算 `ProotHandshakeManifest`（protocol 1 + versionName + ABI + 082 canonical lock 指纹））+ signature permission 同守卫 service 与最小 `ProotRepairActivity`（无 launcher/无 INTERNET/文案入资源）；客户端（`:runtime:proot-client`）`ProotRuntimeProbe`（FLAG_STOPPED 跨包可见）/`ProotLocalStateCheck`/`VerifiedRuntimeStore`（tmp+fsync+rename 锚点，损坏读为无）/`ProotRuntimeSupervisor`（`checkLocalState`→显式 ComponentName + `BIND_AUTO_CREATE` 冷绑定 + 每调用独立 ServiceConnection + 20 s 硬 deadline→握手→锚点比对；`openConnection`/`withConnection` 084 seam；`openRepairActivity` 仅用户点击 + 稳定原因映射；**拒绝即解绑绝不留进程、绝不回退主 App shell**）；`:app` developer flavor `uses-permission`（consumer 不声明）；落实 ADR-0007（启动/Advanced/Registry 刷新零启动——结构性 + 设备断言；`TX_DEBUG_SELF_KILL` debug seam 驱动真实 binder 死亡设备测试）；修复期修订 6 项（`PackageInfoFlags` 守卫 30→**33**——API 30/31/32 真机将 `NoSuchMethodError`，lint NewApi 捕获；**调用方复验从 `onBind` 移入 binder `onTransact` 逐事务执行**（`onBind` 是主线程生命周期回调不携带调用方身份，`getCallingUid()` 返回自身 uid——初稿「实测 uid 正确」结论撤销，AOSP 源码 + 设备探针双证）；companion `<queries>` 补声明两个主 App 包（API 30+ 包可见性）；客户端补 `onNullBinding`/`onBindingDied` 覆写（null binder = 立即 `BIND_REFUSED`，不假超时）；`bindService` `SecurityException` → 稳定 `SIGNATURE_MISMATCH`；SetTextI18n 文案入资源）；验证中固化 5 项环境事实（AndroidJUnitRunner 跑在 `Instr:` 线程主 looper 自由→worker+`future.get(timeout)` 即安全；新鲜 `adb install` 后包为 force-stopped；shell `am start` 受 permission 拒、`su 0 am start` 代用户点击；`pidof` 不匹配 15 字符 comm 用 `ps -A | grep -w`；`am instrument` 原始输出格式解析）；JVM 42 例 + companion 设备 20 例 × 两台（API 29/36 arm64-v8a）+ `scripts/accept-hxa-083-lifecycle.sh` 跨 APK E2E **8/8 × 两台**（三生命周期阶段 + 冷绑定/二次验证/真实进程死亡→冷重绑恢复/用户门控修复入口/null-onBind 立即拒绝）+ spotless/detekt/lint/check-docs/secrets/lockfiles 全过；**初稿「跨 APK 投递 4 例被镜像平台缺陷阻塞」定性撤销**（误诊：实为上列 `onBind` 身份 + `onNullBinding` 两处实现 bug + `<queries>` 缺口，修复后双机全绿；探针日志/AOSP 佐证/原证据链重释见完成记录）；无新 ADR，详见 [HXA-083 完成记录](../completion-records/HXA-083.md)。
- M8 / HXA-082 已完成：RootFS installer（`.partial`/防 Zip Slip/smoke/原子激活/保留一版 rollback 全部落地）：纯 Kotlin 流式 tar 读取器 `TarStream`（512 B 块、typeflag 白名单 `'0'`/`'\0'`/`'5'`/`'2'` + GNU `'L'`/`'K'` 长名续块、256 MiB/10 万成员上限、64 KiB 流式 payload、结尾零块消费保证整档读满可哈希）+ `TarPathSafety` 纯词法防护（成员路径 NUL/换行/反斜杠/`..`/空段拒绝、前导 `/` 剥离、根成员 `/./` 归一 `""` 且只允许目录；symlink 目标词法深度判定，绝对目标按 PRoot chroot 树根基准，悬空合法，宿主绝不解析）；安装器 `RootFsInstaller`（`state/install.lock` 全程排他 + 同 JVM 双锁 held 不崩、启动清扫孤儿/stale `.partial`、解压+SHA-256/size 验证到 `.partial-<rand>/<inst_id>` 单遍流式（设备 heap 201 MB 内，137 MB 存档不整体缓冲）、PRoot 二进制 ≤64 MiB 门、结构 smoke（必需路径 `NOFOLLOW_LINKS` + 全树 ELF 有界 64 KiB 前缀按传入页大小 `PT_LOAD` 预检、`e_phoff` 越界 fail-closed，不执行 native）、`manifest.json` 内嵌 lock 重算、promote rename、**原子激活 active.json→rollback.json→最后删旧 rollback 目录**（任一崩溃点指针不指缺失目录）、失败阶段精确回填且前一版本无损）+ `activateRollback` 原子交换（交换前校验两版本目录 + rollback manifest）；lock 锁定对象修正为**原始 tar**（`f15c9434…`/137,287,680 B = AGP 展开后设备实际读取字节，`§6.4` 步骤 5 闭环；gz `500594df…`/50,955,376 降为构建日志产物；canonical 指纹 `46148505…`、lock 文件 raw `d2d0a59f…`）；`ProotRuntimeInstaller` Android 薄适配（`filesDir/runtime` + 内嵌 lock 基线 + 设备 ABI 门 + 资产流）；体积：APK 61,772,349 B、安装后 61,772,349 B、安装临时峰值 = 解压树 ≈133 MiB + 64 KiB 缓冲（update 最坏 3×133 MiB 磁盘瞬时）；验证中修复 9 项（真实存档根成员 `/./`、`File.exists` 跟随 symlink 误杀绝对 `bin/sh`、lock 指纹测试重言式、设备 OOM×2（137 MB `readBytes` + 51 MB node）、API 29 `Path.of` `NoSuchMethodError`→`Paths.get`、`ByteBuffer.wrap(arr,off,len)` 非切片陷阱、Kotlin/平台小坑、detekt/ktlint 阈值结构化重构）；JVM 83 例 + 设备 12 例 × 两台（API 29/36 arm64-v8a，完整安装流程：137 MB 解压 + smoke + 原子激活 + rollback 交换 + 篡改 fail-closed + 孤儿清扫）+ 资产管线 verify 逐字节一致（assetGate 360 ELF min p_align=16384）+ spotless/detekt/lint/check-docs/secrets/lockfiles 全过；无新 ADR，详见 [HXA-082 完成记录](../completion-records/HXA-082.md)。
- M8 / HXA-081 已完成：构建期固定资产（固定 PRoot/Alpine/bash/git/python/node/ripgrep：真实 `runtime-lock.json` = 5 组件（proot 5.1.107.92 termux deb / proot-loader / libtalloc 2.4.3 / libandroid-shmem 0.7 / alpine-rootfs 3.22.5）+ 53 包 apk 精确 pin（bash 5.2.37-r0、git 2.49.1-r0、python3 3.12.14-r0、nodejs 22.23.2-r0、ripgrep 14.1.1-r0 等，逐包版本+SPDX）；构建管线 `scripts/build-proot-assets.sh`（curl + SHA-256/size 校验 → Docker digest 锁定镜像 + `apk add name=version` → `deterministic_tar.py` 确定性重打包 → assetGate 全量 ELF 扫描 → lock 哈希校验；generate/verify 双模式跑通，verify 对锁定 lock 重建逐字节一致）；ELF `PT_LOAD` 对齐/ABI 检查器 `ElfLoaderAlignChecker`（纯 JVM fail-closed，ELF32/64 LE/BE，构建门禁/安装器预检（HXA-082）/16 KiB 判定（HXA-086）共用；rootfs 360 + PRoot 侧 4 个 ELF 全部 ≥16384 对齐、`e_machine`=EM_AARCH64）；许可证再分发文本入 APK（GPL-2.0-or-later/GPL-3.0-or-later/LGPL-3.0-or-later/BSD-3-Clause + ALPINE-README.md provenance）；体积记录：APK 61,772,291 B、安装后 61,772,291 B、临时峰值 ≈181.6 MiB（存档 48.6 MiB + 解压树 133 MiB）；不在设备下载 executable；JVM 47 例 + 设备 6 例 × 两台（API 29/36 arm64-v8a）+ spotless/detekt/lint/check-docs/secrets/lockfiles 全过；详见 [HXA-081 完成记录](../completion-records/HXA-081.md)）。
- M8 / HXA-080 已完成：Runtime manifest/许可证 schema（`:runtime:proot-core` 纯 JVM 模块：runtime-lock.json/manifest.json/activation 唯一版本真相 schema + 严格 fail-closed codec + canonical 编码指纹 + `RuntimeBaseline` 基线单点强制；JVM 35 例 + 设备 3 例 × 两台（API 29/36 arm64-v8a）；真实 `runtime-lock.json` 与资产归 HXA-081；详见 [HXA-080 完成记录](../completion-records/HXA-080.md)）。
- M5A / HXA-055 已完成：图片输入与 Provider 视觉能力（production 图片源 + 端上归一化 + `VisionLimits` 集中预算 + 连接测试第 5 阶段视觉探测/手动声明 + 归一化 artifact 绑定重验 + 出网披露图片行；JVM 753 例 + consumer 设备套件 64 例 × 两台 + spotless/detekt 全过）。
- M5A / HXA-056 已完成：文本/图片附件端到端硬化与发布边界（fixture E2E 13 例 + 分享草稿 UI 2 例 + `SessionBindProviderTest` 4 例；发送/流式/Tool Loop/历史恢复/脱敏 + 失败矩阵 + 统一稳定拒绝 + 泄露断言；本地草稿绝不自动发送 + 草稿会话单发绑定；三 adapter golden + 1 真实 vision endpoint smoke（本地 SGLang）；consumer 79 + developer 92 × 两台 + JVM 全过 + spotless/detekt 全过）。**M5A 完成。**
- M5B / HXA-057 已完成：persisted SAF tree scope 接线（每次使用 7 步实时复核 + fail-closed + 模型不透明 + `DocumentsContract` 只读浏览后端 + 来源列表/重新授权/移除面板；JVM 新增 27 例 + 设备 13 例；设备验收 24/24 × 两台（API 29/36）+ JVM 458 例全过；设备期发现并修复 2 处缺陷——生产 1（根级文件 walk 错位）、夹具 1（URI 段单复数）；SAF 写后端显式未实现）。
- M5B / HXA-058 已完成（**设备验收由仲裁会话执行**）：文件管理器导入/导出入口（HXA-044 受限管线接入文件管理器，管线仅追加 `onProgress` 尾参、防御逻辑零改动；导入 picker → `input/` + 有界枚举 200k/歧义 fail-closed 规划（目录只计一次、深度 32、文件数 10k、skip 全报告）、导出 `CreateDocument`/已授权树（WRITE 7 步实时复核先于任何字节写出）+ `SafExportVerifier` 字节重读校验 verified 语义（"（已校验）"只报告有证据的 hash 一致，否则仅报平台确认）+ 冲突策略/进度/取消/逐项目结果 UI + 失败矩阵覆盖（同名冲突/部分流/大小谎报/磁盘满（流中断替代证据）/目标撤销/取消/进程回收（崩溃临时回收替代证据）/原子性/临时文件回收）+ 未接线 fail closed；导入/导出是文件管理动作：不创建聊天消息、不发 Provider、不扩大 Agent scope；JVM feature/files 120 + app 双 flavor 各 225 全绿（新增 51 例）+ spotless/detekt/lint/assemble + check-docs/lockfiles/secrets/adr 全过；设备验收由仲裁会话执行全绿：`./gradlew :feature:files:connectedDebugAndroidTest` 35×2 + `:app:connectedConsumerDebugAndroidTest` 94×2（API 29 首轮 1 例已知 E2E flaky 单类复跑绿）+ `:app:connectedDeveloperDebugAndroidTest` 107×2（API 29/36），设备期修复缺陷 8 项、JVM 复验 120+225×2 全绿，M5B 完成）。
- M5B / HXA-059 已完成（**设备阶段由仲裁会话执行完毕**）：Provider 模型自动发现与选择（连接测试第 2 阶段 `listModels` 列表自动带出 UI：测试通过自动带出「后端可用模型」（probe 界 1000 + UI 显示 200 + 过滤），点选预填编辑表单 model 字段（不自动保存），`Unsupported` → 显式「后端未提供模型列表，请手动输入」，phase 2 `Failed` → 既有稳定错误文案、无列表；状态存储向后兼容可选第 8 字段（模型列表 JSON 数组，坏列表只降级、保留 PASSED）；只读 GET 无新增网络/写路径、key 永不入 UI/日志、模型 id 不入 logcat/Room 诊断、模板不写死模型名；JVM `:provider:api` 85 + app 双 flavor 各 237（新增 12 例）+ feature/files 120 全绿 + spotless/detekt/lint/assemble（含双 flavor 设备测试 APK）+ check-docs/secrets/lockfiles 全过；设备阶段：`ProviderModelDiscoveryUiTest` 4/4 × 双设备（API 29/36）+ 真实 SGLang endpoint smoke（`SglangUiSmokeTest`，HXA-056 同款 `10.0.2.2:30008`、空 key）1/1 × 双设备 0 跳过——五阶段连接测试对真实 27B 服务 PASSED、「后端可用模型 (1)」展示真实模型 id、芯片预填保存持久化到行；consumer 98×2 + developer 112×2 + feature 35×2（2 处既有环境 flake 如实记录并有 main 同签名复现证据）；设备期修复 1 个生产缺陷（自托管无 key provider 编辑表单「保存」被静默禁用——`editTemplateFor` 模板猜测凭空加 key 要求）+ 4 处测试断言缺陷 + 1 处既有测试（`AuditScreenTest`）单类复跑缺陷）。**M5B 全部完成**（HXA-057/058/059）。

架构决定的状态与理由见 [ADR 目录](../adr/README.md)；跨里程碑复核、事实修正和历史取舍见[文档复核记录](../history/documentation-review.md)。“有完成记录”不等于当前发布能力，当前可用边界只看本文件的摘要、接口和限制。

## In progress

- 无。

## Next task

- ~~M5B / HXA-059 的设备验收~~（**已由仲裁会话执行完毕**：`ProviderModelDiscoveryUiTest` 4/4×2 + 真实 SGLang endpoint smoke 1/1×2，详见 [HXA-059 完成记录](../completion-records/HXA-059.md)）；**M5B 全部完成**（HXA-057/058/059）；~~M8 / HXA-084~~（**已完成**：PRoot runner + 首次设备真实执行 + 跨 APK Job E2E，详见 [HXA-084 完成记录](../completion-records/HXA-084.md)）；~~M8 / HXA-085~~（**已完成**：`bash` Tool = `code.linux.run`，详见 [HXA-085 完成记录](../completion-records/HXA-085.md)）；~~M8 / HXA-086~~（**模拟器可验证范围已完成**：smoke/隔离/生命周期矩阵/通知停止/同 jobId/wake lock 全绿 × 两台；真机 4/16 KiB smoke、Doze/安全锁屏/热限、最低设备集真机证据待真机——无真机如实停下，详见 [HXA-086 完成记录](../completion-records/HXA-086.md)）；~~M8 / HXA-087~~（**已完成**：同签名 APK 更新/rollback/完整删除/离线法律页，详见 [HXA-087 完成记录](../completion-records/HXA-087.md)）；**M8 全部 9 个 HXA（HXA-080～088）已完成**——本分支工作全部完成，**停止等待用户授权**：授权后执行 FF 合并 main + 合并后复验 + push + worktree/分支清理；在授权前不触碰 main。ADR-0008（Git Workspace）已由所有者接受（2026-09-05）——持久 Git 实现由后续 HXA 按 ADR 执行；M8 真机缺口（4/16 KiB 页真机 smoke、Doze/安全锁屏/热限、最低设备集真机证据，HXA-086/088 记录）待真机；详见 [roadmap §12](roadmap.md)；M6 / HXA-060～069 与 M7 A2A（HXA-077～079）由另一负责人在 m6 分支并行推进，不在本分支范围。

## Blocked

- 无。

## Current interfaces

- `AppContainer` 已组合 Provider、ChatService、Capability/Policy/Approval 与 Tool Pipeline/Scheduler；`ShellRepository` fake 只服务早期壳层 route。
- 导航 route 只承诺稳定入口；会话与 Provider 已有实现，其余页面是否可用以对应 HXA 完成记录为准。
- 生产 Tool Registry 已包含 `time.now`、Workspace 文件工具 `read`/`write`/`edit`/`files.*`（含受限 `files.archive`/`files.extract`）与 QuickJS 代码执行工具 `code.javascript.run`（CODE_EXECUTION/L2/LOCAL_QUICKJS 单并发 lane；consumer + developer 双变体接线，HXA-053）及 PRoot Linux 执行工具 `code.linux.run`（CODE_EXECUTION/L2/`LOCAL_PROOT` 单并发 lane；developer-only 注册 + 每执行四态 gate + 每次审批；详见 [HXA-085 完成记录](../completion-records/HXA-085.md)）；文件工具当前 scope 为 app-private 与 developer All-files，SAF tree 尚未接入工具 scope resolver。
- QuickJS 执行通道已落地并接 Tool：主 App 内 isolated 非导出 Service（每执行唯一 `js_` + 32-hex 实例）+ 主进程 `JsExecutionClient`（同步有界、不重试；wrapper JSON ABI：输入 = 恰一个合法 JSON 文档、输出 = ≤ maxOutputBytes 的单个 JSON 文档、11 状态闭合集、PROTOCOL_VERSION=2；API 见 [HXA-052 完成记录](../completion-records/HXA-052.md)），经 `code.javascript.run` 接生产 Tool 管线（模型仅见 code+input、limits 固定 §4.1 默认不入 schema、§4.8 脱敏审计经 dispatcher 单一 emitter；接线见 [HXA-053 完成记录](../completion-records/HXA-053.md)）；PRoot 规划使用同签名、独立 applicationId/UID 的 Runtime APK，并通过 signature-protected Binder/PFD IPC 连接。
- **M8 / HXA-080**：`:runtime:proot-core`（纯 Kotlin JVM，无 Android 依赖）持有 PRoot Runtime 唯一版本真相 schema——`RuntimeLock`/`RuntimeInstallManifest`/`RuntimeActivation` 严格 fail-closed codec（未知 key 拒绝、闭集版本/ABI、canonical 小写 hex SHA-256、https 真主机 URL、无 traversal 嵌入引用、manifest 解析时重算内嵌 lock 指纹）+ canonical 编码（固定 key 序）`sha256Hex` 指纹 + `RuntimeBaseline.requireBaseline()` 基线单点强制（proot + alpine-rootfs + bash/git/python3/nodejs/ripgrep，组件 ABI 与 lock ABI 一致）；安装器（HXA-082）、握手（HXA-083）、法律页（HXA-087）均消费同一 schema 代码。
- **M8 / HXA-081**：真实 `runtime-lock.json`（5 组件：proot 5.1.107.92 / proot-loader / libtalloc 2.4.3 / libandroid-shmem 0.7 / alpine-rootfs 3.22.5，53 包 apk 精确 pin）+ 资产（生成二进制 gitignore 不入库，`scripts/build-proot-assets.sh` 重建须逐字节对齐 lock 哈希）+ 许可证文本（`licenses/`）。新增 `ElfLoaderAlignChecker`（纯 JVM，ELF32/64 LE/BE `PT_LOAD` 最小 `p_align` + `e_machine`，fail-closed）+ `:runtime:proot-core:assetGate` 任务——构建门禁/安装器预检（HXA-082）/16 KiB 判定（HXA-086）共用；rootfs 内嵌名规则 = lock URL basename 去 `.gz`（AGP 自动展开，HXA-082 与设备测试同一规则）。
- **M8 / HXA-082**：`:runtime:proot-core` 新增安装器状态机——`RootFsInstaller.install(InstallRequest): InstallOutcome`（Success(installId, rollbackId, swept, smoke, members, extractedBytes) / Failure(stage, reason)，stage ∈ lock/state/archive/binaries/smoke/manifest/promote/activate）+ `activateRollback(runtimeRoot, nowEpochMs): RollbackOutcome`（Done/NoActive/NoRollback/Failed，HXA-087 消费）+ `currentActive`/`currentRollback`；`TarStream.scan/extract`（纯 JVM 流式，typeflag 白名单 + 词法 Zip Slip 防护，symlink 目标 `validateSymlinkTarget` 按树深度判定、宿主绝不解析）；`InstallSmoke` 结构检查（必需路径 `NOFOLLOW_LINKS` + ELF 有界 64 KiB 前缀按 `pageSizeBytes` 预检）。目录模型：`runtime/state/`（版本无关激活指针 + `install.lock`）+ `runtime/inst_<12hex>/`（每版本 manifest/bin/rootfs/home/tmp）+ `.partial-<rand>/`（在飞，启动清扫）。`:runtime:proot-app` 的 `ProotRuntimeInstaller` 提供 Android 薄适配（`filesDir/runtime`、内嵌 lock 解析 + 基线 + 设备 ABI 门、资产流构造 `InstallRequest`）。**未含**：PRoot 内执行（HXA-084）、`bash` Tool（HXA-085）。
- **M8 / HXA-083**：新增 `:runtime:proot-ipc`（共享协议模块，无 UI/框架依赖）——`ProotRuntimeProtocol`（FQN 组件常量、protocol 1、`arm64-v8a`、能力闭集 `["handshake"]`、20 s deadline、64 KiB manifest 上限、`TX_HANDSHAKE`/`TX_DEBUG_SELF_KILL`）、`RuntimeTargetDescriptor` + 严格 codec（canonical 指纹）、`PfdManifestChannel`（pipe PFD 单向、超限/截断即协议失败）、`ProotHandshakeClient`（`Descriptor`/`Failed(UnavailableCause)`；`DeadObjectException`/`RemoteException`→稳定原因）、`ProotRuntimeServiceBinder`（无 AIDL；`debugSelfKill` 构造标志，仅 `BuildConfig.DEBUG` 启用崩溃注入 seam）；**协议零跨 APK 异常**（状态字节 + PFD，API 29～36 行为恒定）。companion `ProotRuntimeService`/`ProotCallerVerifier`（调用方 APK 当前签名证书集与本 APK 求交，空即拒；API 33+ `PackageInfoFlags` 守卫）/`ProotHandshakeManifest`（每 bind 现算）/`ProotRepairActivity`（最小修复入口，`INSTALL_PAGE_SIZE_BYTES=4096`，084 接 native `getpagesize`）。客户端 `ProotRuntimeProbe`/`ProotLocalStateCheck`/`VerifiedRuntimeStore`/`ProotRuntimeSupervisor`（冷绑定 seam `openConnection`/`withConnection` 供 084 每 Job 复用 + 每 Job 重握手；`openRepairActivity` 用户门控）。`bash` Tool UI 接线与"修复 Runtime"按钮归 HXA-085；编排验收脚本 `scripts/accept-hxa-083-lifecycle.sh`（预热助手 + 三生命周期阶段 + 冷绑定/SAFE 全套）。
- accepted [ADR-0013](../adr/0013-standard-store-capability-preserving-distribution.md)将 Standard 定义为 Google Play、国内 Android 应用商店和官网的完整产品形态；consumer/developer 仍只是当前构建事实。HXA-122 尚未决定稳定主 applicationId、渠道命名与升级路径，也没有外部 release artifact。
- CLI 订阅后端若实施，将使用另一个有 INTERNET 的独立 UID，凭据由官方 CLI 持有。
- MCP 规划为 Client-only；Skill 规划按 `SKILL.md` 开放规范渐进加载，二者当前均未实现。
- `read`/`write`/`edit`/`files.*` 已实现；`bash` 语义由 developer-only 的 `code.linux.run` 承载（HXA-085；裸 `bash` 短名映射未接线，归后续工具短名工作）。所有工具继续受 scope、Policy、Approval 和执行域约束。

## Known limitations

- HXA-050/051 的环境限制（替代证据已在完成记录）：无 API 34 系统镜像（API 29/36 覆盖 minSdk 与上界）、arm64 Mac 无 x86_64 模拟器镜像（x86_64 仅有 AAR 内 `.so` 存在性 + ELF 对齐证据，未声称设备运行）、设备均为 4 KiB page（16 KiB 以四 ABI PT_LOAD 对齐 16 KiB + 4 KiB 全能力运行为替代证据；HXA-054 沿用该口径，环境无 API 34 / x86_64 镜像，未重复论证）。API 34、x86_64 设备与 16 KiB 真机运行验证归后续设备矩阵。
- **图片视觉的平台事实与环境限制（HXA-055 Phase 1 两台设备固定）**：Bitmap 像素在 **native 内存**而非 JVM heap（API 29 模拟器 48 MiB heap 仍成功解码 32 MP/≈128 MiB bitmap；native threshold 216 MiB）——解码包络必须按 native 内存定界（`VisionLimits.MAX_TOTAL_PIXELS=12 MP` 对 216 MiB 阈值留实测余量，且 32 MP 解码成功**不是**许可）；**SOF 头可伪造且 `inJustDecodeBounds` 信任它**（30000² 可见，65535² 被平台在 bounds 阶段拒绝）但完整解码返回 null（安全）——头尺寸永不入内存数学，字节预算门先于解码；`BitmapFactory` **不自动应用 EXIF 方向**、`Bitmap.compress` 重编码**剥离全部 EXIF**（隐私剥离 = 重编码本身）。环境限制与 HXA-050~054 同口径：API 29/36 arm64-v8a 模拟器 + 4 KiB page，**无物理低内存真机**——`VisionLimits` 取模拟器证据与 Provider 上限的更严者并留余量，低内存真机矩阵归后续设备工作（32 MP 在模拟器可解码的事实不得作为放宽包络的依据）。
- QuickJS 引擎事实（HXA-051/052 设备测试与进程内探针固定）：顶层数组结果 → List、`memoryUsage` 在失败分配后回落基线、跨线程 evaluate 为 ASLR 布局相关 UB（服务永远只在其执行线程上 create/evaluate/close）；**wrapper 模式 OOM 表面形态（HXA-052 固定）**：API 29 64 MiB 堆批量耗尽时 catch 到字面 `null`（引擎无法分配 Error 对象，wrapper 原样重抛 → 宿主空消息 → OOM；用户 `throw null` 同形态不可区分、已接受），API 36 为 Error `"out of memory"`（wrapper 前缀后子串存活）；API 29 1600 万元素循环耗尽 64 MiB → 引擎原生 SIGSEGV（SEGV_ACCERR，永不作测试源）；HXA-051 的 raw 顶层对象 → `null` 事实自 HXA-052 起作废（wrapper 的 `JSON.stringify` 消除）；**环引用 stringify 真实消息为 `circular reference`（HXA-054 两台设备固定）**——V8 措辞 `Converting circular structure to JSON` 不存在于 QuickJS 2021-03-27 二进制（strings 佐证），HXA-052 的 `CIRCULAR_RESULT_MARKER` 是未经设备验证的假设常量，已按固定事实修正（service 环引用 → OUTPUT_LIMIT 重分类因此恢复可达）。Zipline 升级时须复核以上全部事实。
- QuickJS 执行控制面不含 `killProcess`/`System.exit`：超时/取消走 interrupt + 有界宽限 + 解绑，实例回收完全交给系统（Binder death 为主观察信号）；crash 注入 seam 仅 `BuildConfig.DEBUG` + 显式测试标志双门，release 编译移除。
- **API 29 生产迁移缺陷（HXA-053 设备验收复核发现，已在 main 修复）**：`MIGRATION_1_2` 使用 `ALTER TABLE approvals RENAME COLUMN`（HXA-034），API 29 的 SQLite（<3.25）不支持该语法，API 29 设备首次 V1→V2 升级会 `SQLiteException: syntax error`。已改用 copy-and-swap 修复并复测 API 29/36 47/47 无回归（见 [Bug 修复记录](../bug-fixes/2026-09-03-room-migration-sqlite-rename-column.md)）；HXA-053 复核时 API 29 的另 8 例失败（`FilesScreenTest` ×6、`GoalReminderTest` ×2）亦已随 main 的 API 29 修复一并解决，详见下方设备矩阵条目。
- **`ToolScheduler` 跨批 waiter 潜伏死锁（HXA-053 发现，已修复）**：原实现中等待准入的批次只监听本批 future，而准入冲突来自跨批共享的 inFlight 足迹——两个并发且互斥（容量占满或排他 lane）的独立 `scheduleBatch` 会互相等待、永不唤醒。修复：准入等待循环在监听本批 future 之外追加监听调度器全局“槽位状态变化”信号（每次 `releaseSlot` 触发，任何批次），任何批次释放槽位都会唤醒全部准入等待者；信号实例的读-挂接顺序保证不漏唤醒（挂到已完成实例立即回调，漏窗口内的释放必然完成所读实例）。`ToolSchedulerTest` 增加 2 例跨批存活回归（容量阻塞 + 排他写 lane 阻塞，均先证明被阻塞方确实卡在准入等待再释放）——修复前实测复现（释放槽位后另一批 `join` 超时）。生产不变量（turn 串行、批不重叠、Act 一次一调用）下该缺陷原本潜伏；此修复是框架契约的一部分。
- GitHub Actions 已有远端运行证据，但 workflow 不运行模拟器/真机测试；现有 artifact 是短期 debug 安装包，不是签名 release 或发布验收证据。
- Browser、MCP、Skill、PRoot/CLI、Accessibility、Root 等用户可感知执行能力尚未实现；QuickJS 执行已接 `code.javascript.run`（HXA-053），攻击套件与端到端边界测试（无限循环/内存/输出洪泛/eval/fetch/require/进程崩溃/Binder 大输入/取消竞态/verified artifact）已由 HXA-054 完成（两台设备 75 例 androidTest + 84 例 JVM 全过，含真机崩溃/内存/取消设备证据）。
- 设备矩阵：consumer 变体在 API 36 arm64-v8a 模拟器全绿（HXA-048 实测 47/47；迁移修复后复测仍 47/47 无回归）。API 29 已实测，HXA-048 发现的已知失败均已修复：`ProductionMigrationDeviceTest` ×2（`MIGRATION_1_2` 使用 `RENAME COLUMN`，需 SQLite ≥3.25/API 30+，Android 10 上 v1→v2 迁移会崩，既有产品缺陷）已改用 copy-and-swap 修复（见 [Bug 修复记录](../bug-fixes/2026-09-03-room-migration-sqlite-rename-column.md)）；`GoalReminderTest` ×2（POST_NOTIFICATIONS 授予 helper 未对 SDK<33 设防）已修复（helper 在 API<33 时 no-op）；`FilesScreenTest` ×6 的根因**不是**“API 29 模拟器慢 / 超时抖动”，而是两处生产代码调用了 API 29 平台缺失的 `java.*` 方法（`WorkspaceArtifactStore.listDir` 的 `Stream.toList()`（API 31+）与 `ReadWindow.read` 的 `InputStream.skipNBytes`），已修复并复测：API 29 47/47、API 36 47/47 无回归（见 [Bug 修复记录](../bug-fixes/2026-09-03-jvm-stdlib-calls-missing-on-api29.md)）。以上均不再是已知失败；`GoalReminderTest` 在双模拟器并发全量负载下的一次性通知时序 flake 已根因修复（worker 证据槽先于通知张贴的合法窗口改为有界重轮询 + worker 启动边界 120 s → 300 s，见 [Bug 修复记录](../bug-fixes/2026-09-03-goal-reminder-evidence-slot-precedes-post.md)）。多 ABI 与真机矩阵仍待执行。
- 多渠道能力保留分发目前只是 ADR-0013 的产品决定；核心任务矩阵、权限申报、listing、最终 applicationId/签名与真实商店审核均未完成。
- 当前 developer manifest 包含 Advanced 能力声明；即使默认关闭，Android 系统设置仍可能列出相关服务或权限。最终各渠道 manifest 由 HXA-122 按实测和审核证据收口。
- libsu/JitPack 仍是 HXA-094 的未来供应链决策点；当前仓库没有加入相关依赖。
- PRoot/CLI、结构化 Git UI 和持久 Git Workspace 尚未实现；ADR-0008 accepted 前不存在跨 Job `.git` 一致性、remote Git 或凭据能力。
- M8 设备矩阵（HXA-081 环境事实）：仅 API 29/36 arm64-v8a **4 KiB 页模拟器**两台，无真机、无 16 KiB 页设备——16 KiB 维度当前证据为构建期 assetGate 对全部 364 个 runtime ELF 的 `PT_LOAD` 对齐扫描（360/360 ≥16384，字节级），**不构成 16 KiB 真机运行验证**；真机 smoke 与最低设备集裁定归 HXA-086（无真机时停下报告等备机，不假绿）。生成的 rootfs/proot 二进制不入库（gitignore），仓库内为文本 lock + 许可证文本 + 可复现脚本。
- 低内存/后台/热限制实信号、可配置 TurnBudgets UI 和 Plan/Goal 工具入口归 HXA-099；child Agent、Agent graph 与 Workflow 未实现，ADR-0009 仍是 proposed。
- `TurnCoordinator` 是当前生产 Turn/ModelCall 协调器，旧 `TurnReducer` 仅保留兼容；`ChatService` 仍聚合 send gate、ToolCall 准备、Timeline/Approval 投影和 UI facade，后续按真实测试 seam 渐进提取。
- M0 壳层中文 Compose 文本尚未资源化；简体中文/英文资源和硬编码扫描归 HXA-067。
- 全量 developer 设备套件已全绿（2026-09-03 双设备复测：developer 73/73 ×2 零失败、consumer 60/60 ×2 零失败；skip 均为按设计的假设/环境门控）。此前 developer 全量的失败已全部根因修复：① 3 个测试类共 9 例审批/审计测试的顺序相关 flake（探针读 HXA-048 之后按打开会话 scoping 的时间线，跨测试类残留打开会话使 live 卡片行被过滤）——探针改读存储事实源 + 测试自建会话上下文，另补 `@After` 兜底：测试中途夭折遗留的待审批分派会永久占用独占调度槽（每个非读调用都是全屏障），每类结束前 cancel 本类会话遗留 pending 审批并等待结算（见 [Bug 修复记录](../bug-fixes/2026-09-03-approval-device-tests-session-scoped-probe.md)）；② 该修复暴露一个生产缺陷：`ChatService` 刷新遇到不存在的 open session id 会在 IO 协程抛异常**杀死 App 进程**，现降级到会话列表自愈，回归测试 `ChatSessionLifecycleDeviceTest` ×2（见 [Bug 修复记录](../bug-fixes/2026-09-03-stale-open-session-kills-app-process.md)）；③ `AllFilesDeviceTest` 3 例在 API 29 直调 API 30+ 的 `Environment.isExternalStorageManager()` 致 `NoSuchMethodError`——测试补 SDK 门控并断言 API<30 的结构性 UNAVAILABLE 契约（生产侧本已门控，无生产影响；见 [Bug 修复记录](../bug-fixes/2026-09-03-api29-allfiles-device-test-missing-guard.md)）。
- ADR-0012 的持久规则、Trusted Workspace 和精确批量批准尚未实现；生产规则集为空时回退逐次审批。Tasker/Auto.js、Shizuku/ADB 仅为未排期候选。
- 文件能力仍有三项边界：mutation 工具统一 L2、trash 长路径可能触发 `NAME_MAX`、超时 abandon 可能留下计入 scope 配额的临时文件；age-based reclaim 与 SAF scope 接线归后续文件管理工作。
- 文件管理 UI 当前边界：Workspace 恒可写；developer all-files 根本里程碑**只读**（区域守卫拒绝非 workspace 布局的变更，浏览/排序/预览/分享可用，重命名/复制/移动/trash 隐藏）；**M5B / HXA-057 已接入 persisted SAF tree scope（只读浏览后端，设备已验证）**：模型/Tool 只见 `scope:<saf-…>:<相对路径>` + 显示名（`content://` URI 永不进入模型/Room/audit/用户可见错误），每次使用实时 7 步复核 + fail-closed（撤销/provider 消失/重启/只读 grant/URI 变化均 fail closed），SAF 来源在文件管理器只读、变更入口 fail-closed（`SAF 来源为只读`）；设备验收 13 例 × 两台全过（in-APK 文档树 fixture 驱动真 `ContentResolver`），设备期发现并修复 1 处生产 walk 缺陷（根级文件恒 not-found）；**HXA-058 已实现并完成设备验收用户导入/导出入口**（导入 picker → `input/`、导出 `CreateDocument`/已授权树（WRITE 实时复核）+ 字节重读校验 verified 语义；设备 94+107 例 × 两台全绿，设备期修复生产缺陷 3 处[空游标 fail-open×2、工具栏/预览零宽溢出]与夹具/测试架构问题 5 项）。**仍未实现**：SAF **写**后端（浏览树内的通用变更外部文档，无桩/无 TODO——导出到授权树是 HXA-058 任务书要求的受限写路径，不扩大浏览只读边界）。
- 聊天附件（HXA-049 首批）现为**有界 UTF-8 文本**：UI/`ChatService` 可暂存并经系统 picker 导入单文件到当前会话 app-private Workspace（复用 HXA-044 `SafImportPipeline`，单文件导入不依赖 SAF tree grant），发送/重试前对 `message_attachments` 绑定的 SHA-256 快照 fail-closed 再校验，egress disclosure 同时绑定精确 Provider/origin 与被枚举的附件集合；只有经 probe 确认的 UTF-8 txt/md/csv/json 进入模型上下文，且以带来源、`UNTRUSTED` 标记与整文件哈希的有界（≤8 KiB 内联视图）context item 呈现，超限经 `read(offset,maxBytes)` 分块，绝不 base64 进上下文。**图片（HXA-055 已支持）**：magic 确认的 png/jpeg/webp/gif 经端上归一化（字节门/bounds 探针/采样/EXIF 手动旋转/重编码剥离/预算）发送，`message_attachments` 绑定**归一化 artifact** 并在发送/重试/恢复每次重验；视觉能力需连接测试第 5 阶段探测通过或用户手动声明，未确认时图片可本地保存/预览但发送阻塞（可操作错误，无裸 base64 回退、无静默丢图）；**系统分享（HXA-056 已支持）**：`ACTION_SEND`/`ACTION_SEND_MULTIPLE` 的文字/图片以「分享草稿」会话先本地导入/预览（provider-free 会话，发送前必须显式绑定 Provider；文字预填输入框、图片进暂存预览），**绝不自动发送**（ADR-0014 §5：预览后由用户显式发送）。**端到端硬化与发布边界（HXA-056 已回归）**：发送/流式回复/Tool Loop/历史恢复/脱敏、grant 失效、Artifact 缺失/篡改（重试 fail-closed）、重复发送、绑定后 Provider 不可再换（单会话单 egress）、能力快照变化立即撤销图片视觉、请求过大、图片解压炸弹（伪造 SOF fail-closed）、取消与进程回收（持久化幸存、暂存丢弃）；UTF-16、PDF/PPT/DOC、音频、视频及其他二进制只归入 ADR-0014 的封闭 category 并稳定返回 unsupported，不做文档/媒体解析、渲染、OCR 或 Provider upload；日志/Room/audit/用户可见错误不泄露原始 URI、绝对路径、正文或 base64（有设备回归断言）；三 adapter golden request + ≥1 真实 vision endpoint smoke（本地 SGLang 服务、环境明确记录，不替代协议 fixture）。**导入成功不等于模型已理解**：导入只保证文件在端上落盘并可发送，模型是否理解取决于其能力。以上均受 accepted [ADR-0014](../adr/0014-session-attachment-materialization.md)约束。
- Workspace 文件层两项低优先特性经 HXA-048 评估后**维持现状**（目录规模 / 大目录成为实际瓶颈前不改）：`WorkspaceQuota.usageBytes` 每次文件操作全量 walk scope 目录报告当前用量；`files.listDir` 先物化排序整个目录再分页、app 层 TIME/SIZE 排序只作用于按名截断前缀。
- 全项目审查判定为**有意设计、不改动**：`RecoveryCoordinator.canResumeTurn`/`wakeAllowed` 是有测试覆盖、为未实现恢复/继续 UI 预留的门禁 seam；`tools/android`、`tools/browser` 空子项目是 M6 占位模块（验收矩阵与 roadmap M6 已规划其测试）。
