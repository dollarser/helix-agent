# HXA-162：会话时间线与复制

Date: 2026-09-09
Status: completed

## 交付

消息投影保留 turnId；按持久化 Turn 顺序显示用户消息、折叠工具记录及回复，历史失败标记保留在所属轮次。待审批、活动执行和恢复入口保持可见。输入草稿与用户/助手消息提供全文复制，消息使用原生文本选择容器；输入框保留原生选词。

## 验证

`./gradlew :app:testConsumerDebugUnitTest :app:testDeveloperDebugUnitTest :app:assembleDeveloperDebug :app:assembleDeveloperDebugAndroidTest --max-workers=1`、`./gradlew spotlessCheck detekt :app:lintConsumerDebug :app:lintDeveloperDebug --max-workers=1` 通过。新增 JVM 检查消息缺失的旧 Turn 及旧工具不能跑到新答案后面。

专用 API36：`adb -s SERIAL shell am instrument -w -r -e class com.helix.app.ui.MessageCopyDeviceTest,com.helix.app.ui.ConversationComposerDeviceTest,com.helix.app.ui.ChatStopProgressDeviceTest com.helix.agent.developer.test/com.helix.app.HelixAndroidJUnitRunner`：7 项通过，无 skip/failure。验证全文剪贴板一致、长按入口、输入区和停止/失败/Goal；未把一次长按动作当成所有系统选词菜单的完整验收。首轮英文计数字符串触发 PluralsCandidate，改为 Operations: N 后复验。

决策记录：不适用，app 内展示和既有持久化字段接线，无公开契约或批准语义变化。日志保留于忽略的 build/phone-polish。后续继续草稿会话、标题与目录归属；尚未合入 main。
