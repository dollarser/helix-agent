# HXA-166：会话内模型选择

Date: 2026-09-09
Status: completed

## 交付

输入框上方增加模型下拉，先模型、后推理强度，模型菜单同时显示所属 Provider。目录复用通过连接测试的 Provider 后端模型列表并去重，保留默认模型；不自动探测或切换全局 Provider 配置。窄屏模型名称截断，菜单可滚动。

草稿只改内存；已保存会话通过新增 selectModel 专用接口选择后续轮次目标，保留历史与原始首次绑定保护。会话重新打开仍显示选中的模型。活动轮次、待出网确认和草稿发送准备期间不允许切换；存储 UPDATE 额外拒绝归档或存在非终态轮次的会话。轮次开始时复核 Provider/模型，避免目标准备期间被切换。

切换重置推理为默认；不是该 Provider 已探测默认模型时，不显示已验证能力徽标，也不发送沿用的推理强度。模型目录不等于逐模型能力探测，既有 Provider 的其他能力门禁未改造成逐模型能力库。调用快照中的 model 改为会话选中值；请求及工具回填使用会话 modelId。选择本身不发送历史，后续发送沿用出网/附件检查。

## 精确验证

```sh
./gradlew spotlessApply :core:storage:testDebugUnitTest :app:testConsumerDebugUnitTest :app:testDeveloperDebugUnitTest :app:assembleConsumerDebug :app:assembleDeveloperDebug :app:assembleDeveloperDebugAndroidTest detekt :app:lintConsumerDebug :app:lintDeveloperDebug --max-workers=1
./gradlew spotlessCheck --max-workers=1
adb -s SERIAL shell am instrument -w -r -e class com.helix.app.ui.SessionModelDeviceTest,com.helix.app.ui.ConversationComposerDeviceTest,com.helix.app.ui.SessionDraftDeviceTest,com.helix.app.ui.ChatStopProgressDeviceTest com.helix.agent.developer.test/com.helix.app.HelixAndroidJUnitRunner
bash scripts/check-docs.sh
bash scripts/check-i18n.sh
bash scripts/verify-adr.sh
bash scripts/check-secrets.sh
git diff --check
```

最终主机 exit 0：storage 88/88；Consumer 343 项、Developer 367 项，各 4 项既有外部条件 skip，无 failure/error。双 flavor APK、Developer 测试 APK、Detekt、双 flavor app lint 和格式门禁通过。初轮格式长行和复杂度失败后拆分 ComposerToolbar、分解条件与 SQL 字符串再通过，未删除测试。

专用 API36 9/9，无 skip/failure。新增切换测试验证：草稿选择不落库、首次发送实际 HTTP body model=b、跨 Provider 切换 c、历史行不变、推理重置、重新打开保留、第二次 HTTP body model=c、生成期间菜单锁定及底层 SQL 拒绝切换。输入区新增 240dp/1.8 倍字体模型先于推理且无越界断言；原草稿/停止/失败/Goal 回归保留。

真机 API34 覆盖安装成功；原会话模型菜单显示 SGLang / Qwen3.8-27B 当前勾选，右侧推理选择保持可见，打开/关闭菜单正常。本机当前可用菜单只有该模型，多模型和跨 Provider 请求证据来自模拟器本地 HTTP fixture，不冒充真机多模型服务验收。本轮未发起真机模型调用。

Developer APK SHA-256：`136c4d153a93ddd68e82d1949ca3b963c3b71edfdab3378ceb626d10f150d53b`。本地日志保存在忽略的 build/phone-polish，私有截图不入库。

## 决策与边界

决策记录：[ADR-0035](../adr/0035-session-model-selection.md)（accepted），授权来自所有者明确要求会话内 Provider 模型选择。无 schema 迁移或依赖升级，未移除首次绑定保护。

实现位于 codex/phone-chat-polish，未提交、未合入 main、未推送。未宣称全仓库/根 lint/API29/长稳或逐模型能力探测完成。目录刷新仍通过既有 Provider 连接测试入口。
