# HXA-141 完成记录

- 目标：把 HXA-140 的真实 Codex 订阅 smoke 纳入 CLI Runtime 自有的持久 jobId journal，并证明取消、重复提交、边界和进程恢复不重放。
- 前置任务/commit：HXA-140 / `0fe6e92`；accepted [ADR-0007](../adr/0007-companion-runtime-lifecycle.md) 与 [ADR-0021](../adr/0021-third-party-subscription-protocol-adapter.md)。
- 实现：`CodexModelJobRunner` 在执行前原子写 `PENDING`，再进入 `RUNNING` 和封闭终态；同 jobId + request hash 返回已有记录，不同 hash 拒绝；取消关闭 OkHttp 并持久化 `CANCELLED`，迟到成功不能覆盖；初始化把遗留非终态转为 `INTERRUPTED`，绝不执行回调。journal 限 128 条/1 MiB，满额拒绝。
- 隐私：record 只含 jobId、request hash、状态、时间、模型 ID 和成功输出 SHA-256；不保存 prompt、响应正文、token、account id 或异常消息。
- 真人证据：`emulator-5600`，API 36，arm64-v8a，HXA-139 真实 Codex vault；可见 UI 触发后 `gpt-6-astra` 返回 `HELIX_OK`。私有 `record.json` 为 `SUCCEEDED`，output hash 为 `3be35f6b24c4daf535613e2452352a314375bed2e408854f7a5cd0c5cafdc16f`，未出现 `HELIX_OK`、access/refresh token 字段或 account id。
- 边界：这仍是 Runtime 内可见 smoke job，不是跨 APK Provider/Job；没有新增 Binder transaction、PFD、任意用户 prompt、Tool 或 Chat/Act/Goal 注册。
- 决策记录：无需新 ADR；本任务只在 accepted ADR-0007 的“未知状态不重放”和 accepted ADR-0021 的 Runtime-owned credential 边界内实现 companion 私有 journal。跨 APK wire 仍需后续独立 HXA 决定并实现。
- JVM/构建：`./gradlew :runtime:cli-app:testDebugUnitTest :runtime:cli-app:assembleDebug :runtime:cli-app:lintDebug --no-configuration-cache` 成功；53/53 tests，lint、assemble 成功。
- 设备：`ANDROID_SERIAL=emulator-5604 ./gradlew :runtime:cli-app:connectedDebugAndroidTest --no-configuration-cache` 在 `Helix_M11_Test_API_36`（API 36、arm64-v8a）20/20 成功。相同命令在 `emulator-5602` / `Helix_M11_Test_API_29`（API 29、arm64-v8a）运行 20 例，19 例成功；唯一失败为既有 `grokDeviceEndpointIssuesBoundedAnonymousAttempt` 无法连接 `auth.x.ai:443`，IPv6 为 `EHOSTUNREACH`，IPv4 在 20 秒后超时。该外部网络缺口不属于本任务的纯 JVM Job 内核，但 API 29 全量套件未通过，不能计作完整设备门禁。
- 仓库门禁：`./scripts/check-cli-runtime-boundary.sh`、`./scripts/check-lockfiles.sh`、`./scripts/check-secrets.sh`、`./scripts/check-docs.sh`、`./scripts/verify-adr.sh` 与 `git diff --check` 均成功。
- artifact：`runtime/cli-app/build/outputs/apk/debug/cli-app-debug.apk`，7,811,812 bytes，SHA-256 `5c61a03de73a27c9c35fb87cf10fe7e5adf284cdb95a07271be203bf2d028ee5`。
- 已知限制：Runtime journal 尚不能由主 App 查询或对账，统一 `ModelEvent`、Dispatcher egress gate、Verification/Audit 和 Provider 注册均未实现；因此 `agentBackendState` 仍必须是 `NOT_REGISTERED`。API 29 对 `auth.x.ai` 的外部连通性仍缺失。
