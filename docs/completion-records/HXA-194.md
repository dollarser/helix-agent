# HXA-194：命令详情与现有结果导航

> 本页记录当时交付证据；决策链接指向当前主题摘要，不表示当时的测试覆盖了当前契约的全部范围。

日期：2026-09-17。状态：**completed（本任务范围）**。基线为 `182a3141`（HXA-202 交付记录 `e716691e` 与 P0 真机收尾之后），本轮 5 个代码提交 `3ab5d974` → `73e574f6` → `70509225` → `b8ce4219` → `ea258150` 加本记录提交；在 main 工作树与四象限自建独占模拟器验收。工作树中的 Root 等并行会话改动不属于本任务交付，本任务只 stage 自有路径。

## 契约与交付

决策记录：命令详情是独立详情页，只读投影持久事实——Turn、工具调用行、已结算结果行、prepared-job 绑定审计行、app-scope 完整性校验过的结果档案（developer 风味）。打开、旋转、返回与重进只重读同一批事实，不启动、不重放、不提交、不确认；用户明确对账仍走现有会话入口（查看结果），详情页不复制。展示状态为八值派生枚举（运行中/成功/失败/取消/拒绝/未知/证据过期/读取失败），运行中只给状态行"输出在命令结束后展示"。档案流优先于过期持久化 stdout；consumer 风味的浏览接缝只返回绑定+持久化内容（不可达执行入口），developer 额外本地读已验证档案（不冷启动 Runtime）。任务页命令列表与聊天工具行接入真实详情页；路由是独立 NavHost entry，系统返回回到来源页（任务页或聊天工具行）。195 之前不声称实时日志流。未改 Runtime IPC、授权、数据库 schema 或执行上限。

- 切片 ①（`3ab5d974`）：`CommandResultDetail`/`CommandResultProjection`/`CommandResultBrowser` 只读投影模型与 flavor 接缝——`ProotToolModule.browseCommandResult` developer 走 `ProotResultStore` 本地已验证档案读取（异常收敛为 READ_FAILED），consumer 返回空档案；376 行新增，含双 flavor 投影单测。
- 切片 ②（`73e574f6`）：`CommandResultDetailScreen` 独立详情页与路由、`ChatService.commandResult` 只读入口（IO 调度）、任务页命令列表行与聊天工具行入口、返回来源与会话入口按钮。
- 切片 ③（`70509225`）：设备验收——`CommandExecutionDetailsDeviceTest`（androidTest 共享，4 项：旧历史、成功/失败/取消/unknown、返回来源、旋转/返回不重执行）与 `CommandResultBrowseDeviceTest`（developer，3 项：接缝事实、详情页档案优先、重建后同一事实）；同片收口投影与页面实现（275/288 行变更），`TaskJourneyDeviceTest` 增 4 行任务页入口断言。
- 切片 ④（`b8ce4219` + `ea258150` + 本记录）：lint 命名修复（ComposableNaming）；长流布局验收——fixture 改为 48 行 stdout/24 行 stderr，整段全文断言、文件行与绑定行 `performScrollTo` 后显示断言、首开路径异步读取 `waitUntil`；四象限矩阵全绿与文档收口。

## 实际验收

| 门禁 | 结果 |
| --- | --- |
| G1 源检查 | `check-all.sh --source` exit 0：test-review-gates 6/6、docs 439 文件/193 任务、ADR 30、i18n 1272 键三语 parity、secret 扫描 |
| G1 格式 | `spotlessKotlinCheck --rerun-tasks` 与 `git diff --check` exit 0 |
| G1 detekt | 仅剩 2 项（LongMethod/MaxLineLength），均在并行会话未跟踪文件 `ChatScreenProjectionDeviceTest.kt`；本任务文件零发现（如实记录，未改动该文件） |
| G2 | `./gradlew build` exit 0；另显式 `:app:assembleDeveloperDebugAndroidTest` + `:app:assembleConsumerDebugAndroidTest` 双 exit 0（G2 不编译 androidTest 源集，编译证明单独补齐） |
| 四象限设备（194 类） | API29/36 × consumer/developer：consumer `OK (4 tests)` ×2，developer `OK (7 tests)` ×2（含浏览接缝/详情页/重建 3 项） |
| 四象限设备（202 入口回归） | `TaskJourneyDeviceTest` 独立安装 `OK (6 tests)` ×4 象限，矩阵末行 `== matrix finished overall=0`（其全局 totalTurns 基线不与其他 fixture 类共享安装） |
| 单测 | `CommandResultProjectionTest` 20/20 双 flavor：状态派生、档案优先、READ_FAILED 收敛、consumer 接缝边界 |

设备验收使用自建独占模拟器 ×4（1080x2400@420，端口 5554/5556/5558/5560，`-read-only` 干净基线，runner 关闭前卸载 `com.helix.agent[.developer][.test]` 双包）。最终证据 `build/hxa194-device/`（8 个象限×run 目录，含 instrumentation/logcat/manifest）；矩阵驱动 `scripts/debug/2026-09-17/hxa194/run-hxa194-device.sh`（不提交）。前两轮失败证据按轮归档：`build/hxa194-device-stale-apk-1659/`（第 1 轮跑在过期 test APK 上）、`build/hxa194-device-run2-fail-0937/`（第 2 轮暴露 ui-test 语义变化与首开竞态）、`build/hxa194-device-run3-fail-0946/`（第 3 轮暴露长流下方显示断言需滚动）。

验收条款覆盖：旧历史结果、成功/失败/取消/unknown 状态、档案流优先于过期持久化 stdout、返回来源与跨会话身份、旋转/返回不重执行、consumer 无可达执行入口、长输出可滚动布局（48 行 stdout/24 行 stderr，文件行与绑定行滚动到可见后断言）——均在设备验证。证据过期/拒绝/读取失败三个展示状态由投影单测覆盖（设备无纯 fixture 路径，见下）。三语言资源经 check-all i18n parity 门禁。

## 边界与遗留

- 四象限旅程通过 ≠ 全产品套件通过：本轮只跑 194 类与 `TaskJourneyDeviceTest`；全量套件验收属于 206。206 用户路径证据（会话 → 任务 → 命令详情 → 来源）已在本批积累，206 不提前关闭。
- `command-detail-missing`（未找到）无纯 UI fixture 路径：构造不存在的 callId 需要直改数据库，超出本任务设备测试范围；该状态投影由单测覆盖。
- EVIDENCE_EXPIRED/DENIED/READ_FAILED 展示状态由单测覆盖、未做设备 fixture：过期与拒绝依赖 Runtime 审计链的后续事实，只读浏览路径在单次设备安装内无法合法构造。
- 实时日志属 195：运行中命令详情页只显示状态行，不声称实时流。
- 记录时点工作树带并行会话未提交改动（ChatScreenProjection、docs/adr 等）；上表 detekt 2 项均在并行文件，本任务未改动。
- 环境注记（本轮修复与发现，供后续任务复用）：
  - Compose BOM 2026.06.01 → ui-test 1.11.4：`assertTextContains` 默认整值相等（`substring = false` 走 equals 语义），子串匹配需显式 `substring = true`；失败 dump 中 `Text = '[...]'` 的单对方括号是 list 属性的 dump 格式，不是字面文本。第 2 轮长流断言按单行子串写因而失败，已改为整段全文断言（更强：证明全量渲染、两端都在）。
  - 详情页首开路径的异步事实读取必须先 `waitUntil` 状态标签再断言（重建路径本就有该模式）；长流把文件行/绑定行推出可视区后，显示断言前必须 `performScrollTo`。
  - `./gradlew build`（G2）不编译 androidTest/androidTestDeveloper 源集：改设备测试后必须显式 assembleAndroidTest、核对 APK 新于源码再启矩阵，否则矩阵消费旧字节码。
