# HXA-181: 当前规范与历史记录分离

日期：2026-09-09。分支：`codex/phone-chat-polish`；不合并 main。

## 修改

- `status.md` 的当前摘要与 In progress/Next task 不再被旧的“已推送”“新需求暂停”“ADR-0028 完成绑定”或“无活动实现”覆盖；说明本工作树交付与 main/提交/外部门禁的区别。
- 历史里程碑、完成记录、旧 ADR 理由和优化账本保留历史语义；优化账本增加当前入口和取代关系，避免旧待办再次被当成当前指令。
- AGENTS 的 Git ADR-0008 状态修正为 accepted；模式文档的 ADR-0009 接受门槛修正为已接受后的生产启用门禁，不把架构接受写成子 Agent 实现。
- 架构文档更新 ChatService 职责、手动文件变更与 Agent 隔离边界，旧 LOC 表明确为历史评估；README 保留稳定产品边界，不复制旧实现快照。
- ADR-0036 与扩展 ADR-0041 交叉链接；路线、验收矩阵及本批完成记录一致。调试脚本说明区分一次性变更脚本与可重用独占 runner。

决策记录：不适用：修正当前事实与文档入口；既有决定的变更由 HXA-180 / ADR-0041 单独记录，没有重写历史验收结果。

## 验证

`sh scripts/check-docs.sh`、`sh scripts/verify-adr.sh`、`sh scripts/check-i18n.sh`、`sh scripts/check-secrets.sh`、`git diff --check` 全部通过。当前规范中旧 verifier、过时 ADR 状态与 ChatService 热点描述已按源码核对；历史记录里的旧语义保留为当时证据。

## 交付状态

本批 HXA-179～181 已完成。累计工作树未提交/推送/合并 main；外部账号、Root 真机、长稳与商店发布门禁不由文档清理关闭。
