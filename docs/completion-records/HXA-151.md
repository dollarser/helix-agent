# HXA-151：Skill/MCP 原生操作状态收敛

Date: 2026-09-08
Status: completed

## 范围与结论

审查 HXA-148～150 后保留独立 Tool schema、hash 绑定安装、WorkspaceImportSource 和既有导入服务；这些分别承担模型操作契约与平台安装能力，未发现需要重写 Skill/MCP 基础设计的证据。原生创建、Skill 安装和 Connector 导入/移除/Skill 切换重复的操作状态收敛到 app 内部 `ImportActionState`，不引入通用插件框架。

同一页面操作启动时同步设置忙碌状态，拦截重复请求；组合生命周期取消继续传播，失败只报告失败，重试清除错误。安装页重新预览或选择新 ZIP 前清除旧结果，避免校验失败后仍显示旧安装入口；创建页操作开始清除旧结果。Connector 的端点连接生命周期仍保持独立，未借重构改变连接或凭据语义。

决策记录：不适用：这是 accepted [ADR-0029](../adr/0029-skill-and-mcp-authoring-installation.md) 内的局部 UI 修复和复用；不改变跨模块契约、授权、持久化、Runtime 或依赖。

## 验证

- `./gradlew spotlessApply :app:testConsumerDebugUnitTest :app:testDeveloperDebugUnitTest :app:assembleConsumerDebug :app:assembleDeveloperDebug :app:assembleConsumerDebugAndroidTest --max-workers=1`：通过。consumer 328 项（4 项既有外部条件跳过）、developer 352 项（4 项既有外部条件跳过），失败/错误均为 0；外部条件跳过不计作通过。新增状态测试每个 flavor 3 项，重复请求、失败重试、取消均通过。
- `./gradlew spotlessCheck detekt lintDebug --max-workers=1`：通过。
- API29/36 专用模拟器：`adb -s <serial> shell am instrument -w -r -e class com.helix.app.ui.SkillAuthoringUiTest,com.helix.app.ui.SkillInstallationUiTest,com.helix.app.ui.ConnectorInstallationUiTest com.helix.agent.test/com.helix.app.HelixAndroidJUnitRunner`，每台 `OK (6 tests)`，覆盖中英文 320dp/2倍字体。安装页新增“成功预览→源文件损坏→预览失败清掉旧入口→恢复文件→重试→安装启用”的真实回归路径。
- `bash scripts/check-docs.sh`、`bash scripts/verify-adr.sh`、`git diff --check`：通过。
- 初次格式任务名称错误、行长门禁和 Compose 测试多余 import 已纠正后重跑通过；未跳过或删除测试。
- 原始输出与 APK 指纹保留于忽略目录 `build/hxa-151/`。已有真实模型/来源包验收不作为本轮重跑结果；真机、受保护账号与 24h 长稳仍后置。

## 后续

继续浏览器原生引用调查；不将本 UI 收敛当作 JNI/Binder 修复。产品/竞品研究的并行文档变更保持独立。
