# HXA-124 完成记录

- 目标：在 CLI Runtime 独立 UID 中实现 Claude 可见浏览器 OAuth 生命周期，并把 Claude Free/未知套餐严格判定为不具备 Claude Code 订阅资格且不落盘。
- 前置任务/commit：HXA-119 / `c58496f`；accepted [ADR-0021](../adr/0021-third-party-subscription-protocol-adapter.md)。
- 修改文件：`ClaudeOAuth.kt`、`ClaudeLoginActivity.kt`、复用并泛化的 `CodexLoopbackServer.kt`、Runtime manifest/三套 strings、JVM/设备测试、CLI 边界脚本及 M11A roadmap/status/matrix。
- 实现的契约：固定参考 adapter 的 Claude Code OAuth identity 与 authorize/token/profile origin；随机 localhost 端口、PKCE S256/state、三分钟超时、取消、严格有界 JSON、refresh token 轮换与永久失效删除。profile 只提取 `subscriptionType`；`pro/max/team/enterprise` 才保存 Runtime vault，`free`/缺失/未知均删除并 fail closed。
- 未实现/明确排除：未调用 Anthropic Messages/model endpoint，未注册 Agent backend/Provider/Tool/Job，未读取或写入 browser/Claude Code credential，未保存 email/profile PII，未改变 Binder redacted status。没有声称该 OAuth identity/profile endpoint 获 Anthropic 授权，也未进入商店 artifact。
- 自动测试：JVM 共 32/32（Claude 6/6）；API 29 与 API 36 arm64-v8a 各 15/15。覆盖 PKCE/state/path/error/超限、token schema、套餐 allowlist、Free 不落盘、refresh 轮换、瞬时/永久失败、随机 loopback callback、manifest 与仅 INTERNET 权限。
- 手工验证设备：`Helix_M11_Test_API_29`（API 29，arm64-v8a，`emulator-5602`）与 `Helix_M11_Test_API_36`（API 36，arm64-v8a，`emulator-5604`）自动验收后均关闭。真实 Claude Free 账号在 `emulator-5600` authorize 页显示 “Upgrade to Max or Pro”，URL 仍为 `claude.ai/oauth/authorize`，没有 callback/authorization code/token exchange；返回 Runtime 取消后 UI 显示未保存，`subscription-claude.enc` 不存在，已有 Copilot vault 未受影响。
- 验收命令、exit code 和结果：
  - `./gradlew :runtime:cli-app:testDebugUnitTest :runtime:cli-app:assembleDebug --no-configuration-cache`：exit 0，BUILD SUCCESSFUL，JVM 32/32。
  - `ANDROID_SERIAL=emulator-5602 ./gradlew :runtime:cli-app:connectedDebugAndroidTest --no-configuration-cache`：exit 0，API 29 arm64-v8a，15/15。
  - `ANDROID_SERIAL=emulator-5604 ./gradlew :runtime:cli-app:connectedDebugAndroidTest --no-configuration-cache`：exit 0，API 36 arm64-v8a，15/15。
  - `./gradlew :runtime:cli-app:lintDebug --no-configuration-cache`、`./scripts/check-cli-runtime-boundary.sh`、`./scripts/check-lockfiles.sh`、`./scripts/check-secrets.sh`、`./scripts/check-docs.sh`、`./scripts/verify-adr.sh`、`git diff --check`：最终复验见本提交。
- artifact/hash：`runtime/cli-app/build/outputs/apk/debug/cli-app-debug.apk`，7,811,812 bytes，SHA-256 `511e262a8894cde5fe6fe96dbd21920d08d98522cd0d85ba4b2ddb7717811caf`；`aapt2 dump permissions` 仅见 `android.permission.INTERNET` 和 signature permission 声明。
- 权限/数据/许可证变化：未增加权限、依赖或第三方代码；不打包参考插件/Claude CLI。新增的数据只有资格通过后 Runtime UID 私有 Keystore vault 中的 Claude token。
- 决策记录：无需新 ADR；实现保持在 accepted ADR-0021 已授权的非官方个人侧载、Runtime-owned token 边界内，没有把 proposed 决定升级为 accepted。
- 官方事实（访问日期 2026-09-05）：Anthropic [登录与订阅认证说明](https://support.claude.com/en/articles/13189465-log-in-to-your-claude-account) 明确第三方软件优先使用 Console API key；[Claude Code 与 Pro/Max](https://support.claude.com/en/articles/11145838-use-claude-code-with-your-pro-or-max-plan) 和 [Claude Code setup](https://docs.anthropic.com/en/docs/claude-code/getting-started) 将订阅登录能力关联到付费 Claude App 方案；[Claude plans](https://support.claude.com/en/articles/11049762-choose-a-claude-plan) 确认 Free 存在但不构成本任务的 Claude Code 资格。真实 Free 页面的升级门禁与这些官方说明一致。
- 风险或后续任务：没有付费 Claude 账号，因此未取得 token exchange/profile allowlist 的真实正向证据；自动测试证明实现路径，不能把它写成真实订阅可用。OAuth identity、scope/profile schema 和服务条款可漂移；任何模型适配器仍需独立 HXA、授权证据及 Dispatcher/Policy/Approval/Verification/Audit/jobId 对账。
