# HXA-185 完成记录：合并后长稳夹具适配与失败取证

日期：2026-09-10。范围：所有者授权的 HXA-185 七项**夹具/runner/取证**修改（EV-04 新 Goal 机制、Goal 证据版本、夹具 PSS 单位、Autofill 独立探针、Autofill 失败取证、WebView 身份解析、FD 五臂阶段对照）；**仅测试夹具/runner/证据，不新增产品功能**。主机门禁由 Claude 执行；设备 Goal/Autofill/FD五臂/24h 由 Claude 独占模拟器执行（每轮自建 AVD、记 pid/serial/AVD/bootId、拒绝借用、finally 关闭）。全部 attempt 保留，以磁盘为准（exit code / 后台 "completed" 不作为唯一成功条件）。

决策记录：本轮仅测试夹具/runner/取证，未改产品功能/权限/数据库/依赖，无新架构决定（依据已接受 ADR-0040/0004/0033）。关键取舍（方案第 144 行）：24h ON API36 首现 FAIL_FUNCTIONAL（cycle 55，API36 系统 WebView a11y 节点暴露重尾，非产品）→ 取证 + 结束该轮 + 报告归因，**不加宽 a11y 预算（10s 保持）、不改 24h 资源门禁、不改产品**；新 `AutofillInputProbe`（10s 不放宽）+ `device-autofill-failure.json` 按设计更早暴露并确证系统 WebView 现场。FD 五臂为归因透镜（`analyze-fd-phases.py` 契约禁止泄漏判定，goldfish 不确立因果）。

## 交付与验证映射

| 项目 | 修改 | 验证结果 |
| --- | --- | --- |
| EV-04 Goal | 移除旧 `goalCriteria`/`bindGoalCriterion`；奇数轮经 `write`/`read`/`goal.report` complete；偶数轮等真实模型请求后 `stopTask(pause=true)`，核对 CANCELLED Turn、pauseRequestedAt、PAUSED Goal | 双 flavor **16/16 PASS**（含 modelBlockerRequiresExplicitRepair / manualPauseOverridesCompletedReport / cancellationOverridesCompletedReport 三语义）；goalMechanism 核对通过 |
| Goal 证据版本 | soak-done 增加 `goalMechanism=model-report-user-pause-v1`；host 拒绝旧机制 | 同上；缺字段/旧值/奇偶计数错误会失败（runner 自测覆盖） |
| 夹具 PSS | `Debug.getPss` 返回 KB；删除综合夹具额外 `/1024` | 24h 轮 `samples.jsonl` pssKb 与 host PSS 同数量级（67→141MB 平台），不回写旧记录 |
| Autofill | 独立 `AutofillInputProbe` 只取当前 View 所在窗口新节点；显式释放；仅 no-mView 窗口过渡重取，不吞任意异常；就绪等待 ≤10s 且受周期剩余限制 | 短项 `OK (1)`+`OK (6)`；pilot on/off 按设计 INCONCLUSIVE；24h ON 在 cycle 55 以 10s 预算暴露 a11y 重尾（**未加宽到 100s**） |
| Autofill 失败取证 | 失败写 `device-autofill-failure.json`（run/cycle/stage、View identity/attach/focus/windowId、窗口焦点、真实 WebView 包/版本）；原异常仍抛出，采集失败只附加异常；不输出正文/密码/URL | 24h ON 失败轮按设计产出该文件（cycle 55 form-on、`accessibilityFocused=FALSE`、WebView `133.0.6943.137`）；原异常仍抛出（`run finished: 1 tests, 1 failed`） |
| WebView 身份 | 只解析 dumpsys `Current WebView package (name, version)`；保留 `webviewupdate-start.log`；缺失为 unavailable/null，禁止伪造 | 24h manifest `webViewProvider com.google.android.webview 133.0.6943.137`（identity available） |
| FD 归因 | 新 opt-in `DescriptorPhaseProbeDeviceTest` 五臂；记录全描述符（含 goldfish）；不放宽 +8、不注入 GC、不声称定位创建栈 | 五臂全 PASS（各 `OK (1)`）；分析器归因锁定增长在**资源门/设备路径**（activity/combined 升、迁移 0/前台 −2），主导 kind=goldfish（QEMU，真机无）；**归因透镜，非泄漏判定** |

## 主机证据

```sh
python3 scripts/debug/2026-09-10/test_soak_evidence.py
python3 scripts/test-run-browser-autofill-soak-faults.py
python3 scripts/test-run-ev04-mainapp-soak-logic.py
python3 scripts/test-run-ev04-mainapp-soak-fixture-logic.py
./gradlew spotlessApply detekt test lintDebug :app:lintConsumerDebug :app:lintDeveloperDebug --continue --max-workers=1
./gradlew :app:assembleConsumerDebug :app:assembleDeveloperDebug :app:assembleConsumerDebugAndroidTest :app:assembleDeveloperDebugAndroidTest :feature:browser:assembleDebugAndroidTest --max-workers=1
sh scripts/debug/2026-09-09/run-documentation-gates.sh
```

`HXA185-hostvalidation-freeze`：**HOST_GREEN + FROZEN**。4 项 runner 自测 PASS、gradle 门禁（spotless/detekt/test/lint ×3 flavor）BUILD SUCCESSFUL、5-APK assemble BUILD SUCCESSFUL、文档门禁 PASS。冻结制品（逐臂哈希核验 == 冻结，gitCommit `20bd6ae9…`）：

| 制品 | SHA-256 |
| --- | --- |
| app consumer debug | `8d111db1cc203b56f83b0621470653f2f0e4ad10b0fb13c1f9d1f85302937d45` |
| app consumer androidTest | `0e5147938b073f8983d7da34028b1be3060c953de84d2655bb884c714a42d411` |
| app developer debug | `8eb71f0495acb72dbb6e9c64b255bb89ba2f8e14f0444e5e1903068448cf65b8` |
| app developer androidTest | `f230a9583dc8b2c0a94d1837000f6a53ae396315c29237903e2b1af23f097d03` |
| browser androidTest（== HXA-185 冻结，取代 R4 旧加宽版 `f4df02c6…`） | `b9382e9768568bf71f6e4536b3485388e49e08e108d460a68b036f2c29583778` |

`run-browser-autofill-soak.py` 依赖日期目录 `soak_evidence.py`，冻结脚本时一并保存（不重启在途 Python 以换版本）。

## 设备证据

每轮自建 AVD `Helix_EV_Repair_API36`（API36、1080×2400@420、readonly、`-no-snapshot`/`-no-boot-anim`、2048MB/2 cores），拒绝已占用 serial、`emu avd name` 核验身份、finally 只杀自己进程组；**未借用 emulator-5554/5556，未操作 emulator-5598/5576/5582，未操作真机**；长稳期间不安装/清数据/注入 GC/强杀。归属记录在每轮目录 `owner.json`/`closed.json`（exit 0）。

### Goal 短项（双 flavor）

| runId | 结果 | 设备 |
| --- | --- | --- |
| HXA185-goal-short-dev-1 | PASS `OK (16 tests)` | emulator-5562 |
| HXA185-goal-short-consumer-1 | PASS `OK (16 tests)` | emulator-5564 |

核对当前 run `goalMechanism=model-report-user-pause-v1`、实际 Tool 结果、暂停持久字段；blocked 无法直接 Continue、修复确认后才可继续。

### Autofill 短项 + pilot（API36，on/off）

| runId | 结果 | 设备 |
| --- | --- | --- |
| HXA185-autofill-short-a36-autofill-1 | PASS `OK (1 test)`（7.118s） | emulator-5566 |
| HXA185-autofill-short-a36-owner-1 | PASS `OK (6 tests)`（12.154s） | emulator-5568 |
| HXA185-pilot-selftest-on-1 | INCONCLUSIVE（**按设计**：selftest 深度不施加 24h 资源门禁） | emulator-5570 |
| HXA185-pilot-off-1 | INCONCLUSIVE（**按设计**：pilot 深度不施加 24h 门禁） | emulator-5572 |
| HXA185-pilot-on-1 | INCONCLUSIVE（**按设计**） | emulator-5574 |

新 probe 通过短项设备验证后才开 24h（交接顺序满足）。

### FD 五臂对照（归因透镜，非泄漏判定）

| 臂 | runId | 结果 | final fd | 稳态/趋势 |
| --- | --- | --- | ---: | --- |
| idle | HXA185-fd-idle-1 | PASS `OK (1)` 330.5s | 108 | 平（10 slot 零增长） |
| storage | HXA185-fd-storage-1 | PASS `OK (1)` | 108 | 平（迁移 0 净） |
| notification | HXA185-fd-notification-1 | PASS `OK (1)` | 108 | 平（前台 −2） |
| activity | HXA185-fd-activity-1 | PASS `OK (1)` | 133 | 升（127→133，未平台化） |
| combined | HXA185-fd-combined-1 | PASS `OK (1)` | 136 | 升（129→138，未平台化） |

全部在冻结 30s slot 内完成（无需复审/延长配置）；顺序隔离，port 5578 复用（每臂关闭后再开）。combined 臂逐测试分解把增长**精确锁定在资源门/设备路径**（迁移 0 / 前台 −2 / 资源门 +4）；主导新增 kind=goldfish（`/dev/goldfish_*` QEMU，真机无），与 DEFERRED 的 EV-03(U3) goldfish 闸门发现一致。汇总：fd-five-arm-summary（本机忽略产物：`../../build/emulator-verification/hxa185-device/fd-five-arm-summary.md`）。

### 24h（p3）ON API36

| runId | 结果 | 设备 |
| --- | --- | --- |
| HXA185-24h-on-1 | **FAIL_FUNCTIONAL**（7097s ~1.97h，54 cycles 全 ok 后中止） | emulator-5580 |

- 失败点：ON 路径 `runFillAndSave:525 → focusInput:609 → AutofillInputProbe.focus:48`，**10s 就绪截止**（`IllegalStateException: Autofill input readiness deadline; staleWindows=0`），cycle 55（第 40 正式轮）。
- 归因：**API36 系统 WebView a11y 节点暴露重尾**（非产品），与 R4（旧 100s 有界版死在 `focusInput:597`）同一判别；新 10s probe 按设计**更早**（10s）暴露。
- 取证确证：`device-autofill-failure.json`（cycle 55 form-on、lifecycle RESUMED、view 健康=attach/windowFocus/viewFocus/hasWindowToken/VISIBLE/1080×2400，活动窗口 id10 `focused=true` 但 `accessibilityFocused=FALSE`，WebView `com.google.android.webview 133.0.6943.137`）。
- 新 probe 价值兑现：10s 不放宽、`device-autofill-failure.json` 确证系统 WebView 现场、原异常仍抛出（`run finished: 1 tests, 1 failed`），**未吞、未加宽到 100s 追绿**。
- **24h 资源门禁未评估**：门禁只在 phase done 对 24 个 hour-block 求每小时中位数/斜率（FD+8/thread+16/PSS+96MiB，需跑满 24h）；本轮 2h 功能中止。2h 窗口资源**有界**（fd 119→256→249、threads 23→64→64、pssKb 67→141→138MB、binderLocal 全程 1），**不构成泄漏判定**。
- 按方案第 144 行：首次正式失败 → §7 取证 + 结束该轮 + 报告最小复现与归因，**不自动改阈值、不放宽到 100s、不改产品**（已遵守）。

取证：24h ON forensics（本机忽略产物：`../../build/emulator-verification/ev02-autofill-soak/p3-api36-on-24h-hxa185-1-forensics.md`）。

## 诚实边界与结论

- HXA-185 模拟器验证顺序（Goal / Autofill / FD五臂 / 24h）**全部执行完毕**（14 条 run-index 记录）。
- 24h ON API36 = **系统 WebView 特征的功能失败**（非泄漏判定、非夹具/产品缺陷）；24h 资源/泄漏门禁**未触及**。
- FD 五臂是**归因透镜**（`analyze-fd-phases.py` 契约禁止泄漏判定）；goldfish 不确立因果。
- 未读真实密钥/付费账号，未改生产功能，未放宽任何预算/门禁，未为凑时长重试。
- **尚余（更宽 EV-02 范围，非 HXA-185 交接单项）**：浏览器 EV-02 正式 24h 的 **API29** 一臂（方案第 11 行：API29+36 各一次）——API36 ON 已因系统 a11y 重尾功能中止，API29 是否复现需另起独占 API29 轮次；此为 EV-02 主计划范围，需按授权推进。

见 run-index（本机忽略产物：`../../build/emulator-verification/run-index.json`）（`HXA185-*` 14 条）与 [交接](../development/hxa185-claude-test-handoff.md)。
