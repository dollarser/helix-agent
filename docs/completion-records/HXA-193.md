# HXA-193：包内 Runtime 资产准备与覆盖升级验收

日期：2026-09-18。依据 accepted [ADR-RUNTIME-001](../adr/runtime/001-execution-domains.md)。本任务完成范围为当前锁定 Runtime 的可重复准备、包边界、结果恢复、选定旧 APK 覆盖升级；不代表真实订阅账号或签名发行通过。

决策记录：沿用 ADR-RUNTIME-001；默认构建入口使用现有锁定归档，未修改执行域、授权、凭据或二进制版本，无新增架构决定。

## 交付

- developer 单 APK 私有进程、按需初始化及 consumer 排除沿用既有生产接线，不重做共享 UID 架构。
- 默认资产入口改为下载已发布固定归档，校验锁定 hash/大小和 ELF；本地归档/HTTPS URL 覆盖继续受同一校验。CI 复用同一入口，不再依赖配置仓库变量才能避开失效镜像。
- `--rebuild-rootfs` / `--generate-lock` 显式保留维护重建；未知参数、来源冲突、损坏归档拒绝。二进制 lock、包版本与许可证内容未变。
- 真实旧版 `03617128 → 9055438f` APK 覆盖安装已在 API29/36 通过：旧版写入会话/消息/配置与实际 fixture Job 结果，`install -r` 后保留，旧验证锚不错误激活，未知完成状态不重放。详见[升级证据](../evidence/development/apk-replacement-upgrade-2026-09-18.md)。

## 实际验证

| 命令 / 范围 | 结果 |
| --- | --- |
| 无归档环境变量 `./scripts/build-proot-assets.sh` | exit 0；公开固定归档重新下载，137287680 bytes，SHA-256 `674aa3ac68200bfe67b01964573fce2c6780c726fe23ca640e998337cf47f509`，360 ELF 门禁通过 |
| `bash scripts/debug/2026-09-18/hxa193/check-asset-inputs.sh` | exit 0；三类非法输入均拒绝 |
| `bash scripts/check-all.sh --all` | exit 0；源码、格式/静态检查、主机测试、全 lint、Debug/Release、锁与制品边界；允许 Gradle 正常缓存复用 |
| `./gradlew :app:assembleDeveloperDebugAndroidTest` | BUILD SUCCESSFUL |
| `python3 scripts/verify-integrated-runtime-apks.py` 及 `--build-type release` | consumer/developer × debug/release 全部边界通过 |
| `python3 scripts/verify-integrated-runtimes.py --avd HelixApkUpgrade_API29_20260918 --port 5670 --output build/193-closeout/api29` | 35项通过；自有模拟器正常关闭 |
| 同上 API36 / port5672 / `--memory-mb 4096 --cores 4` / `build/193-closeout/api36-functional` | 35项通过（60.182s）；自有模拟器正常关闭 |
| 旧/新 APK 覆盖安装专项 | 两 API 各 seed/verify 通过，见独立升级记录；本轮没有重复宣称再次跑该路径 |

本轮 Runtime 回归 APK SHA-256：`385fb5fab48a3fa4dbe70f7ba2ba263f6bdc17e35e52ec28b08e4da5cba4a972`；测试 APK：`af58a8ea76c0ad972d2afcced1faadbc8a6bfa393a7ef51e3247e2161a260b04`。原始日志在 `build/193-closeout/`，输入诊断及重建失败见[默认入口记录](../evidence/development/runtime-default-archive-2026-09-18.md)。

## 边界

- API36 两次2核/2GiB初始启动出现系统 Keyguard/GMS ANR，系统对话框遮挡最后一项页面断言（各34通过/1失败）。取证见默认入口记录。显式4核/4GiB重跑35项通过；runner默认资源未变，新增资源选项并落盘，不忽略断言或隐藏系统对话框。此项是功能验收，不关闭低内存/系统ANR长稳义务。

- 本轮显式 Docker 重建仍退出2，旧 `xz-libs` / `ca-certificates` 已不在当前镜像。归档安装可重复不等于从移动镜像或源码重建成功；未来更新 RootFS 须独立恢复包输入供应、审查许可证、更新锁并验收，不能简单接受新的最终 hash。
- 已有远端 main CI `35248674608` 两 job 成功，见[历史证据](../evidence/development/hxa-193-upgrade-recovery-2026-09-18.md)。本轮 workflow 简化未推送、未触发新的远端 CI，当前结论只包含上述本地新验证与该历史远端结果。
- API29/36 模拟器不代替真机长稳、OEM、真实付费账号；所选覆盖升级为同签名 versionCode 1 debug 替换，不证明正式签名升级、任意历史格式迁移或商店发行。
