# HXA-193：包内 Runtime 可复现资产与升级验收

> 本页记录当时交付证据；决策链接指向当前主题归纳，不表示当时测试覆盖当前契约的全部范围。

日期：2026-09-18。状态：**completed（本任务范围）**。基线 `c33cb893`（worktree 分支 `codex/runtime-closeout-session-search`）；本记录随收口具名本地提交，不 push、不合并、不发布。完整证据、制品 SHA、日志目录与精确重跑命令见[收口证据](../evidence/development/hxa-193-upgrade-recovery-2026-09-18.md)；CI 现场复核与 PR 运行核验见 [ci-runtime-assets-2026-09-17](ci-runtime-assets-2026-09-17.md)。本范围无可执行未闭合验收项；保留一项外部既有边界（见下）。

## 交付

- 单 APK 集成沿用既有实现：不重做共享 UID 架构，不把 `--help` 或缓存当验收；本轮无生产代码改动。
- 升级恢复验证闭合：新增 `UpgradeRecoveryDeviceTest`（上一版本 SUCCEEDED 结果冷启动取回且恰好对账一次、PENDING 半成品结算 INTERRUPTED 不重放、配置/用户数据跨恢复周期存活），并入规范套件（32→35 例）；旧验证锚不激活由套件内 `IntegratedRuntimeDeviceTest` 既有断言覆盖。
- 资产路径现测：锁定固定归档（`HELIX_ROOTFS_ARCHIVE`）构建 PASS（hash/ELF/许可证门禁未降低、未改 lock、未做非必要依赖升级）；默认 Docker 重建路径现测复现已有边界。

## 验证

API36/API29 独占模拟器（自启自闭、`-read-only -no-snapshot`、1080x2400@420、仅 teardown 自有进程组）规范 35 例各 `OK (35 tests)`，0 失败 0 跳过；APK 身份 `app 7af888c3…` / `test 7abc63ec…`（`artifacts.json`/`closed.json` 留痕）。

实际 CI：main 推送运行 35248674608 实际最终状态 `completed/success`（`runtime-assets`、`verify` 两 job 均 success，`gh run view` 只读核对）；PR #1 运行 35242909407 两 job 通过（2026-09-17 记录）。

本地全门禁：`check-all.sh --source`、`--artifacts` 与 `./gradlew spotlessCheck detekt test lint×8`、8 变体 assemble、`check-lockfiles.sh`（35 files）、`git diff --check` 全部通过；`verify-integrated-runtime-apks.py` consumer/developer × debug/release 4 变体组件/进程/UID/载荷/launcher 全部 verified。

## 边界与后续

- 默认滚动镜像 Docker 重建仍缺锁定的 `xz-libs 5.8.3-r0`（外部上游镜像状态，本轮现测复现 exit 2）；设计上的持久路径为锁定固定归档，CI 与设备安装路径均按其验证，不修改 lock、不降低制品校验。
- 真实订阅行为归 HXA-190；签名身份与发行归 HXA-122/121；本记录不覆盖。
- 仅具名本地提交，不 push/merge/发布；全局 status/roadmap/index 由协调者整合（任务文件内含建议措辞）。
