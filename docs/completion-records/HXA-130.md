# HXA-130 完成记录

- 目标：纠正 HXA-129 对项目所有者意图的误解；只废弃官方 CLI 认证/执行路线，允许已跑通的第三方订阅认证继续进入 developer/Advanced Provider，且不以供应商授权为该渠道前置。
- 前置任务/commit：HXA-129 / `8abc097`。
- 修改文件：`runtime/cli-client` 渠道资格契约和测试、developer App 负向注册测试、ADR-0024/0025，以及 M11 roadmap/status/verification 和架构/产品说明。
- 实现的契约：注册评估显式接收 `DEVELOPER_ADVANCED` 或 `CONSUMER_STORE`。完整技术 evidence 在 developer 渠道不因授权缺失而失败；相同 evidence 在 consumer/store 缺少授权时稳定返回 `DISTRIBUTION_AUTHORIZATION_UNPROVEN`。当前 evidence 仍因模型 Job IPC/统一事件/对账未完成而不可注册。
- 未实现/明确排除：本 HXA 不新增跨 APK模型 transaction、任意 prompt、Provider catalog 项或对话 UI；这些必须在后续 HXA 复用现有 `ModelProvider` 契约逐项实现。token 继续只在独立 Runtime UID。
- 自动测试：`:runtime:cli-client` 11/11；`:app:testDeveloperDebugUnitTest` 271/271。
- 手工验证设备：无；本任务仅修改纯 Kotlin 渠道门禁和规范，不改变 Android APK 行为。
- 验收命令、exit code 和结果：`./gradlew :runtime:cli-client:test :app:testDeveloperDebugUnitTest :runtime:cli-app:assembleDebug --no-configuration-cache` exit 0；`./scripts/check-cli-runtime-boundary.sh`、`check-lockfiles.sh`、`check-secrets.sh`、`check-docs.sh`、`verify-adr.sh` 与 `git diff --check` 均须在提交前通过。
- 权限/数据/许可证变化：无；没有新增依赖、权限、endpoint、OAuth identity 或数据迁移。
- 决策记录：[ADR-0025](../adr/0025-developer-subscription-provider-channel.md)（accepted），取代 ADR-0024；项目所有者明确接受 developer Provider 无需供应商授权，同时保留独立 APK/UID 凭据边界。
- 风险或后续任务：developer Provider 仍可能受协议漂移、账号限制或服务停止影响，必须在 UI 标记非官方/实验。下一 HXA 才能实现 Codex 跨 APK有界模型 Job；consumer/store 保持关闭。
