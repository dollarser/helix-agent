# HXA-144 完成记录

- 日期：2026-09-06。
- 目标：Claude developer/Advanced 订阅 Provider，显式平台 Job 路由，复用既有对话与 Runtime 隔离链路。
- 前置：HXA-136、main 同步 `808e577`、设计提交 `3ea8dcc`。
- 决策记录：[ADR-0027](../adr/0027-subscription-provider-job-routing.md)，accepted（所有者明确“接受继续”）；沿用 ADR-0025/0007，不改变凭据或渠道边界。
- 修改范围：cli-client 封套/client、cli-app Claude adapter/共享 stream/service、developer Provider/账号映射、JVM/设备测试、Runtime opt-in R8 和边界脚本；所有者允许同步 main 的 `ProcessDiagnostics.kt` KTX/crash 持久化修复，未改主工作树。
- 实现：v1 Codex 字节兼容、v2 明确平台并进入 hash；Claude 自有 vault、过期刷新、固定 Messages endpoint、401/403/429/5xx 脱敏、无模型 POST 自动重放；共享 Anthropic encoder/decoder、有界 ModelEvent/PFD、普通 ProviderService/ChatService/Turn 持久链路；工具与图片关闭。
- 未实现：Grok/Copilot 对话 Provider、动态模型目录、真实 Claude 付费模型调用。Claude 真实调用按所有者决定保留未核实；fixture 不证明服务接受当前 OAuth 请求头。

## 自动验收

`./gradlew :app:lintDeveloperDebug :runtime:cli-client:test :runtime:cli-app:testDebugUnitTest :app:testConsumerDebugUnitTest :app:testDeveloperDebugUnitTest :app:assembleDeveloperDebug :app:assembleDeveloperDebugAndroidTest :app:assembleConsumerDebug :runtime:cli-app:assembleDebug :app:assembleDeveloperRelease :app:assembleConsumerRelease :runtime:cli-app:assembleRelease --no-configuration-cache --no-daemon --max-workers=2`：exit 0，1268 tasks；此前 main diagnostics 的 3 个 UseKtx 错误已随授权同步解决。

新增运行中故障 seam 后执行 `./gradlew :app:assembleDeveloperDebug :app:assembleDeveloperDebugAndroidTest :runtime:cli-app:assembleDebug :runtime:cli-app:assembleRelease --no-configuration-cache --no-daemon --max-workers=2`：exit 0，450 tasks。seam 仅 BuildConfig.DEBUG 与固定 fixture model 双条件下等待，release 不进入该分支。

JVM 专项：codec 6/6（v1/v2、平台/版本/字段拒绝），payload job 5/5（异平台同 Job 拒绝、取消迟到输出与恢复），Claude model 3/3（凭据隔离、HTTP 失败、截断流）。App consumer/developer 单测通过。

`./gradlew :runtime:cli-app:assembleRelease -Phelix.cli.r8=true --no-configuration-cache --no-daemon --max-workers=2`：exit 0，实际执行 `minifyReleaseWithR8`。首次 R8 报 OkHttp 可选 JVM TLS provider 类型缺失；采用与既有 M7 相同的 Android 平台 TLS 边界，仅对九个具体可选类型写 dontwarn，其他缺失仍是错误；没有添加 TLS provider 或更换依赖。普通 App release 未开启 minify，不把它冒充 App R8 验收。

`./gradlew :runtime:cli-app:dependencies --configuration releaseRuntimeClasspath --no-configuration-cache --no-daemon --max-workers=2`：exit 0。只新增仓库自有 `provider:anthropic` 模块依赖，外部 closure/version 与既有 Kotlin/kotlinx/OkHttp/Okio/annotations 一致；没有打包参考插件、官方 CLI/SDK 或新增外部许可证义务。

`./scripts/check-cli-runtime-boundary.sh`、`./scripts/check-lockfiles.sh`、`./scripts/check-secrets.sh`、`./scripts/check-docs.sh`、`./scripts/verify-adr.sh`、`git diff --check`：通过。consumer DEX 无订阅实现，Runtime 无新增危险权限/凭据 IPC。

## API 29/36 arm64-v8a

独占 `emulator-5602` / `emulator-5604`，M11 Test AVD，cold boot `-no-snapshot -no-audio -no-window`；未使用并行 M9 的 5590。安装 App/测试 APK 后，两设备执行以下命令，均 0 failure / 0 skip：

```sh
adb -s <serial> shell am instrument -w -r -e oldRuntime true -e class 'com.helix.app.provider.CliRuntimeHandshakeE2eDeviceTest#oldRuntimeRejectsClaudeEnvelopeWithoutCreatingJob' com.helix.agent.developer.test/com.helix.app.HelixAndroidJUnitRunner
```

前置旧 Runtime 从干净 detached `3ea8dcc` 用 `:runtime:cli-app:assembleDebug` 构建并安装。API 29/36 各 `OK (1 test)`：v2 被拒绝且 query Unknown；v1 Codex fixture 成功。不是用当前代码模拟旧 decoder。

换装当前 Runtime 后：

```sh
adb -s <serial> shell am instrument -w -r -e class 'com.helix.app.provider.CliRuntimeHandshakeE2eDeviceTest#claudeRunningCancelAndDeathNeverReplay,com.helix.app.provider.CliRuntimeHandshakeE2eDeviceTest#claudePayloadSurvivesDisconnectAndIsDeletedAfterReconcile,com.helix.app.provider.CliRuntimeHandshakeE2eDeviceTest#modelPayloadUsesPfdAndIsDeletedAfterReconcile,com.helix.app.provider.CodexSubscriptionProviderE2eDeviceTest#claudeProviderUsesTheNormalModelContract,com.helix.app.provider.CodexSubscriptionProviderE2eDeviceTest#developerProviderUsesTheNormalModelContract,com.helix.app.provider.CodexSubscriptionProviderE2eDeviceTest#claudeAccountUsesItsOwnExplicitActivity' com.helix.agent.developer.test/com.helix.app.HelixAndroidJUnitRunner
```

API 29 `OK (6 tests)` / 3.705 s；API 36 `OK (6 tests)` / 4.518 s。验证 RUNNING 取消→CANCELLED、kill→INTERRUPTED、原 jobId 查询稳定、无重放，PFD 对账删除、普通对话与固定 Claude UI 入口。随后两设备 `dumpsys activity services com.helix.runtime.cli` 均 `(nothing)`。

## Artifact / SHA-256

| Artifact | SHA-256 |
| --- | --- |
| developer Debug APK | `1d6e6ca6177e355fadf2a71cfe0cf5dc5e0d6d4d64014841f0444a18985d6141` |
| Runtime Debug APK | `b19bdc5f70aae4e7a9301f99c3ebfe32eac41edc49f05ef1435b1d6ba2260706` |
| 旧 v1 Runtime Debug APK | `1d7c03380dc813ef71ae1c894a85a64b595c8800348d8a194aee5aa68d9c7d91` |
| Runtime R8 unsigned Release APK | `c9dbee5e688c4e7eb0c8acb25a6e9d67606a8a0aa9c85a1417e0a70d827c1c26` |

外部资料准确 URL/访问日期、最初失败与修复历史见 [进展记录](../development/hxa-144-progress.md)。无真实付费 Claude token 消耗；不声明真机、商店分发或签名 release 完成。
