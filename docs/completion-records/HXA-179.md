# HXA-179: ChatService 职责拆分

日期：2026-09-09。工作分支：`codex/phone-chat-polish`；未合并 main。

## 实现

`ChatService` 从 3153 行降至 1867 行，保留会话准入、草稿/附件、Turn 生命周期和公开 facade。提取 `ChatToolCalls`（工具准备/调度/审批）、`ChatToolSettlement`（持久结果）、`ChatToolTimeline`（UI 工具行）、`ChatModelLoop`（模型流与压缩边界）、`ChatGoalActions`（显式 Goal 操作）、`ChatRecoveryActions`（结果恢复）和 `TurnCancelSignal`。共享取消/计时状态仍有唯一所有者；没有复制第二套状态或修改审批、预算、Goal 判定语义。

决策记录：不适用：保持既有契约和执行语义的内部职责提取；没有新增架构或依赖。

## 验证

- `./gradlew spotlessApply test :app:assembleDeveloperDebug :app:assembleDeveloperDebugAndroidTest :app:assembleConsumerDebug :app:assembleConsumerDebugAndroidTest detekt lintDebug :app:lintConsumerDebug :app:lintDeveloperDebug --continue --max-workers=1`：通过，日志 `build/phone-polish/hxa179-host.log`。
- `run-goal-report-regression.sh developer 36` / `consumer 29`：各 79/79，覆盖模型报告、预算、取消、后台任务与压缩。
- `run-chat-responsibility-regression.sh developer 36` / `consumer 29`：各 28/28，覆盖工具调度、审批、审批唤醒、附件重试、会话、停止和工具时间线。
- 脚本均保存于 `scripts/debug/2026-09-09/`；设备证据分别在 `build/debug/2026-09-09/hxa179-{developer36,consumer29,tools36,tools29}`，均有退出码 0 的 `closed.json`。

## 边界

使用独立启动的 API29/36 模拟器，使用后关闭；未操作真机或其他人的模拟器。此次不是 JNI/Binder 长稳验证。累计工作树保持未提交状态，不合并 main。
