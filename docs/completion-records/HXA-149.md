# HXA-149 Skill Installer 与精确内容安装

日期：2026-09-08。

决策记录：accepted ADR-0029；沿用现有 Policy/Approval 和快照存储，无 core/Room/Runtime 变更。

## 交付与验证

内置 skill-installer、L2 LOCAL_MUTATION skills.install、Workspace目录/ZIP与原生ZIP picker、预览/安装/启用结果入口。执行重读有界副本并比对 expectedHash，提交相同副本；新快照禁用，重复 hash 保留启用选择。提交后不把取消误报为无副作用；结果丢失按 SkillKey/hash 查询。安装失败结果明确未确认，不声明事务回滚。

exit 0：HXA-148 所列 Gradle/文档门禁，日志 build/skill-authoring/installer-gates3.log（1112 tasks），新增 JVM 2 项覆盖 ZIP、hash 变化、取消、默认禁用、重复安装及 Repository 重建；真实设备调度器验证 Plan 拒绝、批准后变更拒绝、刷新批准后成功。

```bash
bash scripts/accept-hxa-149-skill-installer.sh emulator-5596
HELIX_SKILL_MODEL=Qwen3.8-27B bash scripts/accept-hxa-149-skill-installer.sh emulator-5598
bash scripts/accept-hxa-125-sample.sh emulator-5596 build/main-verification/qwenwork-current/QwenWork-helix-import.zip
bash scripts/accept-hxa-125-workbuddy.sh emulator-5598 build/main-verification/workbuddy-sample
```

API29/36 每台原生1/1、中文/英文大字体 UI2/2；API36 SGLang Qwen3.8-27B 20.375秒完成 write、skills.preview、批准 skills.install、skills.enable、skills.read，逐个检查持久 COMPLETED 状态。首次调度器测试遗漏 mode 参数（默认 ACT），修正显式传入 PLAN 后验证通过。UI locale 测试宿主补保留 ActivityResultRegistryOwner；生产 picker 保持原生实现。

QwenWork 当前真实 ZIP 4 Skill/2端点、WorkBuddy 两个 ZIP 4 Skill/2端点复验通过，各1/1；QwenWork 脚本对传入 ZIP 计算并验证转移 hash，不再误用旧打包字节 hash。

## 限制

物理设备、长稳和受保护服务仍单列。上述来源包测试只运行导入及读取，不运行源宿主脚本或推导服务账号可用。没有远程市场或静默联网下载。恢复证明固定快照/Repository 重建与幂等请求，未冒称覆盖所有进程强杀时点。
