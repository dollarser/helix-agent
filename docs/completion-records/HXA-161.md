# HXA-161：真机会话输入区与推理参数接线

Date: 2026-09-09
Status: completed

## 交付

所有者确认的布局已实现：输入框上方左侧为 Chat/Plan/Act/Goal 下拉及附件，右侧为推理强度；语音、输入框、发送/停止同排。会话详细信息移入会话设置，键盘弹出时输入区保持可见。模式和强度在运行中锁定，选择不自动发送，Goal 仍打开既有目标配置。

推理误报涉及三段缺口：连接探测固定 false、Chat Completions 流遗漏 reasoning_content、请求装配未传 reasoning。现仅在成功探测中实际观察到 ReasoningDelta 时确认能力，未验证不显示明确不支持；首轮和工具回填携带已选择强度。Qwen3.8 的高档编码为 xhigh，其他模型维持 high；低/中为 low/medium，默认不发送该参数，默认不表示关闭推理。映射参考 [Qwen 官方模板](https://huggingface.co/Qwen/Qwen3.8-27B/blob/main/chat_template.jinja) 并经实际服务核验。

推理开始会触发“正在思考或生成回复…”进度，不混入最终答案。内部运行偏好保存强度，兼容旧三行记录；沿用既有 ReasoningEffort/ModelRequest、审批和 Goal 预算，不新增权限或依赖。

## 精确验证

JDK 17，Android SDK 环境变量指向本地 SDK；独立 worktree 与专用 API36 arm64 模拟器，不操作并行测试设备。

```sh
./gradlew spotlessApply :provider:api:test :provider:openai-chat:test :app:testConsumerDebugUnitTest :app:testDeveloperDebugUnitTest :app:assembleDeveloperDebug :app:assembleDeveloperDebugAndroidTest detekt :app:lintConsumerDebug :app:lintDeveloperDebug --max-workers=1
./gradlew spotlessCheck --max-workers=1
bash scripts/check-i18n.sh
bash scripts/check-docs.sh
bash scripts/verify-adr.sh
bash scripts/check-secrets.sh
git diff --check
```

最终构建命令 exit 0。provider/api 90/90、openai-chat 58/58；consumer 共 338 项（334 通过、4 既有外部条件 skip），developer 共 362 项（358 通过、4 skip），0 failure/error。覆盖推理流解析与非法输入、能力误报、Qwen 高档编码、旧偏好读取/保存和推理进度不泄漏至答案。双 app flavor lint、Detekt 和格式门禁通过；不宣称本轮重跑完整仓库所有测试或根 Debug/Release lint。

```sh
adb -s SERIAL install -r app/build/outputs/apk/developer/debug/app-developer-debug.apk
adb -s SERIAL install -r app/build/outputs/apk/androidTest/developer/debug/app-developer-debug-androidTest.apk
adb -s SERIAL shell am instrument -w -r -e class com.helix.app.ui.ConversationComposerDeviceTest,com.helix.app.ui.ModeLayoutDeviceTest,com.helix.app.ui.RunControlModeUiDeviceTest,com.helix.app.ui.ChatStopProgressDeviceTest com.helix.agent.developer.test/com.helix.app.HelixAndroidJUnitRunner
```

API36 最终 `OK (8 tests)`，无 skip/failure；覆盖窄屏、大字体、模式/强度菜单、运行锁定、停止及 Goal 入口。首轮发现英文大字体附件换行，已修复并复验；旧测试文字断言随图标改为无障碍标签断言，没有删减功能验收。新增进度事件首次编译缺少 textChanged 参数，修正后最终全量上述命令通过。

API34 OnePlus 6T 真机保留用户数据覆盖安装，仅在独立合成会话测试。连接探测确认推理 ✓；中/高两次真实 Chat 请求分别为 reasoning_effort=medium/xhigh，均返回 OK。临时本机转发观察器仅记录模型/强度/预算/是否带工具，无正文或密钥；验证后退出并恢复原直通端口映射。中档与 Act 偏好恢复，键盘弹出及会话设置实机检查通过。最新 APK 覆盖安装成功，原会话保留。本轮未跑真机测试 APK，不将此专项等同于完整真机验收。

| 制品 | SHA-256 |
| --- | --- |
| Developer APK | `b563998744ec92752eb6956f08e48918e13d498920f9a458a8c2ac9fd0abd874` |
| Developer 测试 APK | `97089f6589c056671dec599908f38d83d3b29b116e72eed5cf68028593c79e52` |

专项原始日志保存于被忽略的 build/phone-polish；用户会话与设备私有数据不入库。

## 决策记录与边界

决策记录：不适用。属于既有 ModelRequest/ReasoningEffort 契约内接线、局部解析修复和所有者确认的 UI 调整；内部偏好可兼容迁移，不更换持久化底座或跨模块契约，不改变 accepted ADR。

既有会话中的 PROTOCOL 失败没有完整原始流证据，尚不能归因或宣称修复。文件交付入口、工作区上下文优化、完整真机验收和长稳继续单列。没有提高原有 4096 单次输出预算。API29 未在本检查点复跑。

实现位于独立 codex/phone-chat-polish 分支，基于先前修复提交 22d2481；本记录完成不等同于提交、合入 main 或推送。并行浏览器/长稳工作未被覆盖。
