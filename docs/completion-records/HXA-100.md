# HXA-100 固定评测集（完成记录）

- 日期：2026-09-05
- 目标：建立不少于 40 条、顺序和内容固定的 M10 评测集，并为每次可复现实验强制记录 provider、model、protocol、prompt hash、tool versions、设备和日期等证据。
- 前置任务/commit：HXA-099，`5d5c52d89ac546507cd9e43dbba484ddf86eb540`。
- 修改文件：`evals/m10/fixed-evals.tsv`、固定 SHA-256、运行说明和元数据模板；`testing` 中的 catalog/metadata/digest 回归测试；根 Gradle 测试配置只向 `:testing` 注入仓库内评测目录。
- 实现的契约：固定集共 45 条，ID 唯一并按表格顺序运行，覆盖 Chat、Plan、Goal、三种 Provider 协议、文件、JavaScript、浏览器、MCP、A2A、Skill、Accessibility 和 Root 拒绝路径。每条 prompt 是精确 UTF-8 输入；数据集由 `f27bf8b51e61be248a6e642c22cefc3e5045d0d35e37518377eb9b8cdf85e795` 绑定。结果必须记录数据集和逐 prompt SHA-256、provider/model/服务报告版本、protocol、temperature、所有暴露工具版本、完整 Git commit、真实设备 model/API/ABI/page size、UTC 日期、结果和仓库相对证据路径；字段缺失、digest 变化或 skip 均不能计为通过。Fixture Provider 只证明确定性的 Agent Loop，真实 Provider 才用于衡量模型工具选择，两者不可互相替代。
- 未实现/明确排除：本 HXA 没有声称已在真实模型上跑完 45 条固定集，也没有把单次 SGLang smoke 当作完整评测。没有新增生产 Tool、权限、Provider 行为或依赖；Prompt injection 矩阵属于 HXA-101。
- 自动测试：新增 3 个 JVM 测试，覆盖 45 条固定集的数量、ID/类别/协议/六列边界，元数据必填字段，以及数据集 SHA-256 防漂移。最终全仓 JVM `test` 产物统计为 1,196 tests、0 failures、0 errors、0 skipped。
- 手工验证设备：`sdk_gphone64_arm64` AVD，API 36、arm64-v8a、4,096-byte page。通过 SSH 本地端口转发访问 `http://127.0.0.1:30008/v1`，空 API key；服务报告模型 `/mnt/nvme7/lingzhang/models/Qwen/Qwen3.8-27B`。`SglangUiSmokeTest` 在设备侧通过 `10.0.2.2:30008/v1` 完成真实模型枚举、最小文本、probe-only bounded `echo` schema、视觉 probe、UI 选择和持久化，1/1 passed、0 skipped；这个 probe schema 不是有版本的生产 Tool，因此不把它伪记为固定评测的 tool version。该结果同时关闭 HXA-099 completion record 中记录的 API 36 外部端点剩余项，但不扩张为固定集全量通过声明。
- 验收命令、exit code 和结果：`shasum -a 256 evals/m10/fixed-evals.tsv`，exit 0，digest 与固定文件一致；`./gradlew :testing:test`，exit 0，3/3；`./gradlew test --no-daemon`，exit 0，1,196/1,196、0 skipped；`./gradlew spotlessApply detekt :testing:test --no-daemon`，exit 0；`curl http://127.0.0.1:30008/v1/models`，exit 0，返回上述一个 SGLang 模型；`ANDROID_SERIAL=emulator-5556 ./gradlew :app:connectedDeveloperDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=com.helix.app.provider.SglangUiSmokeTest --no-daemon`，exit 0，1/1、0 skipped。
- 权限/数据/许可证变化：无新权限、依赖或许可证；固定集不含 Secret、真实用户数据或响应正文。真实 SGLang 证据只记录模型标识、端点形态和设备事实，不记录密钥。
- 决策记录：不适用；本任务只固化既有 Provider/Tool/Mode 边界的评测输入与证据 schema，没有形成或修改架构决定。
- 风险或后续任务：45 条固定集尚未在每种真实 Provider 协议和真实设备组合上逐条执行；OpenAI Responses 与 Anthropic Messages 的真实服务、更多模型版本和物理设备均未提供，因此不能计为绿色。后续运行必须保存逐 case 元数据并保持固定 digest；HXA-101 将在此基础上补充 Prompt Injection 矩阵。
