# HXA-148 Skill Creator 与草稿校验

日期：2026-09-08。

决策记录：ADR-0029 已由所有者明确接受。

## 交付

内置 skill-creator、原生创建/完整 SKILL.md 编辑/加载/校验表单、注册 skills.preview。草稿位于 app Workspace work/skills；校验读取 app 的 input/work/output 目录或 ZIP，拒绝越界、符号链接及超限输入，使用临时有界副本并清理。返回内容 hash、文件清单、声明兼容性和工具依赖；不安装或启用。编辑保留 frontmatter，保存检查加载时 hash。

## 验证

以下命令 exit 0（JAVA_HOME 使用 JDK17）：

```bash
./gradlew :extensions:skills:test :app:testConsumerDebugUnitTest :app:testDeveloperDebugUnitTest :app:assembleConsumerDebug :app:assembleDeveloperDebug :app:assembleConsumerDebugAndroidTest :app:assembleDeveloperDebugAndroidTest --no-configuration-cache
./gradlew spotlessCheck detekt lintDebug lintRelease --no-configuration-cache
bash scripts/check-docs.sh
bash scripts/verify-adr.sh
bash scripts/check-i18n.sh
bash scripts/check-secrets.sh
bash scripts/accept-hxa-148-skill-creator.sh emulator-5596
HELIX_SKILL_MODEL=Qwen3.8-27B bash scripts/accept-hxa-148-skill-creator.sh emulator-5598
```

Gradle 分批执行见本地忽略日志 build/skill-authoring/build8.log、gates5.log（1112 tasks）；5 项新增 JVM 测试覆盖内容变化、编辑元数据保留、旧 hash、路径、链接、大小及取消清理。API29/36 各原生2/2、英文/中文320dp且字体2倍 UI2/2；原生恢复验证重新构造服务后重新校验，不冒充进程强杀。API36 真实 SGLang Qwen3.8-27B：ACT 会话实际 write + skills.preview COMPLETED，27.706秒，安装目录未激活。

首次真实评测错误使用 CHAT（无工具），且4096输出预算不足；修正为 ACT、有限32768输出预算后通过。文本声称执行不算证据，本测试检查持久 ToolCall 和实际草稿。模型、服务及运行时点绑定此证据。

## 边界

本项不安装或启用；交由 HXA-149。真机、24小时长稳和独立受保护账号仍后置。来源以当前 app Workspace 为限，外部文件先使用现有导入入口；没有新增远程下载、Runtime、依赖、Room schema 或授权契约。
