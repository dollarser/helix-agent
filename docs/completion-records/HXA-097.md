# HXA-097 Android UI Skill（完成记录）

- 日期：2026-09-05
- 目标：在 HXA-091～093 已验收的 snapshot/token、UI actions 和敏感界面拒绝契约上，实现 developer/Advanced 的 `android-ui-task` 内置 Skill；Skill 只编排既有能力，不授予权限、session、scope 或 approval。
- 前置任务/commit：HXA-091～093；M9 集成 commit `247fd7e`。
- 修改文件：`extensions/skills/src/main/kotlin/com/helix/extensions/skills/BuiltInSkills.kt` 及测试、`tools/automation/src/main/kotlin/com/helix/tools/automation/AutomationTools.kt` 及测试、developer `AutomationModule`、`AppContainer` 与 variant boundary 测试。
- 实现契约：`android-ui-task` 只允许九个 `ui.*` 工具；首次和每次成功动作后重新 snapshot，只使用新 snapshot/find 返回的 opaque node token。package/window 变化、token 过期、checkpoint、敏感界面、断连、失败或用户停止均立即结束；Skill 文本和 `allowed-tools` 只是非可信编排提示，所有调用仍经过 schema、Capability、AutomationSession scope、Policy、Approval、执行限制、验证和审计。
- 自动测试：Automation、Skill、Policy、Framework 与 App 双 flavor JVM 测试覆盖内置 Skill catalog/allowed-tools、developer 注册、consumer 缺席、token-only schema、动态风险和 session/scope 边界；双 flavor assemble 与 variant boundary 门禁通过。
- 手工验证设备：专用 API 29/36 AVD 上 App developer 全量 instrumentation 分别为 136 tests、0 failed 与 135 tests、0 failed；Automation device suite 均 0 failed。真实 OEM/物理设备不在本 HXA 的既有验收声明内。
- 验收命令、exit code 和结果：M9 非真机综合 Gradle 命令和六项静态门禁全部 exit 0；API 29/36 专用模拟器矩阵全部 0 failed。完整命令、设备与计数见 `docs/development/m9-non-device-progress.md`。
- 权限/数据/许可证变化：无新权限、依赖或许可证；consumer flavor 不包含 Accessibility 工具实现，Skill 不请求系统权限、不启动 AutomationSession、不修改 allowlist。
- 决策记录：不适用；本任务只组合 HXA-091～093 已接受的能力和安全边界，没有创建新的执行器或授权模型。
- 风险或后续任务：敏感 package/语义策略仍需在真实 OEM 和发行设备上持续扩充，但不是本 HXA 已声明模拟器范围的缺口；不得将 Skill 存在解释为 Accessibility 已授权或动作可自动批准。
