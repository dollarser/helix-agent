# HXA-038 完成记录

- 目标：把 Provider-neutral 模型流 working set 与 terminal decision 从 `ChatService` 抽成纯 JVM 合同，以 characterization tests 锁定现有安全与协议语义；不改变 Dispatcher、Scheduler、审批、持久回填或设备行为。
- 修改：新增 `app/chat/ModelStreamState.kt`，集中处理文本累积、usage nullable 编码、原始 ToolCall 参数拼接、1,048,576 UTF-16 code unit 总量上限、按 Provider index 的调用顺序、拒绝/错误/取消优先级及截断工具流失败关闭；`ChatService` 只根据纯状态结果推进 Room/UI，并继续把完整调用交给唯一 Dispatcher 路径。`ChatService.kt` 从约 1850 LOC 降至 1713 LOC。
- 测试：新增 `ModelStreamStateTest` 10 个纯 JVM 用例，覆盖首次/后续文本增量、usage 未报告不冒充 0、调用顺序与原始参数、孤儿事件、参数溢出、批内未闭合 sibling、拒绝/错误/取消优先级和非法预算。
- 全局复扫：确认 M1 `TurnReducer` 是逐调用串行合同，而 HXA-037 生产 Tool Round 是批量有界并发；因此没有把旧 reducer 机械接入生产。新增 HXA-039 作为 batch-safe reducer/coordinator 的 ADR 与迁移任务；`ToolDispatcher` 保留单一公开入口，Provider SSE framing 与 repository 文件拆分按测试 seam 后续渐进处理。
- 未实现/排除：未改变 Turn 状态公开契约、Room schema、Provider 协议、Tool 调度、权限/Policy/Approval；未宣称双状态机语义已解决；无设备行为变化，不要求 instrumentation。
- 验收：
  - `./gradlew :app:testConsumerDebugUnitTest` → BUILD SUCCESSFUL，107 tests / 0 failures（含新增 10）。
  - `./gradlew :app:testDeveloperDebugUnitTest` → BUILD SUCCESSFUL，107 tests / 0 failures。
  - `./gradlew spotlessApply spotlessCheck detekt` → BUILD SUCCESSFUL，0 formatting/Detekt issues。
- 决策记录：不适用。本任务是在既有内部契约内提取纯状态与补测试，不改变跨模块公开契约、安全边界或已接受 ADR；HXA-039 若演进/取代 M1 reducer，必须按 ADR-0002 的关系形成新 ADR 后再实现。
