# HXA-154：浏览器生产路径引用核实

Date: 2026-09-08
Status: completed（有界核实完成，原生问题仍 open）

## 交付

测试 APK 增加 ControllerReferenceControlActivity，直接使用生产 BrowserController 和原始 callbacks；覆盖空标签、策略拒绝、立即关闭、停止后关闭、加载完成关闭及清理历史。修复原生导航夹具 destroy 前未移除父 View，增强 JNI 观察开始/结束及溢出校验，保存版本和产物 metadata。生产代码未改动。

## 验证

- API29/WebView91、API36/WebView133：六路径各 400 轮，共 4800 轮；空标签/拒绝不创建被观察的弱引用，其余路径目标 AwContentsIoThreadClient 弱引用创建/删除全部配对，残留 0。
- 原生正/负对照：两台各 100 轮，裸创建仍残留目标类 200 个，挂载导航后销毁残留 0。它们是诊断对照结果，不能当作原生缺陷关闭。
- `./gradlew spotlessApply :feature:browser:testDebugUnitTest :feature:browser:assembleDebugAndroidTest spotlessCheck detekt :feature:browser:lintDebug --max-workers=1`：通过，JVM 116 项，失败/跳过均 0；最后测试标记调整后再次执行构建、spotlessCheck、detekt、lintDebug 通过。
- `bash scripts/diagnostics/build-jni-reference-trace.sh`：严格 NDK 编译通过；Python 编译检查通过。
- API29/36 显式 `BrowserSecurityDeviceTest`：各 `OK (9 tests)`，无跳过；执行前 force-stop 诊断进程，未加载 agent。
- `bash scripts/check-docs.sh`、`bash scripts/verify-adr.sh`、`bash scripts/check-secrets.sh`、`git diff --check`：通过。

详细命令、引用计数、hash 和边界见 [生产路径核实](../development/browser-controller-reference-verification.md)。

决策记录：不适用：测试夹具修复及既有生产路径核实，无架构/生产契约变化。

## 结论与后续

本地导航立即关闭已能释放目标引用，不能要求生产销毁前等待导航完成。实际生产路径已有 lazy host 和 detach/destroy；本轮未发现需修改它们的证据。裸创建及系统 Binder 问题保留 open，真实网络取消/后台窗口、其他发行版本、真机和长稳仍需独立验证。上游修补版验证不得擅自变成 Helix 内置 Chromium。
