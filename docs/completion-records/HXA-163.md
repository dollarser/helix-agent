# HXA-163：草稿会话、标题与目录归属

Date: 2026-09-09
Status: completed

## 交付

会话顶部始终提供新建、列表、设置。新建直接打开未命名内存草稿；首次显式发送才保存 sessions，空白返回列表不落库。标题使用首句压缩空白后的前 20 个 Unicode code point，用户可从列表或会话设置改名。草稿继承当前可用 Provider，尚无 Provider 时保留绑定入口；切换会话重置输入框，避免把上一会话草稿发到另一会话。首次发送准备期间拒绝重复发送。

草稿附件仅保存当前进程中的选择，首次发送再走既有导入、快照、能力及出网批准管线；发送失败不会冒充成功。分享入口保留原有 HXA-056 行为。Goal 沿用配置/显式 Continue，空白草稿不提前创建会话。

会话设置的工作目录选择器复用 FileManagerService 的现有来源和目录浏览，支持归属/清除；目录显示在会话列表和设置中。目录归属是组织元数据，不自动变更工具 cwd、权限、PRoot 工作目录或移动文件。没有引入 Git Workspace 功能。Room v10 新增 sessions.directoryRef，可空，迁移保留旧行。

## 精确验证

```sh
./gradlew :core:storage:testDebugUnitTest :core:storage:assembleDebugAndroidTest --max-workers=1
./gradlew spotlessApply :app:testConsumerDebugUnitTest :app:testDeveloperDebugUnitTest :app:assembleConsumerDebug :app:assembleDeveloperDebug :app:assembleDeveloperDebugAndroidTest detekt :app:lintConsumerDebug :app:lintDeveloperDebug --max-workers=1
./gradlew spotlessCheck --max-workers=1
bash scripts/check-docs.sh
bash scripts/check-i18n.sh
bash scripts/check-secrets.sh
bash scripts/verify-adr.sh
git diff --check
```

最终 exit 0。storage JVM 88/88，consumer 340 项（336 通过、4 既有外部条件 skip），developer 364 项（360 通过、4 skip），均 0 failure/error。双 flavor APK、Developer 测试 APK、格式/Detekt/双 flavor app lint 通过。不宣称完整仓库或根 Debug/Release lint 全量重跑。

```sh
adb -s SERIAL install -r app/build/outputs/apk/developer/debug/app-developer-debug.apk
adb -s SERIAL install -r app/build/outputs/apk/androidTest/developer/debug/app-developer-debug-androidTest.apk
adb -s SERIAL shell am instrument -w -r -e class com.helix.app.ui.SessionDraftDeviceTest,com.helix.app.ui.MessageCopyDeviceTest,com.helix.app.ui.ConversationComposerDeviceTest,com.helix.app.ui.ChatStopProgressDeviceTest,com.helix.app.ui.ProviderFlowTest com.helix.agent.developer.test/com.helix.app.HelixAndroidJUnitRunner
adb -s SERIAL install -r core/storage/build/outputs/apk/androidTest/debug/storage-debug-androidTest.apk
adb -s SERIAL shell am instrument -w -r -e class com.helix.core.storage.RoomMigrationFixtureTest com.helix.core.storage.test/androidx.test.runner.AndroidJUnitRunner
```

专用 API36：UI 9/9、迁移 22/22，无 skip/failure。覆盖空草稿退出、首次发送自动标题、连续两次 send 只生成一轮、重命名/目录重新打开、未测试 Provider 不可绑定、复制与停止/失败/Goal 回归。v9→v10 保留旧标题且新增列 null；schema export 与代码一致，旧完整迁移链通过。首轮迁移比较仍固定 v9 export 而失败，更新实时版本基准为 v10 后 22 项通过，未跳过迁移校验。移除旧创建对话框后清理其未使用资源，通过 lint。

真机 API34：覆盖安装前备份数据库及偏好，安装新版成功，原有会话保留。空白新建返回列表未新增行；选择 work 目录、首次发送自动标题、改名并在列表显示目录已手工验证。输入选词菜单与回复选择容器的复制/全选菜单均经截图确认。HXA-162 的全文复制由设备测试验证剪贴板全文一致；原会话最后显示最新问答，旧执行记录未附着在其后。

本轮真机合成请求遇到 SGLang 服务等待，同期本机 /models 5 秒超时；稍后收到推理进度，但本轮主动停止测试，没有将其记为真实模型完成。合成会话 UX-check 已归档，恢复原会话与 Act/中。先前 HXA-161 的真实中/高成功证据保留，不能替代本轮结果。

最终 Developer APK SHA-256：`63f09cfe829bab78c8f350ddd4317719346bd7d7269dc8aa68151ec4a440a2cf`。本地日志在忽略的 build/phone-polish；私有会话、备份、设备序列号不入库。只使用专用测试模拟器，未操作并行测试模拟器。

## 决策与边界

决策记录：[ADR-0034](../adr/0034-session-drafts-and-directory-metadata.md)，授权来自所有者明确要求草稿落库、可改名及目录归属；不扩大执行或信任边界。新增可空列及迁移有独立测试证据。

实现位于 codex/phone-chat-polish 工作树，未提交、未合入 main、未推送。目录自动注入执行上下文、改变实际 cwd、跨进程恢复未发送草稿不属于本次组织元数据实现。历史 PROTOCOL 根因、长稳和完整真机验收保持独立。
