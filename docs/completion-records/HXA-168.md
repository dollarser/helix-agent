# HXA-168：顶部空间与胶囊选项

Date: 2026-09-09
Status: completed

## 交付

模式、模型、推理和输入复制选项统一使用 ComposerOptionPill 的椭圆边框，复用单行横向滚动容器，保留菜单与锁定语义。新增选项复用同一胶囊组件。

Sessions 路由不再叠加应用 shell 顶栏。会话列表/会话名/新建/更多直接位于页面顶部，移除重复“会话”标题及其占用空间。会话列表页只保留一个标题及新建入口；应用导航按钮移入列表页和会话更多弹窗，文件、设置等页面继续保留原 shell 导航栏。Goal 管理入口位于会话标题之后，避免挤占最顶部。

## 精确验证

```sh
./gradlew spotlessApply :app:testConsumerDebugUnitTest :app:testDeveloperDebugUnitTest :app:assembleConsumerDebug :app:assembleDeveloperDebug :app:assembleDeveloperDebugAndroidTest detekt :app:lintConsumerDebug :app:lintDeveloperDebug --max-workers=1
./gradlew spotlessCheck --max-workers=1
adb -s SERIAL shell am instrument -w -r -e class com.helix.app.ui.ConversationTopBarDeviceTest,com.helix.app.ui.ConversationHeaderDeviceTest,com.helix.app.ui.ConversationComposerDeviceTest,com.helix.app.ui.SessionModelDeviceTest,com.helix.app.ui.ChatStopProgressDeviceTest com.helix.agent.developer.test/com.helix.app.HelixAndroidJUnitRunner
bash scripts/check-docs.sh
bash scripts/check-i18n.sh
bash scripts/verify-adr.sh
bash scripts/check-secrets.sh
git diff --check
```

最终 host exit 0：Consumer JVM 343 / Developer 367 项，各 4 项既有外部条件 skip，均无 failure/error。双 flavor APK、Developer 测试 APK、双 flavor app lint、格式与 Detekt 通过。初轮 HelixApp 新增分支达到复杂度上限，抽取 ShellTopBar 后通过，未增加复杂度豁免。

API36 10/10，无 skip/failure。新增完整 Activity 测试断言：会话路由没有 shell-top-bar，chat-header 与页面同一起始位置；从会话更多进入设置，再返回会话、返回列表、新建草稿均可用。导航测试 helper 沿新入口先打开更多，未删除导航断言。既有窄屏/双倍字体标题操作、单行横向滑动、模型跨 Provider、停止/失败/Goal 回归通过。

真机 API34 覆盖安装成功，原会话保留；最终截图核实状态栏下直接显示会话名与操作、无重复应用标题，输入选项为独立椭圆边框。本轮未发起真实模型请求。

Developer APK SHA-256：`7f16ab910b43ce13ed94310ff266c83457f7f3b6f1ef0cc72df808a5b219c38a`。日志位于忽略的 build/phone-polish；私有截图不入库。

## 决策与边界

决策记录：不适用：仅 app 内部 Compose 导航入口与布局调整，无新存储、依赖、权限或模块间公开契约。

实现位于 codex/phone-chat-polish，未提交、未合入 main、未推送。仅使用专用模拟器，不操作并行验证环境；未宣称全仓库、根 lint、API29 或长稳完成。
