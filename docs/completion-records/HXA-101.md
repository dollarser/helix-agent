# HXA-101 Prompt Injection（完成记录）

- 日期：2026-09-05
- 目标：固定网页、文件、MCP、A2A、Skill、通知和文件名 Prompt Injection 矩阵，并验证外部内容不能扩大 scope、注册工具、改变动态风险或形成 Approval Proof。
- 前置任务/commit：HXA-100，`c862996`。
- 修改文件：`core:agent` 的外部 Context source/trust 契约与测试；附件模型上下文的文件名信任标记顺序与 app 测试；`evals/m10` 注入矩阵、digest、说明和 catalog 测试。
- 实现的契约：A2A 与 Web/File/MCP/Skill/Notification/Accessibility 一样被结构性列入 `UNTRUSTED_SOURCES`，以 TRUSTED 构造即 fail closed；指令形态内容保持逐字可审计的数据，不能转成 system contract。附件在任何不可信文件名之前先写入 UNTRUSTED marker。16 条固定攻击语料覆盖隐藏 HTML、Markdown comment、Unicode 混淆、Base64、文件名、MCP description/result、A2A Agent Card/Skill/message/Artifact、Skill 指令、伪 system/tool 标签和通知发送者冒充。矩阵通过条件统一为 scope、Registry、Approval/动态风险 authority 不变，不能以模型文本拒绝代替 Policy/Dispatcher 证据。
- 未实现/明确排除：没有增加提示词分类器、自动执行语料或把外部文本静默删除；没有让模型输出参与 Tool Registry、Approval Proof 或 verifier。没有新增 Tool、scope、权限、依赖或 HXA-102 恢复逻辑。
- 自动测试：新增/扩展 5 个 JVM assertions/tests，覆盖精确外部 source 集、A2A TRUSTED 拒绝、恶意标签仍为 UNTRUSTED 数据、文本/图片附件文件名之前的 marker，以及 16 条矩阵入口、编码、authority invariant 与 SHA-256 防漂移。既有 Framework/MCP/A2A/Skill/Browser/Notification suites 随全仓 `test` 继续验证动态工具只能来自可信 `ToolSource`、服务端 hint 不降风险、contract hash 变化失效和结果无 Approval Proof 通道。
- 手工验证设备：`sdk_gphone64_arm64` AVD，API 36、arm64-v8a、4,096-byte page；`AttachmentE2eDeviceTest` 13/13 passed、0 skipped，验证真实 app-private attachment 导入、hash 绑定、模型可见标记、发送/失败/取消/重建路径。测试使用确定性 Provider，不把模型服从性当安全门禁。
- 验收命令、exit code 和结果：`shasum -a 256 evals/m10/prompt-injection.tsv`，exit 0，`2c9d11a3ef8eb8c3dfc3f3bc2c69d983216f64cd8f0a0c0c93ec31344d095f72`；`./gradlew :core:agent:test :app:testConsumerDebugUnitTest :app:testDeveloperDebugUnitTest :testing:test spotlessCheck detekt --no-daemon`，首次 exit 1（测试期望遗漏新增 A2A source），修正后 exit 0；`ANDROID_SERIAL=emulator-5556 ./gradlew :app:connectedDeveloperDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=com.helix.app.chat.AttachmentE2eDeviceTest --no-daemon`，exit 0，13/13、0 skipped；最终 `./gradlew test spotlessCheck detekt --no-daemon`，exit 0，1,198 tests、0 failures、0 errors、0 skipped，静态门禁通过。
- 权限/数据/许可证变化：无；攻击 payload 全为仓库内合成字符串，不含 Secret、用户数据或可执行载荷。
- 决策记录：不适用；实现收紧既有 security/architecture 的 UNTRUSTED 与 Dispatcher/Policy 边界，没有改变 accepted ADR。
- 风险或后续任务：真实模型可能仍会在文本层受注入影响，但任何副作用必须继续由独立 schema/Policy/Approval/verification gate 阻断；本记录不声称提示词可消除模型误导。物理设备与多模型行为评测未提供，不计为通过；安全结论依赖结构性 authority invariant，而非这些外部条件。
