# HXA-165：移动对话界面视觉整理

Date: 2026-09-09
Status: completed

## 参考与交付

所有者在 main 工作树提供七张移动端对话截图。本轮直接查看截图，仅参考可见布局，不将截图中的模型自述视为能力事实，也不复制第三方代码或图片资源。截图里的个人对话不纳入仓库。

采用紧凑顶部导航（列表、标题、新建、更多）、右侧浅色用户气泡、无整块底色的助手正文、带无障碍名称的复制图标。顶部标题单行截断，操作保持可达。菜单以勾选显示当前选项。

按所有者后续要求，将附件从上方文字按钮移到输入框内加号；模式继续在左上方、推理强度在右上方。语音、输入、加号、发送/停止使用统一圆角外框。复用现有附件选择器，未改变附件导入或审批。保留 Markdown、原文复制、选词与会话草稿语义。

## 验证

```sh
./gradlew spotlessApply :app:testConsumerDebugUnitTest :app:testDeveloperDebugUnitTest :app:assembleConsumerDebug :app:assembleDeveloperDebug :app:assembleDeveloperDebugAndroidTest detekt :app:lintConsumerDebug :app:lintDeveloperDebug --max-workers=1
./gradlew spotlessCheck --max-workers=1
adb -s SERIAL shell am instrument -w -r -e class com.helix.app.ui.ConversationHeaderDeviceTest,com.helix.app.ui.ConversationComposerDeviceTest,com.helix.app.ui.MessageCopyDeviceTest,com.helix.app.ui.SessionDraftDeviceTest,com.helix.app.ui.ChatStopProgressDeviceTest com.helix.agent.developer.test/com.helix.app.HelixAndroidJUnitRunner
bash scripts/check-docs.sh
bash scripts/check-i18n.sh
bash scripts/verify-adr.sh
bash scripts/check-secrets.sh
git diff --check
```

最终主机 JVM、构建、双 flavor app lint、Detekt、格式门禁通过，API36 11/11 设备用例通过，无 skip/failure。Consumer JVM 343 项、Developer 367 项，各 4 项既有条件 skip，均无 failure/error。初轮新测试使用不存在的 DpRect width/height 属性导致编译失败，已改为边界差；首次设备 11 项中 10 项通过，顶部按钮测得布局为 40dp，已显式设置为 48dp 后重新验收，未删除断言。

真机 API34 已覆盖安装，原有会话保留；回复与输入区截图已核实。输入框内加号打开系统文件选择器，取消返回正常；键盘展开时模式、推理、语音、输入、附件及发送仍可见。没有发送真实模型请求或读取/选择用户文件。本轮不宣称完整仓库测试、API29 或新真实模型验收。

Developer APK SHA-256：`fbf8e52bfdcac3b876bf2ad1883708f8ecafbae0803ed0cd34772e939f255bb3`。日志位于忽略的 build/phone-polish；私有截图不入库。

## 决策与边界

决策记录：不适用：现有 UI 契约内视觉与可达性调整，无新依赖、存储、跨模块接口或权限边界变化。

实现仍在 codex/phone-chat-polish 工作树，未提交、未合入 main、未推送。仅操作专用测试模拟器，不操作 main 上的并行浏览器测试环境。

后续归档：所有者于 HXA-171 明确要求统一整理参考图片，现见 [UI 参考图索引](../references/ui/README.md)；这不改变本记录原始验收范围。
