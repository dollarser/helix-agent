# HXA-147 任务交互与移动界面统一

日期：2026-09-08。交付与非真机、非长稳验收完成；本地累计修改的 Git 收口单列，不声明已提交或推送。

## 目标与前置

在 M0～M11 合并后功能验证基础上，统一会话/Goal、Provider、工具结果与恢复交互。前置为当前 main（M11 合并交接基线 `4572911`）及本轮未提交的 Goal/恢复修复；不把该基线提交当作最终源码快照。官方 Claude Code Desktop、Cursor Agent、Android Compose 参考及移动端取舍见 [调研](../development/hxa147-interaction-research.md)。仅参考交互，不复制第三方实现。

## 实现及验收对应

下表证据位于 `build/main-verification/`，包含精确类列表/命令、安装 SHA-256、原始 instrumentation 状态码及适用截图；各轮属于各自源码快照，不能累加为一次最终全量设备测试。修复历程及旧失败记录见 [进展](../development/hxa147-progress.md)。

| 要求 | 实现与证明 | 证据 |
| --- | --- | --- |
| 新回复与历史阅读 | 末尾跟随、内容增长/视口变化/会话切换、主动上翻保持及显式回到最新；空态从开头显示 | `hxa147-empty-follow-result.json` 24项；最终长文本/时间线组合另28项 |
| 长目标/Provider/工具文本 | 同行展开与收起、完整文本语义、中英文可访问名称、48dp触摸区域；正文可滚动读至末尾 | `hxa147-summary-accessibility-touch-result.json` 28/28；含生产时间线与展开组件 |
| 会话/模式/Provider/文件/浏览器导航 | 紧凑会话设置、模式选择状态、Provider草稿可滚动取消；文件选项面板及浏览器操作换行 | `hxa147-provider-copy-fixed-matrix-*/result.json` 八组；`hxa147-navigation-normal-*/result.json` 普通尺寸八组；文件实际操作40/40 |
| 首次说明与发送确认 | 三语言移除旧ADR/未来规则描述，单列滚动披露明细，动作独立可达 | `hxa147-notice-result.json` 16/16；Consumer/Developer各自说明与长附件可读测试 |
| 审批/停止/重试/空态/加载 | 审批按钮随宽度换行；状态与Turn相符；停止关闭真实loopback流并结算；普通Chat显式重试，终态Goal不显示无效重试 | `hxa147-approval-layout-fixed-result.json` 24/24；`hxa147-retry-eligibility-result.json` 16/16；大字体实际流程矩阵24/24 |
| 小屏/大字体/中英文 | API29/36 × Consumer/Developer × zh-CN/en；320×640dp、2倍字体；设置/内容可滚动，操作在视口内 | `hxa147-font2-matrix-*/result.json` 八组24项及48张状态截图；跨页面上述八组各六张截图 |
| Goal创建/暂停/Continue/预算 | 真实模型UI创建不运行、预算暂停、扩预算不自动运行、显式Continue；短回复无需测试滚动即可完整可见 | `hxa147-final-real-ui-*/` 四组，每组两次模型调用；API36补采原截图并匹配原证据，没有重跑模型 |
| 真实工具与完成证据 | UI确认验收条件、显式批准，write/edit/PRoot实际制品及hash满足条件，Goal/run完成 | `hxa147-tool-completion-current-api*/*/result.json` 六组；分别Responses/Anthropic Messages/Chat Completions。非全协议笛卡尔积重跑 |
| 恢复与M11 | 完整Chat导航读取正常/丢失结果、精确确认，不重放原Job或更改Goal/Turn/Call/预算；四订阅平台完整Chat边界 | `hxa147-recovery-ui-verified-result.json` 四组；`hxa147-subscription-chat-boundary-result.json` 8/8方法。均为脚本模型/隔离Runtime fixture |
| 文件实际操作 | 列表/预览/重命名冲突/回收站/目录/分享及导入导出入口，独立临时AVD避免清空原工作区 | `hxa147-file-actions-result.json` 双API双发行包40/40；临时AVD停止与删除记录保留 |

最后恢复测试修正只改变 `ProotConversationNavigation.kt`：会话关闭时服务保留工具行，原等待条件不能证明会话已经打开；现在同一screen快照同时要求目标session与call。原API36失败保留，补齐清理参数后真实abort、设备marker消失、独立fixture复验通过。`hxa147-recovery-nav-source-delta.json`证明完整宿主快照后仅此测试源码改变，8个生产APK不变。不能把最初缺参数而被assumption跳过的清理算作通过。

## 修改范围与契约

App UI：`ChatScreen`、`ConversationTimeline`、`ExpandableSummary`、`ConversationComposer`、`AdaptiveConversationHeader`、`AdaptiveFileControls`、`TurnProgressLabel`、`EmptyConversationHint`、`ProviderScreen`、`ProviderTemplateNotes`、`FilesScreen`、`ApprovalCardScreen`、首次说明/披露组件和三语言资源；`ChatService.retryTargetFor`只复用现有Goal继续资格。浏览器限于 `BrowserScreen` 操作布局；附对应App UI/设备测试、测试导航辅助及文档。

无新增外部依赖、权限、数据模型或许可证义务，无远程执行/子Agent能力。决策记录：沿用 accepted [ADR-0004](../adr/0004-goal-run-wake-budget-semantics.md)、[ADR-0012](../adr/0012-capability-first-advanced-grants.md)、[ADR-0028](../adr/0028-goal-criterion-verification-bindings.md)。没有形成新的执行/授权/完成证据架构决定，故无新ADR。M11凭据所有权与consumer排除保持不变。

## 命令及结果

JDK17，roadmap所需命令均已执行：

```sh
./gradlew :app:testConsumerDebugUnitTest :app:testDeveloperDebugUnitTest :app:assembleConsumerDebug :app:assembleDeveloperDebug :app:assembleConsumerDebugAndroidTest :app:assembleDeveloperDebugAndroidTest --max-workers=1
./gradlew spotlessCheck detekt lintDebug lintRelease --max-workers=1
bash scripts/check-i18n.sh
bash scripts/check-docs.sh
bash scripts/verify-adr.sh
bash scripts/check-lockfiles.sh
bash scripts/check-secrets.sh
bash scripts/check-cli-runtime-boundary.sh
git diff --check
```

最终宿主将上述Gradle目标与全仓34个JVM任务、App及PRoot/CLI的Debug/Release构建合并执行，完整命令保存在 `hxa147-final-host-command.json`。exit 0、2713/2713，零失败/错误/跳过；34个Test任务实际执行，1185项源码配置指纹在执行期间不变，根Lint/格式/静态检查通过。`hxa147-final-host-audit.json`记录逐任务日志核对与8个生产APK SHA-256。导航测试修改后的测试APK构建/Spotless/Detekt也exit 0。文档、ADR28条、i18n932键、锁文件35项、凭据扫描、CLI边界及diff检查通过。

设备使用显式API29/36（本轮现有AVD5596/5598；文件操作临时AVD5610分别启动）。核心设备命令为：

```sh
adb -s <serial> shell am instrument -w -r -e class <precise-class-list> <package>.test/com.helix.app.HelixAndroidJUnitRunner
python3 scripts/run-proot-goal-delivery-failure.py --serial <serial> --adb <adb-path> --output <new-output-directory>
```

恢复正常路径加 `--normal-completion`；类列表、真实模型opt-in参数及每次安装指纹以表中JSON的完整command为准。状态码逐项核对，不能仅根据adb退出0或JUnit末尾OK判断通过。`hxa147-required-device-audit.json`再次核对关键34组/50项的状态码、安装hash与补采证据。人工复核包括2倍字体英文文件面板、中文真实Goal末行及此前各项截图；不是实际TalkBack语音或真机验收。

## 明确边界

24小时长稳与浏览器JNI/Binder累积调查按所有者要求后置；真机、自然LMK/Doze/OEM热限制、商店签名发布不在本次完成条件。API36两组真实Goal截图曾提取失败，原FAIL保留，后续只补采留存截图，证据与原设备JSON相符。真实服务是当时30008端口Qwen3.8-27B，不证明所有模型/账号可用；Claude/Grok付费调用与并行HXA-125受保护服务验收仍独立开放。全仓原946项App矩阵是功能阶段快照，本阶段按变化影响执行专项，没有冒称946项全部在最终UI APK重新执行。
