# HXA-189：第三轮审查复核、PFD 释放与权限入口修复

> 本页记录当时交付证据；决策链接指向当前主题归纳，不表示当时测试覆盖当前契约的全部范围。


日期：2026-09-10。状态：本轮源码修复、主机门禁与设备交接完成；设备执行尚未完成，不能据此关闭对应设备验收。未提交/推送，不操作 Claude 在途模拟器，不覆盖冻结 Browser APK。

决策记录：沿用 ADR-PERMISSIONS-003 的系统能力与 Agent 授权分离、ADR-RUNTIME-001 的独立 Runtime 生命周期；修复既有所有权契约和用户主动权限入口，不改 IPC/schema、风险级别、后台或审批语义。复用项目已锁定的 Android Test 依赖，没有新增生产依赖或升级版本。共享 transport、R8、完整发行 SBOM、全套 onboarding 等建议按证据和交付边界另列，不作为本轮已实现能力。

## 交付

- PRoot manifest 读端在成功/空流/超限/异常路径确定性关闭，写端使用 AutoCloseOutputStream；新增五个设备回归，详见 [Bug 记录](../bug-fixes/2026-09-10-proot-manifest-pfd-ownership.md)。
- 双 flavor 权限页提供通知申请、日历写权限、Notification Listener 和应用设置入口；返回刷新状态，developer 保留文件权限子页；没有进入页面即申请或自动扩大 Agent 授权。
- 空 Provider 会话列表增加设置入口，修正文案/审批卡 i18n 和空扩展分隔线；新增两项设备 UI 回归。
- status 瘦身、完成索引生成及覆盖门禁、HXA/ADR 历史状态澄清、gitignored 文档链接检查、专项文档导航与脚本分类。
- check-all 接入 CI：source/build/artifacts 同源，补 i18n、CLI 边界、Release 构建；锁文件与任务清单从 settings 派生。
- 全项处置及未采纳理由见 [复核表](../evidence/development/improvement-review-2026-09-10-followup.md)。

## 实际主机验证

环境：JDK 17、Android SDK 36。以下最终命令均 exit 0：

| 命令 | 结果 |
| --- | --- |
| `./scripts/check-all.sh --source` | 脚本逻辑自测4项；文档/索引、42 ADR/当前显式状态词、i18n三语1021键、Secret 检查通过 |
| `./scripts/check-all.sh --build` | Spotless、Detekt、全量JVM、所有库Debug/Release及App双flavor四种lint通过；四App Debug/Release构建通过；35个派生锁文件解析前后哈希一致 |
| `./scripts/check-all.sh --artifacts` | variant隔离与CLI Runtime/订阅入口/普通聊天路径边界通过 |
| `./gradlew :runtime:proot-ipc:assembleDebugAndroidTest :app:compileDeveloperDebugAndroidTestKotlin :app:compileConsumerDebugAndroidTestKotlin` | 新设备夹具APK/双flavor测试编译通过；没有运行 instrumentation |
| `git diff --check` | 通过 |

JVM XML 口径：每个 JVM 模块取 test，每个 Android 库/Runtime 取 testDebugUnitTest，App 取 consumer/developer Debug，避免 Release 重复累计。合计 **2796 项：2788 通过、0失败、0错误、8条件跳过**；包含 PRoot IPC 40项、QuickJS85项、浏览器117项、Root21项。Gradle 的 UP-TO-DATE/from-cache 保留在日志，不宣称全部强制重执行。

最终静态/测试/lint阶段41秒，Debug/Release构建阶段39秒。临时日志在本机忽略目录 `build/debug/2026-09-10/review-{source,host,artifact}-gates.log`，设备编译为 `review-fixtures.log`。初次格式与KTX lint失败已修复，不把失败轮次计为通过。CI工作流已配置，本轮验证来自本地同入口，不冒充远端CI执行。

## 设备边界与剩余工作

新增 PFD 五项、权限/Provider UI 两项已编译，**未执行**。系统权限允许/拒绝/永久拒绝/设置恢复、API29与API36兼容性、原顶部/All-files回归，交给 Claude 自建独占模拟器执行；具体类、命令与条件见复核表末节。不能借用在途 EV-02 模拟器；长稳的资源门限与结论保持原记录。

冻结 Browser 测试APK仍为 `b9382e9768568bf71f6e4536b3485388e49e08e108d460a68b036f2c29583778`。本轮没有启动、关闭或接管任何模拟器，没有安装真机App，没有改变其他任务的设备数据。并行源码/测试记录保留；本记录不表示全部未提交工作由本轮产生。

完整 SBOM/NOTICE、R8设备行为、签名/发行仍是发布前工作；深色模式/完整onboarding/备份/搜索为产品增量，未暗中扩成当前修复范围。
