# HXA-167：单行横向选项栏

Date: 2026-09-09
Status: completed

## 交付

输入框上方统一为模式→模型→推理→输入原文复制的单行选项。使用 ComposerOptionRow 的 horizontalScroll，在窄屏、大字体或新增选项时左右滚动，不使用换行或增加第二排。输入原文复制也移入该行，避免输入内容出现后额外占用一排。

后续新增输入框选项应复用 ComposerToolbar 的 trailingOptions/ComposerOptionRow，不另起 Row/FlowRow；现有模型选项仍通过 modelSelector 注入。仅更改布局，保持模型切换、推理、菜单锁定、发送和停止语义。

## 精确验证

```sh
./gradlew spotlessApply :app:testConsumerDebugUnitTest :app:testDeveloperDebugUnitTest :app:assembleConsumerDebug :app:assembleDeveloperDebug :app:assembleDeveloperDebugAndroidTest detekt :app:lintConsumerDebug :app:lintDeveloperDebug --max-workers=1
./gradlew spotlessApply :app:assembleDeveloperDebugAndroidTest spotlessCheck detekt --max-workers=1
adb -s SERIAL shell am instrument -w -r -e class com.helix.app.ui.ConversationComposerDeviceTest,com.helix.app.ui.SessionModelDeviceTest,com.helix.app.ui.ChatStopProgressDeviceTest com.helix.agent.developer.test/com.helix.app.HelixAndroidJUnitRunner
adb -s SERIAL shell am instrument -w -r -e class com.helix.app.ui.ConversationComposerDeviceTest com.helix.agent.developer.test/com.helix.app.HelixAndroidJUnitRunner
bash scripts/check-docs.sh
bash scripts/check-i18n.sh
bash scripts/verify-adr.sh
bash scripts/check-secrets.sh
git diff --check
```

最终 host exit 0：双 app JVM（Consumer 343 / Developer 367 项，各 4 项既有条件 skip，无失败）、双 flavor APK、Developer 测试 APK、双 flavor app lint、格式和 Detekt 通过。生产代码验证通过后仅补充测试；最终测试 APK、spotlessCheck、Detekt 重跑通过。

API36 首轮 8/8；补充复制选项断言后 Composer 类最终 4/4，其中为重复执行，共 8 个不同用例。240dp/1.8 倍字体下验证真实 swipeLeft 导致横向位移、屏幕外推理/复制可滚动到可见、返回首项、各项处于同一行。原模型跨 Provider 切换及停止/失败/Goal 回归通过。补充测试初次误用顶边相等判定不同高度图标与文字处于同一行，失败后改验垂直中心对齐（允许像素取整误差），未删除滑动/可见性断言。

真机 API34 覆盖安装成功，截图核实模式、Qwen3.8-27B 与推理选项处于同一行；当前文字长度在真机上可完整放下，超宽滑动证据来自专用窄屏模拟器。未发送真机模型请求，未操作其他测试模拟器。

Developer APK SHA-256：`441292fcd022250b0210ce11b0efbf0625c1c8d6285eaeb7653648cd8a7e7506`。验证日志在忽略的 build/phone-polish，真机私有截图不入库。

## 决策与边界

决策记录：不适用：既有输入 UI 的可逆布局调整，无依赖、存储、权限或跨模块契约变化。

实现位于 codex/phone-chat-polish，未提交、未合入 main、未推送。未宣称全仓库、根 lint、API29 或长稳验收完成。
