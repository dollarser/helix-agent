# HXA-145 完成记录

- 日期：2026-09-06；前置 HXA-144 / `8631c12`。
- 目标/实现：Grok 受管理 developer Provider、固定 GrokLoginActivity 入口、Runtime 独立 vault/过期刷新、Responses 编解码、`store=false` 与输出预算。Claude/Grok 共用 `SubscriptionHttpModel` 的超时、取消、凭据获取、HTTP 失败与流限额，未复制两套生命周期逻辑。文本-only，工具/图片能力关闭。
- 修改模块：cli-app、developer Provider/设备测试、边界脚本与任务文档；无 Room/IPC schema/新外部依赖。
- 决策记录：沿用 accepted [ADR-0027](../adr/0027-subscription-provider-job-routing.md)、ADR-0025；在既定封套内增加已列举的平台，不形成新架构决定。
- 真实 Grok 付费调用：按所有者决定保持未核实；不将 fixture 或登录成功宣称为模型服务可用。Copilot Provider 尚未实现。

## 证据和命令

访问日期 2026-09-06：[官方 Responses 对照](https://docs.x.ai/developers/model-capabilities/text/comparison)、[官方文本生成](https://docs.x.ai/developers/model-capabilities/text/generate-text)、[官方 Grok Build 来源](https://github.com/xai-org/grok-build)。公开文档支持 Responses 形态及 store=false，但不证明消费订阅资格；最初猜测的 inference/responses 文档 URL 无法读取，改用上述真实链接。[参考插件 Grok adapter](https://github.com/V1ki/dsh-plugin-subscriptions/blob/main/src/providers/grok.ts) 与本地 0.7.0/MIT 包仅用于比对，未复制代码、导入凭据或打包 CLI。

`./gradlew :runtime:cli-app:testDebugUnitTest :runtime:cli-client:test :app:testConsumerDebugUnitTest :app:testDeveloperDebugUnitTest :app:lintDeveloperDebug :runtime:cli-app:assembleDebug :app:assembleDeveloperDebug :app:assembleDeveloperDebugAndroidTest :app:assembleConsumerDebug --no-configuration-cache --no-daemon --max-workers=2`：exit 0，696 tasks。

新增 Grok JVM fixture 后：`./gradlew :runtime:cli-app:testDebugUnitTest :runtime:cli-app:assembleRelease -Phelix.cli.r8=true --no-configuration-cache --no-daemon --max-workers=2`：最终 exit 0，128 tasks；Grok 3/3、Claude 回归 3/3，通过凭据隔离、刷新、预算/存储字段、quota 单次请求与终态解析。首轮 fixture 缺少 SSE event 字段导致失败，补成现有 Responses reader 所要求的命名事件后通过，未删除测试或放宽生产解析。

API 29 `emulator-5602` / API 36 `emulator-5604`，均 arm64-v8a 独占 M11 Test AVD，安装当前 Runtime/developer/测试 APK 后执行：

```sh
adb -s <serial> shell am instrument -w -r -e class 'com.helix.app.provider.CodexSubscriptionProviderE2eDeviceTest#grokProviderUsesTheNormalModelContract,com.helix.app.provider.CodexSubscriptionProviderE2eDeviceTest#grokAccountUsesItsOwnExplicitActivity,com.helix.app.provider.CodexSubscriptionProviderE2eDeviceTest#claudeProviderUsesTheNormalModelContract,com.helix.app.provider.CodexSubscriptionProviderE2eDeviceTest#developerProviderUsesTheNormalModelContract,com.helix.app.provider.CliRuntimeHandshakeE2eDeviceTest#grokRunningCancelAndDeathNeverReplay,com.helix.app.provider.CliRuntimeHandshakeE2eDeviceTest#grokPayloadSurvivesDisconnectAndIsDeletedAfterReconcile' com.helix.agent.developer.test/com.helix.app.HelixAndroidJUnitRunner
```

两台均 `OK (6 tests)`，API 29 4.018 s / API 36 5.746 s，无 skip。覆盖三 Provider 对话、Grok UI、RUNNING 取消/死亡恢复、PFD 对账删除。两设备 `dumpsys activity services com.helix.runtime.cli` 均 `(nothing)`。

`check-cli-runtime-boundary.sh`、`check-lockfiles.sh`、`check-secrets.sh`、`check-docs.sh`、`verify-adr.sh`、`check-i18n.sh`（均位于 scripts）、`git diff --check` 全通过。无新增外部依赖、许可证或权限；APK/consumer DEX 检查沿用 HXA-144 脚本。Runtime R8 实际执行，仍仅沿用已审计的可选 TLS 类型豁免。

SHA-256：developer Debug `3fb9c3ce6832502e39df1abb0c6bfcb437b5e296ba2795dcea5881f0b0d3baff`；Runtime Debug `1b6ec096b1c9ca704a22a57d7488fdb7bdf9cb06f60c739c96a0980ac3018177`；Runtime R8 unsigned Release `4986de09ad2c30a66a1acc05ee507b25d1acd4796955706e50705560322f2334`。未消耗真实订阅额度；最终分发/真机/外部账号与动态目录不在本次已验收范围。
