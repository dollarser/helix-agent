# HXA-206：核心产品闭环综合验收

日期：2026-09-21。所有者授权完成 199/206 后，在独立 `codex/acceptance-199-206` 工作树执行，基线 `215b7d81`。本记录关闭默认本地核心产品范围，不关闭终端物理专项、真实外部账号或发行。逐场景、制品、失败原因和原始日志入口见[验收记录](../evidence/development/acceptance-199-206-2026-09-21.md)。

## 交付与修复

- 实际聊天/Dispatcher/文件/产物旅程验证读→写→读→打开。三预设与 CUSTOM、ASK 批准、DISABLED 和 CUSTOM DENY、取消、显式重试及进程恢复均按现有授权和结算路径执行。
- 集成 Tasks、Plan 审阅到执行、能力准备/修复、扩展安装/启用/调用/禁用、搜索/主题和 JSONL 导出。不增加专用执行器、规划器或 Agent 工具；普通文件任务不需要 Goal、PRoot 或子 Agent。
- 修复只读 Git status/diff 可能执行仓库 clean filter 的问题：仓库局部禁止外部进程，根目录/子目录比较实际工作树字节，保留配置。详细不变式见[Git 修复记录](../bug-fixes/2026-09-21-readonly-git-filter.md)。
- 验收器从类名/声明值校验改为原始逐方法事件、实际 APK、owner/closed 和固定源清单；条件跳过、失败和重复执行不计通过。两阶段恢复保留原始日志，并按已记录的旧 PID 分离，不能丢弃当前进程失败。

决策记录：[ADR-PERMISSIONS-001](../adr/permissions/001-session-authorization.md)、[ADR-PERMISSIONS-003](../adr/permissions/003-dispatch-and-audit.md)、[ADR-WORKSPACE-003](../adr/workspace/003-git-boundaries.md)、[ADR-RUNTIME-001](../adr/runtime/001-execution-domains.md) 均为 accepted；本轮没有改动决策，不增加远端 Git、凭据或 Agent PTY 权限。

## 验证

- `./scripts/check-all.sh --all` 最终 exit `0`，日志 `build/acceptance-baseline/full-gates-final-v2.log`：源码、格式、detekt、完整 debug/release lint、全部 JVM 测试、双 flavor debug/release APK、36 个锁文件及变体/订阅边界通过。storage/双 app 测试 APK 构建、release 集成 Runtime APK 检查另外 exit `0`。
- API29/36 × consumer/developer 共 52 个独占批次：**432 方法通过、20 条件跳过、0 失败、0 缺失**，另 **4 次扩展独立进程恢复通过**。跳过仅为 consumer 不适用的私网 MCP/Linux fixture 和单列执行的 seed/recover 阶段，原因保留；不把跳过计为通过或把方法数当产品成功率。
- 固定完整文件任务 **16/16 成功**；同 fixture 在保存的基线包上也为 **16/16**，使用同一测试 APK，五个指标没有退化。Full/Workspace/Custom 各 5 次语义操作、0 张审批卡，Read-only 6 次、1 张；产物可打开、写执行一次、打开新增模型调用为 0。
- Git debug 四象限及实际非 debuggable release 四象限通过；release 每次 3 次普通 UI 操作打开 diff，恶意命令标记不存在、配置原文未变、指定 HTTP fixture 请求为 0。最终独立准备类在修正阶段限制后四象限重验通过。
- 四象限 JSONL 实际系统选择器与独立解析通过；浅/深主题、大字体、页面和进程恢复适用矩阵通过。完整关联/中断/取消与资源限制复用本轮已执行的导出类；picker 小会话不冒充完整备份。
- 新增设备类 `ProductFileJourneyDeviceTest`、`GitReadOnlyBoundaryDeviceTest`、`GitReleaseFixtureDeviceTest` 均实际执行。基线对照、升级和普通页面恢复另列，owned 进程全部正常关闭。

## 边界与后续

使用固定模型/loopback 协议验证产品链路，不评价真实模型能力；consumer 公共 MCP 及 HXA-125/190 真实账号/服务不由 developer 私网 fixture 替代。release 包使用本机 debug 证书进行替换验证，不是签名发行。

主体矩阵保留执行时的主/测试 APK 哈希；最后只修改独立 release 准备类的阶段保护，该类不在主体 52 批次内，已单独重跑。未受影响类的源哈希重新复核，不伪称所有批次都使用最后一次测试 APK。

HXA-199 的模拟器、实际默认时长、双 shell 恢复与覆盖升级已经补验，OEM/HOME/锁屏/Doze/热压/真实 16 KiB 仍无物理证据，保持未完成。196 原真机缺口、125/190 外部账号和 120→122→121→123 发行边界继续独立记账。本轮只作本地具名提交，不自动推送、main 合并或发布。
