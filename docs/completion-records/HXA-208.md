# HXA-208：完整 Goal 工具与前后台连续运行

> 本页记录当时交付证据；决策链接指向当前主题归纳，不表示当时测试覆盖当前契约的全部范围。


日期：2026-09-16。状态：**completed（本任务范围）**。基线 `main` 的 `bcc2a6dc`；本记录随 Goal 实现提交。完整证据、失败分诊及精确重跑命令见[专项验收](../evidence/development/hxa208-goal-acceptance-2026-09-16.md)。本范围无已知待实现项或未解决验收阻断，不代替其他产品任务或发布验收。

## 决策记录：

采用所有者明确授权的 [ADR-GOAL-001](../adr/goal/001-lifecycle-and-completion.md)，部分替代 ADR-GOAL-001/0039 每轮手动继续约束，保留 ADR-GOAL-001 完成判断。参考 DSH 的激活、同会话空闲准入和模型工具面；继续使用 Helix 原 Loop、Room、累计预算及 Dispatcher，不重建 event sourcing 或新增授权体系。

## 交付

- Goal 模式发送和继续可前后台自动跨轮推进；轮间保持用户启动的前台服务。前轮、激活、会话、Provider 快照和预算共同限定下一轮；停止及新消息撤销旧准入。多个目标优先沿用最近实际执行的目标。
- 完整 `create_goal` / `get_goal` / `update_goal`，包含目标、预算编辑、读取状态/激活/版本、暂停/恢复及结构化完成报告；兼容 `goal.report`。自动轮不能伪造当前直接用户请求来新建、改目标或放大预算。
- Room19 保存归属及 CAS 待编辑，成功结算后应用，取消/失败/进程恢复丢弃；UI 目标、预算编辑同样检查版本。Plan 目标不绕过审阅，删除及会话清理覆盖新持久数据。
- Android 启动或提升服务拒绝、服务丢失与超时都有持久暂停原因；系统中断后显式继续。进程恢复不重新激活、不补充已消耗额度、不重放未知副作用。

## 验证

`./scripts/check-all.sh --all` 全通过：主机4414通过/8既有条件跳过/0失败；Debug/Release build、lint、依赖锁、制品边界通过。双 flavor AndroidTest 与存储 AndroidTest 编译通过。

API29/36 × consumer/developer：主矩阵各45通过；随后 FGS 补充各11通过，最终 Goal 创建/编辑/新输入/选择专项各10通过，全部0失败/0跳过。各表为分批执行、包含重复回归，不把其和当作独立用例数。真实两次 SIGKILL × 四象限通过；API36真实服务超时回调双flavor通过；Room完整迁移链API29/36各32通过。独占模拟器全部finally关闭，既有其他模拟器未操作。

可复跑总入口：`bash scripts/debug/2026-09-16/run-goal-acceptance.sh`。新加入的两项用例已在专项矩阵通过，当前总入口会一并纳入；未声称历史45项日志已经包含后来新增用例。

## 边界与后续

本次没有依赖未来的 Schedule、Channel、Hooks、Code Mode 或子 Agent。外部事件自动激活仍需单独授权；系统强停/回收可中断运行，不承诺无限后台驻留。真实账号、OEM真机和长稳未以loopback或模拟器替代。其他产品链继续按status/roadmap，不重做本HXA。仅具名本地提交，不push/merge；独立审批ADR草案不夹带。
