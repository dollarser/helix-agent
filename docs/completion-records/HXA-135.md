# HXA-135 完成记录

- 目标：把已验证的 Codex 订阅 Runtime 通道注册为 developer/Advanced 的普通 `ModelProvider`，与 API-key Provider 共用会话选择和 ChatService 执行路径，同时保持 consumer/store 完全排除。
- 前置任务/commit：HXA-134 / `2bfc296`。
- 修改文件：`ProviderFactory`/`ProviderService` 扩展 seam、Provider UI 受管理标记和实验披露、consumer/developer 同 FQN 渠道模块、developer `CodexSubscriptionProvider`、AppContainer 接线、资源、设备 E2E、边界脚本和 M11 状态文档。
- 注册与 UI：developer 首次初始化写入稳定 `subscription-codex` Provider 配置行，显示 “Codex Subscription (experimental)” 和非官方/协议漂移/账号风险提示；配置、删除和 capability 手工覆盖被禁止。该行与 API-key Provider 共用 Provider 列表、Session 外键和对话选择，不要求日常打开 Runtime APK。
- 执行路径：`CodexSubscriptionProvider.stream(ModelRequest)` 只调用 HXA-134 的 `CliModelJobClient`，返回统一 `ModelEvent`；`ChatService` 继续负责请求历史、egress、turn/model-call/message 持久化、终态校验与取消。主 App 仍不持有或读取 token/account id。
- 工具边界：首版只记录真实证明过的 streaming；`toolCalls=false`、`vision=false`、其余未证明能力为 false/null。订阅模型因此不能进入依赖工具能力的 Act/Goal，也不存在把 CLI/订阅能力解释为 Helix Tool Approval 的路径。
- 自动与构建证据：准确矩阵命令 `./gradlew :app:testConsumerDebugUnitTest :app:testDeveloperDebugUnitTest :app:assembleConsumerDebug :app:assembleDeveloperDebug :app:assembleDeveloperDebugAndroidTest :app:lintDeveloperDebug :runtime:cli-client:test :runtime:cli-app:testDebugUnitTest :runtime:cli-app:assembleDebug --no-configuration-cache` 通过（696 actionable tasks）；app consumer/developer JVM 分别执行 262/274 tests。`./scripts/check-cli-runtime-boundary.sh` 通过，consumer APK DEX 字符串扫描不含 `CodexSubscriptionProvider`、`runtime/cli/client` 或 `subscription-codex`。额外运行 `lintConsumerDebug` 暴露仓库既有 9 个 PRoot developer-only 字符串 `UnusedResources`，不属于本 HXA，consumer APK 构建仍通过。
- 设备证据：独占 `emulator-5602`（API 29，arm64-v8a）与 `emulator-5604`（API 36，arm64-v8a）各通过 `adb ... am instrument ... CodexSubscriptionProviderE2eDeviceTest#developerProviderUsesTheNormalModelContract` 1/1。测试证明受管理 Provider 注册、连接测试、保守 capability、直接统一事件，以及 `ChatService.createSession/send` 后 `COMPLETED` Turn、1 条 model-call 和持久 assistant `HELIX_OK`；两台设备的 `dumpsys activity services com.helix.runtime.cli` 最终均无 `ConnectionRecord`/`Bindings:`。
- 真实账号证据边界：HXA-140/141 已在独占 API 36 上使用真实 Codex 订阅 vault 两次得到 `gpt-6-astra` 的 `HELIX_OK`；本 HXA 没有触碰保留登录态的 `emulator-5600`，因此没有把既有 Runtime 真实调用误报为“Helix 对话入口重新实测”。
- 权限/许可证/供应链：无新增权限、Maven repository、依赖版本、第三方代码或 endpoint。`runtime:cli-client` 仍只属于 `developerImplementation`，consumer 的同名模块是 no-op。
- 产物证据：consumer APK `50191309` bytes / SHA-256 `45ad3e7b3fc088c4a7b6f2eb0d4f84a6f77c8ee75d5b7067010789071bf30a34`；developer APK `52907402` bytes / SHA-256 `87b68b4f27c0fe2d415107a794f872c746b99c3e1ec9e9ca1b20ac1c3432499d`；developer androidTest APK `3504220` bytes / SHA-256 `33bd24031733149ff2cb8c9469d68c293531abc6eb2aa02ff1e6c85025b9eda1`；CLI Runtime APK `9093903` bytes / SHA-256 `18c9e7f240059b0dcca99049fb5d3a58004a58b265a052df0a287dc493d509f2`。
- 仓库门禁：`./scripts/check-lockfiles.sh`（34 lockfiles）、`./scripts/check-secrets.sh`、`./scripts/check-docs.sh`（170 Markdown / 119 HXA）、`./scripts/verify-adr.sh`（25 ADR）、`git diff --check` 均通过。
- 决策记录：无需新 ADR；这是 accepted ADR-0025 要求的 developer Provider 选择整合，并继续遵守 accepted ADR-0007 的独立 UID、签名 Binder、冷绑定和 jobId 对账。
- 后续：真实账号从 Helix 对话入口的小额度验收、模型目录/重新认证入口，以及 Claude/Grok/Copilot Provider，必须以各自资格和 endpoint 证据作为后续独立 HXA；consumer/store 继续 fail closed。
