# HXA-202：任务过程与跨页面操作导航

> 本页记录当时交付证据；决策链接指向当前主题摘要，不表示当时的测试覆盖了当前契约的全部范围。

日期：2026-09-17。状态：**completed（本任务范围）**。基线为 `fea0ac0d`，本轮 4 个代码提交 `50ff7432`～`a21c5478` 加本记录提交；在 main 工作树与 `32788bf8` 的隔离 worktree（干净提交状态）验收。并行交付的 HXA-094/095 Root 验收（`1daac902`，merge `32788bf8`）与本任务代码零重叠，不属于本轮交付。

## 契约与交付

决策记录：不新增执行器、任务状态枚举或持久状态机，任务页状态是 Turn/Goal/审批/结算持久事实的只读投影；六类展示状态只由持久事实派生——PAUSED 不计为运行、单个 Turn 结束不标记 Goal 完成、不伪造进度百分比、"需要确认"只指示未知副作用与恢复事实。停止以稳定 ID 为目标，走 RUNNING_TOOL → CANCELLING → CANCELLED 的既有持久路径，CANCELLING 一直显示到持久结算完成，竞态（重复点击、页面重建、目标已结束）全部幂等。观察/打开页面、刷新与重建不启动任务（不产生新 Turn/Goal run/审批记录）。实时日志在 195 接入；195 交付前界面说明任务结束后可查看过程，不声称实时流。

- 切片 ①（`50ff7432`）：新增 `TasksDashboard` 状态投影与 `TasksDashboardProjectionTest` 单测；TasksScreen、GoalSummaryUi、BackgroundTaskDialog 全部改为读稳定 ID 事实。
- 切片 ②（`1bd655fe`）：跨页入口按 sessionId/goalId/turnId/planId 定位；Goal 入口实际切换到归属聊天页且不调用 Continue；新增 `TaskArtifactsDialog` 按真实归属列出产物。
- 切片 ③（`22ce99a2`）：TurnCoordinator 精确停止持久化 CANCELLING → CANCELLED，竞态安全；`TurnCoordinatorTest` 新增覆盖。
- 切片 ④（`a21c5478`）：`TaskJourneyDeviceTest`（767 行）旅程设备测试，6 项。
- 切片 ⑤（本记录）：公共 P1/P3 与主机 `--all` 收口，记录准确范围。

## 实际验收

| 门禁 | 结果 |
| --- | --- |
| P1（每任务） | `check-all.sh --source` + `spotlessCheck detekt` + `git diff --check` 全部 exit 0，记录时点主线源，日志 `build/slice5-p1.log`（13:33） |
| P3（产品 UI/构建） | 双 flavor 单测、4 个 APK 组装（含 AndroidTest 包）、双 flavor lint、runner `--help` 全部 exit 0，日志 `build/slice5-p3.log`（13:33） |
| 主机 `--all`（本任务代码） | `check-all.sh --all` exit 0（source+build+artifact 全段），源 `a21c5478`，日志 `build/slice5-checkall.log`（13:08） |
| 主机 `--all`（主线 `32788bf8`） | 隔离 worktree 复验 exit 0（source+build+artifact 全段），日志 `build/slice5-checkall-head.log`；worktree 唯一额外改动是把 `ProotNetworkProbeDeviceTest.kt` 的既有格式化违规修正（该违规随 `e28f4d14` 提交进入 main，当前并行会话正在其工作树内修复、未提交，本任务未改动该文件），其余全部为干净提交状态 |
| 四象限设备 | API29/36 × consumer/developer 各 `OK (6 tests)`（12.6s/19.5s/14.1s/20.6s）；`build/hxa202-matrix/` manifest 4 条均 exit 0，矩阵日志 `build/slice4-matrix.log` 末行 `RESULT: PASS (all four quadrants zero)` |
| 真实进程恢复 | 每象限同安装两阶段：setup 记录进程 PID 后杀进程（`recovery-setup.txt` 记录 "Process crashed"），verify 阶段以**不同 PID** 打开同一数据库，历史、产物归属与 Goal 状态原样恢复且零新增执行 |

设备验收使用自建独占模拟器 ×4（1080x2400@420，端口 5566/5568/5570/5572，`-read-only` 干净基线磁盘、finally 仅关闭自启进程组）；矩阵先把 4 个 APK 从当前源码全部重建（产物与源码对应），再按象限跑 `adb shell am instrument` 两阶段协议。六项旅程与验收条款一一对应：跨会话任务按稳定 ID 打开各自聊天页（`crossSessionTasksOpenTheirOwnChatPages`）、Goal 入口切换归属会话且不调 Continue（`goalEntrySwitchesToTheOwningChatPageWithoutContinuing`）、打开/重建/前后台不启动任何任务（`openingRecreatingAndBackgroundingTheDashboardStartsNothing`）、CANCELLING 持久显示至结算且竞态幂等（`stopPersistsCancellingUntilSettledAndIsRaceSafe`）、产物按真实归属列出（`taskArtifactsDialogListsByRealOwnership`）、历史跨进程重启不重放（`journeyHistorySurvivesARestartWithoutReexecution`）。单象限冒烟证据 `build/hxa202-smoke29/`（OK 6 tests，12.5s）。

rotation 覆盖说明：测试源无 UiDevice 依赖、AVD 竖向锁定，配置变化经由同一 activity 重建路径覆盖（`recreate` ×2）加真实 moveTaskToBack(false) 前后台循环（见测试 KDoc）。

## 全套边界与后续

- 四象限旅程通过 ≠ 全产品设备套件通过：本轮只跑 `TaskJourneyDeviceTest`（6 项）与投影单测；全量套件验收属于 206。206 用户路径证据已在本批积累（会话 → 任务 → 产物 → 来源），但 206 未提前关闭。
- 任务页与工具结果接入真实命令详情是 194 的范围；产物打开/导出与来源返回闭环是 203 的范围；202 只提供稳定 ID 入口、跨会话定位与按真实归属的产物查询。
- 195 之前不声称实时日志流，界面文案以"结束后可查看"为限。
- 记录时点 main 工作树带有并行会话的未提交进行中改动（Goal*/proot 测试与 GoalLifecycleService.kt 等）；该时点 main 树 `--all` 仅在那两个进行中文件报 spotless/detekt 违规（证据 `build/slice5-checkall-2.log`）。另主线 HEAD 提交本身在 `ProotNetworkProbeDeviceTest.kt` 有一处 spotless 格式化违规（随 `e28f4d14` 进入 main）：并行会话已在其工作树内修复（未提交），上表 worktree 复验即套用该已知修复后全绿。本任务未改动这些文件。
- 环境注记：本 checkout 的 Gradle 9.5.0 命令行任务路径只接受冒号形式（`:runtime:cli-app:assembleDebug`），旧斜杠形式会被 `DefaultBuildTaskSelector` 直接拒绝；仓库门禁命令（check-all.sh、verification-matrix.md）均为冒号形式。
