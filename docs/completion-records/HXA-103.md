# HXA-103 资源/稳定性（完成记录）

- 日期：2026-09-05
- 目标：收口 API 29/34/35/36 的资源与生命周期门禁，补上 WebView 延迟回调/FD 泄漏回归，并复核 dataSync FGS、PRoot/CLI 生命周期边界。
- 前置任务/commit：HXA-102，`0d1ede9`。
- 修改文件：`feature:browser` 的 `WebViewTabHost` 与 `WebViewResourceLifecycleDeviceTest`；本 completion record。
- 实现的契约：新 fixed evaluation 会先移除旧 deadline；WebView 销毁会立即失效所有 evaluation、移除 Handler callback、暂停页面、解绑 WebView/WebChrome/download client 后销毁，避免 deadline 闭包与页面 callback 在 tab 结束后保活。销毁后的 evaluation fail closed 为 `null`。设备测试做 2 次预热和 12 次真实 WebView create/destroy，要求目标测试进程 FD 漂移不超过 8，并等待超过 5 秒 deadline 证明无迟到 callback。
- 既有契约复核：`DataSyncForegroundServiceDeviceTest` 覆盖声明的 `dataSync` 类型、可停止通知、等待审批即停止及 API 35+ `onTimeout()`；`AndroidResourceGateDeviceTest` 覆盖真实前后台与 memory/thermal 采样；旋转由两个 RunControl UI suite 覆盖配置重建。PRoot HXA-086 acceptance 脚本仍覆盖冷绑定、空闲进程回收、强停拒绝、主/companion 分阶段 kill、同 jobId 不重放、停止通知，并采样 companion UID 无 FGS/wake lock；CLI companion 维持 bound-only、无 FGS/wake lock，API 36 manifest suite 3/3 passed。
- 自动测试：`./gradlew test spotlessCheck detekt --no-daemon` 最终 exit 0；解析全仓 XML 为 2,392 tests（consumer/developer 等 variant 均计入）、0 failure、0 error、0 skipped。首次同命令因新增设备测试显式 GC 被 detekt 拒绝（exit 1），移除该调用后通过。首次 app 设备矩阵误填不存在的聚合类名 `RunControlUiDeviceTest`（exit 1）；改用实际 `RunControlModeUiDeviceTest` 与 `RunControlSettingsUiDeviceTest` 后四台均通过。
- 设备矩阵：`Helix_API_29` API 29、arm64-v8a、4,096-byte page；`Helix_Verification_Root_API34` API 34、arm64-v8a、4,096-byte page；`Helix_Verification_API35` API 35、arm64-v8a、4,096-byte page；`Helix_API_36` API 36、arm64-v8a、4,096-byte page。WebView resource suite 每台 2/2、0 skipped；app FGS/resource/rotation suites 每台 6/6、0 skipped。API 35 报告曾提示仅取到 1/2 case 的 logcat，但 instrumentation 2/2 passed，不把缺失 logcat 当作 24 小时证据。
- PRoot/CLI 设备结果：`scripts/accept-hxa-086-lifecycle.sh emulator-5556` 在构建、四 APK 安装后，于首个 RootFS host setup 真实失败（exit 1）：`Failure(stage=archive, reason=runtime/rootfs/alpine-minirootfs-3.22.5-aarch64.tar)`，因此本次没有把后续 PRoot lifecycle phase 计作通过。`ANDROID_SERIAL=emulator-5556 ./gradlew :runtime:cli-app:connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=com.helix.runtime.cli.app.CliRuntimeManifestDeviceTest --no-daemon` exit 0，3/3、0 skipped。
- 未验证门禁：本次没有 24 小时连续运行、真实 Doze、secure keyguard、OEM 热限制、物理设备、16 KiB page-size、真实低内存 kill 后全链恢复证据；断网仅有既有 deterministic transport/browser failure tests，没有 24 小时网络切换 soak。PRoot RootFS archive 不在仓库且设备安装前置失败，故 PRoot lifecycle 本轮为 blocked evidence，不是绿色。上述项必须在具备 RootFS 与物理设备的 release matrix 中完成，skip 或旧 HXA 记录均不计作本次通过。
- 权限/数据/许可证变化：无新权限、依赖、网络、数据格式或许可证；未引入 wake lock，也未把 FGS 当作进程存活保证。
- 决策记录：不适用；这是既有生命周期契约的资源释放修复与设备门禁，没有改变 accepted ADR。
- 风险或后续任务：4 KiB emulator 的短时资源门禁证明回归边界，不代替长稳或物理设备验收。HXA-103 代码范围完成，但上述外部矩阵仍是发布阻断项，不能据此声称 M10 release gate 全绿。

## 2026-09-08 当前 API35 与真实内存压力补验

本轮使用当前主 APK，在独立 API35 模拟器运行 Consumer/Developer 的 FGS/resource/模式与配置重建，每变体7/7；WebView 两项普通资源生命周期2/2，未运行两个长稳/原生压力入口。记录 `build/main-verification/current-resource-api35-result.json`、`current-resource-api35-browser/result.json`。本轮启动的 API35 模拟器已正常关闭，既有 API29/36 继续供当前任务使用。

新增显式宿主夹具 `scripts/accept-resource-pressure.py`、`scripts/fixtures/resource-pressure.c` 与 ResourcePressureDeviceTest。原生分配器填充不可轻易压缩的实际内存页，上限1280MiB、45秒，测试等待上限90秒；宿主只对明确选定的 emulator 运行。App 保持真实前台，通过生产 PlatformDeviceResourceProbe/AndroidResourceGate 观察平台 MemoryInfo。低内存时额度1；只有宿主确认分配器退出后才接受健康状态恢复至2。

最终 API29 分配904MiB、API36分配804MiB，各1/1通过，allocatorExit=0。恢复时可用内存分别1170432000、1028665344字节，阈值226492416字节；逐样本及首次低内存值见 `resource-pressure-release-summary.json` 和两个 `resource-pressure-release-api*/memory.csv`。主 App 持续存活，证明真实低内存门控及释放恢复，不是自然 LMK/OOM 杀 App 后恢复。原始首轮观测及加强退出确认前的结果分别保留，不合并为唯一案例数。

精确命令由结果 JSON 保存：`python3 scripts/accept-resource-pressure.py --serial <emulator> --adb <adb> --compiler <NDK aarch64-linux-android29-clang> --output <new-directory> --max-mib 1280`。两台各自产生的远端二进制、CSV 与退出确认标记均核实不存在，检查命令/结果收入摘要。测试构建/Spotless/Detekt通过（`resource-pressure-release-verified-build.log`），Python编译检查通过。无生产修改；2713 JVM/Release与前轮受影响设备证据保持其原快照。

结合已完成的实际旋转、SSE断开、前后台/通知停止、后端强杀不重放、PRoot冷绑定与显式forced idle、普通WebView/QuickJS/文件模块回归，本轮已定义的非真机、非长稳资源场景收口。真机自然Doze/安全锁屏/OEM热限制、自然低内存kill与24小时长稳仍为独立发布矩阵，不能据本轮声明发布门禁全绿。
