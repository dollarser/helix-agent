# HXA-158：未使用 WebView 分配修复与 JNI/Binder 折中验证

Date: 2026-09-08
Status: completed

## 交付

WebViewTabHost 延迟分配，未使用宿主的生命周期/历史操作不创建 View；销毁幂等，禁止销毁后重新初始化。只修现有分配策略，不新增功能或更换内核。增加宿主旧/新 JNI 对照、生产 Binder 采样、未使用/首次使用/销毁后访问回归。资源 FD 测试仍显式加载真实页面，不因延迟分配变成只测试空对象。

系统 JNI/Binder 缺陷仍开放，本检查点完成的是有实测证据的应用规避与方案评估。详见 [规避报告](../development/native-reference-mitigation.md) 和 [Bug 记录](../bug-fixes/2026-09-08-unused-webview-allocation.md)。

## 精确验证

- `./gradlew :feature:browser:assembleDebugAndroidTest --max-workers=1`：旧生产实现加诊断夹具构建 exit 0。
- 两台各运行 `python3 scripts/diagnostics/run-jni-reference-trace.py SERIAL --count 240 --host-only`：旧实现目标类 new/delete/live=960/480/480；新实现=0/0/0。同一个 agent 二进制；旧日志保存为 host-unused-240-before。
- 新实现上述命令加 `--evaluate-host`：API29 1680/1680/0、API36 1440/1440/0。
- 两台各运行 `python3 scripts/diagnostics/run-jni-reference-trace.py SERIAL --count 1000 --scenario settled-close`：均 10000/10000/0；同一 PID 下采样 Binder 多次下降，无强制 GC/重启。
- 两台各运行 `python3 scripts/diagnostics/run-jni-reference-trace.py SERIAL --count 100 --scenario network-stop`：均 1000/1000/0，服务端收到每一轮请求。省略 scenario 的裸平台正对照仍 400/200/200，系统 bug 未消失。
- `./gradlew spotlessApply :feature:browser:testDebugUnitTest :feature:browser:assembleDebugAndroidTest spotlessCheck detekt :feature:browser:lintDebug :app:assembleConsumerDebug :app:assembleDeveloperDebug --max-workers=1`：exit 0。浏览器 JVM 116 项，0 failure/error/skip；双 flavor APK 构建通过。
- 最后补强 FD 测试后重新运行上条命令去掉两个 app assemble 任务：exit 0，最终测试 APK 已安装回归。
- API29/36 专用 arm64 模拟器，先 force-stop 清除测试 agent，安装最终测试 APK；`adb -s SERIAL shell am instrument -w -r -e class com.helix.feature.browser.BrowserSecurityDeviceTest,com.helix.feature.browser.BrowserActionsDeviceTest,com.helix.feature.browser.BrowserSnapshotDeviceTest,com.helix.feature.browser.webview.WebViewResourceLifecycleDeviceTest com.helix.feature.browser.test/androidx.test.runner.AndroidJUnitRunner`：各 `OK (29 tests)`，实际各 27 项通过、2 项 opt-in（长稳/裸平台大循环）assumption skip，0 failure；裸平台另有上述有界正对照，24h 未执行。
- `bash scripts/diagnostics/build-jni-reference-trace.sh`：JDK17/NDK28.2.13676358 严格编译 exit 0；起初系统 java_home 探测无注册 JDK，随后使用实际已安装 JDK 完成；不是产品失败。
- Python 编译、docs/ADR/secrets/diff 门禁通过。

## 决策与剩余边界

决策记录：不适用：宿主内部延迟分配保持既有运行时/权限/IPC 契约。报告比较了进程隔离、复用池、更换内核、系统更新和销毁前导航，未实施新架构。Activity Context/原生 Autofill 完整性、更多 WebView 发行版、真机、24h 长稳仍未验收。历史失败没有更改为通过。
