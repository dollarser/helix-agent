# HXA-155：浏览器网络取消与 Activity 生命周期

Date: 2026-09-08
Status: completed（有界验证；原生问题未关闭）

生产 controller 的真实 HTTP 慢响应关闭、停止、后台恢复与 Activity 重建在 API29/36 各 100 轮完成，合计 800 轮；每轮有唯一服务端请求记录，目标弱引用均创建/删除 1000/1000、残留 0。初版时序不足与 API29 socket 环境异常独立保留，不算通过。

修改仅在测试 APK、诊断脚本和 docs：请求到达协调、真实 task 后台、重建计数恢复；不改生产，不触发 GC。另交付未发送的上游复现材料，原生 JNI/Binder 缺陷仍 open。

验证：`./gradlew spotlessApply :feature:browser:testDebugUnitTest :feature:browser:assembleDebugAndroidTest spotlessCheck detekt :feature:browser:lintDebug --max-workers=1` 通过，JVM 116 项；夹具协调修正后再执行构建/格式/静态检查/lint 通过；两台各四条诊断命令见 [详细证据](../development/browser-network-reference-verification.md)。Python 编译检查、docs/ADR/secrets/diff 门禁通过。

决策记录：不适用：测试诊断，不改变生产契约、执行域或回收策略。
