# HXA-200：三态审批偏好、审计与恢复验收

> 本页记录当时交付证据；决策链接指向当前主题归纳，不表示当时测试覆盖当前契约的全部范围。


日期：2026-09-15。状态：**completed（本任务范围）**。验收基线为 `397608fc` 加本轮修复，在 `worktree-harness-2.0` 的实际工作树执行；其中还含其他所有方的并行WIP。本记录不表示整个分支已集成、全部产品已完成或可以发布。后端依赖已满足，HXA-201可继续工具设置与审批卡。

## 契约与交付

决策记录：沿用accepted ADR-PERMISSIONS-003与ADR-PERMISSIONS-003，不修改授权规则：UNSET沿用既有Policy；显式ASK与失效ALLOW保持询问；命中规则DENY > ASK > ALLOW；ALLOW不创建Capability、扩大scope或替代高风险精确proof。NEW_DEFAULT遵循当前可信built-in登记/版本基线，不宣称扩展工具普遍默认ASK或跨版本永久ASK。Room保持schema18。

历史六个gap提交（`79fd7d85`、`ad5b9912`、`c63172a0`、`0ca65b4a`、`ea91efe6`、`fa01dde6`）只作为演进证据。曾错误接受的跨scope ASK覆盖与等待后DENY仍执行已在`397608fc`纠正，按修复后契约验收，不修改ADR追认错误期望。

本轮补齐：

- 持久审计保存最新评估、实际呈现审批卡、承诺执行开始三个阶段的快照；包含有效偏好、来源、Policy与最终解析原因、规则ID/revision/scope及契约有效性。未到达阶段为空；变更/删除设置不改写历史快照。来源与范围引用哈希化、字段白名单、version1手写JSON，旧payload仍可读取，不凭缺字段推断允许。
- 偏好写入与开始重验沿用生产服务短时同步边界，等待审批与执行长任务不持锁；审计证据来自做出该决定的同一次读取。
- Broker决定写入前在同一锁下确认活跃等待者；停止、完成或进程重启后的迟到批准被拒绝，不改变旧PENDING记录。用户先批准后停止的真实决定仍保留；未开始的proof不消费。
- 统一审批等待取消类型，修复Broker单独取消被审计为TOOL_FAILED的偏差。调用与审计均持久结算；真实ChatService.send/stop和不同PID的恢复验证无旧卡复活、无重放。
- 本地模型验收揭示Provider连接检查误拒绝Completed后合法Usage，改为检查最后一个非Usage事件；仍要求非空文本、Completed且无Error，覆盖缺Completed反例。模型fixture区分空tools列表。
- JGit以锁定、可审查的本地构建补丁消除两个TrustAll空实现，不suppress、不升级依赖。保持正常TLS校验，禁用校验路径抛CertificateException；Debug APK实际方法调用与Release构建/lint均验证。

## 实际验证

在项目要求的JDK17/Android SDK环境，以下命令全部exit0。Gradle缓存命中/UP-TO-DATE按原日志保留，测试数量来自对应XML，不声称全部强制重执行。

```sh
./scripts/check-all.sh --source
./gradlew spotlessCheck detekt \
 :core:model:test :core:policy:test :core:agent:test :tools:framework:test \
 :core:storage:testDebugUnitTest \
 :app:testConsumerDebugUnitTest :app:testDeveloperDebugUnitTest \
 :core:storage:assembleDebugAndroidTest \
 :app:assembleConsumerDebug :app:assembleDeveloperDebug \
 :app:assembleConsumerDebugAndroidTest :app:assembleDeveloperDebugAndroidTest \
 :app:lintConsumerDebug :app:lintDeveloperDebug --continue --console=plain
./gradlew spotlessApply :app:assembleConsumerRelease :app:assembleDeveloperRelease \
 :app:lintConsumerRelease :app:lintDeveloperRelease --continue --console=plain
python3 scripts/debug/2026-09-09/run-owned-emulator.py --help
python3 scripts/debug/2026-09-15/verify-apref-closeout.py
python3 scripts/debug/2026-09-15/verify-apref-closeout.py --process-restart
python3 scripts/debug/2026-09-15/verify-jgit-patch.py
python3 scripts/debug/2026-09-15/summarize-hxa200-acceptance.py
git diff --check
```

| 范围 | 实际结果 |
| --- | --- |
| P1 | source、Spotless、Detekt、diff检查通过 |
| P2核心 | model142、policy1177、agent271、framework164、storage89，共1843通过；含实际PolicyEngine有界组合1024项 |
| P2/P3 app主机 | consumer515通过/4条件跳过；developer541通过/4条件跳过。与核心合计2899通过、8既有外部材料条件跳过、0失败 |
| P3构建与lint | 双flavor Debug、androidTest构建；Debug lint各0错误/0警告。追加双flavor Release构建/lint各0错误/0警告 |
| 当前设备矩阵 | API29/36 × consumer/developer，各25项（Preference10 + Scheduler13 + Stop2），共100通过，0失败/跳过 |
| 当前真实进程恢复 | API29/36 × consumer/developer，各Stop2 + Restart1，共12通过，0失败/跳过；一次安装后直接instrumentation，force-stop并断言PID变化 |
| 存储迁移/证明生命周期 | 同日先前独占运行API29/36各38，共76通过，0失败/跳过；本轮未改schema，复用该证据，不冒称本轮再次执行 |

主机日志：`build/hxa200-final-host3.log`、`build/hxa200-release.log`、`build/hxa200-final-static2.log`、`build/hxa200-final-source5.log`；汇总及APK哈希：`build/hxa200-final-evidence.json`。设备矩阵：`build/hxa200-closeout-20260915-183132/`；最终恢复：`build/hxa200-closeout-20260915-184419/`；复用存储：`build/hxa200-closeout-20260915-174702/`。

所有设备运行使用自建独占API29/36模拟器，拒绝已有serial；finally仅结束owned进程，输出owned-exit及adb-final证据。恢复专用用例需`hxa200RecoveryPhase=restart`，普通全类扫描不会错误要求预先存在另一个进程的夹具；专用runner显式提供参数并严格拒绝零测试、skip或失败。阶段原始instrumentation状态保留，不伪造JUnit XML。

## JGit补丁边界

输入坐标`org.eclipse.jgit:org.eclipse.jgit:6.10.0.202406032230-r`，SHA256 `43f92f3adb681a5f3006b979e8d341c12a8cfd8029f287c42bcf0a80377565ae`；输出jar SHA256 `d3357ddf7460952828858650346088f62d34e548a1bb894f0db03e536785aa8e`。条目比较确认只替换NoCheckX509TrustManager.class、移除失效的两份上游jar签名；about.html及其余原始条目内容保留。原Maven缓存不变，校验和变化时构建失败。锁文件仅改变JGit配置归属，既有解析版本不升级。

实现、维护与来源见[补丁说明](../../config/jgit/README.md)和[第三方声明](../../THIRD_PARTY_NOTICES.md)。这解决当前TrustAll lint及对应空信任管理器实现，不替代[HXA-206 承接的历史 R1](HXA-206.md)现有只读Git配置/filter/transport产品边界验收，不新增remote Git授权。后续实际缺陷修复见[只读 Git filter](../bug-fixes/2026-09-21-readonly-git-filter.md)。Release构建/lint不是签名发行或Release真机验收。

## 验收结论与后续

HXA-200原剩余验收（模式/风险/能力、scope/身份/版本、精确proof、排队与取消、审计及恢复）已具备本任务要求的实现与有界主机/设备证据，关闭本任务。审计UI可读呈现、工具设置、权限解释和审批卡产品体验归HXA-201；继续复用现有服务与持久事实，UI不访问DAO。具体交接见[HXA-201 Prompt](../bug-fixes/2026-09-15-tool-preference-start-boundary.md)。

未执行完整仓库`check-all --build/--all`、全产品旅程、真实账号、外部服务、长稳或发布验收；不得把本任务P1/P2/P3通过扩大成这些结论。共享status/roadmap/matrix同步HXA-200当前结论但保留其他所有方改动，不在本切片整文件提交。独立代码与本记录允许本地commit；不push、merge或改写历史。
