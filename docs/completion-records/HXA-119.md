# HXA-119 完成记录：GitHub Copilot Free 第三方侧载 Device Flow 登录生命周期

**日期**：2026-09-05

**状态**：代码、自动化设备验收与真实账号登录成功；Copilot adapter 仍未注册。本结论不表示
GitHub/Microsoft 授权 Helix，也不表示 Copilot 模型可调用。

## 实现与边界

- CLI Runtime 固定使用 `Iv1.b507a08c87ecfe98`，依 GitHub Device Flow 请求 device/user code，
  严格固定 verification URL、响应大小与 15 分钟最大有效期；首次轮询至少等待 5 秒，
  `slow_down` 每次增加 5 秒。
- Device Code 生成后同时稳定显示完整 `https://github.com/login/device`；用户记下 code 后点击
  按钮打开系统浏览器，避免自动跳转遮住 code。拒绝、过期、取消和未知协议均 fail closed。
- GitHub token 必须先经 `https://api.github.com/copilot_internal/v2/token` 完成 entitlement/token
  exchange，成功后 GitHub/Copilot token 才同时写入 Runtime UID Android Keystore vault。exchange
  的 I/O 失败最多在内存中重试五次；Device Flow 轮询的瞬时 I/O 失败保持同一 attempt，并按服务端
  interval 继续到成功、取消或到期。HTTP 403 等确定拒绝不重试、不落盘。
- UI、异常、Binder status 只显示稳定状态，不返回 token。本 HXA 没有模型 endpoint、Provider、
  Tool、Job、外部 credential 导入或新的 Android 权限。
- API 29/36 edge-to-edge 下以 system bar inset 与 ActionBar 高度避让，确保非官方风险披露不会被
  系统栏遮挡；Device Code 不写入系统剪贴板。
- client id 与 internal endpoint 仅是项目所有者接受的非官方个人侧载例外；它们可随时失效，且
  client id 公开不等于注册者或 GitHub/Microsoft 授权 Helix。商店/官方发行门禁未满足。

## 当前官方事实与第三方证据

- 访问日期 2026-09-05：GitHub 官方 Device Flow 文档要求注册 client id、按 `interval` 轮询，
  并规定 `slow_down` 增加 5 秒：
  https://docs.github.com/en/apps/oauth-apps/building-oauth-apps/authorizing-oauth-apps 。
- GitHub 官方 Copilot SDK 文档支持 signed-in user 或项目自有 OAuth App，且用户订阅路线要求
  Copilot subscription：
  https://docs.github.com/en/copilot/how-tos/copilot-sdk/auth/authenticate 。
- 本地 `dsh-plugin-subscriptions@0.7.0` 只作为固定 client id、endpoint 与响应形态证据；Helix
  未复制其生产代码。MIT 许可证不授予服务端身份使用权。

## 真实尝试与已知限制

- 共享 API 36 首轮真人授权后的 exchange 因网络 I/O 失败，没有 HTTP 403；不能把该尝试表述为
  账号无 Copilot 资格。后续只重试 exchange 仍不够，独占模拟器证明 Device Flow poll 自身也会
  遭遇瞬时 I/O，因此修复为在同一 attempt 内继续按 interval 轮询。
- 新建独占 `Helix_M11_API_36`（`emulator-5600`，API 36、arm64-v8a），启用宿主键盘后重新授权。
  UI 最终显示 `Login and Copilot entitlement verified`；Runtime 私有目录出现 0600 的
  `subscription-copilot.enc`，证明 GitHub Device Flow、Copilot entitlement exchange 与 vault
  落盘正向完成。检查只验证文件存在、权限与 UI 状态，没有读取 token 内容。
- 真人 logout 未执行，以保留本次登录供后续独立 Provider HXA 使用；模型调用仍无证据，adapter
  保持 `NOT_REGISTERED`。

## 验收命令与结果

- `./gradlew spotlessApply :runtime:cli-app:testDebugUnitTest :runtime:cli-app:lintDebug
  :runtime:cli-app:assembleDebug --no-configuration-cache --no-daemon`：exit 0，JVM 26/26，lint/build
  通过。
- `ANDROID_SERIAL=emulator-5602 ./gradlew :runtime:cli-app:connectedDebugAndroidTest
  --no-configuration-cache --no-daemon`：独占 `Helix_M11_Test_API_29`，API 29、arm64-v8a，12/12。
- `ANDROID_SERIAL=emulator-5604 ./gradlew :runtime:cli-app:connectedDebugAndroidTest
  --no-configuration-cache --no-daemon`：独占 `Helix_M11_Test_API_36`，API 36、arm64-v8a，12/12。
- `./scripts/check-cli-runtime-boundary.sh`：exit 0；仅登录 endpoint、固定侧载身份、仅 INTERNET，
  无模型 endpoint 或 credential import。
- `./scripts/check-lockfiles.sh`、`./scripts/check-secrets.sh`、`./scripts/check-docs.sh`、
  `./scripts/verify-adr.sh` 与 `git diff --check`：exit 0。
- debug APK：7,811,812 bytes；SHA-256
  `4acdbe6dd2b9379055c07a922e38c63376884b79c87f885a74747dbc50dec0e6`。

## 决策记录：ADR-0023 accepted

本 HXA 修改 accepted ADR-0021 的 Copilot 身份限制。项目所有者明确接受固定 client id 或其他已
注册 Device Flow identity 后，新增 accepted
[ADR-0023](../adr/0023-copilot-third-party-device-flow-identity.md)。该决定只允许个人侧载登录实验，
不改变 ADR-0007 隔离、不满足商店授权，也不把 proposed ADR-0022 的官方 SDK 平台负向结论改成
已实现。
