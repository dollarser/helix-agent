# HXA-207：现有扩展来源的添加到使用闭环

> 本页记录当时交付证据；决策链接指向当前主题摘要，不表示当时的测试覆盖了当前契约的全部范围。

日期：2026-09-18。状态：**completed（本任务范围）**。基线 `5bfce200`（== main）；本任务 1 个具名提交（测试类 + 5 个日期目录脚本 + 任务规格状态更新 + 本记录）；在 `Helix-extensions-207` 工作树与四象限自建独占模拟器验收，只 stage 本任务自有路径，不夹带并行会话（HXA-191 主题）改动。

## 契约与交付

决策记录：闭环全部**复用**统一 registry / discovery / authorization / execution / audit 管线与既有生产服务——`McpAppService`（registerDisabled / testConnection / enable / disable / delete）、`ConnectorService`（幂等安装 / skill 启用）、skill 导入（预览哈希 + 取消钩子）、连接时 SSRF 门（`McpSsrfEndpointGate` → `SsrfAddressPolicy`，**每次调用重新授权**）、session 权限编辑与审批 broker、持久 `SafetyProfileStore` / `LanScopeStore`。**产品代码零改动**，无新增业务专用 Agent 状态机、规划器、工作流、每按钮工具或认证协议；连接/安装成功不等于启用；禁用后发现、模型 schema 与执行入口一致拒绝。

- 设备测试（`app/src/androidTest/kotlin/com/helix/app/ExtensionJourneyDeviceTest.kt`，12 项）：Skill 预览后变化拒装/取消不提交；Connector 重复导入幂等/非可移植配置拒装；MCP 连接成功但未启用（developer 真实握手未启用 → 无工具暴露、discovery 不可见、执行入口 UNKNOWN_TOOL、无审批卡；consumer STANDARD 下统一门直接 `LAN_NOT_ALLOWED`，各面一致拒绝）；启用后真实调用成功（FULL_ACCESS 免卡、模型可见结果与持久化结果均为 fixture 固定文本）；启用后调用失败（传输失败非 side-effect-free，无有界重试）；**失败原地修复**（既有用户路径：删死端点 → 同 id 重注册恢复地址 → 重测 → 重启用 → 同名工具再次成功）；工具级禁用三面同刻拒绝（FULL_ACCESS 下先于任何卡片）与 READ_ONLY 审批卡拒绝不执行；重启后实际作用域（seed → force-stop → recover 断言**新 PID**：skill 启用与 session 权限模式持久，内存 MCP 连接不自动重启用）。
- developer 泳道 = 用户自身动作（持久 profile 切 ADVANCED + 添加精确 `http://127.0.0.1:<port>` LAN scope），退出即还原；consumer 构建拒绝 ADVANCED（ADR-0005），验收面即一致拒绝面（5 项 assumeTrue 跳过）。
- 脚本（`scripts/debug/2026-09-18/`，具名提交）：`with-host-slot.py`（同机多 worktree 共享内核文件锁）、`run-owned-emulator-207.py`（独占模拟器 runner，端口上限 5750；boot 后 `logcat -G 16M` + 捕获 `-t 20000`）、`run-hxa207-device.py`（四象限驱动，5690/5692/5694/5696 独占端口、matrix→restart、拒占）、`hxa207-restart-recover.py`（recover after-script）、`summarize-hxa207-device.py`（8 证据目录校验 + summary.json）。

## 实际验收

| 门禁 | 结果 |
| --- | --- |
| G1 源检查 | 通过（`check-all.sh --all` 内 source 5 项 + secret scan + 依赖锁） |
| G1 格式 | 通过：spotless（`spotlessApply` 修复 6 处行宽后）+ `git diff --check` 干净 |
| G1 detekt | 通过（随 `check-all.sh --all` 全绿） |
| G2 | 通过：`check-all.sh --all` 内全变体 lint/assemble + 双 `assembleAndroidTest`（413 tasks） |
| 单测 | `:extensions:skills:test :extensions:mcp:test :app:testConsumerDebugUnitTest :app:testDeveloperDebugUnitTest` 全绿（随全量执行，0 失败） |
| 四象限设备·矩阵 | 全绿：4×`OK (12 tests)`，`run finished: 12 tests, 0 failed`；跳过逐象限 logcat `assumption failed` 行数校验：consumer 每象限 7（5 developer 泳道 + 2 重启）、developer 每象限 2（重启对） |
| 四象限设备·重启 | 全绿：4×seed `OK (1 test)` + 4×recover `OK (1 test)`（force-stop 后新进程，`assertNotEquals` 断言新 PID） |

设备：自建独占模拟器 8 轮（API29/36 × consumer/developer，端口 5690/5692/5694/5696，AVD `HelixApkUpgrade_API29/36_20260918`，1080x2400@420、airplane mode、runner 拒绝已占 serial、finally 仅关闭自有进程组），汇总 `30 matrix + 8 restart passed, 18 matrix skips, 0 failed; 8 owned runs`。制品身份（与构建 APK sha256 一致）：consumer app `6405c01b703d8cbb…`/test `04d77ec88c4f888e…`，developer app `2865e6ecf661e347…`/test `0d20c76eb9777cc4…`。

验收条款覆盖：内容预览后变化、导入取消、重复点击、无效配置、连接成功但未启用、启用后实际调用与可读结果、调用失败、失败原地修复、工具禁用一致拒绝、操作询问（READ_ONLY 审批）、重启后实际作用域（新 PID）、双 flavor × 双 API。真实外部服务/账号验收归 HXA-125（in-APK `InAppMcpServer` 为明确合成 fixture，不冒充通过）；consumer 真实 MCP 调用需公网端点（ADVANCED flavor 编译期禁用），本轮 consumer 面为一致拒绝面。

证据：`build/hxa207-device/`（8 运行目录 + summary.json，gitignored）、`build/hxa-207-host-all.log`、`build/hxa-207-device-run.log`、`build/handoff-207.md`（交接，含共享文档待更新清单）。
