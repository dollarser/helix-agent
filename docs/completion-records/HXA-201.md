# HXA-201：工具设置与审批卡

> 本页记录当时交付证据；决策链接指向当前主题归纳，不表示当时测试覆盖当前契约的全部范围。


日期：2026-09-16。状态：**completed（本任务范围）**。基线为 `aefc9789` 加本轮修复，在 Harness 工作树验收；并行 WIP 不属于本任务交付。HXA-202 的本项依赖已满足，未在本轮实现 HXA-202。

## 契约与交付

决策记录：沿用 accepted ADR-PERMISSIONS-003、ADR-PERMISSIONS-003，不新增或修改 ADR。UNSET 沿用既有 Policy；DENY > ASK > ALLOW；设置不授予 Capability，不扩大文件范围，不替代高风险精确 proof。Room schema、执行器、Runtime 和 Goal 语义不变。

- 衔接 Slice 1～3，补齐全局、会话和 Workspace 的选择、读写、继承来源、搜索及选定范围恢复默认。生产偏好 source 从当前持久会话目录解析 Workspace；缺失会话不猜测默认工作区、不阻断独立上下文。设置、曝光与分派共用现有服务，UI 不访问 DAO。
- 保存继续在 IO 上串行执行；服务变更刷新实际状态。切换范围清除旧行，防止异步加载期间操作旧范围。三语言说明区分偏好、系统权限与本次批准。
- 卡片携带呈现时的 contractHash，旧卡不能把 ALLOW 绑定到未经审阅的新版本。动态风险升至 L2/L3 时隐藏未来 ALLOW；保存 GLOBAL 设置不批准当前调用，等待调用仍按执行开始时的新规则检查。
- 实际 ChatScreen 展示保存结果、工具不可用、契约变化或保存无法确认；异常不伪装成功，协程取消保留取消语义。
- 保留原测试并补齐 Room/Dispatcher 执行、真实卡片点击、Activity recreate、不同 PID 重启、跨范围恢复和缺失会话边界。旧 ApprovalFlow 停止 fixture 改为真实 ChatService.send/stop，直接断言未决定/未消费、零执行和迟到批准被拒绝。

## 实际验收

完整命令、逐项映射、截图、APK哈希与全套失败表见[验收证据](../evidence/development/hxa201-acceptance-2026-09-16.md)，实现缺陷与修复见[修复记录](../bug-fixes/2026-09-16-tool-preference-settings.md)。

| 门禁 | 结果 |
| --- | --- |
| P1 | check-all --source、Spotless、Detekt、diff检查通过；文档/ADR/三语言/密钥扫描通过 |
| P2 | 核心1843通过；app consumer529/developer555通过，各4项既有条件跳过；合计2927通过/8跳过/0失败 |
| P3 | 双flavor Debug主/测试APK及storage测试APK构建；app Debug lint各0错误/0警告 |
| 专项设备 | API29/36 × consumer/developer，每象限普通52通过/1重启阶段跳过，随后真实重启16通过/0跳过；共272次通过、0失败 |
| 完整API29 consumer对照 | 匹配基线347通过/26跳过/19失败；当前355通过/26跳过/18失败。没有新增失败用例，剩余18项均有基线复现；完整套件仍红 |

最终专项归档 `build/hxa201-device-20260916-002808/`；主机 `build/hxa201-closeout/final-host3.log`、`final-static4.log` 和 `host-summary.json`。普通阶段的重启跳过不是完整覆盖缺失：同一安装保留数据，force-stop后由新PID实际执行该用例。所有模拟器均为自建独占进程，finally清理，最终adb列表为空。

## 全套边界与后续

本轮全套对照发现并修复了缺失会话解析引入的回归，修复后完整重跑，不能把它列为旧债。剩余18项与匹配基线首个异常一致。Connector重建在匹配基线和当前均通过，但较早诊断有波动，未作针对性修复。最初基线脚本未正确应用并行补丁，已修正、重建匹配基线并完整重跑；旧基线仅留作诊断材料。任务范围可关闭不等于全仓/全产品/发布通过。

JGit TrustAll 已由 HXA-200 修复，本次 lint 保持绿。API29 GitStatus 的 `readNBytes` 是独立兼容性失败，已在基线复现，不称作依赖漂移。具体附件、Goal、Provider、任务与文件失败及后续归属见验收表；HXA-202继续任务入口，203/204/205分别处理交付、恢复与准备体验，Git问题归192。

未执行完整仓库 check-all --build/--all、Release设备、真实账号/外部服务、长稳或发行验收。本轮允许具名本地提交，未push、merge或改写历史；共享文档只提交本任务状态变更，保留其他所有方内容。
