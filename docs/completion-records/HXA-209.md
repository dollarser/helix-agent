# HXA-209：会话授权预设、工具禁用与自定义权限

> 本页记录当时交付证据；决策链接指向当前主题归纳，不表示当时测试覆盖当前契约的全部范围。

日期：2026-09-17。状态：**completed（本任务范围）**。基线为 `446f0cf6`，本轮 16 个提交 `db5b5a62`～`dc6c9fc8`，在 main 工作树验收；并行 HXA-094/095 的 Root 改动不属于本任务交付。HXA-209 是本批首个大型功能，交付后按[交接指引](../development/implementation-guide.md) 收口 HXA-192 的 Plan 授权联动。

## 契约与交付

决策记录：沿用 accepted [ADR-PERMISSIONS-001](../adr/permissions/001-session-authorization.md) 与 [ADR-PERMISSIONS-003](../adr/permissions/003-dispatch-and-audit.md)，不新增或修改 ADR。统一禁网按所有者决定移出本轮范围：保留共享 UID Runtime，不新增 CUSTOM 工具联网 DENY 或离线 Shell 开关，不为已撤下要求拒绝 Shell；具体工具禁用、原有网络策略、文件与远端写限制仍有效（见[隔离 PRoot 可行性](../evidence/development/isolated-proot-feasibility-2026-09-16.md)）。

- 可用工具收敛为 ENABLED/DISABLED，删除旧三态 ASK/ALLOW 的第二条授权链（`61bf11e0` 移除 HXA-200 三态偏好表）；禁用工具从模型 schema、tools.search、会话曝光窗口与执行入口一致移除，重新启用仅恢复可用性，不恢复工具级 ASK。
- 三个会话预设（完全免确认/工作目录与联网/只读与联网）与 CUSTOM 编译为同一种操作权限配置，单一 resolver，多效果先合并再按 DENY > ASK > ALLOW 判定，不实现四条执行链（`4d05fbb4` 配置+resolver、`7f809732` 分派接线）。
- CUSTOM 按操作类别与范围配置 ALLOW/ASK/DENY；文件修改 DENY 跨 write/edit、Shell 重定向、tee、sed -i、脚本、删除、移动、解压、下载保存生效，不能以单次批准绕过；未知效果且可能触犯 DENY 的调用直接拒绝，不出卡（`340a6e6d` 服务与效果分类、`937e753e` 自定义规则编辑）。
- 删除专项仅按 ADR 的有限集合拦截显式 `rm -rf dir` 命令规则，命中进入精确一次性审批、任何模式不得豁免；不承诺拦截所有等价删除（Phase A `RmCommandRule`，`db5b5a62`）。
- 授权配置与执行开始共用线性化点，CAS 冲突或存储失败不显示保存成功；模式/规则收紧时排队与 pending 未开始调用重新判定，运行中任务显示并可精确停止，旧 proof 不覆盖新禁止（`05d18c71` 写服务+审计、`86a11685` 收紧提示）。
- 数据转换 v21→v22 移除旧 ASK/ALLOW，转换默认 READ_ONLY 且保留用户文件、会话、凭据与审计；新会话默认独立管理，复制/导入不继承来源授权；进程恢复不激活 Goal、不重放未知副作用（`06a06e3a` 自定义草案持久化、`0d3dbff4` 设置分区）。

## 实际验收

完整命令、逐项映射与失败表见[执行域效果边界与证据矩阵](../evidence/development/2026-09-16-hxa209-execution-domain-matrix.md)。

| 门禁 | 结果 |
| --- | --- |
| 主机 `--all` | `./scripts/check-all.sh --all` exit 0：source（review-gates/docs/ADR/i18n/secrets）+ build（spotless/detekt/单测/lint/双 flavor 组装）+ artifacts（variant/CLI 边界）全绿 |
| APK 组装 | `:app:assembleConsumerDebugAndroidTest`、`:app:assembleDeveloperDebugAndroidTest`、`:core:storage:assembleDebugAndroidTest` 均 BUILD SUCCESSFUL |
| 专项设备 | API29/36 × consumer/developer 四象限，每象限 HXA-209 设备用例 OK（9 tests）；自建独占模拟器，finally 关闭 |
| 存储迁移 | API29/36 双象限 OK（35 tests），v19/v20→v22 转换保留用户数据、无 UNIQUE 冲突 |
| 进程恢复 | 一次安装后手动 instrumentation 两阶段真实进程重启，恢复设置但不激活 Goal、不重放副作用 |
| 三语言/密钥 | check-i18n.sh、check-secrets.sh 通过；窄屏/大字体界面显示真实模式、可用工具与 DENY 来源 |

主机门禁归档 `build/hxa209-d8-checkall-20260917-003350.log`；设备四象限矩阵 `build/hxa209-d8-matrix-20260917-001244/`、存储重试 `build/hxa209-d8-storage-retry-20260917-002116/`。全部模拟器均为自建独占进程、1080x2400@420，finally 清理后 adb 列表为空。主机单测含 D5 `SessionPermissionPolicyTest`、D6 新会话默认、D7 `SessionToolAvailabilityTest`；设备含 D8 `SessionPermissionDeviceTest`、`SessionPermissionRecoveryDeviceTest`、`ExecutionEffectBoundaryDeviceTest`，同时断言副作用与可用性，不只断言返回码。

## 全套边界与后续

本记录只证明 HXA-209 范围（会话授权预设、工具二态、CUSTOM 效果限制、有限 rm 命令规则、数据转换与恢复、四象限设备）通过；不等于 HXA-192 的 Plan 授权联动、202～206 核心体验、194～199 终端链或发行验收完成。未执行：Release 设备矩阵、真实账号/外部服务、长稳与签名发行。统一禁网已按所有者决定移出范围，保留共享 UID Runtime，不声称 PRoot 离线或隔离 App/订阅凭据。

后续按[交接指引](../development/implementation-guide.md) 收口 HXA-192 的 Plan 授权联动，再推进 202～205、207 与 206；191（主题/会话搜索）可独立安排。原 HXA-200/201 的已完成记录保持历史证据，本记录不沿用其旧三态断言作为新方案验收。
