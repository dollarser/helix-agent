# HXA-159：大文件职责重构与浏览器 Context 评估

Date: 2026-09-08
Status: completed

## 交付

| 入口 | 改前→改后行数 | 职责边界 |
| --- | --- | --- |
| FilesScreen | 1874→84 | 入口组合状态、加载 effects、动作与分组面板；同一个 composition 持有状态与取消标志；保留 picker 和公开签名 |
| ChatService | 3590→2935 | 提取 ChatRequestAssembler、ChatScreenProjection、StagedAttachmentProcessor 与不可变暂存记录；会话/Turn/审批/取消与最终附件准入仍由服务统一持有 |
| BrowserTools | 1537→30 | 导航、历史、检查、交互、输出按工具组分文件；共享 schema 为模块内部实现；注册入口/顺序、12 个工具对象契约保持一致 |
| AppContainer | 833→718 | AppFileServices 聚合已有 SAF/file 服务装配，共用原有 grant store、workspace 与 scope resolver；保持手工注入与共享实例 |

这是按职责拆分，总代码量因显式依赖和文件边界会略增；不把入口缩短等同于运行时性能提升。ChatService 仍较长，剩余复杂度主要在状态编排；本轮不通过多个可写 owner 或零散 extension 拆散状态机。ToolDispatcher 核心管线保持集中。

[Context 分析](../development/browser-context-options.md) 建议 Activity 级 WebView 所有权与应用级逻辑状态分离，比较 MutableContextWrapper、主题包装和自定义 JS 对话框的边界。未迁移 Context，也未关闭 Autofill；系统 JNI/Binder、真机与后置长稳的原有边界保持。

## 精确验证

```sh
./gradlew spotlessApply :app:testConsumerDebugUnitTest :app:testDeveloperDebugUnitTest :tools:browser:test :app:assembleConsumerDebug :app:assembleDeveloperDebug :app:assembleConsumerDebugAndroidTest spotlessCheck detekt lintDebug --max-workers=1
```

最终 exit 0。consumer 336 项（332 通过、4 项既有外部条件 skip），developer 360 项（356 通过、4 skip），browser tools 36 项（36 通过）；0 failure/error。双 flavor 构建、consumer 测试 APK、Spotless、Detekt、完整 lintDebug 通过。本次使用既有功能、失败/取消/恢复回归，不删除或放宽测试。13 个 Browser 工具/注册对象的 Kotlin token 与改前逐一比较一致（12 个工具及注册入口），移除注释/空白后无差异。

两台专用 arm64 模拟器 API29/36 安装同一最终 consumer APK 和测试 APK，运行：

```sh
adb -s SERIAL install -r -t app/build/outputs/apk/consumer/debug/app-consumer-debug.apk
adb -s SERIAL install -r -t app/build/outputs/apk/androidTest/consumer/debug/app-consumer-debug-androidTest.apk
adb -s SERIAL shell am instrument -w -r -e class com.helix.app.ui.FilesScreenTest,com.helix.app.ui.FilesImportExportUiTest,com.helix.app.ui.ChatStopProgressDeviceTest,com.helix.app.ui.ApprovalCardScreenTest,com.helix.app.chat.AttachmentE2eDeviceTest,com.helix.app.chat.ChatServiceAttachmentRetryDeviceTest,com.helix.app.chat.ChatSessionLifecycleDeviceTest,com.helix.app.chat.GoalRunCoordinatorDeviceTest,com.helix.app.chat.GoalTurnBindingDeviceTest,com.helix.app.chat.GoalModelCancellationDeviceTest,com.helix.app.ApprovalFlowDeviceTest,com.helix.app.McpToolDiscoveryDeviceTest com.helix.agent.test/com.helix.app.HelixAndroidJUnitRunner
```

最终 API29/36 各 `OK (74 tests)`：各 74 通过、0 failure/error/skip。覆盖文件列表/预览/冲突/导入导出、附件门禁与重试、会话归档/切换、Goal 绑定/预算/取消、审批与工具发现。

API36 首次批次为 73 通过、1 失败：`longTextPreviewKeepsTheMetadataReachable` 等待 `files-preview-text` 超时，单独复跑亦超时。诊断观察到文件成功读取；重新构建/安装、移除诊断日志后，同一最终产物上的该用例连续 5 次通过。没有为通过而延长等待、改断言或跳过测试；首次超时根因尚未确定，不据重跑结果宣称根因已修复。后续若再次出现，应保留现场、对比 APK 与调度状态，避免仅重试掩盖问题。

最终 consumer APK SHA-256：`0dc846657ed29f377e367beb207bdd235189bcc1df7aabd5dfe15d9cd3ca6bfc`；测试 APK：`88bfffd50743b63ba7182d72e6c2a1742d73277ac0d89e1d5a989d447ade808b`。本地构建/设备日志保留在被忽略的 `build/reports/hxa159/`。

文档、ADR、i18n、secrets、diff 门禁通过。

## 决策与剩余边界

决策记录：不适用：应用内职责组织调整保持既有模块方向、公开接口、权限、持久化与运行时契约，不引入 DI 框架或新功能。Context 所有权迁移只有分析，涉及后台/恢复契约时应按 ADR 约定另行推进。

这里的设备用例为有界模拟器回归，不是全仓全部仪器测试、真实模型/受保护账号、Autofill、真机或长稳验收。新增需求继续暂停；既有系统问题与外部验收缺口没有改为完成。
