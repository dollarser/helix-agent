# HXA-131 完成记录

- 目标：在跨 APK模型 Job 前建立 developer App 与订阅 Runtime 唯一共享的 Binder 握手契约，消除 companion 私有常量复制漂移。
- 前置任务/commit：HXA-130 / `c1acf87`。
- 修改文件：`runtime:cli-client` 共享协议和测试；`runtime:cli-app` 依赖与 status binder/baseline/caller verifier；边界脚本和 M11 状态文档。
- 实现的契约：共享 protocol version、interface descriptor、signature permission、显式 Runtime package/service、status transaction/reply、16 KiB status cap 和 20 秒 bind deadline；Runtime status transaction 改为消费该共享对象。
- 未实现/明确排除：无握手客户端、模型 Job transaction、prompt、PFD、Provider 或 token 通道；这些按后续 HXA 顺序实现，当前仍 `NOT_REGISTERED`。
- 自动测试：`runtime:cli-client` 12/12；`runtime:cli-app` 53/53；CLI app lint 成功。
- 手工验证设备：无；本 HXA 不改变 wire 值或 APK 行为，设备跨 APK冷绑定证据属于 HXA-132。
- 验收命令、exit code 和结果：`./gradlew :runtime:cli-client:test :runtime:cli-app:testDebugUnitTest :runtime:cli-app:assembleDebug :runtime:cli-app:lintDebug --no-configuration-cache` exit 0；`check-cli-runtime-boundary.sh`、`check-lockfiles.sh`、`check-secrets.sh`、`check-docs.sh`、`verify-adr.sh` 与 `git diff --check` 均 exit 0。APK 7,811,986 bytes，SHA-256 `67fcd4ffda75b6df67b0f7de3b06b0fd32f4de49cfb98293efe7d9028123b202`。
- 权限/数据/许可证变化：无；没有新增 Maven 依赖、权限、endpoint、数据或凭据迁移。
- 决策记录：无需新 ADR；这是 accepted ADR-0007 与 ADR-0025 下的共享契约归属整理，不增加 transaction 或改变凭据边界。
- 风险或后续任务：共享契约尚未证明主 App 冷绑定；HXA-132 必须补本地 package/signature 检查、显式绑定、status handshake、空闲解绑及 API 29/36 跨 APK E2E。
