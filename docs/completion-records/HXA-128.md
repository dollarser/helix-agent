# HXA-128：CLI/stdio 可移植性 Spike

Date: 2026-09-08
Status: completed（有界可移植性评估，不是新增第三方 CLI 支持）

交付只读样本清单脚本、固定底座/版本与授权依赖矩阵，以及现有 stdio 桥复测。QwenWork 149 文件、WorkBuddy 50 文件均无 ELF 可执行资产；wecom 明确依赖源宿主包装器与凭据管理，外部业务 CLI 版本/ABI/完整许可证没有足够证据，未运行或安装。

`./gradlew :extensions:mcp:test :app:assembleDeveloperDebugAndroidTest --max-workers=1` 通过；API29/36 精确复跑四条 stdio 协议/取消/噪声/输出上限用例，各 4/4，无跳过。样本清单、Python 编译及 docs/ADR/secrets/diff 门禁通过，见 [完整评估](../development/connector-stdio-portability.md)。

决策记录：不适用：维持 HXA-073 已有离线 PRoot 支持边界；本次没有采用新 Runtime、CLI、许可证或联网方案，未作约束未来的新架构决定。

后续若要求执行具体第三方 CLI，先取得固定版本/ABI/许可证/网络凭据条件，需改变执行域时另行 ADR；这项外部依赖不伪装成本检查点已新增支持。
