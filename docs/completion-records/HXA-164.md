# HXA-164：即时消息发布与 Markdown

Date: 2026-09-09
Status: completed

## 交付

普通会话的用户消息已由 TurnCoordinator 持久化，但 launchTurn 只发布轮次状态，未刷新消息列表，导致等模型结束才出现。现在在释放模型工作协程前刷新当前会话并发布 WAITING_MODEL；切换到其他会话时不发布旧会话状态。不创建虚假的乐观消息，不改变审批或模型调用语义。

助手回复使用原生 Compose 渲染常用 Markdown：标题、粗体、斜体、列表、引用、代码块、HTTP(S) 链接、分隔线和表格。未闭合代码围栏仍显示已有内容。用户输入保持纯文本；回复继续支持长按选择，全文复制仍复制完整原始 Markdown。无新增依赖、WebView、HTML 执行或远程图片自动加载。这是常用语法子集，不宣称完整 CommonMark/GFM 兼容。

## 精确验证

```sh
./gradlew spotlessApply :app:testConsumerDebugUnitTest :app:testDeveloperDebugUnitTest :app:assembleConsumerDebug :app:assembleDeveloperDebug :app:assembleDeveloperDebugAndroidTest detekt :app:lintConsumerDebug :app:lintDeveloperDebug --max-workers=1
./gradlew spotlessApply :app:assembleDeveloperDebugAndroidTest spotlessCheck detekt --max-workers=1
adb -s SERIAL shell am instrument -w -r -e class com.helix.app.ui.ChatStopProgressDeviceTest,com.helix.app.ui.MessageCopyDeviceTest,com.helix.app.ui.ConversationComposerDeviceTest,com.helix.app.ui.SessionDraftDeviceTest com.helix.agent.developer.test/com.helix.app.HelixAndroidJUnitRunner
adb -s SERIAL shell am instrument -w -r -e class com.helix.app.ui.MessageCopyDeviceTest com.helix.agent.developer.test/com.helix.app.HelixAndroidJUnitRunner
bash scripts/check-docs.sh
bash scripts/check-i18n.sh
bash scripts/verify-adr.sh
bash scripts/check-secrets.sh
git diff --check
```

主机最终 exit 0。Consumer JVM 343 项（339 通过、4 既有外部条件 skip），Developer JVM 367 项（363 通过、4 skip），均无 failure/error。双 flavor APK、Developer 测试 APK、双 flavor app lint、格式和 Detekt 通过。最初格式及复杂度检查失败后修正，再通过门禁；未删除或跳过测试。

专用 API36 首轮设备回归 9/9；增加表格断言后复制/Markdown 类 3/3，其中 2 项与首轮重复，共 10 个不同用例，无失败或 skip。服务器保持流连接未返回时即断言用户消息显示，覆盖普通会话、停止及 Goal 相关路径；Markdown 显示、未闭合格式、表格单元格、剪贴板原文和长按选择有测试证据。未将模型最终完成后的截图作为即时显示的证明。

真机 API34 覆盖安装成功，原会话保留并重新打开，Markdown 粗体、列表、行内代码显示已核实。该轮未发起真实 SGLang 请求，不宣称新的真实模型端到端成功，也不宣称完整仓库测试或根 Debug/Release lint 全量重跑。

Developer APK SHA-256：`48d208c305026f33e486ff1bb3ae9e53570016745b0648e36112b6b548284c8a`。本地验证日志保存在忽略的 build/phone-polish；真机截图与私有会话不入库。

## 决策与边界

决策记录：不适用。修复既有消息发布时机和展示层渲染，无存储、权限或执行边界变化。现有全文复制契约保留。

实现位于 codex/phone-chat-polish 工作树，未提交、未合入 main、未推送。未操作并行测试模拟器。
