> 历史任务规格：当前交付和实际验收见[完成记录](../../completion-records/HXA-219.md)。

# HXA-219：产物就地预览

分类：开发中。2026-09-22 所有者授权提交第一批并继续产物就地预览，`@`/`/`引用留到后续批次。前置为[HXA-218](../../completion-records/HXA-218.md)已通过的完整主机与四象限UI基线；当前无已知强制门禁失败。

## 用户结果与范围

输入区上方在有产物时显示本会话入口。打开底部面板后浏览本会话工具产物，原地查看文本或图片；返回列表或关闭后继续当前会话，保留输入和消息位置。列表采用会话归属，不过滤全局最近窗口；用户输入附件不冒充工具产物。

允许模块为app/chat的只读投影、app/ui、三语言资源、相关测试、验证脚本及文档。复用既有文件可用性检查、大小上限、缺失/撤权/变化提示，不执行文件、发消息或授予权限，不修改核心存储契约。文件导出/分享仍走原有产物中心。本批不实现Diff编辑、终端协同或快捷引用。

## 范围与验收要求

- 会话切换取消旧查询并清除面板选择；工具状态变化、Turn结束、前台恢复和打开面板重新查询。
- 每次选中或重新打开文件重新校验；取消面板不写草稿、不启动Turn。长列表按需组合，固定标题与关闭入口，复用有界文本/图片预览。
- 设备验证会话归属、输入附件排除、全局窗口截断边界、草稿保留、关闭与重开、文件变化和缺失。保留第一批回归并覆盖现有产物中心。

## 验证入口

使用JDK17与共享host slot，workers=2、parallel=false、daemon=false。执行 `./scripts/check-all.sh --all` 和双渠道测试APK组装，再执行 `python3 scripts/debug/2026-09-22/accept-ui-refactor.py --output build/ui-artifact-devices --capture --first-port 5660`。四象限均检查真实方法集合、零失败、非零执行与自有模拟器退出。

## 交付

完成实现和全部本切片门禁后，记录真实测试数、制品哈希及截图检查，更新状态和完成索引。

遵循[工作目录ADR](../../adr/workspace/README.md)与[权限ADR](../../adr/permissions/README.md)，无新增架构决定。设备截图仅作为本切片呈现证据，不代替物理设备、真实账号或全产品验收。
