# 191 / 207 与 Runtime 产品分支整合

日期：2026-09-18。目标为 `codex/hxa-196-product`，不是 main 推送或发行验收。

## 整合范围

- 191：`4621666c`、`3c84eafa`，合并提交 `48151c86`。接入显式 Compose 深浅主题和窗口主题、主题设备测试及原交付证据。
- 207：`4bd33db4`，合并提交 `c9a9eaf7`。接入扩展添加、启用、调用、禁用、修复和进程重启旅程。
- batch B、193/195、196 平台核心、Root 验收和 CI 优化分支原已在目标历史中，不重复合并。`v0.0.1` 是历史版本分支，不纳入本轮。
- 原工作树均未改写、删除或清理；main 的独立 review 文件保留。未 push、未合并 main。

## 审查修正

191 的主题测试原先仅根据设备当前模式选择断言；请求切换到 dark 失败时仍可能作为 light 通过。主题 runner 现在传入 `expectedNight`，verify 阶段先断言系统模式确实符合请求，再验证实际渲染颜色。模式未生效必须失败，不能转为浅色通过。

191 仍有 API29 真正深色、订阅登录内容配色和对话框实测缺口。分支完成记录转为[交付复审证据](hxa-191-delivery-review-2026-09-18.md)，任务保留为收尾验收。207 完成记录保留，删除重复活动任务规格并同步引用与完成索引。任务总数为 16 项未闭合义务，206 仍只维护计划。

## 合并后验证

共享主机锁内执行：

```sh
./gradlew spotlessApply
./scripts/check-all.sh --all
./gradlew :app:assembleConsumerDebugAndroidTest :app:assembleDeveloperDebugAndroidTest
python3 scripts/debug/2026-09-18/run-hxa207-device.py
python3 scripts/debug/2026-09-18/summarize-hxa207-device.py
python3 scripts/debug/2026-09-18/run-191-theme-device.py consumer 36 5710 build/merged-theme-consumer-api36
python3 scripts/debug/2026-09-18/run-191-theme-device.py developer 36 5712 build/merged-theme-developer-api36
python3 scripts/debug/2026-09-18/summarize-merged-theme.py
```

完整流水线 exit 0，主机门禁通过，日志 `build/merged-191-207-gates.log`。207 在合并后的 APK 上重新验证：30 项矩阵用例 + 8 项重启用例通过、18 项条件跳过、0 失败，8 个独占运行均正常退出，汇总 `build/hxa207-device/summary.json`。

主题在 API36 consumer/developer 各验证浅色和深色，每模式 6 项，共 **24 项通过**；四次 setup 真进程死亡后 verify 在新进程验证。两个模拟器正常退出，实际 `Night mode: no/yes` 与请求一致，新增 expectedNight 断言通过。API29 不调用无法证明深色的旧测试来充当通过；原证据与本次固定整合源码证据分开。全部定向设备验证合计 62 项通过、18 项条件跳过，不等于 206 或真机全产品验收。

## 后续

196 继续异步工具、Goal 时间预留/结算、任务投影与结果恢复；197、211 仍未实现。191 缺口保留在其任务中，207 的 fixture 与 consumer 条件跳过不代替 HXA-125 真实服务或 206 整体验收。
