# 199 / 206 本轮验收记录

状态：[206 本地核心产品范围已完成](../../completion-records/HXA-206.md)；199 的本地可执行项通过，关键物理设备项未验，保持未完成。基线 `215b7d81`，独立分支 `codex/acceptance-199-206`。主机、模拟器、真实设备和发布分别记账。

## 已确认的缺陷与修复范围

- 原验收器允许仅凭测试类名声称某方法通过，也允许把 synthetic JSON 改成 `real` 后直接得到 PASS。本轮要求精确的 TestRunner 开始/结束记录，排除失败、条件跳过及重复执行；结构化测量必须与原始日志中的同名测量一致。实际批次还校验复制的 APK、owner/closed PID 和预先固定的方法集合。
- 旧 owned runner 在结束时截取日志尾部。`hxa198-runtime-final-api36` 虽有 `OK (49 tests)`，逐方法日志已从中间开始，无法通过新的完整证据检查。本轮改成执行期间持续采集指定标签，准备/恢复阶段另存，不能用旧截断记录冒充本轮逐方法验收。
- API29 两阶段恢复中，清日志后仍可能读到旧准备进程的 TestRunner 记录。本轮按测试持久化的旧 PID 分离阶段，同时保留完整原始日志、准备尾部、验证日志和哈希；收集器校验分离结果与原始内容一致，不忽略验证进程的重复、失败或跳过。主题使用自己的 PID 标记路径，扩展和 APK 覆盖升级复用同一分离函数。旧失败日志保留，不通过删除重复检测来宣称成功。
- 锁定 JGit 的普通 `status/diff` 会执行仓库配置的 clean filter。独立新建仓库的复现产生 `configuredCommandExecuted=true`，命令仅写测试目录标记。修复为仓库局部禁止启动进程的 FS 和不执行 filter 的工作树迭代器；子目录继续使用同一迭代器，查看的是实际存储字节，不改写用户配置。状态页不递归执行子模块工作树检查；仍可呈现仓库记录的 gitlink 变化。

JGit 修复的轻量验证直接编译当前五个纯 JVM Git 文件及其测试，15/15 通过，包含 required filter、子目录与配置原文不变。它不替代完整 Gradle、Android 或 release 实际执行。原始输出位于忽略目录 `build/acceptance-baseline/git-filter-probe.log`、`git-reader-probe-v4.log`。

完整主机门禁 `build/acceptance-baseline/full-gates-v3.log` 已以 exit `0` 通过，包括全量 lint、测试、双 flavor debug/release 构建、锁文件、APK 边界及存储/应用测试 APK。Git JVM 用例在两个 flavor 均为 15/15。此前格式与 detekt 失败均已修复，未放宽规则。

API36 developer 首批文件/交付/授权为 31/31，原始批次 `build/hxa206-target-api36-v2`。文件固定旅程的 Full、Workspace、Custom 为 5 次语义操作和 0 张审批卡，Read-only 为 6 次和 1 张；全部只写入一次、可打开产物，打开时新增模型调用为 0。首轮测试请求漏传 `overwrite=true`，导致已有文件覆盖被正确拒绝；本轮修正 fixture 请求，保留成功、内容和审批断言。

199 首轮 API29 为 72/72，API36 Runtime 为 48/49；唯一失败是已有短租期 fixture 读到刚创建、尚未写完的空 PID 文件，抛出 `NumberFormatException`。测试脚本改为写临时文件后 rename 发布 PID，仍保留 7 秒测试租期、4 秒等待上限和全部进程回收断言。未修改 Runtime 生产代码；API29/API36 受影响 Runtime 批次均已重新 49/49 通过，首轮失败记录不删除。API29 日志/Job 16 与双终端 7 复用未受此 fixture 修改影响的批次；API36 同两组也分别 16/16、7/7 通过。日志固定用例的首包/取消结算为 API29 `41/633 ms`、API36 `39/117 ms`，采样定义见测试源码，不作通用性能保证。

## 默认时长验收结果

API36 / 4 KiB 独占模拟器：脱离操作窗口为开机单调时间 `59,160–61,290 ms`，第二终端在 `1,851,210 ms` 仍运行，在 `1,861,470 ms` 观测为 `STOPPED / IDLE / PROCESS_TREE_EXIT`；原 shell 与子进程均已退出。采样约每 10 秒一次，这些数值是观测边界，不伪造精确到毫秒的停止时刻。

第一终端实际启动 `49,122 ms`，默认截止 `7,249,122 ms`；在 `7,242,560 ms` 仍运行，在 `7,252,720 ms` 观测为 `STOPPED / LEASE_EXPIRED / PROCESS_TREE_EXIT`，即启动后 `7203.598 s`。两个终端的原 shell 与子进程均已退出，停止证明产生后 admission 身份仍保留，普通页面的两次显式结算最终返回可开始会话状态。Runtime 同一 PID 保留，FD `88 → 86`、线程 `20 → 18`；owned emulator `11742` 已以 exit `0` 关闭。命令整体 exit `0`，原始记录为 `build/hxa199-duration-api36-v1/{long-duration-result.json,long-duration-samples.jsonl,owner.json,closed.json}`。这证明模拟器上的真实默认时长，不替代 OEM、Doze、热压或物理 16 KiB 验收。

## 执行入口与证据界限

- `scripts/debug/2026-09-21/run-acceptance-matrix.py --scope 199|206 --output <新目录>`：在已有 APK 构建后通过共享 host slot 执行，复用既有 owned runner。`--plan-only` 仅检查源文件/方法并固定清单，不执行设备测试。分组保持需要进程恢复、全局 fixture 隔离的旅程各自安装。
- `scripts/verify-terminal-runtime.py` 和 `scripts/verify-product-journeys.py` 新增 `--owned-run <批次目录> --expected-methods <清单> --output <新目录>`。`DEVICE_BATCH_PASS` 只证明该 instrumentation 批次，不能作为整个 HXA 或真机验收通过。
- 默认 2 小时租约与 30 分钟脱离：`run-terminal-actual-duration.py` 在 instrumentation 退出后驱动普通应用界面；保留 journal 中实际启动/截止时间，采样原始 Runtime、shell 和子进程身份。没有缩短租期，也没有修改时钟/journal。当前运行使用基线生产 APK；后续代码变更若影响终端实现则需重跑。
- Git release：`run-git-release-matrix.py` 使用本机 debug 证书签名 release APK，仅为覆盖安装和实际 UI 验证。检查安装后的非 debuggable 属性与 APK 哈希、可打开 diff、仓库命令标记及指定 loopback HTTP fixture；这不是发行签名或外部网络全面审计。
- 206 新文件旅程使用固定模型协议 fixture，经真实聊天、Dispatcher、文件和产物页面完成读→写→读→打开。记录成功执行的语义 UI 操作、审批行、写调用和打开产物后的模型调用变化；初始测试资料、Provider 与会话配置属于 fixture，不计入操作数。它证明产品链路，不证明模型能力。

## 206 四象限与 release 结果

API29/36 × consumer/developer 共 52 个独占 instrumentation 批次。按最终测试源码、实际 API、复制的主/测试 APK 哈希及完整逐方法日志重新收集后，得到 **452 个方法记录：432 通过、20 条件跳过、0 失败、0 缺失**，另有 **4 次扩展独立恢复方法通过**。这是测试覆盖计数，不是用户任务成功率。

20 条跳过具体为：consumer 两 API 各 5 个需要 developer Advanced 精确 LAN scope 的 loopback MCP 方法、各 1 个 Linux 修复方法，以及四象限常规扩展批次各 2 个专用于分阶段执行的方法。后者已通过独立 seed → force-stop → recover 旅程实际执行。consumer 的公共 MCP 能力没有被删除；本地私网 fixture 的条件限制不能推导成公共 MCP 或真实外部服务已验收。

最终采纳的批次根（后列同名批次覆盖前列）为：`build/hxa206-matrix-v2`、`build/hxa206-task-logcheck-v3`、`build/hxa206-matrix-v3`、`build/hxa206-matrix-v4`、`build/hxa206-matrix-v5`。汇总命令 `summarize-product-matrix.py --input … --output build/hxa206-summary-v1` 重新校验原始事实，以 exit `0` 写出 `matrix.json` 与 `matrix.md`。矩阵执行器遇到已记录条件跳过时最终 exit `1`，不把它改成全通过；真正的 runner/断言错误会当场中止。

| 固定产品路径 | 本轮结果及观测边界 |
| --- | --- |
| 读资料 → 修改测试文件 → 再读 → 打开产物 | 四模式 × 四象限，16/16 固定任务成功；Full/Workspace/Custom 为 5 次语义操作、0 卡；Read-only 为 6 次、1 卡。每次只写入一次，打开产物新增模型调用 0 |
| 工具 DISABLED / CUSTOM DENY | 四象限真实 Dispatcher 拒绝，0 执行、0 审批；重新启用仍走预设决定，不由模型授予权限 |
| 长流停止与失败后显式重试 | 四象限停止按钮一次操作完成持久 CANCELLED、流断开及预算结算；失败路径在用户重试前只有原 Turn，显式一次重试只产生一个新 Turn |
| 重启恢复与未知副作用 | 四象限持久 fixture、实际 PID 更换；查询两次仍不重放原工具，已取消任务不自动继续。重连/授权入口仅修复条件，不代替用户重试 |
| Tasks 与 Plan | 跨会话定位、取消中状态、Plan 审阅到执行和授权联动通过；恢复批次保留旧/新进程证据 |
| 扩展添加/启用/使用/禁用/修复 | developer 双 API 真实 loopback 协议、Dispatcher 与持久结果通过；禁用后不曝光/执行，修复后显式新调用。安装取消、配置无效、重复导入与重启保留在各适用变体通过 |
| 准备、搜索与主题 | 四象限只读准备与搜索、真实进程恢复、浅/深主题及大字体通过；developer 另验订阅页面。不把应用查看操作变成 Agent 工具 |
| JSONL 导出 | 四象限实际 DocumentsUI 与独立解析通过，另有完整关联/取消/中断/有界导出 fixture。实际 picker 小会话每份 5 条记录、无未解析引用；`materialsComplete=false` 如实保留，不宣称完整备份 |
| Git 只读 status/diff | debug 四象限恶意配置测试通过；release 四象限普通页面各 3 次操作打开 diff，配置未变、命令标记不存在、指定 HTTP fixture 请求数 0 |

除新增文件旅程记录运行时操作数外，表内“停止一次/重试一次/查询两次”是已通过测试的固定动作及断言，不冒充真人耗时或全站点击测量。纯 Dispatcher、进程恢复及状态断言不计人工步骤；没有统一模拟用户评分。没有产物的拒绝/停止场景，产物可打开性不适用。

Git release 最终原始证据为 `build/hxa206-git-release-v2/{consumer,developer}-api{29,36}`（v1 也全部通过，v2 补验准备类阶段限制）：四个 seed 方法和四条 release UI 旅程均通过，owned 进程均关闭。每次校验安装路径上的 APK 与测试签名包相同，且非 debuggable。仅使用本机 debug 证书使 debug → release 替换可行，不是发行签名或商店验收。无网络结论仅覆盖指定恶意 fixture，不作任意仓库/所有网络路径的无限保证。

人工复核 API29 consumer 与 API36 developer 的深色/导出截图：文字、按钮与系统栏可读，没有观察到裁切。截图是固定页面采样，不是所有机型的视觉保证。四份实际 picker JSONL 均由 `scripts/validate-session-export.py` 独立解析；丰富关联材料另由已有导出旅程验证。

### 制品身份和包体

本轮最终生产 APK（本地提交绑定本记录；未推送或合入 main）：

| 制品 | SHA-256 | 相对保存的 `215b7d81` 基线大小变化 |
| --- | --- | ---: |
| consumer debug | `87efc66ae59b57baeee6c4b511f76002b3468d9dfc4894b6a64d98f624222c79` | 57,954,120 → 58,045,688 B，+91,568 B |
| developer debug | `385d8db59ff72699e057e02fa5081f2a08d508c43b2aa4ace3d4e3db10b5eae9` | 123,465,058 → 123,557,550 B，+92,492 B |
| consumer release unsigned | `6b6bf59907de4e372b8f198cd5d34d9159cb040b9c71ea522a8f25d5b3e39d71` | 45,458,443 → 45,458,443 B，0 B |
| developer release unsigned | `f4f9a74bab2011b3c66e523d946324d913f85f9bfd6292422a15c197c93a17a3` | 104,143,460 → 104,146,792 B，+3,332 B |

release 行比较构建输出的未签名包；实际设备使用测试签名包，consumer SHA `6689dce5a563a7d99facdb31bb6c621e45f9a8a0e365564ec08bf42c297604fd`，developer SHA `68245f5dd306b347d78c382bd8b38df35b2794a1c19a3b27afca4439618d43c4`。变化是整个构建产物之差，不把压缩/构建布局差异全部归因于 Git 代码，也不代表相对“没有终端”的版本增量。

## 199 双 API 恢复、升级与专项范围

最终适用 Runtime 设备矩阵 **144/144 方法通过**：API29/36 各 Runtime/单终端 49、日志/Job 16、双终端 7。对应原始根为 `build/hxa199-api29-runtime-v2`、`build/hxa199-matrix-v1` 中未受短租期 fixture 修改影响的 API29 日志/双终端批次，以及 `build/hxa199-api36-v2`。所有使用同一最终 developer 生产 APK；API29 日志/双终端保留较早测试 APK，变更只涉及另一 Runtime 批次里的 PID 发布方法，该批次已双 API 重跑。

| 固定专项场景 | 实际执行/通过 | 限定 |
| --- | --- | --- |
| 日志首包、截断/重连与停止结算 | 双 API 适用批次通过 | 首包 API29 41 ms、API36 39 ms；取消至终态 633/117 ms，含 fixture 轮询，不是产品延迟上限 |
| 后台 Job 原身份查询/取消/收取与失败结算 | 双 API 适用批次通过 | 不把模拟器的后台存活视为 OEM/Doze 真机验收 |
| 单终端 REPL、长输出、输入边界与租期 | 双 API Runtime/页面批次通过 | 短租期边界与下面真实默认时长分别记账 |
| 双会话与容量/输入连接/Runtime 死亡 | 双 API 适用批次通过 | 只证明两个手动 shell，不增加 Agent 并发或 PTY 输入 |
| 普通页面杀主进程 → 两个原 shell 重连 → 显式结算 | 2/2 旅程通过 | 同一 boot、Runtime PID 不变，两 shell 各只启动一次；最终 admission 释放 |
| 默认两小时运行中到期、30 分钟脱离 idle | API36 各 1/1 场景通过 | 实际等待，4 KiB 模拟器；不覆盖热压或真实 16 KiB |
| 旧 APK 覆盖升级后历史/结果/旧锚对账 | API29/36 各 1/1 旅程通过 | developer debug → debug，实际 `install -r`，未清数据；旧中断请求是序列化 fixture，不冒充曾执行命令 |
| consumer 排除 / developer 集成 | debug/release APK 边界通过 | 制品检查，不是商店或签名发行 |
| OEM/HOME/安全锁屏/Doze/热压/物理长稳/真实 16 KiB | 未执行 | 没有物理设备；199 保持未完成 |

普通页面双终端恢复原始记录为 `build/hxa199-dual-final-v1-api{29,36}`，各 11 个 instrumentation 方法先通过（含 1 个页面准备方法），另各一条宿主 UI 旅程通过；不把准备方法混入恢复场景成功率。API29 主 PID `4886 → 5408`，Runtime `5151`，两 shell `5196/5332`；API36 主 PID `4819 → 5664`，Runtime `5207`，两 shell `5243/5607`。原启动计数均 `[1,1]`，owned 进程退出均为 `0`。

冷启动测量在用户开始会话前确认 Runtime 不存在：点击开始至 UI RUNNING 为 API29 `2857 ms`、API36 `4933 ms`；至首个 shell 文件回执为 `4661/8376 ms`。计时包含宿主 UIAutomator、键盘操作、ADB 和轮询开销，不能当成 native fork 延迟或性能 SLA。两小时试验的 FD/线程前后变化仅是同一 Runtime 的小样本，未执行整机温度/内存长稳评价。

覆盖升级原始记录为 `build/hxa199-upgrade-final-v1/api{29,36}`。旧生产 SHA `23aa72cc54f24f56aacc71353bba320d250c94246e5ae05a4f061391e206d6a2`，来源 `03617128`；旧测试 SHA `bad3fa7ad8f036e76b10beee1f99100e5c33a583d10c9dc54016ba08e471106c` 的编译来源见[旧包记录](apk-replacement-upgrade-2026-09-18.md)。新生产为上表 developer debug，新测试 SHA `98aa66638f09d3dba9ab08dc12ad4f87661dbae0a4129a95ca1e610de2b397d0`。两个实际安装路径上的 old/new APK 都经拉取校验；会话/消息/Provider/文件原文保持，已完成 CLI 结果仍可取回且显式结算，旧 pending 只转 INTERRUPTED，不重放，旧 Runtime 验证锚不激活新环境。

默认两小时试验采用基线生产 SHA `7bf9a4a778e0dc536bc4a1654e42ba95ed1100f0ef2ace71564840527d1d8a65`、测试 SHA `7425c6525a104e1ad940ab46016f886ec6a24e001abc350e5de07b9b07642cb8`。本轮生产变更仅在 Git 查看器，终端/Runtime 源码未变，因此按未受影响范围复用；明确不冒充在最终 Git APK 上重跑了两小时。

Runtime 锁未改变：`runtime/proot-app/src/main/assets/runtime/runtime-lock.json` SHA `8f1fc7e0d3405ec1a0adf0d129e39b60f83206df3ec4f7275950de0833c71ce5`，其中 Alpine 3.22.5 aarch64 原归档 SHA `674aa3ac68200bfe67b01964573fce2c6780c726fe23ca640e998337cf47f509`。包内资产遵循既有固定归档与验证入口，不提交下载内容或签名材料。

## 同 fixture 前后对照与可复现入口

`build/hxa206-baseline-file-v1` 在保存的 `215b7d81` 双 flavor 生产 APK 上执行同一测试 APK/同一四模式 fixture，API29/36 各自使用新独占模拟器。`compare-product-file-fixture.py` 重新检查前后原始方法日志、fixture 源哈希、同一测试 APK 和不同生产 APK，得到 **前 16/16、后 16/16，五个指标全部相同**：用户步骤、审批卡、产物可打开、打开新增模型调用、写执行数。对照文件为 `build/hxa206-summary-v1/baseline-comparison.json`；这说明所测链路未退化，不宣称任务效率有提升。

矩阵使用的 consumer/developer 测试 APK SHA 分别为 `c922dfc3dc6610ad39d67bf56e482752316c4db66a4bec0b64a35fb801b15e83` / `98aa66638f09d3dba9ab08dc12ad4f87661dbae0a4129a95ca1e610de2b397d0`。最后仅给独立 `GitReleaseFixtureDeviceTest` 补上无显式阶段时的 assumption：普通套件不能替换自身 APK，应交由两 APK runner；显式错误阶段仍失败，prepare 的所有断言不变。重新构建后的测试 SHA 为 `520f4b452cfa8b05ce2c1a7645c235cd06c17a119a9a466d49f12bdb241edce1` / `9e519400e8057a8958d699aa8ec16697175fe035cf903356a8d7a8410e545922`，针对该 fixture 重跑 release 四象限。主体 52 批次不包含此类，源哈希全部未变，使用保存的原测试 APK重新汇总至 `build/hxa206-summary-v2`，不将其伪称为新测试 APK 下重跑。

重型命令均包在 `python3 scripts/debug/2026-09-18/with-host-slot.py -- <命令>` 中串行执行。JDK17/Android SDK 使用本机配置；模拟器均为新启动的 `Helix191_API29` / `Helix191_API36`、arm64、4 核/4 GiB，自有 PID/端口，不借用已有设备。原始日志与 APK 放忽略的 `build/`：

| 命令入口 | 结果 / 输出 |
| --- | --- |
| `./scripts/check-all.sh --all` | 首轮 `full-gates-v3.log` 与最终 `full-gates-final-v2.log` 均 exit 0；不以 Debug CI 替代 |
| `./gradlew :core:storage:assembleDebugAndroidTest :app:assembleConsumerDebugAndroidTest :app:assembleDeveloperDebugAndroidTest` | exit 0；后续 fixture 改动重建两 app 测试 APK exit 0 |
| `python3 scripts/verify-integrated-runtime-apks.py --build-type release` | exit 0，consumer/developer release 边界通过 |
| `python3 scripts/debug/2026-09-09/run-owned-emulator.py --help` | exit 0，现有独占入口可用 |
| `python3 scripts/debug/2026-09-21/run-acceptance-matrix.py --scope 199 --output <新目录>` | 受影响批次重验后 144 项通过；分组和最终输出根见上文 |
| `python3 scripts/debug/2026-09-21/run-dual-terminal-recovery-gates.py hxa199-dual-final-v1` | exit 0，双 API 普通双 shell 恢复通过 |
| `python3 scripts/debug/2026-09-21/run-acceptance-upgrades.py --old-apk <保存的旧主包> --old-test-apk <对应旧测试包> --output build/hxa199-upgrade-final-v1` | exit 0，实际覆盖升级 2/2 |
| `python3 scripts/debug/2026-09-21/run-acceptance-matrix.py --scope 206 --output <新目录>` | 最终 52 批次如上；已记录的条件跳过使汇总 exit 1，零断言失败 |
| `python3 scripts/debug/2026-09-21/run-git-release-matrix.py --output build/hxa206-git-release-v2 --first-port 5730` | 最终 exit 0，修正阶段 fixture 后四象限定向重跑通过 |
| `python3 scripts/debug/2026-09-21/run-acceptance-matrix.py --scope 206 --group file-task --app-directory build/acceptance-baseline/before-apks --output build/hxa206-baseline-file-v1 --first-port 5600` | exit 0，基线固定任务 16/16，与最终结果逐项比较相同 |
| `python3 scripts/validate-session-export.py <实际 picker JSONL>` | 四象限各 exit 0；材料完整性保持 false |
| `python3 scripts/verify-terminal-runtime.py --owned-run build/hxa199-api36-v2/developer-api36-runtime --expected-methods build/hxa199-api36-v2/developer-api36-runtime-expected.json --output build/hxa199-owned-cli-final` | exit 0，DEVICE_BATCH_PASS，只代表该批次 |
| `python3 scripts/verify-product-journeys.py --owned-run build/hxa206-matrix-v5/developer-api36-file-task --expected-methods build/hxa206-matrix-v5/developer-api36-file-task-expected.json --output build/hxa206-owned-cli-final` | exit 0，DEVICE_BATCH_PASS，只代表该批次 |

P1/P2/P3、G1/G2/G4 的源码/格式/静态分析、模块测试、debug/release 构建与边界由完整 `--all` 加上述测试 APK/release 检查覆盖。G3 复用 `verify-integrated-runtimes.py` 的实际 `CASES`，通过同一 owned runner 加入本任务适用类，替换为现场存在的 AVD 名并保存逐方法证明；没有删掉既有 Runtime 用例换取通过。

## 尚未闭合

- 199：物理 OEM/HOME/安全锁屏/Doze/热压/长稳与真实 16 KiB 设备未提供。实际 30 分钟/2 小时、完整主机、双 API、覆盖升级与本地专项统计已经完成；仍不创建 199 完成记录。旧 196 真机豁免不替代这些要求。
- 206：本地核心产品范围已完成；条件跳过不计通过。真实外部账号/服务、物理整机稳定性与签名发行不在本次默认 fixture 结论内。
- 本轮按任务范围仅保留本地具名提交；未推送、main 合并或发布。结束时 ADB 无连接设备，所有本轮 owned emulator 已退出，没有清理他人的模拟器。
