# HXA-193/191 整合审查（2026-09-18）

来源：Claude Code 分支 `codex/runtime-closeout-session-search` 的 `def15f4a`，整合至含能力规划规则 `2dc518ae` 的主线。本文区分原分支证据与本次重新执行结果。

## 审查结论

- 191仅搜索切片，深色主题未实现；不新增191完整完成记录。已交付搜索基于现有Room/ContentStore，不新增模型工具或读取模型上下文，不扩大Agent权限。
- 原193测试使用当前codec写入构造历史记录，验证Runtime冷启动与对账；API29/36各35项原始日志已核对。没有实际旧APK覆盖升级，不能声明整个193升级范围完成。撤回原完成记录，恢复任务规格并保留证据，固定归档验证与滚动镜像旧包缺失分开记账。
- 原191分支因193状态文档未同步导致的源码门禁失败由本次整合修复，不能把该分支称作全门禁通过。

## 搜索修正

- 同步IO搜索原本不响应Job取消，旧结果可能在清空/切换后重新写入。扫描增加协作取消检查；发布与清空/新请求共享锁，锁内再次校验Job状态。
- 输入态保留用户尾部空格，查询归一化只在仓储进行，允许连续输入多词短语；更换查询时清除旧命中，避免将旧结果误认成新查询结果。
- Unicode片段定位使用与匹配相同的ignoreCase比较，避免lowercase长度/字符映射差异导致找不到命中或偏移错误。
- 命中数量上限也标记truncated；显示跳过正文数量和不完整范围，避免暗示全文穷尽。仍是有界扫描，不宣称全历史索引或性能上限已经评测。

## 验证

原分支的API29/36双flavor搜索及Room矩阵见[原记录](hxa-191-session-search-2026-09-18.md)，不是本次修正后测试结果。本次实际执行如下：

- 固定归档 `build-proot-assets.sh`：hash、360 ELF与资产准备通过；首次新工作树构建缺资产后补齐，未绕过校验。
- 定向 `:core:storage:testDebugUnitTest :app:testConsumerDebugUnitTest :app:testDeveloperDebugUnitTest` 与双flavor app/test、storage test APK构建通过；搜索仓储15项，含新增Unicode、截断和协作取消测试。
- 专用runner四象限（API29/36 × consumer/developer）每组app 8/8 + storage 4/4，共48项，无主验证失败；setup的预期强杀独立记账，不计成主验证通过。四份closed.json均exit 0。
- 首组完成后ADB短暂保留旧serial，runner拒绝复用；剩余三组改独立端口5664/5666/5668，未复用或停止外部设备。首组5662结果独立保留。
- 设备所装APK身份保存在各目录artifacts.json。其后仅做注释换行和英文计数标签措辞修正；最终制品由完整主机门禁检查，设备功能证据对应记录哈希，不冒称相同字节。
- 完整门禁首次发现注释行过长，第二次发现英文计数PluralsCandidate；修正文案和注释后重跑，未禁用检查。最终 `./scripts/check-all.sh --all` exit 0：源码、Spotless/Detekt、全主机测试、全Lint、Debug/Release构建、35份依赖锁、APK/订阅边界全部通过（all-r3.log）。

独占设备使用本次专用AVD，入口为 `scripts/debug/2026-09-18/run-integration-193-191.sh`，日志在忽略的 `build/integration-193-191/`。

本次只本地整合，不推送、不发布；不整合另一工作树的204/205进行中改动。
