# 193 / 195 与批次 B 合并验证

日期：2026-09-18。范围：用户授权合并到本地 main，不推送或发布。

## 来源与冲突处理

main 起点为 `24c36c5a`；先快进纳入 `codex/runtime-193-195` 的 `7bda8749`，再合并 `codex/batch-b-recovery-readiness` 的 `f3b5c621`。两条分支的 HXA-193/195 与 HXA-204/205 交付均保留。

三语资源保留两侧新增内容，任务状态与下一候选更新为 207 → 191 剩余部分 → 206。删除已完成的 204/205 活动任务文件前，将较新版本的应用能力与 Agent 能力边界补入完成记录。PRoot flavor seam 同时保留日志观察与准备状态投影。

Claude 工作区两个未跟踪的 204/205 矩阵脚本原样保留，合并前后 SHA-256 相同。未删除其他分支或 worktree。

## 主机验证

以下命令在合并后的源码执行并 exit 0：

```sh
./gradlew spotlessApply :app:assembleConsumerDebug :app:assembleDeveloperDebug :app:assembleConsumerDebugAndroidTest :app:assembleDeveloperDebugAndroidTest
bash scripts/check-all.sh --all
python3 scripts/verify-integrated-runtime-apks.py --build-type release
```

完整主机门禁包含格式、静态分析、单元测试、lint、双变体 Debug/Release、35 个依赖锁及变体/订阅/Runtime 边界。首轮遇到 Spotless 遍历生成目录与并行编译竞态；修复文件树排除方式后重新通过完整门禁，见[修复记录](../../bug-fixes/2026-09-18-spotless-generated-tree-race.md)。没有跳过失败检查。

日志保存在忽略目录 `build/merge-193-195-batch-b/`：`device-build.log`、`host-all.log`（初次失败）、`host-final.log`、`release-boundaries.log`。

## 合并后的设备矩阵

使用 API29/36、consumer/developer，10 次独占新模拟器进程，每次 4 核 / 4096 MiB，均记录正常退出。恢复与准备页采用两阶段进程恢复并核对新 PID；准备页启用飞行模式。

| API | flavor | 恢复、停止与工具结果读取 | 准备页 | 日志与 Runtime |
| --- | --- | --- | --- | --- |
| 29 | consumer | 15 通过 | 6 通过 / 1 条件跳过 | 不适用 |
| 29 | developer | 15 通过 | 7 通过 | 8 通过 |
| 36 | consumer | 15 通过 | 6 通过 / 1 条件跳过 | 不适用 |
| 36 | developer | 15 通过 | 7 通过 | 8 通过 |

合计 **102 通过、2 条件跳过、0 失败**。跳过为 consumer 不适用的 developer Runtime 修复场景；按 TestRunner assumption 记录单独计数，没有把 JUnit 的 `OK (7 tests)` 全部计为通过。

执行与核验脚本：

```sh
python3 scripts/debug/2026-09-18/run-merged-runtime-readiness.py
python3 scripts/debug/2026-09-18/run-merged-runtime-readiness.py --start-index 1
python3 scripts/debug/2026-09-18/summarize-merged-matrix.py
```

第一组完成后，ADB 短暂保留已退出模拟器的 serial，下一次启动被独占检查拒绝。改为每组独立端口后从索引 1 继续；此前通过且正常退出的证据保留，没有借用或终止他人的设备。汇总脚本核对每组当前 APK 哈希、结果、条件跳过及退出状态，输出 `matrix-summary.json`。

| 制品 | SHA-256 |
| --- | --- |
| consumer app | `6c56dd5fe9d17a8d59c3e79adf4d60f3278cf58d89beeb8b1300e7545b628619` |
| consumer test | `8d256ddaba30a0274e6d00c1951f6acfce8b6ffecf47bfe71d1342c228e857f9` |
| developer app | `54379ca10e47176709d0e2905036675c6cf2e1662d1d2a840764d41c5c697b19` |
| developer test | `8363d12daa9056ada04d422d6d371bed92b87ad7b771e488f5940f3d872379c8` |

## 验收范围

这是合并后交互和运行时接线的定向回归，不等于 HXA-206 全产品验收、真实账号、真机 OEM/Doze/热压或签名发行。来源分支历史 CI 不作为本次合并提交的 CI 证据；本次未推送，未运行远端 CI。
