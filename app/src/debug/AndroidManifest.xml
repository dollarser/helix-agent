<?xml version="1.0" encoding="utf-8"?>
<!-- HXA-069 (test-only, debug build type only): the locale-applying activity the headless UI
     fixtures use to verify a Material3 dialog (which composes in the HOST activity's context,
     unreachable by a LocalContext override) renders the pinned zh-CN copy on every API level.
     Merges into the debug variants' manifest; never part of a release build. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <activity
            android:name="com.helix.app.ui.LocalizedComposeActivity"
            android:exported="false"
            android:theme="@style/Theme.Helix" />
    </application>
</manifest>
