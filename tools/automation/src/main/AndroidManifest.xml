<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application>
        <service
            android:name="com.helix.tools.automation.HelixAccessibilityService"
            android:description="@string/automation_service_description"
            android:exported="true"
            android:foregroundServiceType="specialUse"
            android:label="@string/automation_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/helix_accessibility_service" />
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="User-started, time-bounded Accessibility automation session with an immediate stop action" />
        </service>

        <receiver
            android:name="com.helix.tools.automation.AutomationStopReceiver"
            android:exported="false" />
    </application>
</manifest>
