from pathlib import Path
root=Path('<HELIX_CHECKOUT>')
def edit(p,a,b):
 p=root/p;s=p.read_text();assert a in s,p;p.write_text(s.replace(a,b,1))
p='app/src/main/kotlin/com/helix/app/ui/ChatScreen.kt'
edit(p,'import androidx.activity.compose.rememberLauncherForActivityResult','import androidx.activity.compose.BackHandler\nimport androidx.compose.runtime.saveable.rememberSaveable\nimport androidx.activity.compose.rememberLauncherForActivityResult')
edit(p,') {\n    LazyColumn(\n        modifier = Modifier.fillMaxSize().padding(8.dp),',') {\n    var archivedOnly by rememberSaveable { mutableStateOf(false) }\n    val visibleSessions = sessions.filter { it.isArchived == archivedOnly }\n    BackHandler(archivedOnly) { archivedOnly = false }\n    LazyColumn(\n        modifier = Modifier.fillMaxSize().padding(8.dp),')
edit(p,'stringResource(R.string.chat_session_header),','stringResource(if (archivedOnly) R.string.chat_archived_list else R.string.chat_session_header),')
edit(p,'        if (sessions.isEmpty()) {','        item(key = "archive-navigation") {\n            TextButton({ archivedOnly = !archivedOnly }, Modifier.testTag("chat-archive-list-toggle")) {\n                Text(stringResource(if (archivedOnly) R.string.chat_active_list else R.string.chat_archived_list))\n            }\n        }\n        if (visibleSessions.isEmpty()) {')
edit(p,'stringResource(R.string.chat_empty_sessions_hint),','stringResource(if (archivedOnly) R.string.chat_empty_archive else R.string.chat_empty_sessions_hint),')
edit(p,'        items(sessions, key = { it.id })','        items(visibleSessions, key = { it.id })')
edit(p,'            reasoning = runControl.reasoning,','            contextUsage = screen.contextUsage,\n            reasoning = runControl.reasoning,')
edit('app/src/main/kotlin/com/helix/app/chat/ChatUiModels.kt','    val directoryRef: String? = null,\n) {\n    val isSending','    val directoryRef: String? = null,\n    val contextUsage: ChatContextUsage = ChatContextUsage(),\n) {\n    val isSending')
edit('app/src/main/kotlin/com/helix/app/chat/ChatService.kt','                messages = projection.messagesFor(sessionId, current),','                messages = projection.messagesFor(sessionId, current),\n                contextUsage = ChatContextProjection.read(storage, sessionId),')
edit('app/src/main/kotlin/com/helix/app/ui/ConversationComposer.kt','    modelSelector: (@Composable () -> Unit)? = null,','    modelSelector: (@Composable () -> Unit)? = null,\n    contextUsage: com.helix.app.chat.ChatContextUsage = com.helix.app.chat.ChatContextUsage(),')
edit('app/src/main/kotlin/com/helix/app/ui/ConversationComposer.kt','            if (input.isNotEmpty()) CopyTextButton(input, "chat-copy-input")','            ContextWindowIndicator(contextUsage)\n            if (input.isNotEmpty()) CopyTextButton(input, "chat-copy-input")')
edit('app/src/androidTest/kotlin/com/helix/app/ui/ConversationTopBarDeviceTest.kt','            compose\n                .onNode(\n                    hasTestTag("chat-archive")','            compose.onNodeWithTag("chat-session-$id").assertDoesNotExist()\n            compose.onNodeWithTag("chat-archive-list-toggle").performClick()\n            compose\n                .onNode(\n                    hasTestTag("chat-archive")')
# Separate shared-storage home; existing selected sources remain available.
p='app/src/main/kotlin/com/helix/app/ui/FilesHome.kt'
edit(p,'import androidx.compose.material3.Card','import androidx.activity.compose.BackHandler\nimport androidx.compose.runtime.getValue\nimport androidx.compose.runtime.setValue\nimport androidx.compose.runtime.mutableStateOf\nimport androidx.compose.runtime.saveable.rememberSaveable\nimport androidx.compose.material3.Card')
edit(p,') {\n    Column(',') {\n    var sharedOpen by rememberSaveable { mutableStateOf(false) }\n    BackHandler(sharedOpen) { sharedOpen = false }\n    Column(')
edit(p,'        state.sources.forEachIndexed { index, source ->','        if (!sharedOpen) {\n            FileLocationCard(stringResource(R.string.files_shared), stringResource(R.string.files_shared_detail), "files-shared-open") { sharedOpen = true }\n        } else {\n            TextButton({ sharedOpen = false }, Modifier.testTag("files-shared-back")) { Text(stringResource(R.string.files_local)) }\n        }\n        state.sources.forEachIndexed { index, source ->\n            if ((index == 0) == sharedOpen) return@forEachIndexed')
edit(p,'        Text(stringResource(R.string.files_quick_access), style = MaterialTheme.typography.titleMedium)','        if (!sharedOpen) {\n        Text(stringResource(R.string.files_quick_access), style = MaterialTheme.typography.titleMedium)')
edit(p,'        Card(Modifier.fillMaxWidth()) {','        }\n        Card(Modifier.fillMaxWidth()) {')
edit(p,'                TextButton(onPermissions, modifier = Modifier.testTag("files-permissions")) {\n                    Text(stringResource(R.string.files_access_settings))\n                }','                if (sharedOpen) {\n                    Text(stringResource(R.string.files_shared_limits), style = MaterialTheme.typography.bodySmall)\n                    TextButton(onPermissions, modifier = Modifier.testTag("files-permissions")) {\n                        Text(stringResource(R.string.files_access_settings))\n                    }\n                }')
strings={
'chat_archived_list':('Archived sessions','已归档会话'), 'chat_active_list':('Back to sessions','返回会话列表'), 'chat_empty_archive':('No archived sessions','暂无已归档会话'),
'files_shared':('Shared storage','共享存储'), 'files_shared_detail':('Browse granted folders and manage access','浏览已授权文件夹与管理访问范围'), 'files_shared_limits':('Select a folder to browse its contents. Android restricts access to storage roots and private app directories. All-files access, when available in this build, is managed in Permissions. Shared sources currently support browsing, preview and sharing.','选择文件夹后可浏览其中内容。Android 限制存储根目录和应用私有目录的访问；当前版本如提供所有文件访问，可在权限页管理。共享来源目前支持浏览、预览与分享。'),
'chat_context_title':('Context window','上下文窗口'), 'chat_context_unknown':('Unknown','未知'), 'chat_context_usage':('Last request input: %1$s tokens\nModel context limit: %2$s tokens','上次请求输入：%1$s tokens\n模型上下文上限：%2$s tokens'), 'chat_context_explanation':('The ring uses input usage reported for the latest request to the currently selected model, not cumulative billing. It excludes subsequent output and unsent text. A question mark means usage or the context limit is unavailable. Limits are not inferred from model names or turn budgets. Automatic compaction and a context-window setting are not connected to chat yet; start a new session if context becomes too large.','圆环使用当前所选模型最近一次请求返回的输入用量，不是累计计费；不包含此后输出和未发送文字。问号表示用量或窗口上限未知。不会根据模型名或轮次预算猜测上限。当前对话尚未接入自动压缩与窗口设置；上下文过大时请新建会话。'), 'chat_context_close':('Close','关闭')}
from xml.sax.saxutils import escape
for loc,index in [('values',0),('values-zh-rCN',1)]:
 p=root/f'app/src/main/res/{loc}/strings.xml';s=p.read_text();s=s.replace('</resources>',''.join(f'    <string name="{k}">{escape(v[index]).replace(chr(10),chr(92)+"n")}</string>\n' for k,v in strings.items())+'</resources>');p.write_text(s)
